{"key":"py-00ee94b152e8","id":"py-00ee94b152e8","program":"\nimport json,sys\nn=bad=0\nfor ln in open('$d/log.jsonl'):\n    r=json.loads(ln)\n    if r['shim']=='lua-embed': n+=1\n    \nprint('lua records parsed:', n)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:08:45.522Z","count":1,"stdin_sample":null}
{"key":"py-032fae657b63","id":"py-032fae657b63","program":"import pathlib\np = pathlib.Path(\"Makefile\")\ns = p.read_text()\ndef rep(old, new):\n    global s\n    assert s.count(old) == 1, old\n    s = s.replace(old, new)\n\nrep('''#   make                 build, preferring the checkout's freshly built library\n#   make run             build and run it\n#   make valgrind        the same, under valgrind if this machine has one\n#   make LIBDIR=... INCDIR=...   link something else entirely\n''', '''#   make                 build embed, preferring the checkout's freshly built library\n#   make run             build and run it, from a temp directory (see below)\n#   make valgrind        the same, under valgrind if this machine has one\n#   make quickstart      build quickstart.c, the minimal host and the file to copy\n#   make LIBDIR=... INCDIR=...   link something else entirely\n#\n# `run` executes embed from a directory mktemp made, never from here: one of\n# its programs writes a file into whatever the cwd is, and a checkout is the one\n# place a program's own writes must not land (CLAUDE.md invariant 4). The\n# temp directory is removed afterwards whether or not the run passed.\n''')\n\nrep('''# $(wildcard) rather than a bare path: a missing header must reach the message\n# below, not make's own \"No rule to make target\".\nheader := $(wildcard $(INCDIR)/lypning.h)\nlib    := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n''', '''# $(wildcard) rather than a bare path: a missing header or library must reach\n# the message below, not make's own \"No rule to make target\" — or, for the\n# library, a bare `ld: library not found` that names neither the directory that\n# was searched nor the command that fills it.\nheader := $(wildcard $(INCDIR)/lypning.h)\nlib    := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n\n# One message for both halves, so a wheel user with neither reads it once. The\n# two build lines are the same build: `lypning build --lib` runs the cargo line\n# and then asserts the refusal contract on what it produced.\ndefine need_library\n\t@test -n \"$(header)\" -a -n \"$(lib)\" || { \\\\\n\t  test -n \"$(header)\" || echo \"no lypning.h under $(INCDIR).\"; \\\\\n\t  test -n \"$(lib)\"    || echo \"no liblypning.so or liblypning.dylib under $(LIBDIR).\"; \\\\\n\t  echo \"build it:  lypning build --lib\"; \\\\\n\t  echo \"      or:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"; \\\\\n\t  echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"; \\\\\n\t  exit 1; }\nendef\n''')\n\nrep('''BIN := embed\n# Written by one of the programs, removed by the same run — unless the run\n# aborts, which is what the assertions are for.\nLITTER := lypning-embed-example.txt\n\n.PHONY: all run valgrind clean\nall: $(BIN)\n\n$(BIN): embed.c $(header) $(lib) $(MAKEFILE_LIST)\n\t@test -n \"$(header)\" || { \\\\\n\t  echo \"no lypning.h under $(INCDIR).\"; \\\\\n\t  echo \"build it:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"; \\\\\n\t  echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"; \\\\\n\t  exit 1; }\n\t$(CC) $(CFLAGS) -I$(INCDIR) embed.c -o $@ $(link) $(LDFLAGS) $(LDLIBS)\n\t@echo \"$@: header $(INCDIR)/lypning.h, library $(LIBDIR) ($(from))\"\n\nrun: $(BIN)\n\t./$(BIN)\n\n# The example frees every handle it takes, so a leak here is the library's or\n# ours and either is worth knowing. --trace-children stays off on purpose: the\n# fallback exec's python3, and valgrinding CPython measures CPython.\nvalgrind: $(BIN)\n\t@command -v valgrind >/dev/null 2>&1 || { \\\\\n\t  echo \"valgrind is not installed on this machine; skipping\"; exit 0; }; \\\\\n\tvalgrind --leak-check=full --show-leak-kinds=definite,indirect \\\\\n\t         --errors-for-leak-kinds=definite,indirect --error-exitcode=9 \\\\\n\t         --trace-children=no ./$(BIN)\n\nclean:\n\trm -f $(BIN) $(LITTER)''', '''BIN   := embed\nQUICK := quickstart\n# Written by one of the programs, removed by the same run — unless the run\n# aborts, which is what the assertions are for. Since `run` starts in a temp\n# directory this only matters to someone who ran ./embed by hand from here.\nLITTER := lypning-embed-example.txt\n\n.PHONY: all run valgrind quickstart clean\nall: $(BIN)\n\n$(BIN): embed.c $(header) $(lib) $(MAKEFILE_LIST)\n\t$(need_library)\n\t$(CC) $(CFLAGS) -I$(INCDIR) embed.c -o $@ $(link) $(LDFLAGS) $(LDLIBS)\n\t@echo \"$@: header $(INCDIR)/lypning.h, library $(LIBDIR) ($(from))\"\n\nquickstart: $(QUICK)\n$(QUICK): quickstart.c $(header) $(lib) $(MAKEFILE_LIST)\n\t$(need_library)\n\t$(CC) $(CFLAGS) -I$(INCDIR) quickstart.c -o $@ $(link) $(LDFLAGS) $(LDLIBS)\n\t@echo \"$@: header $(INCDIR)/lypning.h, library $(LIBDIR) ($(from))\"\n\n# Run from a fresh temp directory and remove it after, carrying the exit\n# status across the cleanup so a failed run still fails make.\ndefine in_tmpdir\n\t@d=$$(mktemp -d) && (cd \"$$d\" && $(1)); s=$$?; rm -rf \"$$d\"; exit $$s\nendef\n\nrun: $(BIN)\n\t$(call in_tmpdir,$(here)/$(BIN))\n\n# The example frees every handle it takes, so a leak here is the library's or\n# ours and either is worth knowing. --trace-children stays off on purpose: the\n# fallback exec's python3, and valgrinding CPython measures CPython.\nvalgrind: $(BIN)\n\t@command -v valgrind >/dev/null 2>&1 || { \\\\\n\t  echo \"valgrind is not installed on this machine; skipping\"; exit 0; }; \\\\\n\t$(call in_tmpdir,valgrind --leak-check=full --show-leak-kinds=definite$(,)indirect \\\\\n\t         --errors-for-leak-kinds=definite$(,)indirect --error-exitcode=9 \\\\\n\t         --trace-children=no $(here)/$(BIN))\n\nclean:\n\trm -f $(BIN) $(QUICK) $(LITTER)''')\np.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:23:33.275Z","count":1,"stdin_sample":null}
{"key":"py-03f4d33362b8","id":"py-03f4d33362b8","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        v=r.get('result') or r.get('value') or {}\n        print('DONE', v.get('host'), 'verdict' if 'probes_pass' in v else 'report', v.get('builds',''), v.get('probes_pass',''), v.get('contract_test_passes',''))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:12:36.330Z","count":1,"stdin_sample":null}
{"key":"py-08257bceaa38","id":"py-08257bceaa38","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_9f7e1130-3b4/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'host' not in v: continue\n    print('='*100); print('HOST', v['host'], 'built', v['built'], 'ran', v['ran'])\n    print('LOG:', (v.get('build_log_tail') or '')[:600])\n    for f in v['findings']:\n        print('- [%s] %s:%s — %s' % (f['kind'], f['file'], f.get('line'), f['summary']))\n        print('    ', f['detail'][:900].replace('\\n',' '))\n    print('PLAN:', v['simple_example_plan'][:2500])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:14:30.355Z","count":1,"stdin_sample":null}
{"key":"py-09e9db01fc4d","id":"py-09e9db01fc4d","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nprint('REPORT KEYS:', list(d['reports'].keys()))\nprint('VERDICT KEYS:', list(d['verdicts'].keys()) if isinstance(d['verdicts'],dict) else len(d['verdicts']))\nfor k,v in d['reports'].items():\n    if k.startswith('python-plumbing'):\n        print('=== REPORT', k); print(json.dumps(v,indent=1))\nvs=d['verdicts']\nfor k,v in (vs.items() if isinstance(vs,dict) else enumerate(vs)):\n    if 'plumbing' in str(k) or 'plumbing' in json.dumps(v)[:400]:\n        print('=== VERDICT', k); print(json.dumps(v,indent=1))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:57.637Z","count":1,"stdin_sample":null}
{"key":"py-0ba3920f6402","id":"py-0ba3920f6402","program":"import sys,json; print(list(json.loads(sys.stdin.readline()).keys()))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:08:41.939Z","count":2,"stdin_sample":null}
{"key":"py-0ce628827360","id":"py-0ce628827360","program":"p = \"test.lua\"; s = open(p).read()\nold = '''local function probe(src, extra)\n  local p = io.popen(\"luajit \" .. qs .. \" \" .. shell_quote(src) .. \" \" .. (extra or \"\") .. \" 2>/tmp/lypning-lua-qs.err; echo \\\\\"exit=$?\\\\\"\")\n  local o = p:read(\"*a\")\n  p:close()\n  local e = io.open(\"/tmp/lypning-lua-qs.err\", \"rb\")\n  local err = e and e:read(\"*a\") or \"\"\n  if e then e:close() end\n  os.remove(\"/tmp/lypning-lua-qs.err\")\n  return o, err\nend'''\nnew = '''local function probe(src, extra)\n  local errfile = os.tmpname()\n  local p = io.popen(\"luajit \" .. qs .. \" \" .. shell_quote(src) .. \" \" .. (extra or \"\") ..\n                     \" 2>\" .. shell_quote(errfile) .. \"; echo \\\\\"exit=$?\\\\\"\")\n  local o = p:read(\"*a\")\n  p:close()\n  local e = io.open(errfile, \"rb\")\n  local err = e and e:read(\"*a\") or \"\"\n  if e then e:close() end\n  os.remove(errfile)\n  return o, err\nend'''\nassert old in s; open(p, \"w\").write(s.replace(old, new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:36:06.403Z","count":1,"stdin_sample":null}
{"key":"py-0d8e54278ffb","id":"py-0d8e54278ffb","program":"import json,sys; print([ (p['name'],p['rust_version']) for p in json.load(sys.stdin)['packages']])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:00:18.837Z","count":2,"stdin_sample":null}
{"key":"py-0f5258acfb59","id":"py-0f5258acfb59","program":"\nimport sys; sys.path.insert(0,'src')\nfrom lypning import embed\nfrom pathlib import Path\nfull = Path('src/lypning/assets/rust/target/release-lib/liblypning.dylib').read_bytes()\nPath('/tmp/half.dylib').write_bytes(full[:200_000]); print('half:', embed._looks_like_a_library(Path('/tmp/half.dylib')))\nPath('/tmp/tiny.dylib').write_bytes(full[:100]); print('tiny:', embed._looks_like_a_library(Path('/tmp/tiny.dylib')))\nPath('/tmp/junk.dylib').write_bytes(b'not an elf file'); print('junk:', embed._looks_like_a_library(Path('/tmp/junk.dylib')))\nprint('fat /bin/ls:', repr(embed._looks_like_a_library(Path('/bin/ls'))))\nfat = Path('/bin/ls').read_bytes(); Path('/tmp/halfls').write_bytes(fat[:len(fat)//2]); print('half fat:', embed._looks_like_a_library(Path('/tmp/halfls')))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:24:56.785Z","count":1,"stdin_sample":null}
{"key":"py-109dc0ca1266","id":"py-109dc0ca1266","program":"p=\"Sources/Lypning/Lypning.swift\"\ns=open(p).read()\nold='''#if LYPNING_LIBRARY_MISSING\n// Package.swift looked under ../rust/target/release-lib and $LYPNING_HOME/lib\n// (~/.lypning/lib) and found no liblypning. Said here, once, instead of by the\n// linker as a page of undefined symbols after everything else compiled.\n#error(\"no liblypning to link: run `lypning build --lib`, or point LYPNING_HOME at a tree that has lib/liblypning.dylib (or .so)\")\n#endif\n'''\nnew='''#if LYPNING_LIBRARY_MISSING\n// Package.swift looked under ../rust/target/release-lib and $LYPNING_HOME/lib\n// (~/.lypning/lib) and found no liblypning. Said here, once, instead of by the\n// linker as a page of undefined symbols after everything else compiled.\n//\n// The message names the cache because SwiftPM keeps what a manifest decided,\n// keyed on the manifest's text and the environment, not on the files it\n// looked at: without the purge + reset this line would still be printed after\n// the library was built, and a message that outlives its cause is a lie.\n#error(\"no liblypning to link: run `lypning build --lib` (or point LYPNING_HOME at a tree with lib/liblypning.dylib or .so), then `swift package purge-cache && swift package reset`, because SwiftPM cached this manifest's answer\")\n#endif\n'''\nassert old in s; s=s.replace(old,new); open(p,\"w\").write(s); print(\"Lypning.swift edited\")\n\np=\"Package.swift\"\ns=open(p).read()\nold='''// `swift build` writes .build/ beside this file. In a read-only wheel tree\n// give it somewhere else: `swift build --scratch-path \"$TMPDIR/lypning-swift\"`.\n'''\nnew='''// `swift build` writes .build/ beside this file. In a read-only wheel tree\n// give it somewhere else: `swift build --scratch-path \"$TMPDIR/lypning-swift\"`.\n//\n// ONE CAVEAT, measured rather than assumed: SwiftPM caches what this manifest\n// decided, keyed on the manifest's text and the environment, not on the files\n// it looked at. So the choice below is frozen for a given tree and shell until\n// `swift package purge-cache && swift package reset`. Build the library first\n// and there is nothing to notice; build it second and that pair is the fix.\n// Passing both directories unconditionally would sidestep the cache, but the\n// linker warns about a search path that does not exist, and one of the two\n// never exists in either shape of the tree.\n'''\nassert old in s; s=s.replace(old,new); open(p,\"w\").write(s); print(\"Package.swift edited\")\n\np=\"README.md\"\ns=open(p).read()\nold='''`lypning build --lib` puts the library. If neither has it, the first compile\nstops on one line naming `lypning build --lib`, before the linker can print a\npage of undefined symbols. The paths go in as `unsafeFlags`,\nso this package is consumed in-tree or by `.package(path:)`, never from a\nregistry; that is the honest way to link a library whose location is decided\nby a tool SwiftPM knows nothing about.\n'''\nnew='''`lypning build --lib` puts the library. If neither has it, the first compile\nstops on one line naming `lypning build --lib`, before the linker can print a\npage of undefined symbols. Build the library first and that is all there is to\nit; build it second and run `swift package purge-cache && swift package reset`\nonce, because SwiftPM caches what `Package.swift` decided, keyed on its text\nand your environment rather than on the files it looked at. The paths go in as\n`unsafeFlags`, so this package is consumed in-tree or by `.package(path:)`,\nnever from a registry; that is the honest way to link a library whose location\nis decided by a tool SwiftPM knows nothing about.\n'''\nassert old in s; s=s.replace(old,new); open(p,\"w\").write(s); print(\"README edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:01:59.589Z","count":1,"stdin_sample":null}
{"key":"py-11b18b8de8cc","id":"py-11b18b8de8cc","program":"#!/bin/sh\npython3 -c \"$@\"; rc=$?; [ $rc -eq 1 ] && python3 -c \"$@\"; exit $rc","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:22:29.977Z","count":2,"stdin_sample":null}
{"key":"py-134ff41c8647","id":"py-134ff41c8647","program":"\nimport json,sys\nfor ln in open(\"/tmp/lua-capture.jsonl\"):\n    r = json.loads(ln); print(sorted(r.keys()) == sorted([\"kind\",\"ts\",\"session\",\"shim\",\"pid\",\"program\",\"module\",\"script\",\"argv_tail\",\"stdin_pipe\",\"stdin_kind\",\"exit_code\",\"wall_ms\"]), r[\"shim\"], r[\"exit_code\"], repr(r[\"program\"])[:60])\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:34:25.288Z","count":1,"stdin_sample":null}
{"key":"py-13a783c4d6c6","id":"py-13a783c4d6c6","program":"\nfrom lypning import embed; ok,why=embed.check_refusal_contract(); print('library contract:', ok, why)\nlib=embed.Library(); print(lib.run('import random; random.seed(1); print(random.randint(1,100))').stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T11:39:08.477Z","count":1,"stdin_sample":null}
{"key":"py-158270f873aa","id":"py-158270f873aa","program":"p='src/lypning/cli.py'; s=open(p).read()\nold='''                   help=\"build the embeddable C ABI (the shared library, liblypning.a and \"\n                        \"the headers) into ~/.lypning/lib and ~/.lypning/include — for C, C++, \"\n                        \"Rust and Node hosts that run programs in-process instead of \"\n                        \"spawning them\")'''\nnew='''                   help=\"build the embeddable C ABI (the shared library, liblypning.a and \"\n                        \"the headers) into ~/.lypning/lib and ~/.lypning/include — for every \"\n                        \"host that runs programs in-process instead of spawning them \"\n                        \"(the table in docs/EMBEDDING.md section 4)\")'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new)); print('cli.py edited')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:21:00.306Z","count":1,"stdin_sample":null}
{"key":"py-16ce032fc923","id":"py-16ce032fc923","program":"import re\ns = open(\".github/workflows/ci.yml\").read()\nm = re.search(r\"cat >\\\"\\$RUNNER_TEMP/probes.sh\\\" <<'EOF'\\n(.*?)\\n          EOF\\n\", s, re.S)\nprint(\"\\n\".join(l[10:] for l in m.group(1).splitlines()))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:22:29.977Z","count":1,"stdin_sample":null}
{"key":"py-17764d751e95","id":"py-17764d751e95","program":"import json,os\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_5840e5e0-41b/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        v=r.get('result') or r.get('value') or {}\n        print('DONE', (v.get('task') or 'review')[:70])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:08:05.986Z","count":1,"stdin_sample":null}
{"key":"py-1885c57aefb1","id":"py-1885c57aefb1","program":"import pathlib\np = pathlib.Path(\"lypning.h\")\ns = p.read_text()\ndef rep(old, new):\n    global s\n    assert s.count(old) == 1, old\n    s = s.replace(old, new)\n\nrep(''' *     lypning_result *r = lypning_run(q);\n *     if (lypning_result_should_fall_onward(r)) {\n *         run_it_on_python3(src);          // your existing path, unchanged\n *     } else {\n *         use(lypning_result_stdout(r, &n),\n *             lypning_result_exit_code(r));\n *     }\n *     lypning_result_free(r);\n *\n * What makes that safe''', ''' * The complete minimal host. Nothing in it is optional, and there is nothing\n * else to it (assets/examples/c/quickstart.c is this, buildable):\n *\n *     lypning_request *q = lypning_request_new(src, len);\n *     if (q == NULL) {                        // not UTF-8: that is CPython's\n *         return run_it_on_python3(src);      // to reject, not ours to report\n *     }\n *     lypning_request_set_step_limit(q, 10000000);   // no process to kill\n *     lypning_result *r = lypning_run(q);\n *     if (lypning_result_should_fall_onward(r)) {\n *         run_it_on_python3(src);              // your existing path, unchanged\n *     } else {\n *         use(lypning_result_stdout(r, &n),    // bytes, with a length\n *             lypning_result_stderr(r, &m),\n *             lypning_result_exit_code(r));    // a traceback IS the answer: 1\n *     }\n *     lypning_result_free(r);\n *     lypning_request_free(q);\n *\n * What makes that safe''')\n\nrep(''' *   * Every handle is opaque and is freed with its own _free. Freeing NULL is\n *     fine; every accessor tolerates NULL and answers 0, \"\" or NULL.\n''', ''' *   * Every handle is opaque and is freed with its own _free. Freeing NULL is\n *     fine, and every accessor tolerates a NULL handle: integers answer 0 (or\n *     -1 where the comment says so), every `const char *` answers \"\" and\n *     NEVER NULL, and the two byte buffers answer NULL with a length of 0.\n *     So a host that forgot a check reads an empty string, not a fault.\n''')\n\nrep('''/* The runtime version, e.g. \"0.1.0\". Static storage; do not free. */\nconst char *lypning_version(void);''', '''/* The runtime version, e.g. \"0.1.0\". Static storage, never NULL; do not free. */\nconst char *lypning_version(void);''')\n\nrep('''lypning_route *lypning_route_new(const char *src, size_t len);\n/* \"lypning\", \"lypning-mp\" or \"cpython\". */\nconst char *lypning_route_engine(const lypning_route *r);\n/* The construct that pushed it past lypning (\"module\", \"async\", …), or \"\". */\nconst char *lypning_route_kind(const lypning_route *r);\n/* Its detail (\"import re\"), or \"\". */\nconst char *lypning_route_detail(const lypning_route *r);\nsize_t lypning_route_import_count(const lypning_route *r);\n/* The i'th import, or NULL when i is out of range. SORTED AND DEDUPLICATED, not\n * in source order: the question is which modules a program needs, which has no\n * order. For the one import that decided the tier, read lypning_route_detail. */\nconst char *lypning_route_import(const lypning_route *r, size_t i);''', '''/* NULL if `src` is not UTF-8 or is NULL itself. */\nlypning_route *lypning_route_new(const char *src, size_t len);\n/* \"lypning\", \"lypning-mp\" or \"cpython\". \"\" for a NULL handle. */\nconst char *lypning_route_engine(const lypning_route *r);\n/* The construct that pushed it past lypning (\"module\", \"async\", …), or \"\".\n * Also \"\" for a NULL handle. */\nconst char *lypning_route_kind(const lypning_route *r);\n/* Its detail (\"import re\"), or \"\". Also \"\" for a NULL handle. */\nconst char *lypning_route_detail(const lypning_route *r);\n/* 0 for a NULL handle. */\nsize_t lypning_route_import_count(const lypning_route *r);\n/* The i'th import, or NULL when i is out of range. SORTED AND DEDUPLICATED, not\n * in source order: the question is which modules a program needs, which has no\n * order. For the one import that decided the tier, read lypning_route_detail.\n * NULL is the loop terminator and only that: a NULL handle answers \"\", like\n * every other string accessor, and has an import count of 0. */\nconst char *lypning_route_import(const lypning_route *r, size_t i);''')\n\nrep('''/* `src` is UTF-8 Python source, `len` bytes. NULL if it is not UTF-8. */\nlypning_request *lypning_request_new(const char *src, size_t len);''', '''/* `src` is UTF-8 Python source, `len` bytes. NULL if it is not UTF-8.\n *\n * A NULL here MUST BE ROUTED ONWARD, exactly like a refusal: lypning has run\n * none of it and has nothing to say about it, and whether the bytes are a\n * program at all is CPython's to decide with its own message. It is not an\n * error to report, and not a reason to stop. The snippet at the top of this\n * file shows the branch. */\nlypning_request *lypning_request_new(const char *src, size_t len);''')\n\nrep('''/* The program's stdout. Empty after a refusal, by the commit barrier. The\n * pointer is non-NULL even when the length is 0. `len` may be NULL. */\nconst uint8_t *lypning_result_stdout(const lypning_result *r, size_t *len);''', '''/* The program's stdout. Empty after a refusal, by the commit barrier. The\n * pointer is non-NULL even when the length is 0; NULL, with `*len` set to 0,\n * only for a NULL result. `len` may be NULL. */\nconst uint8_t *lypning_result_stdout(const lypning_result *r, size_t *len);''')\n\nrep('''/* The refusal's two halves, so you can branch on the kind without parsing the\n * line apart: \"module\" / \"import re\". \"\" when the run was not a refusal. */\nconst char *lypning_result_kind(const lypning_result *r);''', '''/* The refusal's two halves, so you can branch on the kind without parsing the\n * line apart: \"module\" / \"import re\". \"\" when the run was not a refusal, and\n * \"\" for a NULL result. */\nconst char *lypning_result_kind(const lypning_result *r);''')\n\np.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:24:15.468Z","count":1,"stdin_sample":null}
{"key":"py-1a880a64975c","id":"py-1a880a64975c","program":"from lypning import embed; l=embed.Library(); print(l.route(\"import subprocess; print(1)\")); o=l.run(\"import subprocess; print(1)\"); print(o.status_name, o.exit_code, o.fall_onward, o.stdout, o.stderr)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:31:05.054Z","count":1,"stdin_sample":null}
{"key":"py-1b80e4a352d3","id":"py-1b80e4a352d3","program":"import _ctypes, os; print(_ctypes.__file__, os.path.isfile(_ctypes.__file__))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:09:26.992Z","count":1,"stdin_sample":null}
{"key":"py-1cd9f811dee4","id":"py-1cd9f811dee4","program":"import pathlib\np = pathlib.Path(\"tests/test_packaging.py\"); s = p.read_text()\nold = '''def test_manifest_prunes_every_build_directory() -> None:\n    \"\"\"`include_package_data = true` makes MANIFEST.in a wheel-contents file.\n\n    These three directories are where cargo and the MicroPython make put their\n    object files. Nothing names them today; the prunes are what keeps a future\n    `graft src` from putting a gigabyte inside `pip install lypning`.\n    \"\"\"\n    text = MANIFEST.read_text()\n    for d in (\"src/lypning/assets/rust/target\",\n              \"src/lypning/assets/micropython/build\",\n              \"src/lypning/assets/micropython/.build\"):\n        assert re.search(r\"^prune %s$\" % re.escape(d), text, re.M), d\n'''\nnew = '''def test_manifest_prunes_every_build_directory() -> None:\n    \"\"\"`include_package_data = true` makes MANIFEST.in a wheel-contents file.\n\n    These directories are where cargo, the MicroPython make, npm and SwiftPM\n    put their object files: the core crate, the MicroPython tree, and one per\n    host over the C ABI that has a build step (the Rust example and the Node\n    addon each grow a cargo `target/`, npm a `node_modules/`, SwiftPM a\n    `.build/` plus the `.swiftpm/` an IDE adds, the swiftc Makefile a\n    `build/`). Go writes only to GOCACHE and LuaJIT has no build step, so\n    neither has a prune and neither is missing one.\n\n    Nothing in MANIFEST.in names any of them today; the prunes are what keeps\n    a future `graft src` from putting a gigabyte inside `pip install lypning`.\n    Every prune the file has is pinned here, so dropping one is a deliberate\n    edit in two places rather than a line lost in a tidy-up: the failure it\n    guards against is invisible in the checkout and visible only in the size\n    of a wheel nobody weighs.\n    \"\"\"\n    text = MANIFEST.read_text()\n    for d in (\"src/lypning/assets/rust/target\",\n              \"src/lypning/assets/micropython/build\",\n              \"src/lypning/assets/micropython/.build\",\n              \"src/lypning/assets/examples/rust/target\",\n              \"src/lypning/assets/node/target\",\n              \"src/lypning/assets/node/node_modules\",\n              \"src/lypning/assets/swift/.build\",\n              \"src/lypning/assets/swift/.swiftpm\",\n              \"src/lypning/assets/swift/build\"):\n        assert re.search(r\"^prune %s$\" % re.escape(d), text, re.M), d\n'''\nassert old in s\np.write_text(s.replace(old, new))\nprint(\"test edit ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:08:23.186Z","count":1,"stdin_sample":null}
{"key":"py-1dc0493d5373","id":"py-1dc0493d5373","program":"import pathlib\nr = pathlib.Path(\"README.md\"); s = r.read_text()\nold = \"\"\"`FallOnward` is true exactly when lypning declined *and left nothing behind*:\nnothing printed, no file touched, no stdin consumed, which is what makes the\nretry safe. `Run` never returns an error and never panics for a program; a\nrefusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` or an argument that is not UTF-8 (the ABI will not build a request for\none, and that too is CPython's to reject with its own message). `Route(src)` asks without\nrunning. `quickstart/main.go` is the complete minimal host, and the file to\ncopy.\n\"\"\"\nnew = \"\"\"`FallOnward` is true exactly when lypning declined *and left nothing behind*:\nnothing printed, no file touched, no stdin consumed, which is what makes the\nretry safe. `Run` never returns an error and never panics for a program; a\nrefusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` or an argument that is not UTF-8 (the ABI will not build a request for\none, and that too is CPython's to reject with its own message). `Route(src)`\nasks without running. `quickstart/main.go` is the complete minimal host, and\nthe file to copy.\n\"\"\"\nassert old in s; r.write_text(s.replace(old, new)); print(\"rewrapped\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:45:33.253Z","count":1,"stdin_sample":null}
{"key":"py-1f0bef1fe1d2","id":"py-1f0bef1fe1d2","program":"p=\"test.lua\"; s=open(p).read()\nold='''local function far(code)\n  local p = io.popen(\"LYPNING_HOME=/nonexistent luajit -e \" .. shell_quote('''\nnew='''-- $LYPNING_LIB is cleared too (empty is unset), or a suite run with it set\n-- would hand this child the very library the check says is absent.\nlocal function far(code)\n  local p = io.popen(\"LYPNING_LIB= LYPNING_HOME=/nonexistent luajit -e \" .. shell_quote('''\nassert s.count(old)==1; open(p,\"w\").write(s.replace(old,new)); print(\"edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:29:37.876Z","count":1,"stdin_sample":null}
{"key":"py-1fc38126553d","id":"py-1fc38126553d","program":"import time,re\nt0=time.time()\nwhile time.time()-t0 < 540:\n    s=open('/tmp/lyp-final-pytest.log').read()\n    if 'EXIT' in s: break\n    time.sleep(20)\nm=re.findall(r'\\[ *\\d+%\\]', s); n=len(re.sub(r'\\[ *\\d+%\\]|\\n|EXIT.*','',s))\nprint('progress', m[-1] if m else '?', 'chars', n, 'done' if 'EXIT' in s else 'running')\nif 'EXIT' in s: print(s[-1500:])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:19:40.227Z","count":1,"stdin_sample":null}
{"key":"py-2043fa844984","id":"py-2043fa844984","program":"import re, os\ndef sec(path, a, b):\n    t = open(path, encoding=\"utf-8\").read(); i = t.index(a); j = t.index(b, i); return t[i:j]\ntext = sec(\"docs/EMBEDDING.md\", \"## 4.\", \"## 5.\") + sec(\"README.md\", \"## 3b.\", \"## 4.\")\npaths = sorted({p for p in re.findall(r\"`([A-Za-z0-9_./-]+)`\", text) if \"/\" in p})\nbad = [p for p in paths if not (os.path.exists(p) or os.path.exists(\"src/lypning/\" + p))]\nprint(len(paths), \"paths cited;\", \"missing:\", bad or \"none\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:26:49.252Z","count":1,"stdin_sample":null}
{"key":"py-20a045f3cfa3","id":"py-20a045f3cfa3","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor k,v in d['reports'].items():\n    print('=====',k)\n    for f in ('files_created','files_modified','build_and_run','ci_step','left_undone'):\n        print('--',f,':',v.get(f))\nprint('##### VERDICTS')\nfor k,v in d['verdicts'].items():\n    print('=====',k)\n    print(v.get('open') if isinstance(v,dict) else v)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:10.946Z","count":1,"stdin_sample":null}
{"key":"py-20ff7b014acf","id":"py-20ff7b014acf","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nn=0\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        n+=1; v=r.get('result') or r.get('value') or {}\n        print(n, v.get('host'), 'verdict' if 'probes_pass' in v else 'report', v.get('builds',''), v.get('probes_pass',''), v.get('contract_test_passes',''))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:32:54.468Z","count":1,"stdin_sample":null}
{"key":"py-2314ec6f985b","id":"py-2314ec6f985b","program":"p='src/lypning/assets/go/lypning.go'; s=open(p).read()\nold='''// and a link error naming -llypning means neither is true yet: run\n// `lypning build --lib` first. The checkout default is harmless when the\n// directory is absent, because the linker only complains about a -L it cannot\n// use once it also fails to find the library.\n'''\nnew='''// and a link error naming -llypning means neither is true yet: run\n// `lypning build --lib` first. The checkout default costs nothing when the\n// directory is absent: GNU ld ignores a -L it cannot find, and macOS ld warns\n// \"search path not found\" and then links exactly as it would have. Neither\n// changes which library the binary loads.\n'''\nassert old in s; s=s.replace(old,new); open(p,'w').write(s)\np='src/lypning/assets/go/README.md'; s=open(p).read()\nold='''`lypning lib --libs` carries `-Wl,-rpath` on purpose: without it the binary\nlinks cleanly and dies at exec, because nothing runs `ldconfig` over\n`~/.lypning/lib`.\n'''\nnew='''`lypning lib --libs` carries `-Wl,-rpath` on purpose: without it the binary\nlinks cleanly and dies at exec, because nothing runs `ldconfig` over\n`~/.lypning/lib`.\n\nThe checkout default stays in the directives either way, and costs nothing\nwhen its directory is absent: GNU ld ignores a `-L` it cannot find, and macOS\nld says so (`ld: warning: search path '...' not found`) and then links exactly\nas it would have. macOS ld also notes that cgo hands it `CGO_LDFLAGS` twice\n(`duplicate -rpath`, `ignoring duplicate libraries`); those are Go's, not\nlypning's, and none of the three changes which library the binary loads. In a\ncheckout the build is warning-free.\n'''\nassert old in s; s=s.replace(old,new); open(p,'w').write(s)\nprint('docs edited')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:11:28.442Z","count":1,"stdin_sample":null}
{"key":"py-24fcf37abc4a","id":"py-24fcf37abc4a","program":"from pathlib import Path\np = Path(\"tests/test_embed.py\")\ns = p.read_text()\n\nold = '''import subprocess\nimport threading\nfrom pathlib import Path\n\nimport pytest\n\nfrom typing import List\n\nfrom lypning import embed, engines, paths\nfrom conftest import _INSTALLED_LIBRARY\n\n\ndef _INSTALLED_LIBRARY_OR_SKIP() -> embed.Library:\n    if _INSTALLED_LIBRARY is None:\n        pytest.skip(\"the C ABI is not built (`lypning build --lib`)\")\n    return embed.Library(_INSTALLED_LIBRARY)\n'''\nnew = '''import struct\nimport subprocess\nimport threading\nfrom pathlib import Path\n\nimport pytest\n\nfrom typing import List\n\nfrom lypning import build, embed, engines, paths\nfrom conftest import _INSTALLED_LIBRARY\n\n\ndef _INSTALLED_LIBRARY_OR_SKIP() -> embed.Library:\n    if _INSTALLED_LIBRARY is None:\n        pytest.skip(\"the C ABI is not built (`lypning build --lib`)\")\n    try:\n        return embed.Library(_INSTALLED_LIBRARY)\n    except embed.LibraryError as e:\n        # The same rule the `lypning_lib` fixture holds: a stale build is a\n        # skip with a reason, not a failure reported against the runtime.\n        pytest.skip(\"the C ABI at %s is not usable: %s\" % (_INSTALLED_LIBRARY, e))\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''    nm = subprocess.run([\"nm\", \"-D\", \"--defined-only\", str(lypning_lib.path)],\n                        capture_output=True, text=True, check=False)\n    if nm.returncode != 0:\n        pytest.skip(\"nm is not available\")\n    exported = {line.split()[-1] for line in nm.stdout.splitlines()\n                if \" lypning_\" in line}\n'''\nnew = '''    try:\n        exported = {n for n in build.exported_symbols(lypning_lib.path)\n                    if n.startswith(\"lypning_\")}\n    except OSError as e:\n        pytest.skip(\"cannot list the library's exported symbols: %s\" % e)\n    assert exported, \"nm listed no lypning_ symbols at all, which is not a C ABI\"\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''    half = tmp_path / \"liblypning.so\"\n    half.write_bytes(lypning_lib.path.read_bytes()[:200_000])\n'''\nnew = '''    half = tmp_path / embed.LIB_NAME\n    half.write_bytes(lypning_lib.path.read_bytes()[:200_000])\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''def test_a_foreign_library_is_named_as_such(monkeypatch, tmp_path):\n    other = Path(\"/lib/x86_64-linux-gnu/libm.so.6\")\n    if not other.is_file():\n        pytest.skip(\"no other shared library to point at\")\n    monkeypatch.setenv(\"LYPNING_LIB\", str(other))\n    usable, why = engines.library_ready()\n    assert usable is False\n    assert \"lypning\" in why\n\n\ndef test_engines_reports_an_unusable_library_rather_than_failing_runs(monkeypatch, tmp_path):\n    fake = tmp_path / \"liblypning.so\"\n    fake.write_bytes(b\"not an elf file\")\n'''\nnew = '''def test_a_foreign_library_is_named_as_such(monkeypatch, tmp_path):\n    \"\"\"A shared object that loads and is not ours must be reported as not\n    ours, not as a crash on the first missing symbol. ``_ctypes`` is the one\n    shared object every CPython that can run this suite is guaranteed to have,\n    on every platform.\"\"\"\n    import _ctypes\n    other = Path(getattr(_ctypes, \"__file__\", \"\") or \"\")\n    if not other.is_file() or other.suffix not in (\".so\", \".dylib\", \".pyd\"):\n        pytest.skip(\"_ctypes is built into this interpreter; no other shared library to point at\")\n    monkeypatch.setenv(\"LYPNING_LIB\", str(other))\n    usable, why = engines.library_ready()\n    assert usable is False\n    assert \"lypning\" in why\n\n\ndef test_engines_reports_an_unusable_library_rather_than_failing_runs(monkeypatch, tmp_path):\n    fake = tmp_path / embed.LIB_NAME\n    fake.write_bytes(b\"not an elf file\")\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\ns += '''\n\n# --- the shallow format check, without a compiler --------------------------\n\n\ndef _macho_dylib(linkedit_end: int) -> bytes:\n    \"\"\"A minimal Mach-O 64 image: a header and one LC_SEGMENT_64 for\n    ``__LINKEDIT`` whose file extent ends at ``linkedit_end``.\"\"\"\n    seg = struct.pack(\"<II16sQQQQIIII\", 0x19, 72, b\"__LINKEDIT\", 0, 0,\n                      0, linkedit_end, 0, 0, 0, 0)\n    header = struct.pack(\"<IiiIIIII\", 0xFEEDFACF, 0x0100000C, 0, 6, 1, len(seg), 0, 0)\n    return header + seg\n\n\ndef test_the_format_check_reads_mach_o_the_mach_o_way(tmp_path):\n    \"\"\"The check is not \"starts with the right bytes\": a Mach-O image ends\n    with its ``__LINKEDIT`` segment, so that is the extent it asks about, the\n    way it asks ELF about the section table.\"\"\"\n    whole = tmp_path / embed.LIB_NAME\n    image = _macho_dylib(linkedit_end=4096)\n    whole.write_bytes(image + b\"\\\\0\" * (4096 - len(image)))\n    assert embed._looks_like_a_library(whole) == \"\"\n\n    short = tmp_path / (\"short-\" + embed.LIB_NAME)\n    short.write_bytes(image + b\"\\\\0\" * 100)\n    why = embed._looks_like_a_library(short)\n    assert \"truncated\" in why and \"4096\" in why\n\n    headerless = tmp_path / (\"headerless-\" + embed.LIB_NAME)\n    headerless.write_bytes(image[:40])\n    assert \"truncated\" in embed._looks_like_a_library(headerless)\n\n\ndef test_the_format_check_still_reads_elf(tmp_path):\n    \"\"\"The ELF path is untouched by the Mach-O one: a header whose section\n    table lies past the end of the file is the truncation it always caught.\"\"\"\n    head = bytearray(64)\n    head[:4] = b\"\\\\x7fELF\"\n    head[4] = 2          # 64-bit\n    head[5] = 1          # little-endian\n    head[0x28:0x30] = (1 << 20).to_bytes(8, \"little\")   # e_shoff\n    head[0x3A:0x3C] = (64).to_bytes(2, \"little\")         # e_shentsize\n    head[0x3C:0x3E] = (4).to_bytes(2, \"little\")          # e_shnum\n    elf = tmp_path / \"liblypning.so\"\n    elf.write_bytes(bytes(head))\n    assert \"truncated\" in embed._looks_like_a_library(elf)\n\n\ndef test_the_shared_library_name_is_decided_once():\n    \"\"\"The build, the installer and discovery must agree on the file name, or\n    a build reports ok about a file discovery never finds.\"\"\"\n    assert build.LIB_SHARED == embed.LIB_NAME\n    assert embed.shared_library_name(\"darwin\") == \"liblypning.dylib\"\n    assert embed.shared_library_name(\"linux\") == \"liblypning.so\"\n    assert embed.find_library.__doc__  # the candidates below read LIB_NAME\n    assert (embed.lib_dir() / embed.LIB_NAME).name == embed.LIB_NAME\n'''\np.write_text(s)\nprint(\"test_embed ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:26:29.455Z","count":1,"stdin_sample":null}
{"key":"py-2504ccf9f734","id":"py-2504ccf9f734","program":"p = \"src/lypning/assets/node/README.md\"\ns = open(p).read()\nold = \"const lypning = require('./index.js');\\n\\n// stepLimit:\"\nnew = \"const lypning = require('./index.js');\\n\\nconst [src, ...args] = process.argv.slice(2);\\n\\n// stepLimit:\"\nassert s.count(old) == 1\nopen(p, \"w\").write(s.replace(old, new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:14:44.172Z","count":1,"stdin_sample":null}
{"key":"py-27bd0f0e59c3","id":"py-27bd0f0e59c3","program":"from pathlib import Path\np = Path(\"src/lypning/embed.py\")\ns = p.read_text()\n\nold = \"\"\"import ctypes\nimport os\nfrom dataclasses import dataclass, field\n\"\"\"\nnew = \"\"\"import ctypes\nimport os\nimport sys\nfrom dataclasses import dataclass, field\n\"\"\"\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''LIB_NAME = \"liblypning.so\"\n'''\nnew = '''\ndef shared_library_name(platform: str = sys.platform) -> str:\n    \"\"\"``liblypning.dylib`` on macOS, ``liblypning.so`` everywhere else.\n\n    The ONE place the file name is decided. Every other spelling in the package\n    — the build's output, the installer's destination, discovery, the help text\n    — reads :data:`LIB_NAME`, because two spellings is a build that succeeds on\n    one platform and installs a file nothing will ever find on the other.\n    \"\"\"\n    return \"liblypning.dylib\" if platform == \"darwin\" else \"liblypning.so\"\n\n\n#: The shared library's file name on THIS platform.\nLIB_NAME = shared_library_name()\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = s[s.index(\"def _looks_like_a_library(path: Path) -> str:\"):s.index(\"# --- results ---\")]\nnew = '''_ELF_MAGIC = b\"\\\\x7fELF\"\n#: MH_MAGIC_64 as it sits on disk (little-endian), and its byte-swapped twin.\n_MACHO_64_MAGICS = (b\"\\\\xcf\\\\xfa\\\\xed\\\\xfe\", b\"\\\\xfe\\\\xed\\\\xfa\\\\xcf\")\n#: A universal (\"fat\") image: several Mach-O slices behind one big-endian table.\n_FAT_MAGIC = b\"\\\\xca\\\\xfe\\\\xba\\\\xbe\"\n_LC_SEGMENT_64 = 0x19\n_MACHO_64_HEADER = 32\n\n\ndef _looks_like_a_library(path: Path) -> str:\n    \"\"\"``\"\"`` if the file can be handed to ``dlopen``, else the reason it cannot.\n\n    Deliberately shallow: the ELF or Mach-O magic and the size the file's own\n    headers imply. It cannot prove a library is loadable — only the loader can\n    — but it catches the case that would otherwise be fatal rather than\n    reportable, a file truncated mid-write.\n\n    Each format says where its last byte is in its own way. ELF puts the\n    section table last, so its end is the file's end. A Mach-O image ends with\n    its ``__LINKEDIT`` segment (symbol and relocation tables), so the check\n    walks the load commands to that segment and asks whether it fits. A fat\n    image is a table of slices, and every slice has to fit.\n    \"\"\"\n    try:\n        size = path.stat().st_size\n        with open(path, \"rb\") as fh:\n            head = fh.read(64)\n            if head[:4] in _MACHO_64_MAGICS or head[:4] == _FAT_MAGIC:\n                # The load commands sit right after the 32-byte header, and\n                # `sizeofcmds` (offset 20) says how far they run.\n                order = \"little\" if head[:4] == _MACHO_64_MAGICS[0] else \"big\"\n                want = 8 + 20 * 64 if head[:4] == _FAT_MAGIC else \\\\\n                    _MACHO_64_HEADER + int.from_bytes(head[20:24], order)\n                head += fh.read(max(0, want - len(head)))\n    except OSError as e:\n        return str(e)\n    if head[:4] == _ELF_MAGIC:\n        return _elf_truncation(head, size)\n    if head[:4] in _MACHO_64_MAGICS:\n        return _macho_truncation(head, size)\n    if head[:4] == _FAT_MAGIC:\n        return _fat_truncation(head, size)\n    return \"not an ELF or Mach-O file\"\n\n\ndef _elf_truncation(head: bytes, size: int) -> str:\n    if len(head) < 64:\n        return \"the ELF header is truncated (%d bytes)\" % len(head)\n    if head[4] != 2:\n        return \"not a 64-bit ELF file\"\n    little = head[5] == 1\n    order = \"little\" if little else \"big\"\n    # e_shoff (8 bytes at 0x28) plus e_shentsize * e_shnum: the end of the\n    # section table, which is the last thing a complete object file contains.\n    shoff = int.from_bytes(head[0x28:0x30], order)\n    shentsize = int.from_bytes(head[0x3A:0x3C], order)\n    shnum = int.from_bytes(head[0x3C:0x3E], order)\n    end = shoff + shentsize * shnum\n    if end > size:\n        return \"truncated — its headers describe %d bytes and the file is %d\" % (end, size)\n    return \"\"\n\n\ndef _macho_truncation(head: bytes, size: int) -> str:\n    order = \"little\" if head[:4] == _MACHO_64_MAGICS[0] else \"big\"\n    if len(head) < _MACHO_64_HEADER:\n        return \"the Mach-O header is truncated (%d bytes)\" % len(head)\n    ncmds = int.from_bytes(head[16:20], order)\n    sizeofcmds = int.from_bytes(head[20:24], order)\n    if _MACHO_64_HEADER + sizeofcmds > size:\n        return \"truncated — its load commands run to byte %d and the file is %d\" % (\n            _MACHO_64_HEADER + sizeofcmds, size)\n    off = _MACHO_64_HEADER\n    for _ in range(ncmds):\n        if off + 8 > len(head):\n            return \"truncated — the load commands end before %d were read\" % ncmds\n        cmd = int.from_bytes(head[off:off + 4], order)\n        cmdsize = int.from_bytes(head[off + 4:off + 8], order)\n        if cmdsize < 8 or off + cmdsize > len(head):\n            return \"a load command at byte %d claims %d bytes the file lacks\" % (off, cmdsize)\n        # segment_command_64: cmd, cmdsize, segname[16], vmaddr, vmsize,\n        # fileoff, filesize, …  — the last segment in the file is __LINKEDIT.\n        if cmd == _LC_SEGMENT_64 and head[off + 8:off + 24].rstrip(b\"\\\\0\") == b\"__LINKEDIT\":\n            fileoff = int.from_bytes(head[off + 40:off + 48], order)\n            filesize = int.from_bytes(head[off + 48:off + 56], order)\n            end = fileoff + filesize\n            if end > size:\n                return \"truncated — its headers describe %d bytes and the file is %d\" % (end, size)\n            return \"\"\n        off += cmdsize\n    return \"no __LINKEDIT segment — not a linked Mach-O image\"\n\n\ndef _fat_truncation(head: bytes, size: int) -> str:\n    nfat = int.from_bytes(head[4:8], \"big\")\n    if nfat == 0 or nfat > 64:\n        return \"a fat header naming %d slices is not a library\" % nfat\n    for i in range(nfat):\n        # fat_arch: cputype, cpusubtype, offset, size, align — five big-endian u32.\n        entry = head[8 + 20 * i:8 + 20 * (i + 1)]\n        if len(entry) < 20:\n            return \"the fat header is truncated (%d bytes)\" % len(head)\n        offset = int.from_bytes(entry[8:12], \"big\")\n        length = int.from_bytes(entry[12:16], \"big\")\n        if offset + length > size:\n            return \"truncated — slice %d ends at byte %d and the file is %d\" % (\n                i, offset + length, size)\n    return \"\"\n\n\n'''\ns = s.replace(old, new, 1)\n\nold = '''    @property\n    def refused(self) -> bool:\n        \"\"\"A refusal is not a failure: it means run it on CPython.\"\"\"\n        return self.status == UNSUPPORTED\n'''\nnew = '''    @property\n    def refused(self) -> bool:\n        \"\"\"Was this run a refusal? Informational — branch on :attr:`fall_onward`.\n\n        A refusal is not a failure: it means run it on CPython. But it is not\n        the only outcome that means that. A :data:`BUSY` that executed nothing\n        and a :data:`PANIC` that reached no commit want the same treatment, and\n        :attr:`fall_onward` is the one predicate that folds all three in, so it\n        is the one a host branches on. This property answers the narrower\n        question, for a host that wants to log why.\n        \"\"\"\n        return self.status == UNSUPPORTED\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''            raise LibraryError(\n                \"liblypning not found — run `lypning build --lib` \"\n                \"(or point $LYPNING_LIB at a built library)\")'''\nnew = '''            raise LibraryError(\n                \"%s not found — run `lypning build --lib` \"\n                \"(or point $LYPNING_LIB at a built library)\" % LIB_NAME)'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\np.write_text(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:24:38.914Z","count":1,"stdin_sample":null}
{"key":"py-28c02d8ffe77","id":"py-28c02d8ffe77","program":"import time, pathlib\np = pathlib.Path(\"/tmp/conformance-lib.txt\")\nfor _ in range(590):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nprint(p.read_text(errors=\"replace\")[-3000:] if p.exists() else \"no output file yet\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:27:36.443Z","count":1,"stdin_sample":null}
{"key":"py-2992977974b8","id":"py-2992977974b8","program":"import time, pathlib, subprocess\np = pathlib.Path(\"/tmp/conformance-lib.txt\")\nfor _ in range(590):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nt = p.read_text(errors=\"replace\") if p.exists() else \"\"\nprint(\"DONE\" if \"conformance exit\" in t else \"STILL RUNNING\", len(t), \"bytes\")\nprint(t[-3500:])\nprint(subprocess.run(\"ps -o etime=,pcpu=,command= -p $(pgrep -f 'lypning conformance' | head -1)\", shell=True, capture_output=True, text=True).stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:47:56.024Z","count":1,"stdin_sample":null}
{"key":"py-2abafb299a49","id":"py-2abafb299a49","program":"import struct, shutil\nfrom pathlib import Path\nsrc = Path(\"src/lypning/assets/rust/target/release-lib/liblypning.dylib\")\nhalf = Path(\"/tmp/lyp-half.dylib\"); half.write_bytes(src.read_bytes()[:200_000])\n\ndef looks_like_macho(path):\n    size = path.stat().st_size\n    with open(path, \"rb\") as fh:\n        head = fh.read(32)\n        if head[:4] != b\"\\xcf\\xfa\\xed\\xfe\":          # MH_MAGIC_64, little-endian\n            return \"not a 64-bit Mach-O file\"\n        ncmds, sizeofcmds = struct.unpack_from(\"<II\", head, 16)\n        cmds = fh.read(sizeofcmds)\n    off = 0\n    for _ in range(ncmds):\n        if off + 8 > len(cmds):\n            return \"the load commands are truncated\"\n        cmd, cmdsize = struct.unpack_from(\"<II\", cmds, off)\n        if cmd == 0x19:                                # LC_SEGMENT_64\n            name = cmds[off+8:off+24].rstrip(b\"\\0\")\n            fileoff, filesize = struct.unpack_from(\"<QQ\", cmds, off + 40)\n            if name == b\"__LINKEDIT\" and fileoff + filesize > size:\n                return \"truncated — its headers describe %d bytes and the file is %d\" % (fileoff + filesize, size)\n        off += cmdsize\n    return \"\"\nprint(\"real:\", repr(looks_like_macho(src)))\nprint(\"half:\", repr(looks_like_macho(half)))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:10:45.346Z","count":1,"stdin_sample":null}
{"key":"py-2ce05d5dfa4c","id":"py-2ce05d5dfa4c","program":"p='src/lypning/embed.py'; s=open(p).read()\nold='''  * It is **one host among every host** (the table in ``docs/EMBEDDING.md``\n    section 4), and they all call the same symbols; a Python one written\n    against the same header is what lets the test suite exercise the ABI\n    without a compiler in the loop.'''\nnew='''  * It is **one of the hosts**. Every host in the table in ``docs/EMBEDDING.md``\n    section 4 calls the same symbols; a Python one written against the same\n    header is what lets the test suite exercise the ABI without a compiler in\n    the loop.'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new)); print('reworded')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:21:49.103Z","count":1,"stdin_sample":null}
{"key":"py-2f1f72a0c54f","id":"py-2f1f72a0c54f","program":"import json,sys\nrecs=[json.loads(l) for l in open(sys.argv[1])]\nlua=[r for r in recs if r[\"shim\"]==\"lua-embed\"]; py=[r for r in recs if r[\"shim\"]==\"python-embed\"]\nprint(\"lua\",len(lua),\"py\",len(py))\nprint(\"same keys:\", all(set(a)==set(b) for a,b in zip(lua,py)))\nbad=[(a[\"program\"][:40],a[\"exit_code\"],b[\"exit_code\"]) for a,b in zip(lua,py) if a[\"program\"]!=b[\"program\"] or a[\"exit_code\"]!=b[\"exit_code\"] or a[\"argv_tail\"]!=b[\"argv_tail\"]]\nprint(\"disagreements:\",len(bad), bad[:5])\nprint(\"sample lua rec:\", json.dumps(lua[0])[:300])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:17:20.074Z","count":1,"stdin_sample":null}
{"key":"py-2f4c128eb58b","id":"py-2f4c128eb58b","program":"import json,os\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nif os.path.exists(p):\n    for ln in open(p):\n        try: r=json.loads(ln)\n        except: continue\n        if r.get('type')=='result':\n            v=r.get('result') or r.get('value') or {}\n            print('DONE', r.get('label'), v.get('host'), 'builds=',v.get('builds'),'probes=',v.get('probes_pass'),'contract=',v.get('contract_test_passes'), 'undone=', (v.get('left_undone') or '')[:200])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:29:45.309Z","count":1,"stdin_sample":null}
{"key":"py-30bc98e0a51b","id":"py-30bc98e0a51b","program":"import re, pathlib\np = pathlib.Path(\"Makefile\"); s = p.read_text()\nold_wild = '''# $(wildcard) rather than bare paths: a missing header or library must reach\n# the messages below, not make's own \"No rule to make target\".\nheaders := $(wildcard $(INCDIR)/lypning.hpp $(INCDIR)/lypning.h)\nlib     := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n'''\nnew_wild = '''# $(wildcard) rather than bare paths: a missing header or library must reach\n# the message below, not make's own \"No rule to make target\" — or, for the\n# library, a bare `ld: library not found` that names neither the directory that\n# was searched nor the command that fills it.\nheaders := $(wildcard $(INCDIR)/lypning.hpp $(INCDIR)/lypning.h)\nlib     := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n\n# A phony prerequisite stands in for whichever of the two is missing. A missing\n# file simply drops out of a rule, so without this an old binary would be \"up\n# to date\" against a library that is not there — and the message would print\n# only for someone who had never built before, which is not who needs it.\n# Both headers count as one: lypning.hpp includes lypning.h and is useless\n# without it, and `lypning build --lib` installs the pair.\nmissing := $(if $(filter 2,$(words $(headers))),,missing-header) $(if $(lib),,missing-library)\n.PHONY: missing-header missing-library\nmissing-header missing-library:\n\t@test \"$(words $(headers))\" = 2 || echo \"no lypning.hpp and lypning.h under $(INCDIR).\"\n\t@test -n \"$(lib)\" || echo \"no liblypning.so or liblypning.dylib under $(LIBDIR).\"\n\t@echo \"build it:  lypning build --lib\"\n\t@echo \"      or:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"\n\t@echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"\n\t@exit 1\n'''\nassert old_wild in s; s = s.replace(old_wild, new_wild)\nold_rule = '''$(BIN) quickstart: %: %.cpp $(headers) $(lib) $(MAKEFILE_LIST)\n\t@test -f \"$(INCDIR)/lypning.hpp\" -a -f \"$(INCDIR)/lypning.h\" || { \\\\\n\t  echo \"no lypning.hpp/lypning.h under $(INCDIR).\"; \\\\\n\t  echo \"build and install them:  lypning build --lib\"; \\\\\n\t  echo \"or, in a checkout:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"; \\\\\n\t  echo \"or point at a copy you already have:  make INCDIR=... LIBDIR=...\"; \\\\\n\t  exit 1; }\n\t@test -n \"$(lib)\" || { \\\\\n\t  echo \"no liblypning under $(LIBDIR).\"; \\\\\n\t  echo \"build it:  lypning build --lib\"; \\\\\n\t  echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"; \\\\\n\t  exit 1; }\n\t$(CXX)'''\nnew_rule = '''$(BIN) $(QUICK): %: %.cpp $(headers) $(lib) $(missing) $(MAKEFILE_LIST)\n\t$(CXX)'''\nassert old_rule in s; s = s.replace(old_rule, new_rule)\ns = s.replace('''BIN := embed\n\n.PHONY: all run clean''', '''BIN   := embed\n# Not built by default: it is the file to copy, not the test to run.\nQUICK := quickstart\n\n.PHONY: all run clean''')\ns = s.replace('''# From a scratch directory, not this one. The programs run with this process's\n# working directory, and the ones CPython answers run with no restriction at\n# all — CLAUDE.md invariant 4 applies to a library call exactly as to a spawn.\nrun: $(BIN)\n\t@d=$$(mktemp -d); (cd \"$$d\" && \"$(here)/$(BIN)\"); rc=$$?; rm -rf \"$$d\"; exit $$rc\n\nclean:\n\trm -f $(BIN) quickstart''', '''# From a directory mktemp made, never this one. The programs run with this\n# process's working directory, and the ones CPython answers run with no\n# restriction at all — CLAUDE.md invariant 4 applies to a library call exactly\n# as to a spawn. The exit status is carried across the cleanup so a failed run\n# still fails make.\nrun: $(BIN)\n\t@d=$$(mktemp -d) && (cd \"$$d\" && \"$(here)/$(BIN)\"); s=$$?; rm -rf \"$$d\"; exit $$s\n\nclean:\n\trm -f $(BIN) $(QUICK)''')\np.write_text(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:05:32.681Z","count":1,"stdin_sample":null}
{"key":"py-3249730719b2","id":"py-3249730719b2","program":"import re\np='lypning_test.go'; s=open(p).read()\ns=s.replace('''\tif r.Kind == \"\" || !strings.HasPrefix(string(r.Stderr), \"lypning: unsupported: \"+r.Kind+\": \") {\n\t\tt.Fatalf(\"kind %q stderr %q\", r.Kind, r.Stderr)\n\t}\n\tt.Logf(\"step limit refuses as kind %q: %q\", r.Kind, r.Detail)\n''','''\tif r.Kind != \"steps\" || !strings.HasPrefix(string(r.Stderr), \"lypning: unsupported: steps: \") {\n\t\tt.Fatalf(\"kind %q stderr %q\", r.Kind, r.Stderr)\n\t}\n''')\ns=s.replace('''\tt.Logf(\"output limit refuses as kind %q: %q\", r.Kind, r.Detail)\n''','''\tif r.Kind != \"output\" {\n\t\tt.Fatalf(\"kind %q, want output: %s\", r.Kind, r.Stderr)\n\t}\n''')\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:28:22.593Z","count":1,"stdin_sample":null}
{"key":"py-36fb05c18642","id":"py-36fb05c18642","program":"import time,re\nt0=time.time()\nwhile time.time()-t0 < 570:\n    s=open('/tmp/lyp-final-pytest.log').read()\n    if 'EXIT' in s: break\n    time.sleep(20)\nm=re.findall(r'\\[ *\\d+%\\]', s); n=len(re.sub(r'\\[ *\\d+%\\]|\\n|EXIT.*','',s))\nprint('progress', m[-1] if m else '?', 'chars', n, 'done' if 'EXIT' in s else 'running', 'F' in s and 'FAILURES-SEEN' or '')\nif 'EXIT' in s: print(s[-1500:])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:28:57.949Z","count":1,"stdin_sample":null}
{"key":"py-386c6a65e16c","id":"py-386c6a65e16c","program":"import re, os\ndef sec(path, a, b):\n    t = open(path, encoding=\"utf-8\").read(); i = t.index(a); j = t.index(b, i); return t[i:j]\ntext = sec(\"docs/EMBEDDING.md\", \"## 4.\", \"## 5.\") + sec(\"README.md\", \"## 3b.\", \"## 4.\")\npaths = sorted({p for p in re.findall(r\"`([A-Za-z0-9_./-]+)`\", text) if \"/\" in p and not p.startswith(\"cd \")})\nbad = [p for p in paths if not (os.path.exists(p) or os.path.exists(\"src/lypning/\" + p))]\nprint(len(paths), \"paths;\", \"missing:\", bad or \"none\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:25:37.844Z","count":1,"stdin_sample":null}
{"key":"py-386d780c1869","id":"py-386d780c1869","program":"p='src/lypning/cli.py'; s=open(p).read()\nold='''                   help=\"build the embeddable C ABI (the shared library, liblypning.a and \"\n                        \"the headers) into ~/.lypning/lib and ~/.lypning/include — for C, C++, \"\n                        \"Rust and Node hosts that run programs in-process instead of \"\n                        \"spawning them\")'''\nnew='''                   help=\"build the embeddable C ABI (the shared library, liblypning.a and \"\n                        \"the headers) into ~/.lypning/lib and ~/.lypning/include — for every \"\n                        \"host that runs programs in-process instead of spawning them \"\n                        \"(the table in docs/EMBEDDING.md section 4)\")'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new)); print('cli.py edited')\np='src/lypning/embed.py'; s=open(p).read()\nold='''  * It is the **fourth binding**. C, C++, Rust and Node all call the same\n    symbols; a Python one written against the same header is what lets the test\n    suite exercise the ABI without a compiler in the loop.'''\nnew='''  * It is **one of the hosts**. Every host in the table in ``docs/EMBEDDING.md``\n    section 4 calls the same symbols; a Python one written against the same\n    header is what lets the test suite exercise the ABI without a compiler in\n    the loop.'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new)); print('embed.py edited')\nimport ast\nfor f in ['src/lypning/cli.py','src/lypning/embed.py','src/lypning/engines.py']:\n    ast.parse(open(f).read(), f, feature_version=(3,9))\nprint('3.9 ok')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:23:29.606Z","count":1,"stdin_sample":null}
{"key":"py-3a28ec09ccef","id":"py-3a28ec09ccef","program":"from lypning import build; s=build.exported_symbols('/tmp/lypning-home-b/lib/liblypning.dylib'); print(len([n for n in s if n.startswith('lypning_')]), 'lypning_ symbols; sample', sorted(n for n in s if n.startswith('lypning_'))[:3])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:25:34.447Z","count":1,"stdin_sample":null}
{"key":"py-3c252474dd6b","id":"py-3c252474dd6b","program":"import pathlib\np = pathlib.Path(\"Cargo.toml\")\ns = p.read_text()\nold = '''# The floor is let-else, which `examples/quickstart.rs` uses on its one branch,\n# and nothing in the crate it depends on needs anything newer. Stated so that\n# an older cargo says \"requires rustc 1.65\" instead of failing on the syntax.\nrust-version = \"1.65\"\n'''\nnew = '''# The floor is the crate's, not this file's: `Option::is_some_and` (1.70) in\n# `../../rust/src/eval.rs` and `json.rs`. The let-else on the quickstart's one\n# branch needs only 1.65 and sits under it. Stated so that an older cargo says\n# \"requires rustc 1.70\" instead of failing on a method it has never heard of.\n# (The committed Cargo.lock is format 4, which cargo before 1.78 cannot read;\n# delete it and cargo will write one it can.)\nrust-version = \"1.70\"\n'''\nassert s.count(old) == 1\np.write_text(s.replace(old, new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:18:04.908Z","count":1,"stdin_sample":null}
{"key":"py-3c89d843d108","id":"py-3c89d843d108","program":"p=\"Tests/LypningTests/LypningTests.swift\"; s=open(p).read()\nold='''    func testRefusalIsNotAnError() {\n        let r = Lypning.run(\"import subprocess\")\n        XCTAssertEqual(r.status, .unsupported)\n        XCTAssertEqual(r.exitCode, 90)\n        XCTAssertEqual(r.stdout, [], \"a refused run produced stdout\")\n'''\nnew='''    func testRefusalIsNotAnError() {\n        // The print after the import is the point: a refusal is decided before\n        // anything runs, so it must leave stdout empty even when the program\n        // would have printed.\n        let r = Lypning.run(\"import subprocess; print(1)\")\n        XCTAssertEqual(r.status, .unsupported)\n        XCTAssertEqual(r.exitCode, 90)\n        XCTAssertEqual(r.stdout, [], \"a refused run produced stdout\")\n'''\nassert old in s; s=s.replace(old,new)\nold2='''    func testOkRun() {\n'''\nnew2='''    func testANULInTheSourceIsARefusalNotHalfAProgram() {\n        // A lexer that read the zero as end-of-input would run half a program\n        // and report success. The runtime refuses; for it to see the byte at\n        // all, the binding must have handed over the whole buffer with its\n        // length, not a C string that stops at the first NUL.\n        let r = Lypning.run(\"print(1)\\\\u{0}print(2)\")\n        XCTAssertEqual(r.status, .unsupported)\n        XCTAssertEqual(r.kind, \"source\")\n        XCTAssertEqual(r.stdout, [])\n        XCTAssertTrue(r.fallOnward)\n    }\n\n    func testOkRun() {\n'''\nassert s.count(old2)==1; s=s.replace(old2,new2,1); open(p,\"w\").write(s)\n\np=\"README.md\"; s=open(p).read()\nold='''`lypning build --lib` puts the library. If neither has it the link fails on\n`-llypning`; run `lypning build --lib` first. The paths go in as `unsafeFlags`,\n'''\nnew='''`lypning build --lib` puts the library. If neither has it, the first compile\nstops on one line naming `lypning build --lib`, before the linker can print a\npage of undefined symbols. The paths go in as `unsafeFlags`,\n'''\nassert old in s; s=s.replace(old,new); open(p,\"w\").write(s)\n\np=\"Sources/quickstart/main.swift\"; s=open(p).read()\nold='''// quickstart: lypning in this process, CPython for whatever it refuses.\n// Usage: quickstart \"<python source>\" [args...]\n// Build+run, from src/lypning/assets/swift:\n//   swift build -c release && .build/release/quickstart \"print(sum(range(10)))\"\n// No SwiftPM: make && ./quickstart \"print(sum(range(10)))\"\n'''\nnew='''// quickstart: lypning in this process, CPython for whatever it refuses.\n// Usage: quickstart \"<python source>\" [args...]   (args become sys.argv[1:])\n// Build+run, from the repository root:\n//   swift build -c release --package-path src/lypning/assets/swift && src/lypning/assets/swift/.build/release/quickstart \"print(sum(range(10)))\"\n// No SwiftPM: make -C src/lypning/assets/swift quickstart && src/lypning/assets/swift/quickstart \"print(sum(range(10)))\"\n'''\nassert old in s; s=s.replace(old,new); open(p,\"w\").write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:17:20.344Z","count":1,"stdin_sample":null}
{"key":"py-3d2934d2ed31","id":"py-3d2934d2ed31","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_9f7e1130-3b4/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'recommended_three' in v:\n        print('JUDGE:', v['recommended_three']); print(v['notes'][:1500])\n        for x in v['ranking']: print('  ', x['language'], x['score'], '-', x['reasoning'][:300])\n    elif 'picks' in v and 'Zero' not in v['angle']:\n        print('PROPOSAL', v['angle'][:60]); \n        for pk in v['picks']: print('  ', pk['language'], '| testable', pk['testable_here'], '|', pk['mechanism'][:250])\n        print('  runners-up:', v['runners_up'][:600])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:15:16.902Z","count":1,"stdin_sample":null}
{"key":"py-3fe44b7f36c3","id":"py-3fe44b7f36c3","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_9f7e1130-3b4/journal.jsonl'\nseen=0\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'recommended_three' in v:\n        seen+=1\n        if seen==2:\n            print('JUDGE 2 (contrarian):', v['recommended_three']); print(v['notes'][:1200])\n            for x in v['ranking'][:6]: print('  ', x['language'], x['score'])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:19:23.787Z","count":1,"stdin_sample":null}
{"key":"py-403485a04334","id":"py-403485a04334","program":"from lypning import embed; print(embed.find_library())","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T18:59:10.355Z","count":4,"stdin_sample":null}
{"key":"py-4186d3fe3ab1","id":"py-4186d3fe3ab1","program":"import re\ns=open('/tmp/lyp-final-pytest.log').read()\np=re.sub(r'\\[ *\\d+%\\]|\\n|EXIT.*','',s)\nprint('chars',len(p),'F at positions',[i+1 for i,c in enumerate(p) if c=='F'], 'E at',[i+1 for i,c in enumerate(p) if c=='E'])\nprint('last 80:',p[-80:])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:38:51.744Z","count":1,"stdin_sample":null}
{"key":"py-43c16448a30b","id":"py-43c16448a30b","program":"p='src/lypning/assets/include/lypning.hpp'\ns=open(p).read()\nold = \"\"\"    /// Did the run pass the point where its effects stop being reversible?\n    /// True for any run that finished, whether or not it touched a file. A refusal with `false` here is observably a\n    /// no-op, which is the entire basis of the retry.\n\"\"\"\nnew = \"\"\"    /// Did the run pass the point where its effects stop being reversible?\n    /// True for any run that finished, whether or not it touched a file. A\n    /// refusal with `false` here is observably a no-op, which is the entire\n    /// basis of the retry.\n\"\"\"\nassert s.count(old)==1; s=s.replace(old,new)\nold = \"\"\"        case LYPNING_BUSY:\n            status_ = Status::Busy;\n            break;\n        default:\n\"\"\"\nnew = \"\"\"        case LYPNING_BUSY:\n            status_ = Status::Busy;\n            break;\n        case LYPNING_PANIC:\n            status_ = Status::Panic;\n            break;\n        default:\n\"\"\"\nassert s.count(old)==1; s=s.replace(old,new)\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:22:19.234Z","count":1,"stdin_sample":null}
{"key":"py-46ade3c53598","id":"py-46ade3c53598","program":"p = \"Cargo.toml\"\ns = open(p).read()\nold = 'edition = \"2021\"\\nlicense = \"MIT\"\\n'\nnew = ('edition = \"2021\"\\n'\n       '# The floor is let-else, which `examples/quickstart.rs` uses on its one branch,\\n'\n       '# and nothing in the crate it depends on needs anything newer. Stated so that\\n'\n       '# an older cargo says \"requires rustc 1.65\" instead of failing on the syntax.\\n'\n       'rust-version = \"1.65\"\\n'\n       'license = \"MIT\"\\n')\nassert s.count(old) == 1\nopen(p, \"w\").write(s.replace(old, new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:22:50.426Z","count":1,"stdin_sample":null}
{"key":"py-4957848b392e","id":"py-4957848b392e","program":"import sys,json; print(json.loads(sys.stdin.readline())['program'])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:33:40.663Z","count":1,"stdin_sample":null}
{"key":"py-496453849043","id":"py-496453849043","program":"import re,os,pathlib\ndef sect(path, start, end):\n    t=open(path).read(); i=t.index(start); j=t.index(end,i+1); return t[i:j]\ntexts = {'EMBEDDING s4': sect('docs/EMBEDDING.md','\\n## 4','\\n## 5')}\nr=open('README.md').read()\nm=re.search(r'\\n### 3b[^\\n]*', r) or re.search(r'\\n#+ [^\\n]*3b[^\\n]*', r)\nif m:\n    i=m.start(); j=re.search(r'\\n#+ ', r[i+1:]).start()+i+1; texts['README 3b']=r[i:j]\nelse: print('README 3b header not found')\nfor name,t in texts.items():\n    for p in sorted(set(re.findall(r'`([^`\\s]*?/[^`\\s]*?)`', t))):\n        p2=p.split(' ')[0]\n        ok = os.path.exists(p2) or os.path.exists('src/lypning/'+p2)\n        if not ok: print(name, 'MISSING', p)\n    print(name, 'checked', len(set(re.findall(r'`([^`\\s]*?/[^`\\s]*?)`', t))))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:55:46.091Z","count":1,"stdin_sample":null}
{"key":"py-4d0b2f3d1622","id":"py-4d0b2f3d1622","program":"import pathlib, sys\np = pathlib.Path(\"/Users/user/dev/lypning-bindings/.github/workflows/ci.yml\"); s = p.read_text()\nif \"hosts-macos\" in s:\n    print(\"ci.yml already carries the edit\"); sys.exit(0)\n\nPROBES = '''        run: |\n          cat >\"$RUNNER_TEMP/probes.sh\" <<'EOF'\n          probes() {\n            # $@ is the host's quickstart command; each line below is one probe.\n            test \"$(\"$@\" 'print(sum(range(10)))')\" = 45\n            test \"$(\"$@\" 'import subprocess; print(1)')\" = 1\n            test \"$(\"$@\" 'import sys; print(sys.argv[1:])' a b)\" = \"['a', 'b']\"\n            rc=0; \"$@\" 'print(1/0)' >/dev/null 2>\"$RUNNER_TEMP/probe.err\" || rc=$?\n            test \"$rc\" -eq 1\n            test \"$(grep -c ZeroDivisionError \"$RUNNER_TEMP/probe.err\")\" -eq 1\n            rc=0; \"$@\" 'import sys; sys.exit(3)' >/dev/null || rc=$?\n            test \"$rc\" -eq 3\n            echo \"$1: 45 in-process; refusal answered once by CPython; argv; traceback exit 1, not retried; exit 3\"\n          }\n          EOF\n'''\n\nhosts_ubuntu = '''      # --- every host over the ABI, each held to the one quickstart contract --\n      #\n      # docs/EMBEDDING.md section 4 is the table of hosts; the steps below are\n      # that table, executed, for every host a Linux runner can build. Each\n      # gates on its own toolchain rather than on the runner image: a tool that\n      # is not there is not a red host, it is a host that says so and exits 0,\n      # because the image changes under us and a missing compiler is not a\n      # wrong answer. What is asserted is the same five probes for every\n      # quickstart: an answer in-process, a refusal answered ONCE by CPython,\n      # argv passed through, a traceback that is exit 1 and NOT retried, and\n      # sys.exit carried unchanged. They are written once, as a shell function,\n      # so no host can pass by having skipped one. ZeroDivisionError is COUNTED\n      # rather than grepped for: a host that fell onward after a traceback\n      # would print it twice, and that is the retry the contract forbids.\n      - name: the quickstart contract, as one function every host is run through\n''' + PROBES + '''      - name: C — the contract example, then the file a host author copies\n        # The Makefile picks the checkout's freshly built library and fails\n        # naming `lypning build --lib` if it is absent. The fallback branch is\n        # execvp(\"python3\"), so the runner's python3 is the second tier.\n        run: |\n          command -v cc >/dev/null || { echo \"no C compiler: C host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          make -C src/lypning/assets/examples/c run\n          make -C src/lypning/assets/examples/c quickstart\n          probes src/lypning/assets/examples/c/quickstart\n      - name: C++ — header-only RAII over the same ABI\n        run: |\n          command -v c++ >/dev/null || { echo \"no c++: C++ host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          make -C src/lypning/assets/examples/cpp run\n          make -C src/lypning/assets/examples/cpp quickstart\n          probes src/lypning/assets/examples/cpp/quickstart\n      - name: Rust — the crate by path, no ABI in between\n        # Nothing to link: this host compiles ../../rust from source, so it is\n        # the one host `build --lib` does nothing for. `cargo run` passes the\n        # example's exit status through, which the sys.exit probe relies on.\n        run: |\n          command -v cargo >/dev/null || { echo \"no cargo: Rust host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          M=src/lypning/assets/examples/rust/Cargo.toml\n          cargo run --release --manifest-path \"$M\"\n          probes cargo run -q --release --manifest-path \"$M\" --example quickstart --\n      - name: Node — a plain cargo build links the addon, node loads it\n        # No npm dependencies, no node-gyp: cargo and node are all it needs.\n        # Loading the addon is the assertion build.rs exists for: the Node-API\n        # symbols resolve from the node process at dlopen, or not at all.\n        run: |\n          { command -v cargo && command -v node; } >/dev/null || { echo \"no cargo or no node: Node host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd src/lypning/assets/node\n          cargo build --release\n          probes node quickstart.js\n          npm test\n      - name: Python — ctypes over the library, stdlib only\n        # No compiler in the loop: the only thing this host needs is the\n        # library `build --lib` produced above, which is why it has no gate.\n        run: |\n          . \"$RUNNER_TEMP/probes.sh\"\n          probes python src/lypning/assets/examples/python/quickstart.py\n      - uses: actions/setup-go@v5\n        id: go\n        # A setup failure (a download outage, a retired version string) is a\n        # missing toolchain, and the step below treats it as one: skipped and\n        # said so, not red. The same rule as every other host's gate.\n        continue-on-error: true\n        with:\n          go-version: stable\n          cache: false   # no go.sum to key on: the module has zero dependencies\n      - name: Go — cgo over the unchanged header\n        # Built, then run: `go run` reports every non-zero exit as 1 with an\n        # \"exit status N\" line of its own, so the sys.exit probe cannot pass\n        # through it. CGO_LDFLAGS is what `lypning lib --libs` printed, the\n        # flags a Go module outside the checkout would use, so this links the\n        # INSTALLED library and not only the one the #cgo directives find.\n        if: steps.go.outcome == 'success'\n        env:\n          CGO_ENABLED: \"1\"\n        run: |\n          command -v go >/dev/null || { echo \"no go: Go host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          export CGO_LDFLAGS=\"$(lypning lib --libs)\"\n          cd src/lypning/assets/go\n          go vet ./...\n          go test ./...\n          go build -o \"$RUNNER_TEMP/go-quickstart\" ./quickstart\n          probes \"$RUNNER_TEMP/go-quickstart\"\n      - name: LuaJIT — the ffi over the header, read at load, no build step\n        # PUC Lua has no ffi, so it is luajit or nothing. An apt outage is a\n        # missing toolchain and not a red host, hence the tolerated install.\n        run: |\n          sudo apt-get install -y -qq luajit >/dev/null 2>&1 || echo \"apt could not install luajit\"\n          command -v luajit >/dev/null || { echo \"no luajit: Lua host not run\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd src/lypning/assets/lua\n          luajit test.lua\n          probes luajit quickstart.lua\n'''\n\nanchor = '''      - name: conformance — the library arm, scored like a spawned one\n        # The whole corpus through the C ABI in this process, against CPython,\n        # by the rules the spawned arms are scored by. It must reach MISMATCH 0\n        # AND agree with the `lypning` arm program for program: the two\n        # disagreeing is the drift this arm exists to catch.\n        run: lypning conformance --engine lypning --engine library\n'''\nassert anchor in s\ns = s.replace(anchor, anchor + hosts_ubuntu)\n\nmacos_job = '''  # --- the library tier on the other operating system the wheel claims -----\n  #\n  # pyproject.toml carries `Operating System :: MacOS`, and this job is what\n  # makes that a fact rather than a classifier. It is not a mirror of `core`:\n  # the static-musl core, its gate and its conformance battery are Linux\n  # artefacts, and `build --rust` on a Mac builds for the host and says so.\n  # What a Mac adds is the host Linux cannot run at all (Swift) and the one\n  # link detail Linux never sees: the library's install name must be\n  # `@rpath/liblypning.dylib`, or every host linked with `lypning lib --libs`\n  # links clean and fails at load. The hosts that need only cc, cargo or node\n  # are proven on ubuntu above and not repeated here.\n  hosts-macos:\n    name: macos — library tier, C, Python and Swift hosts\n    runs-on: macos-latest\n    timeout-minutes: 40\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n      - name: install\n        run: |\n          python -m pip install --upgrade pip\n          pip install -e \".[dev]\"\n      - name: build the core, then the library\n        # Both refuse to report ok until the refusal contract holds on what\n        # they just produced; `lib` and `status` print what was installed and\n        # where, which is the line to read when a host step below fails.\n        run: |\n          lypning build --rust\n          lypning build --lib\n          lypning lib\n          lypning status\n      - name: pytest, on a Mac\n        run: python -m pytest tests -q\n      - name: the quickstart contract, as one function every host is run through\n        # The same function the core job defines, and for the same reason:\n        # the five probes are the contract, and a host passes all of them or\n        # none. See the comment there.\n''' + PROBES + '''      - name: C — compiled against what `lypning lib` printed, run from outside the tree\n        # From $RUNNER_TEMP so nothing can resolve back to the checkout: this\n        # is a user's host, linked with the flags the CLI hands out. `otool`\n        # pins the install name those flags depend on.\n        run: |\n          gcc $(lypning lib --cflags) src/lypning/assets/examples/c/quickstart.c \\\\\n              -o \"$RUNNER_TEMP/quickstart\" $(lypning lib --libs)\n          otool -L \"$RUNNER_TEMP/quickstart\" | grep -q '@rpath/liblypning.dylib'\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd \"$RUNNER_TEMP\" && probes ./quickstart\n      - name: Python — ctypes, which dlopens by absolute path and needs no rpath\n        run: |\n          . \"$RUNNER_TEMP/probes.sh\"\n          probes python src/lypning/assets/examples/python/quickstart.py\n      - name: Swift — the module map over the header, its tests, then the quickstart\n        # Package.swift decides the library directory when it is first\n        # evaluated and SwiftPM caches the decision, which is why the library\n        # is built before this step and the README says to do the same.\n        # macos-latest ships a toolchain; a runner without one says so.\n        run: |\n          command -v swift >/dev/null || { echo \"no swift toolchain: Swift host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          P=src/lypning/assets/swift\n          swift build -c release --package-path \"$P\" -Xswiftc -warnings-as-errors\n          swift test --package-path \"$P\"\n          probes \"$P/.build/release/quickstart\"\n      - name: nothing above wrote into the tree\n        # Every build here writes to an ignored path or to $RUNNER_TEMP; an\n        # untracked file is a build artefact .gitignore does not know about,\n        # which is the same file that would ride into the next wheel.\n        run: |\n          dirty=$(git status --porcelain)\n          test -z \"$dirty\" || { echo \"$dirty\"; echo \"a host build left files in the tree\"; exit 1; }\n\n'''\nanchor2 = '''  # --- can the tier be built at all -----------------------------------------\n'''\nassert anchor2 in s\ns = s.replace(anchor2, macos_job + anchor2)\np.write_text(s)\nprint(\"ci edit applied\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:25:14.899Z","count":1,"stdin_sample":null}
{"key":"py-4e3739c8cfa0","id":"py-4e3739c8cfa0","program":"import json\ng=[json.loads(l) for l in open('/tmp/lyp-verify-go.jsonl')]\np=[json.loads(l) for l in open('/tmp/lyp-verify-py.jsonl')]\nprint(\"keys go:\", list(g[0]))\nprint(\"keys py:\", list(p[0]))\nprint(\"same keys order:\", all(list(a)==list(b) for a,b in zip(g,p)))\ndiff=[(a['program'][:40],a['exit_code'],b['exit_code']) for a,b in zip(g,p) if a['program']!=b['program'] or a['exit_code']!=b['exit_code'] or a['argv_tail']!=b['argv_tail']]\nprint(\"program/exit/argv disagreements:\", len(diff), diff[:5])\nprint(\"go types:\", {k:type(v).__name__ for k,v in g[0].items()})\nprint(\"py types:\", {k:type(v).__name__ for k,v in p[0].items()})\nprint(\"html-escaped in go log:\", sum(1 for l in open('/tmp/lyp-verify-go.jsonl') if '\\\\u003c' in l))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:26:36.205Z","count":1,"stdin_sample":null}
{"key":"py-4e9365eb1ac2","id":"py-4e9365eb1ac2","program":"import pathlib, re\ndef T(s):\n    out=[]\n    for line in s.split(\"\\n\"):\n        m = re.match(r\"^((?:    )+)\", line)\n        if m:\n            n = len(m.group(1))//4\n            line = \"\\t\"*n + line[len(m.group(1)):]\n        out.append(line)\n    return \"\\n\".join(out)\np = pathlib.Path(\"lypning.go\"); s = p.read_text()\nold_snip = \"//\\tr := lypning.Run(src, nil)\\n//\\tif r.FallOnward { runOnPython3(src) } // lypning ran NOTHING; your existing path\\n//\\telse            { use(r.Stdout) }     // bytes: output is not always UTF-8\\n\"\nnew_snip = \"//\\tr := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\\n//\\tif r.FallOnward {\\n//\\t\\trunOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\\n//\\t} else {\\n//\\t\\tuse(r.Stdout, r.Stderr, r.ExitCode) // bytes; a traceback IS the answer: exit 1\\n//\\t}\\n\"\nassert old_snip in s, \"snip\"; s = s.replace(old_snip, new_snip)\n\nold_nil = T(\"\"\"    if q == nil {\n        // The same shape the runtime gives a NUL byte in the source: a\n        // refusal of kind \"source\", so a host branches on it like any other.\n        const detail = \"not UTF-8\"\n        return Result{\n            Status:     Unsupported,\n            ExitCode:   UnsupportedExit,\n            Stdout:     []byte{},\n            Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n            Kind:       \"source\",\n            Detail:     detail,\n            FallOnward: true,\n        }\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n    }\n\"\"\")\nnew_nil = T(\"\"\"    if q == nil {\n        return onward(\"not UTF-8\")\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        rc := C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n        if rc != 0 {\n            return onward(\"sys.argv[0] not UTF-8\")\n        }\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        rc := C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n        if rc != 0 {\n            // The ABI declines an argument that is not UTF-8. Dropping it and\n            // running anyway would run a different program than the one\n            // CPython would see, which is exactly the silent disagreement a\n            // refusal exists to prevent.\n            return onward(\"argv not UTF-8\")\n        }\n    }\n\"\"\")\nassert old_nil in s, \"nil\"; s = s.replace(old_nil, new_nil)\n\nold_run = \"// Run runs src in this goroutine's thread\"\nnew_run = T(\"\"\"// onward is the Result for the one thing the ABI will not build a request\n// for: bytes that are not UTF-8, in the source or in argv. It has the same\n// shape the runtime gives a NUL byte in the source, a refusal of kind\n// \"source\", so a host branches on it like any other, and FallOnward is true\n// because CPython would have seen those bytes and has its own message for them.\nfunc onward(detail string) Result {\n    return Result{\n        Status:     Unsupported,\n        ExitCode:   UnsupportedExit,\n        Stdout:     []byte{},\n        Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n        Kind:       \"source\",\n        Detail:     detail,\n        FallOnward: true,\n    }\n}\n\n\"\"\") + old_run\nassert s.count(old_run) == 1, \"run\"; s = s.replace(old_run, new_run, 1)\nold_doc = \"// build a request for, a src that is not UTF-8, because that program too still\\n// needs an answer and CPython can give it one.\"\nnew_doc = \"// build a request for, a src or an argument that is not UTF-8, because that\\n// program too still needs an answer and CPython can give it one.\"\nassert old_doc in s, \"doc\"; s = s.replace(old_doc, new_doc)\np.write_text(s)\n\nt = pathlib.Path(\"lypning_test.go\"); s = t.read_text()\nold_t = \"func TestNULInSourceRefuses(t *testing.T) {\"\nnew_t = T(\"\"\"// An argument is bytes too. The ABI declines one that is not UTF-8, and the\n// binding must route the whole program onward rather than run it with one\n// argument fewer than CPython would see.\nfunc TestNonUTF8ArgFallsOnward(t *testing.T) {\n    r := lypning.Run(\"import sys; print(len(sys.argv[1:]))\", &lypning.Options{Args: []string{\"a\\\\xff\", \"b\"}})\n    if !r.FallOnward || r.Status != lypning.Unsupported || r.ExitCode != 90 || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v status %v exit %d stdout %q\", r.FallOnward, r.Status, r.ExitCode, r.Stdout)\n    }\n    if r.Kind != \"source\" || !strings.HasPrefix(string(r.Stderr), \"lypning: unsupported: source: \") {\n        t.Fatalf(\"kind %q stderr %q\", r.Kind, r.Stderr)\n    }\n    r = lypning.Run(\"print(1)\", &lypning.Options{Filename: \"p\\\\xff.py\"})\n    if !r.FallOnward || r.Kind != \"source\" || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v kind %q stdout %q\", r.FallOnward, r.Kind, r.Stdout)\n    }\n}\n\n\"\"\") + old_t\nassert s.count(old_t) == 1, \"test\"; s = s.replace(old_t, new_t)\nt.write_text(s)\n\nr = pathlib.Path(\"README.md\"); s = r.read_text()\nold_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000})\nif r.FallOnward { runOnPython3(src) }  // lypning ran NOTHING; your existing path\nelse            { use(r.Stdout) }      // []byte: output is not always UTF-8\n\"\"\"\nnew_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\nif r.FallOnward {\n\\trunOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\n} else {\n\\tuse(r.Stdout, r.Stderr, r.ExitCode) // []byte; a traceback IS the answer: exit 1\n}\n\"\"\"\nassert old_r in s, \"readme snip\"; s = s.replace(old_r, new_r)\nold_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` that is not UTF-8 (the ABI will not build a request for one, and that too\nis CPython's to reject with its own message).\"\"\"\nnew_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` or an argument that is not UTF-8 (the ABI will not build a request for\none, and that too is CPython's to reject with its own message).\"\"\"\nassert old_r2 in s, \"readme utf8\"; s = s.replace(old_r2, new_r2)\nold_r3 = \"\"\"* **A C compiler.** `go build` with no `cc` fails with `cgo: C compiler \"gcc\"\n  not found`, and `CGO_ENABLED=0` compiles this package out entirely (the\n  build reports no Go files). Neither is a lypning property; both are what\n  linking a native library through Go means.\"\"\"\nnew_r3 = \"\"\"* **A C compiler.** `go build` with no C compiler fails with `cgo: C compiler\n  \"<cc>\" not found`, and `CGO_ENABLED=0` compiles this package out entirely\n  (`build constraints exclude all Go files`). Neither is a lypning property;\n  both are what linking a native library through Go means.\"\"\"\nassert old_r3 in s, \"readme cc\"; s = s.replace(old_r3, new_r3)\nr.write_text(s)\nprint(\"edits applied\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:38:56.796Z","count":1,"stdin_sample":null}
{"key":"py-5266728af653","id":"py-5266728af653","program":"import lyp_macos_shim, sys; from lypning.cli import main; sys.argv=['lypning','conformance','--engine','library','--limit','40']; sys.exit(main())","argv_tail":["2>"],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:10:02.594Z","count":1,"stdin_sample":null}
{"key":"py-5358baba72ab","id":"py-5358baba72ab","program":"import json,sys; [json.loads(l) for l in open('$T/log.jsonl')]; print('log is valid JSONL')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:29:46.882Z","count":1,"stdin_sample":null}
{"key":"py-56ba51aba32a","id":"py-56ba51aba32a","program":"from pathlib import Path\ndef edit(path, pairs):\n    p = Path(path); s = p.read_text(encoding=\"utf-8\")\n    for old, new in pairs:\n        assert s.count(old) == 1, (path, old[:60], s.count(old))\n        s = s.replace(old, new)\n    p.write_text(s, encoding=\"utf-8\")\nedit(\"src/lypning/assets/rust/src/capi.rs\", [\n(\"\"\"//! There are four hosts in this repository (C, C++, Rust, Node) and only one of\n//! them speaks Rust. So this file is the real API, and everything else is a\n//! convenience over it: a C header, a header-only C++ wrapper, a Node addon and\n//! a ctypes module all call exactly these symbols. Adding a capability here\n//! adds it everywhere; adding it anywhere else adds it in one place and quietly\n//! makes the four disagree.\n\"\"\", \"\"\"//! Every host in this repository binds to it (the table in docs/EMBEDDING.md\n//! section 4 is the list) and only one of them speaks Rust. So this file is the\n//! real API, and everything else is a convenience over it: a C header, a\n//! header-only C++ wrapper, a Node addon, a ctypes module and every binding\n//! after them call exactly these symbols. Adding a capability here adds it\n//! everywhere; adding it anywhere else adds it in one place and quietly makes\n//! the hosts disagree.\n\"\"\"),\n])\nedit(\"study/hosts/prepare.py\", [\n(\"\"\"A JSON parser is a dependency, and the C host has none — so the set is a\ndirectory of plain files rather than a JSONL the five hosts would each have to\nparse differently. One directory per program: the program itself, its stdin,\nand its arguments one per line. Every host walks it the same way, which is the\nonly reason the five results are comparable.\n\"\"\", \"\"\"A JSON parser is a dependency, and the C host has none — so the set is a\ndirectory of plain files rather than a JSONL every host would have to parse\nits own way. One directory per program: the program itself, its stdin, and its\narguments one per line. Every host walks it the same way, which is the only\nreason the results are comparable across hosts.\n\"\"\"),\n(\"\"\"        # them every file-reading program raises, and five hosts agree on a\n        # FileNotFoundError instead of on what the subset takes.\n\"\"\", \"\"\"        # them every file-reading program raises, and every host agrees on a\n        # FileNotFoundError instead of on what the subset takes.\n\"\"\"),\n])\nedit(\"study/hosts/Makefile\", [\n(\"\"\"# cgo finds the checkout's library through the binding's own #cgo directives;\n# LIBDIR is passed anyway so `make LIBDIR=...` means the same thing for every\n# driver. A directory named run_go exists, so the target is phony on purpose.\nrun_go: | check\n\t@command -v go >/dev/null || { echo \"no go on PATH: the Go driver needs a Go toolchain\"; exit 1; }\n\tcd $(here)/run_go && CGO_LDFLAGS=\"-L$(LIBDIR) -Wl,-rpath,$(LIBDIR)\" go build -o run_go .\n\"\"\", \"\"\"# cgo finds the checkout's library through the binding's own #cgo directives,\n# so CGO_LDFLAGS is set only when LIBDIR points elsewhere (an installed copy,\n# or `make LIBDIR=...`), which keeps `make LIBDIR=...` meaning the same thing\n# for every driver without macOS ld warning about a duplicate -rpath. A\n# directory named run_go exists, so the target is phony on purpose.\nifeq ($(LIBDIR),$(checkout_lib))\ngo_ldflags :=\nelse\ngo_ldflags := -L$(LIBDIR) -Wl,-rpath,$(LIBDIR)\nendif\nrun_go: | check\n\t@command -v go >/dev/null || { echo \"no go on PATH: the Go driver needs a Go toolchain\"; exit 1; }\n\tcd $(here)/run_go && CGO_LDFLAGS=\"$(go_ldflags)\" go build -o run_go .\n\"\"\"),\n])\nprint(\"capi/prepare/Makefile ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:25:12.781Z","count":1,"stdin_sample":null}
{"key":"py-58d7f482d004","id":"py-58d7f482d004","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nprint(type(d), list(d.keys()) if isinstance(d,dict) else len(d))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:30.130Z","count":1,"stdin_sample":null}
{"key":"py-595f10ea3255","id":"py-595f10ea3255","program":"\nimport json,collections\nc=collections.Counter(json.loads(l).get('shim') for l in open('/tmp/lypning-study-log.jsonl'))\nprint(dict(c))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:20:12.974Z","count":1,"stdin_sample":null}
{"key":"py-5de8cfe3d4f2","id":"py-5de8cfe3d4f2","program":"import os; print('/usr/lib/libSystem.B.dylib is_file:', os.path.isfile('/usr/lib/libSystem.B.dylib'))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:09:26.992Z","count":1,"stdin_sample":null}
{"key":"py-66cf423a7287","id":"py-66cf423a7287","program":"import json,os\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_5840e5e0-41b/journal.jsonl'\nif os.path.exists(p):\n    for ln in open(p):\n        try: r=json.loads(ln)\n        except: continue\n        if r.get('type')=='result':\n            v=r.get('result') or r.get('value') or {}\n            print('DONE', v.get('task','review')[:60], '| undone:', (v.get('left_undone') or '')[:300])\nelse: print('no journal yet')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:16:55.954Z","count":2,"stdin_sample":null}
{"key":"py-6b4b29a3c47e","id":"py-6b4b29a3c47e","program":"import time,re\nt0=time.time()\nwhile time.time()-t0 < 570:\n    s=open('/tmp/lyp-final-pytest.log').read()\n    if 'EXIT' in s: break\n    time.sleep(20)\nm=re.findall(r'\\[ *\\d+%\\]', s); p=re.sub(r'\\[ *\\d+%\\]|\\n|EXIT.*','',s)\nprint('progress', m[-1] if m else '?', 'chars', len(p), 'done' if 'EXIT' in s else 'running', 'F at',[i+1 for i,c in enumerate(p) if c=='F'])\nif 'EXIT' in s:\n    i=s.find('short test summary'); print(s[i:] if i>=0 else s[-2500:])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:39:13.761Z","count":2,"stdin_sample":null}
{"key":"py-70035cc26b21","id":"py-70035cc26b21","program":"from lypning import embed\nembed._looks_like_a_library = lambda p: \"\"\nlib = embed.Library(\"$D/liblypning.dylib\")\nprint(\"abi\", lib.abi, \"version\", lib.version)\nprint(\"contract\", embed.check_refusal_contract(\"$D/liblypning.dylib\"))\no = lib.run(\"print(sum(range(10)))\")\nprint(o.status_name, o.exit_code, o.stdout, o.fall_onward)\no = lib.run(\"import subprocess\")\nprint(o.status_name, o.exit_code, o.stdout, o.stderr, o.fall_onward)\nprint(lib.route(\"import re\"))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:06:56.859Z","count":1,"stdin_sample":null}
{"key":"py-721ebb1de50a","id":"py-721ebb1de50a","program":"p='src/lypning/assets/include/lypning.hpp'\ns=open(p).read()\nold = \"\"\"    case Status::Panic:\n        return \"panic\";\n    }\n    return \"unknown\";\n}\n\"\"\"\nnew = \"\"\"    case Status::Panic:\n        return \"panic\";\n    default:\n        // Reachable only through a cast from an integer this header does not\n        // number; spelled out so -Wswitch-default has nothing to say.\n        return \"unknown\";\n    }\n}\n\"\"\"\nassert s.count(old)==1; s=s.replace(old,new); open(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:25:23.871Z","count":1,"stdin_sample":null}
{"key":"py-741586b73bbd","id":"py-741586b73bbd","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nn=0\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        n+=1; v=r.get('result') or r.get('value') or {}\n        if n>18: print(n, v.get('host'), 'verdict' if 'probes_pass' in v else 'report', v.get('builds',''), v.get('probes_pass',''), v.get('contract_test_passes',''))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:03:16.820Z","count":1,"stdin_sample":null}
{"key":"py-760f8e406045","id":"py-760f8e406045","program":"from pathlib import Path\n\ndef edit(path, pairs):\n    p = Path(path); s = p.read_text(encoding=\"utf-8\")\n    for old, new in pairs:\n        assert s.count(old) == 1, (path, old[:60], s.count(old))\n        s = s.replace(old, new)\n    p.write_text(s, encoding=\"utf-8\")\n\n# ---------------------------------------------------------------- EMBEDDING.md\nE = Path(\"docs/EMBEDDING.md\").read_text(encoding=\"utf-8\")\nstart = E.index(\"## 3. Building and linking\"); end = E.index(\"## 5. Rules of the API surface\")\nnew34 = r'''## 3. Building and linking\n\n```bash\nlypning build --lib          # the shared library, liblypning.a, and the headers\nlypning lib                  # where they went, and the exact cc line\ncc $(lypning lib --cflags) my_harness.c $(lypning lib --libs)\n```\n\nThe artefacts are the shared library (`liblypning.so` on Linux,\n`liblypning.dylib` on macOS), the static archive `liblypning.a`, and the two\nheaders, `lypning.h` and `lypning.hpp`. On macOS the shared library is built\nwith an `@rpath` install name (`assets/rust/build.rs` says why), so the\n`-rpath` a host links with is what decides where it is found at load time, and\nthe `-Wl,-rpath` in `lypning lib --libs` is not decoration.\n\nThe library is built for the **host** target, not the static-musl target the\nbinary uses: a shared object has to match the libc of the process that loads it,\nso a musl build would be unloadable by every glibc host. `--target musl` is\nstill available for a host that is itself musl.\n\nIt is also built with a different cargo profile — `release-lib`, which differs\nfrom `release` in exactly one setting, `panic = \"unwind\"`. That is not a\npreference. `abort` is right for a binary, where there is nothing to save, and\ncatastrophic for a library, where it kills an application that only asked to run\na one-liner. Every C ABI entry point catches at the boundary, and `capi.rs`\nrefuses to compile under `abort` rather than trusting a comment.\n\n**If you link the static archive** into a host built with `panic = \"abort\"` or\n`-fno-exceptions`, you take that guard away again — the strategy is chosen by\nthe final link, not by our crate. The shared object is the supported artefact.\n\n## 4. The hosts, one ABI\n\nThis table is the one place the hosts are counted. Everything else in the\nrepository says \"every host\" and points here, so the next binding is one row\nand not fifteen edits. Paths are under `src/lypning/` in a checkout and under\nthe package's `assets/` in a wheel; the run column is written from the\nrepository root, after `lypning build --lib`.\n\n| host | binding | quickstart | contract test | build and run |\n|---|---|---|---|---|\n| C | `assets/include/lypning.h`, the ABI itself | `assets/examples/c/quickstart.c` | `assets/examples/c/embed.c` (`make -C src/lypning/assets/examples/c run`) | `make -C src/lypning/assets/examples/c quickstart && src/lypning/assets/examples/c/quickstart \"print(sum(range(10)))\"` |\n| C++ | `assets/include/lypning.hpp`, header-only RAII over the ABI | `assets/examples/cpp/quickstart.cpp` | `assets/examples/cpp/embed.cpp` (`make -C src/lypning/assets/examples/cpp run`) | `make -C src/lypning/assets/examples/cpp quickstart && src/lypning/assets/examples/cpp/quickstart \"print(sum(range(10)))\"` |\n| Rust | the crate directly, no FFI: `lypning::run`, `Request::new`, `Outcome::should_fall_onward` | `assets/examples/rust/examples/quickstart.rs` | `assets/examples/rust/src/main.rs` (`cargo run --release`) | `cargo run --release --manifest-path src/lypning/assets/examples/rust/Cargo.toml --example quickstart -- \"print(sum(range(10)))\"` |\n| Node | `assets/node/`, a Node-API addon with no npm dependencies | `assets/node/quickstart.js` | `assets/node/example.js` (`npm test`) | `cd src/lypning/assets/node && cargo build --release && node quickstart.js \"print(sum(range(10)))\"` |\n| Python | `lypning.embed`, `ctypes`, stdlib only | `assets/examples/python/quickstart.py` | `tests/test_embed.py` | `python3 src/lypning/assets/examples/python/quickstart.py \"print(sum(range(10)))\"` |\n| Go | `assets/go/`, cgo over the unchanged header, zero modules | `assets/go/quickstart/main.go` | `assets/go/lypning_test.go` (`go test`) | `cd src/lypning/assets/go && go run ./quickstart \"print(sum(range(10)))\"` |\n| Swift | `assets/swift/`, a Clang module map over the header (SwiftPM, or plain `swiftc` via its Makefile) | `assets/swift/Sources/quickstart/main.swift` | `assets/swift/Tests/LypningTests` (`swift test`) | `swift build -c release --package-path src/lypning/assets/swift && src/lypning/assets/swift/.build/release/quickstart \"print(sum(range(10)))\"` |\n| LuaJIT | `assets/lua/lypning.lua`, LuaJIT `ffi` over the header, read at load; no build step | `assets/lua/quickstart.lua` | `assets/lua/test.lua` (`luajit test.lua`) | `luajit src/lypning/assets/lua/quickstart.lua \"print(sum(range(10)))\"` |\n\nThe C ABI is the real API; every other row is a convenience over it. Add a\ncapability there and it exists everywhere; add it anywhere else and the hosts\nquietly disagree.\n\n**The quickstart contract.** Every quickstart is `quickstart \"<python source>\"\n[args...]`: it runs the program in-process with a 10M step limit, and the\narguments become `sys.argv[1:]`. If `should_fall_onward` is true it runs\n`python3 -c` exactly once and exits with CPython's code. Otherwise it writes the\nprogram's stdout and stderr bytes and exits with the program's own code, so a\ntraceback is exit 1 and is never retried. Five probes cover the contract, and\nevery host answers them byte for byte the same:\n\n```\nquickstart \"print(sum(range(10)))\"                 45\nquickstart \"import subprocess; print(1)\"           1, via CPython, once\nquickstart \"import sys; print(sys.argv[1:])\" a b   ['a', 'b']\nquickstart \"print(1/0)\"                            traceback on stderr, exit 1, not retried\nquickstart \"import sys; sys.exit(3)\"               exit 3\n```\n\nWhere a host differs from the rest, it is for one reason each:\n\n*Rust* has no FFI to cross. The example crate compiles the runtime from source\nand links no library, so `lypning build --lib` is not a prerequisite and the\n`Outcome` it branches on is the same struct the C ABI wraps.\n\n*Node* is a separate `cargo build` in `assets/node/`: the addon links the\nruntime statically, so `lypning build --lib` does not produce it and does not\nhave to have run.\n\n*Go* finds a checkout's library through the binding's own `#cgo` directives.\nAgainst an installed library it needs `CGO_LDFLAGS=\"$(lypning lib --libs)\"`,\nwhich carries the `-rpath` the binary will need at load time.\n\n*Swift* passes the library path as `unsafeFlags`, which SwiftPM refuses in a\ndependency, so the package is consumed in-tree or by `.package(path:)`. The\nMakefile beside it is the same binding through plain `swiftc`, with no `.build/`.\n\n*LuaJIT* reads `lypning.h` at load and hands it to `ffi.cdef`, so there is one\nsource of truth for the prototypes and no build step. PUC Lua has no `ffi` and\nis refused with a message that says so.\n\n**They have been run against each other rather than assumed to agree.** The\nprompting study ([PROMPTING.md](PROMPTING.md) §7) drove the original five hosts\n(C, C++, Rust, Node, Python) over one shared set of 393 agent-written programs\non 2026-08-23, each host with its own copy of the set and each program with its\nown working directory, and all five answered identically: 341 ran, 52 refused,\n0 other. On **2026-09-02**, on macOS arm64 (clang, cargo, node 26, go 1.26,\nswift 6.3, luajit 2.1), the same battery over every row of the table above\nprinted:\n\n```\nc-embed      393 programs: 341 ran, 52 refused, 0 other\ncpp-embed    393 programs: 341 ran, 52 refused, 0 other\nrust-embed   393 programs: 341 ran, 52 refused, 0 other\nnode-embed   393 programs: 341 ran, 52 refused, 0 other\npython-embed 393 programs: 341 ran, 52 refused, 0 other\ngo-embed     393 programs: 341 ran, 52 refused, 0 other\nswift-embed  393 programs: 341 ran, 52 refused, 0 other\nlua-embed    393 programs: 341 ran, 52 refused, 0 other\n```\n\n3144 capture records, 393 per host, and `git status` unchanged afterwards. That\nincludes the refusal path, which is the half that has only ever broken\nsilently; a disagreement there is a binding bug and nothing else in the project\nwould notice one. The recipe, from a checkout with the toolchains on `PATH`:\n\n```bash\nlypning build --lib                               # the library every driver but Rust and Node loads\nexport LYPNING_LOG=/tmp/lypning-study-log.jsonl   # the drivers append the capture record themselves\npython3 study/hosts/prepare.py                    # lay out the shared program set\nmake -C study/hosts                               # C, C++, Rust, Go, Swift drivers and the Node addon\nsh study/hosts/run_all.sh                         # every host, one summary line each\ngit status                                        # the programs ran behind a net; check anyway\n```\n\n**What the ABI does not have is a capture hook.** A `lypning_run()` spawns no\ninterpreter, so neither of the capture feeds in [CAPTURE.md](CAPTURE.md) can see\nit, and a harness that wants its programs to reach the corpus has to write the\nrecord itself. `study/hosts/capture.h` is a working example in about forty lines\nof C and argues that the right home for it is here, where every host would\ninherit it at once.\n\n### The branch, per host\n\nThe same four lines in each quickstart, copied from the files rather than\nparaphrased, C first. What they share is the shape: test `should_fall_onward`,\nhand a refusal to `python3 -c` once, and otherwise return the program's own\nbytes and code.\n\n```c\n    if (r == NULL || lypning_result_should_fall_onward(r)) {\n        /* A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once. */\n        /* ... */\n        execvp(\"python3\", py);\n```\n\n```cpp\n    if (r.should_fall_onward()) {\n        // A refusal is not an error: lypning ran none of it and wrote nothing,\n        // so CPython runs it once, on the same empty stdin lypning was given.\n        // ...\n        execvp(python3, cargv.data());\n```\n\n```rust\n    if r.should_fall_onward() {\n        // A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n        let _ = std::io::stdout().flush();\n        let status = Command::new(\"python3\")\n```\n\n```js\nif (r.fallOnward) {\n  // A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n  const p = spawnSync('python3', ['-c', src, ...args], { stdio: ['ignore', 'inherit', 'inherit'] });\n  process.exit(p.status ?? 1);\n```\n\n```python\n    if out.fall_onward:\n        # A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n        sys.stdout.flush()\n        return subprocess.run([sys.executable, \"-c\", src, *rest], stdin=subprocess.DEVNULL).returncode\n```\n\n```go\n    if r.FallOnward {\n        // A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n        cmd := exec.Command(\"python3\", append([]string{\"-c\", src}, args...)...)\n```\n\n```swift\nif r.fallOnward {\n    // A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n    // ...\n    execvp(\"python3\", cargv)\n```\n\n```lua\nif r.fall_onward then\n  -- A refusal is not an error: lypning ran none of it and wrote nothing, so CPython runs it once.\n  -- ...\n  local status, how, code = os.execute(cmd .. \" </dev/null\")\n```\n\n'''\nE = E[:start] + new34 + E[end:]\nE = E.replace(\"\"\"  the main thread and once on a 1 MB one. The last row of that table is the\n  exception and is honest about it: no program is known that panics the\n  interpreter, so what the suite pins there is the routing, not a crash.\n\"\"\", \"\"\"  the main thread and once on a 1 MB one. The last row of that table is the\n  exception and is honest about it: no program is known that panics the\n  interpreter, so what the suite pins there is the routing, not a crash.\n* `tests/test_hosts.py` drives every quickstart in §4 through the five probes\n  and compares the bytes per host, per probe, so the bindings are checked\n  against each other and not only against the header; CI runs the same\n  quickstarts on Linux and macOS. A host missing its toolchain is skipped and\n  named, never failed.\n\"\"\")\nPath(\"docs/EMBEDDING.md\").write_text(E, encoding=\"utf-8\")\n\n# ---------------------------------------------------------------- README.md\nedit(\"README.md\", [\n(\"\"\"```bash\nlypning build --lib                       # the C ABI, into ~/.lypning/lib\ngcc $(lypning lib --cflags) h.c $(lypning lib --libs)\n```\n\"\"\", \"\"\"```bash\nlypning build --lib                       # the C ABI, into ~/.lypning/lib\ngcc $(lypning lib --cflags) h.c $(lypning lib --libs)\npython3 src/lypning/assets/examples/python/quickstart.py \"print(sum(range(10)))\"   # no compiler needed\n```\n\"\"\"),\n(\"\"\"Five hosts, one ABI: C and C++ headers in `assets/include/`, a Node addon with\nno npm dependencies in `assets/node/`, the Rust crate directly, and\n`lypning.embed` for Python via `ctypes`. Runnable examples for each live in\n`assets/examples/`.\n\"\"\", \"\"\"One ABI, and a host for every language a harness is likely to be written in.\nEach has a quickstart that is the file to copy, and every quickstart obeys the\nsame contract, `quickstart \"<python source>\" [args...]`. The count, the binding\nfor each, and its contract test live in one place, the table in\n`docs/EMBEDDING.md` §4; this is the run line for each, from the repository root:\n\n| host | quickstart | run |\n|---|---|---|\n| C | `src/lypning/assets/examples/c/quickstart.c` | `make -C src/lypning/assets/examples/c quickstart && src/lypning/assets/examples/c/quickstart \"print(sum(range(10)))\"` |\n| C++ | `src/lypning/assets/examples/cpp/quickstart.cpp` | `make -C src/lypning/assets/examples/cpp quickstart && src/lypning/assets/examples/cpp/quickstart \"print(sum(range(10)))\"` |\n| Rust | `src/lypning/assets/examples/rust/examples/quickstart.rs` | `cargo run --release --manifest-path src/lypning/assets/examples/rust/Cargo.toml --example quickstart -- \"print(sum(range(10)))\"` |\n| Node | `src/lypning/assets/node/quickstart.js` | `cd src/lypning/assets/node && cargo build --release && node quickstart.js \"print(sum(range(10)))\"` |\n| Python | `src/lypning/assets/examples/python/quickstart.py` | `python3 src/lypning/assets/examples/python/quickstart.py \"print(sum(range(10)))\"` |\n| Go | `src/lypning/assets/go/quickstart/main.go` | `cd src/lypning/assets/go && go run ./quickstart \"print(sum(range(10)))\"` |\n| Swift | `src/lypning/assets/swift/Sources/quickstart/main.swift` | `swift build -c release --package-path src/lypning/assets/swift && src/lypning/assets/swift/.build/release/quickstart \"print(sum(range(10)))\"` |\n| LuaJIT | `src/lypning/assets/lua/quickstart.lua` | `luajit src/lypning/assets/lua/quickstart.lua \"print(sum(range(10)))\"` |\n\"\"\"),\n(\"\"\"  bench.py             four arms, interleaved, min of repeats, two totals — and corpus-time\n\"\"\", \"\"\"  bench.py             four arms, interleaved, min of repeats, two totals — and corpus-time\n  perf.py              one construct at a time against CPython — where the interpreter's time goes\n  pool.py              the warm CPython backstop: opt-in, off by default\n\"\"\"),\n(\"\"\"  shim.py              python/python3 on PATH, and when to refuse to install it\n  assets/rust/         the Rust core — zero crates, size-tuned release profile\n\"\"\", \"\"\"  shim.py              python/python3 on PATH, and when to refuse to install it\n  embed.py             the library tier from Python: liblypning over ctypes, and its contract check\n  assets/rust/         the Rust core — zero crates, size-tuned release profile\n  assets/include/      lypning.h, the C ABI, and lypning.hpp over it\n  assets/examples/     the C, C++, Rust and Python hosts: each a quickstart and a contract test\n  assets/node/         the Node-API addon, its own cargo build, no npm dependencies\n  assets/go/           the Go binding, cgo over the header, zero modules\n  assets/swift/        the Swift binding, a module map over the header (SwiftPM or swiftc)\n  assets/lua/          the LuaJIT binding, ffi over the header read at load\n\"\"\"),\n(\"\"\"site/                  the GitHub Pages generator: build.py, index.md, style.css\n\"\"\", \"\"\"site/                  the GitHub Pages generator: build.py, index.md, style.css\nstudy/                 the prompting study: tasks, prompts, scoring, and hosts/ (every host, one battery)\n\"\"\"),\n(\"the C ABI, the five hosts over it, and what a refusal means when there is no exit code\",\n \"the C ABI, the hosts over it, and what a refusal means when there is no exit code\"),\n])\n\n# ---------------------------------------------------------------- CAPTURE.md\nedit(\"docs/CAPTURE.md\", [\n(\"\"\"that links `liblypning` and calls `lypning_run()` — any of the five in\n[EMBEDDING.md](EMBEDDING.md) — spawns nothing, so neither feed sees it: five\nhosts can run ten thousand programs and the corpus will not grow by one. The\n\"\"\", \"\"\"that links `liblypning` and calls `lypning_run()` — any host in the table in\n[EMBEDDING.md](EMBEDDING.md) §4 — spawns nothing, so neither feed sees it: a\nhost can run ten thousand programs and the corpus will not grow by one. The\n\"\"\"),\n])\n\n# ---------------------------------------------------------------- PROMPTING.md\nedit(\"docs/PROMPTING.md\", [\n(\"\"\"A host that links `liblypning` and calls `lypning_run()` spawns nothing. Five\nhosts can run ten thousand programs and the corpus will not grow by one — and\n\"\"\", \"\"\"A host that links `liblypning` and calls `lypning_run()` spawns nothing. A\nhost can run ten thousand programs and the corpus will not grow by one — and\n\"\"\"),\n(\"\"\"§7 is what this study did about it. The durable fix belongs in the C ABI, where\nall five hosts would inherit it at once.\n\"\"\", \"\"\"§7 is what this study did about it. The durable fix belongs in the C ABI, where\nevery host would inherit it at once.\n\"\"\"),\n(\"\"\"## 7. The five hosts, and the capture loop\n\nThe study's programs were then run through **every host the C ABI has** — C,\nC++, Rust, Node and Python — over one shared program set, with each host given\n\"\"\", \"\"\"## 7. Every host, and the capture loop\n\nThe study's programs were then run through **every host the C ABI had on\n2026-08-23** — C, C++, Rust, Node and Python; the table in\n[EMBEDDING.md](EMBEDDING.md) §4 has grown since — over one shared program set, with each host given\n\"\"\"),\n(\"\"\"**Five bindings, one ABI, byte-identical answers** — including on the refusal\npath, which is the half that has only ever broken silently. That agreement is\nthe point of having one ABI and four conveniences over it, and it is asserted\nhere rather than assumed.\n\"\"\", \"\"\"**One ABI, byte-identical answers from every binding** — including on the\nrefusal path, which is the half that has only ever broken silently. That\nagreement is the point of having one ABI and conveniences over it, and it is\nasserted here rather than assumed.\n\nRe-run on **2026-09-02**, on macOS arm64 (clang, cargo, node 26, go 1.26,\nswift 6.3, luajit 2.1), over every host EMBEDDING.md §4 lists by then:\n\n```\nhost          programs   ran   refused   other\nc-embed            393   341        52       0\ncpp-embed          393   341        52       0\nrust-embed         393   341        52       0\nnode-embed         393   341        52       0\npython-embed       393   341        52       0\ngo-embed           393   341        52       0\nswift-embed        393   341        52       0\nlua-embed          393   341        52       0\n```\n\n3144 records in the log, 393 per host, and `git status` unchanged afterwards.\n\"\"\"),\n(\"\"\"make -C study/hosts && (cd study/hosts/run_rust && cargo build --release)\nsh study/hosts/run_all.sh                         # all five hosts, into the same log\n\"\"\", \"\"\"make -C study/hosts                               # every driver with a build step, and the Node addon\nsh study/hosts/run_all.sh                         # every host, into the same log\n\"\"\"),\n(\"| `study/hosts/` | the five host drivers and the capture record they write |\",\n \"| `study/hosts/` | one driver per host in EMBEDDING.md §4, and the capture record each writes |\"),\n])\n\n# ---------------------------------------------------------------- site\nedit(\"site/build.py\", [\n(\"the C ABI, the five bindings, and what a refusal means with no exit code\",\n \"the C ABI, the bindings over it, and what a refusal means with no exit code\"),\n])\nedit(\"site/index.md\", [\n(\"The C ABI, the five hosts over it, and what a refusal means with no exit code.\",\n \"The C ABI, the hosts over it, and what a refusal means with no exit code.\"),\n])\n\n# ---------------------------------------------------------------- embed.py docstring\nedit(\"src/lypning/embed.py\", [\n(\"\"\"  * It is the **fourth binding**. C, C++, Rust and Node all call the same\n    symbols; a Python one written against the same header is what lets the test\n    suite exercise the ABI without a compiler in the loop.\n\"\"\", \"\"\"  * It is **one binding among several** (``docs/EMBEDDING.md`` §4 is the\n    list). Every host calls the same symbols; a Python one written against the\n    same header is what lets the test suite exercise the ABI without a compiler\n    in the loop.\n\"\"\"),\n])\n\n# ---------------------------------------------------------------- capi.rs doc comment\nedit(\"src/lypning/assets/rust/src/capi.rs\", [\n(\"\"\"//! There are four hosts in this repository (C, C++, Rust, Node) and only one of\n//! them speaks Rust. So this file is the real API, and everything else is a\n//! convenience over it: a C header, a header-only C++ wrapper, a Node addon and\n//! a ctypes module all call exactly these symbols. Adding a capability here\n//! adds it everywhere; adding it anywhere else adds it in one place and quietly\n//! makes the four disagree.\n\"\"\", \"\"\"//! Every host in this repository binds to it (the table in docs/EMBEDDING.md\n//! section 4 is the list) and only one of them speaks Rust. So this file is the\n//! real API, and everything else is a convenience over it: a C header, a\n//! header-only C++ wrapper, a Node addon, a ctypes module and every binding\n//! after them call exactly these symbols. Adding a capability here adds it\n//! everywhere; adding it anywhere else adds it in one place and quietly makes\n//! the hosts disagree.\n\"\"\"),\n])\n\n# ---------------------------------------------------------------- prepare.py comments\nedit(\"study/hosts/prepare.py\", [\n(\"\"\"A JSON parser is a dependency, and the C host has none — so the set is a\ndirectory of plain files rather than a JSONL the five hosts would each have to\nparse differently. One directory per program: the program itself, its stdin,\nand its arguments one per line. Every host walks it the same way, which is the\nonly reason the five results are comparable.\n\"\"\", \"\"\"A JSON parser is a dependency, and the C host has none — so the set is a\ndirectory of plain files rather than a JSONL every host would have to parse\nits own way. One directory per program: the program itself, its stdin, and its\narguments one per line. Every host walks it the same way, which is the only\nreason the results are comparable across hosts.\n\"\"\"),\n(\"\"\"        # them every file-reading program raises, and five hosts agree on a\n        # FileNotFoundError instead of on what the subset takes.\n\"\"\", \"\"\"        # them every file-reading program raises, and every host agrees on a\n        # FileNotFoundError instead of on what the subset takes.\n\"\"\"),\n])\n\n# ---------------------------------------------------------------- Makefile: no duplicate -rpath on the checkout path\nedit(\"study/hosts/Makefile\", [\n(\"\"\"# cgo finds the checkout's library through the binding's own #cgo directives;\n# LIBDIR is passed anyway so `make LIBDIR=...` means the same thing for every\n# driver. A directory named run_go exists, so the target is phony on purpose.\nrun_go: | check\n\t@command -v go >/dev/null || { echo \"no go on PATH: the Go driver needs a Go toolchain\"; exit 1; }\n\tcd $(here)/run_go && CGO_LDFLAGS=\"-L$(LIBDIR) -Wl,-rpath,$(LIBDIR)\" go build -o run_go .\n\"\"\", \"\"\"# cgo finds the checkout's library through the binding's own #cgo directives,\n# so CGO_LDFLAGS is set only when LIBDIR points elsewhere (an installed copy,\n# or `make LIBDIR=...`), which keeps `make LIBDIR=...` meaning the same thing\n# for every driver without macOS ld warning about a duplicate -rpath. A\n# directory named run_go exists, so the target is phony on purpose.\nifeq ($(LIBDIR),$(checkout_lib))\ngo_ldflags :=\nelse\ngo_ldflags := -L$(LIBDIR) -Wl,-rpath,$(LIBDIR)\nendif\nrun_go: | check\n\t@command -v go >/dev/null || { echo \"no go on PATH: the Go driver needs a Go toolchain\"; exit 1; }\n\tcd $(here)/run_go && CGO_LDFLAGS=\"$(go_ldflags)\" go build -o run_go .\n\"\"\"),\n])\nprint(\"edits ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:24:39.909Z","count":1,"stdin_sample":null}
{"key":"py-762c9e73337f","id":"py-762c9e73337f","program":"p='/tmp/swift-absent/Package.swift'; s=open(p).read()\ns=s.replace('let libdir = hasLibrary(checkout) ? checkout : installed',\n'''if !hasLibrary(checkout) && !hasLibrary(installed) {\n    fatalError(\"no liblypning under \\\\(checkout) or \\\\(installed); build it:  lypning build --lib\")\n}\nlet libdir = hasLibrary(checkout) ? checkout : installed''')\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:35:57.702Z","count":1,"stdin_sample":null}
{"key":"py-76b2befef104","id":"py-76b2befef104","program":"import pathlib\nT = lambda s: s.replace(\"    \", \"\\t\")\np = pathlib.Path(\"lypning.go\"); s = p.read_text()\nold_snip = \"//\\tr := lypning.Run(src, nil)\\n//\\tif r.FallOnward { runOnPython3(src) } // lypning ran NOTHING; your existing path\\n//\\telse            { use(r.Stdout) }     // bytes: output is not always UTF-8\\n\"\nnew_snip = \"//\\tr := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\\n//\\tif r.FallOnward {\\n//\\t\\trunOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\\n//\\t} else {\\n//\\t\\tuse(r.Stdout, r.Stderr, r.ExitCode) // bytes; a traceback IS the answer: exit 1\\n//\\t}\\n\"\nassert old_snip in s, \"snip\"; s = s.replace(old_snip, new_snip)\n\nold_nil = T(\"\"\"    if q == nil {\n        // The same shape the runtime gives a NUL byte in the source: a\n        // refusal of kind \"source\", so a host branches on it like any other.\n        const detail = \"not UTF-8\"\n        return Result{\n            Status:     Unsupported,\n            ExitCode:   UnsupportedExit,\n            Stdout:     []byte{},\n            Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n            Kind:       \"source\",\n            Detail:     detail,\n            FallOnward: true,\n        }\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n    }\n\"\"\")\nnew_nil = T(\"\"\"    if q == nil {\n        return onward(\"not UTF-8\")\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        rc := C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n        if rc != 0 {\n            return onward(\"sys.argv[0] not UTF-8\")\n        }\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        rc := C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n        if rc != 0 {\n            // The ABI declines an argument that is not UTF-8. Dropping it and\n            // running anyway would run a different program than the one\n            // CPython would see, which is exactly the silent disagreement a\n            // refusal exists to prevent.\n            return onward(\"argv not UTF-8\")\n        }\n    }\n\"\"\")\nassert old_nil in s, \"nil\"; s = s.replace(old_nil, new_nil)\n\nold_run = \"// Run runs src in this goroutine's thread\"\nnew_run = T(\"\"\"// onward is the Result for the one thing the ABI will not build a request\n// for: bytes that are not UTF-8, in the source or in argv. It has the same\n// shape the runtime gives a NUL byte in the source, a refusal of kind\n// \"source\", so a host branches on it like any other, and FallOnward is true\n// because CPython would have seen those bytes and has its own message for them.\nfunc onward(detail string) Result {\n    return Result{\n        Status:     Unsupported,\n        ExitCode:   UnsupportedExit,\n        Stdout:     []byte{},\n        Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n        Kind:       \"source\",\n        Detail:     detail,\n        FallOnward: true,\n    }\n}\n\n\"\"\") + old_run\nassert s.count(old_run) == 1, \"run\"; s = s.replace(old_run, new_run, 1)\nold_doc = \"// build a request for, a src that is not UTF-8, because that program too still\\n// needs an answer and CPython can give it one.\"\nnew_doc = \"// build a request for, a src or an argument that is not UTF-8, because that\\n// program too still needs an answer and CPython can give it one.\"\nassert old_doc in s, \"doc\"; s = s.replace(old_doc, new_doc)\np.write_text(s)\n\nt = pathlib.Path(\"lypning_test.go\"); s = t.read_text()\nold_t = \"func TestNULInSourceRefuses(t *testing.T) {\"\nnew_t = T(\"\"\"// An argument is bytes too. The ABI declines one that is not UTF-8, and the\n// binding must route the whole program onward rather than run it with one\n// argument fewer than CPython would see.\nfunc TestNonUTF8ArgFallsOnward(t *testing.T) {\n    r := lypning.Run(\"import sys; print(len(sys.argv[1:]))\", &lypning.Options{Args: []string{\"a\\\\xff\", \"b\"}})\n    if !r.FallOnward || r.Status != lypning.Unsupported || r.ExitCode != 90 || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v status %v exit %d stdout %q\", r.FallOnward, r.Status, r.ExitCode, r.Stdout)\n    }\n    if r.Kind != \"source\" || !strings.HasPrefix(string(r.Stderr), \"lypning: unsupported: source: \") {\n        t.Fatalf(\"kind %q stderr %q\", r.Kind, r.Stderr)\n    }\n    r = lypning.Run(\"print(1)\", &lypning.Options{Filename: \"p\\\\xff.py\"})\n    if !r.FallOnward || r.Kind != \"source\" || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v kind %q stdout %q\", r.FallOnward, r.Kind, r.Stdout)\n    }\n}\n\n\"\"\") + old_t\nassert s.count(old_t) == 1, \"test\"; s = s.replace(old_t, new_t)\nt.write_text(s)\n\nr = pathlib.Path(\"README.md\"); s = r.read_text()\nold_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000})\nif r.FallOnward { runOnPython3(src) }  // lypning ran NOTHING; your existing path\nelse            { use(r.Stdout) }      // []byte: output is not always UTF-8\n\"\"\"\nnew_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\nif r.FallOnward {\n\\trunOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\n} else {\n\\tuse(r.Stdout, r.Stderr, r.ExitCode) // []byte; a traceback IS the answer: exit 1\n}\n\"\"\"\nassert old_r in s, \"readme snip\"; s = s.replace(old_r, new_r)\nold_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` that is not UTF-8 (the ABI will not build a request for one, and that too\nis CPython's to reject with its own message).\"\"\"\nnew_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` or an argument that is not UTF-8 (the ABI will not build a request for\none, and that too is CPython's to reject with its own message).\"\"\"\nassert old_r2 in s, \"readme utf8\"; s = s.replace(old_r2, new_r2)\nold_r3 = \"\"\"* **A C compiler.** `go build` with no `cc` fails with `cgo: C compiler \"gcc\"\n  not found`, and `CGO_ENABLED=0` compiles this package out entirely (the\n  build reports no Go files). Neither is a lypning property; both are what\n  linking a native library through Go means.\"\"\"\nnew_r3 = \"\"\"* **A C compiler.** `go build` with no C compiler fails with `cgo: C compiler\n  \"<cc>\" not found`, and `CGO_ENABLED=0` compiles this package out entirely\n  (`build constraints exclude all Go files`). Neither is a lypning property;\n  both are what linking a native library through Go means.\"\"\"\nassert old_r3 in s, \"readme cc\"; s = s.replace(old_r3, new_r3)\nr.write_text(s)\nprint(\"edits applied\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:35:18.751Z","count":1,"stdin_sample":null}
{"key":"py-773f5a5d74fa","id":"py-773f5a5d74fa","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nprint(type(d), list(d.keys()) if isinstance(d,dict) else len(d))\nfor k,v in (d.items() if isinstance(d,dict) else enumerate(d)):\n    print('=====',k)\n    if isinstance(v,dict):\n        for kk in ('files_created','files_modified','doc_row','doc_snippet','build_and_run','ci_step','left_undone'):\n            print('--',kk,':',v.get(kk))\n        print('-- verdict:', v.get('verdict'))\n    else: print(v)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:45.787Z","count":1,"stdin_sample":null}
{"key":"py-78ab8b51d60f","id":"py-78ab8b51d60f","program":"import re,io\np='main.go'; s=open(p).read()\nold='''    line, err := json.Marshal(rec)\n    if err != nil {\n        return\n    }\n    // Best-effort, exactly like the shim: a lost sighting, never a failed run.\n    fh, err := os.OpenFile(logPath, os.O_WRONLY|os.O_APPEND|os.O_CREATE, 0o644)\n    if err != nil {\n        return\n    }\n    defer fh.Close()\n    fh.Write(append(line, '\\\\n'))\n'''.replace('    ','\\t')\nnew='''    // Best-effort, exactly like the shim: a lost sighting, never a failed run.\n    fh, err := os.OpenFile(logPath, os.O_WRONLY|os.O_APPEND|os.O_CREATE, 0o644)\n    if err != nil {\n        return\n    }\n    defer fh.Close()\n    enc := json.NewEncoder(fh)\n    // Not HTML: `<` in a program stays `<`, as it does in every other host's log.\n    enc.SetEscapeHTML(false)\n    enc.Encode(rec)\n'''.replace('    ','\\t')\nassert old in s, 'capture block'\ns=s.replace(old,new)\nold2='''        if e.IsDir() && !strings.HasPrefix(e.Name(), \".\") {'''.replace('    ','\\t')\nnew2='''        if e.IsDir() {'''.replace('    ','\\t')\nassert old2 in s, 'dir filter'\ns=s.replace(old2,new2)\nold3='''            for _, a := range strings.Split(string(raw), \"\\\\n\") {\n                if a != \"\" {\n                    args = append(args, a)\n                }\n            }'''.replace('    ','\\t')\nnew3='''            for _, a := range strings.Split(string(raw), \"\\\\n\") {\n                if a = strings.TrimRight(a, \"\\\\r\"); a != \"\" {\n                    args = append(args, a)\n                }\n            }'''.replace('    ','\\t')\nassert old3 in s, 'args split'\ns=s.replace(old3,new3)\nopen(p,'w').write(s)\nprint('run_go edited')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:09:34.199Z","count":1,"stdin_sample":null}
{"key":"py-7f0a9488c5d0","id":"py-7f0a9488c5d0","program":"import re,io\np='src/lypning/assets/examples/cpp/embed.cpp'\ns=open(p).read()\nold_head = s[:s.index('#include \"lypning.hpp\"')]\nnew_head = '''/*\n * embed.cpp — the contract test for the C++ header: route, run in-process, and\n * on a refusal fall onward to CPython, with every refusal checked against the\n * properties a host branches on.\n *\n * This is the file that fails when the header stops being right. The file to\n * COPY is quickstart.cpp beside it: the same branch in forty lines, with\n * nothing else on the page.\n *\n * The one thing this example exists to show: THE REFUSAL PATH IS ORDINARY\n * CODE. There is no try/catch anywhere below, nothing is logged as an error,\n * and the entire decision is\n *\n *     if (r.should_fall_onward()) ... else ...\n *\n * A harness that gets that one line right gets everything: the programs\n * lypning accepts cost no process at all, and the ones it refuses cost exactly\n * what they cost today — one python3 spawn — because a refused run wrote\n * nothing, read nothing and touched nothing.\n *\n * The fallback here is deliberately the crude, honest one: posix_spawnp of\n * python3 with three pipes. That is what a host already has, and lypning is\n * only worth linking if it can be dropped in front of it unchanged.\n *\n * Build and run:  make run        (see the Makefile beside this file)\n */\n\n'''\ns = new_head + s[len(old_head):]\nassert s.count(\"/etc/hostname\")==1\ns = s.replace(\"/etc/hostname\",\"/etc/hosts\")\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:22:08.973Z","count":1,"stdin_sample":null}
{"key":"py-7f7dd274d369","id":"py-7f7dd274d369","program":"import pathlib\np = pathlib.Path(\"Makefile\")\ns = p.read_text()\nold = '''.PHONY: all run valgrind quickstart clean\nall: $(BIN)\n'''\nnew = '''.PHONY: all run valgrind clean\nall: $(BIN)\n'''\nassert s.count(old) == 1; s = s.replace(old, new)\nold = '''quickstart: $(QUICK)\n$(QUICK): quickstart.c $(header) $(lib) $(MAKEFILE_LIST)\n'''\nnew = '''# Not built by default: it is the file to copy, not the test to run.\n$(QUICK): quickstart.c $(header) $(lib) $(MAKEFILE_LIST)\n'''\nassert s.count(old) == 1; s = s.replace(old, new)\np.write_text(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:28:09.509Z","count":1,"stdin_sample":null}
{"key":"py-80f2f0b75db0","id":"py-80f2f0b75db0","program":"from pathlib import Path\np = Path(\"docs/EMBEDDING.md\"); s = p.read_text(encoding=\"utf-8\")\nold = \"\"\"prompting study ([PROMPTING.md](PROMPTING.md) §7) drove the original five hosts\n(C, C++, Rust, Node, Python) over one shared set of 393 agent-written programs\non 2026-08-23, each host with its own copy of the set and each program with its\nown working directory, and all five answered identically: 341 ran, 52 refused,\n0 other.\"\"\"\nnew = \"\"\"prompting study ([PROMPTING.md](PROMPTING.md) §7) drove every host of that date\n(C, C++, Rust, Node, Python) over one shared set of 393 agent-written programs\non 2026-08-23, each host with its own copy of the set and each program with its\nown working directory, and all of them answered identically: 341 ran, 52\nrefused, 0 other.\"\"\"\nassert s.count(old) == 1; s = s.replace(old, new)\nold2 = \"Makefile beside it is the same binding through plain `swiftc`, with no `.build/`.\"\nnew2 = \"Makefile beside it is the same binding through plain `swiftc`, and writes no `.build` directory.\"\nassert s.count(old2) == 1; s = s.replace(old2, new2)\np.write_text(s, encoding=\"utf-8\"); print(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:26:49.252Z","count":1,"stdin_sample":null}
{"key":"py-8194ad78d771","id":"py-8194ad78d771","program":"p=\"/Users/user/dev/lypning-bindings/src/lypning/assets/examples/python/quickstart.py\"\ns=open(p).read()\nold='    lib = embed.Library()  # LibraryError already says `lypning build --lib`; let it through\\n'\nnew=('    try:\\n'\n     '        lib = embed.Library()\\n'\n     '    except embed.LibraryError as e:  # absent or unusable: one line, and it names `lypning build --lib`\\n'\n     '        sys.stderr.write(\"quickstart: %s\\\\n\" % e)\\n'\n     '        return 1\\n')\nassert s.count(old)==1\nopen(p,\"w\").write(s.replace(old,new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:32:43.925Z","count":1,"stdin_sample":null}
{"key":"py-8509aded09d9","id":"py-8509aded09d9","program":"\nimport time; from lypning import embed; L=embed.Library(); t=time.perf_counter(); o=L.run('x=0\\nfor i in range(20000000): x+=1\\nprint(x)', step_limit=10_000_000); print(o.status_name, '%.2fs' % (time.perf_counter()-t))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:13:07.443Z","count":1,"stdin_sample":null}
{"key":"py-863ddc3b2189","id":"py-863ddc3b2189","program":"import ast; [ast.parse(open(f).read(), f, feature_version=(3,9)) for f in ['src/lypning/embed.py','src/lypning/cli.py','src/lypning/engines.py']]; print('3.9 ok')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:21:14.051Z","count":1,"stdin_sample":null}
{"key":"py-870514d91392","id":"py-870514d91392","program":"\nfrom lypning import engines\nr = engines.run(engines.LYPNING, 'print(1)', timeout=10.0); print(r.returncode, repr(r.stdout), repr(r.stderr[:200]), r.wall_ns/1e6, 'ms')\nprint(engines.library_binary_drift())\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:32:20.156Z","count":1,"stdin_sample":null}
{"key":"py-87fedcba08ea","id":"py-87fedcba08ea","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nn=0\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        n+=1; v=r.get('result') or r.get('value') or {}\n        if n>20: print(n, v.get('host'), 'verdict' if 'probes_pass' in v else 'report', v.get('builds',''), v.get('probes_pass',''), v.get('contract_test_passes',''))\nprint('total results', n)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:33:47.647Z","count":1,"stdin_sample":null}
{"key":"py-88771ac40c1c","id":"py-88771ac40c1c","program":"\nfrom pathlib import Path\nfrom lypning import embed, build\nreal = Path('src/lypning/assets/rust/target/release-lib/liblypning.dylib')\nprint('real', real.stat().st_size, repr(embed._looks_like_a_library(real)))\nt = Path('/tmp/lyp-verify-trunc.dylib'); t.write_bytes(real.read_bytes()[:200000])\nprint('trunc', repr(embed._looks_like_a_library(t)))\nx = Path('/tmp/lyp-verify-text.dylib'); x.write_text('hello world, not a library\\n')\nprint('text', repr(embed._looks_like_a_library(x)))\ninst = Path('/tmp/lypning-home-b/lib/liblypning.dylib')\nprint('installed', inst.exists(), repr(embed._looks_like_a_library(inst)) if inst.exists() else '-')\ns = build.exported_symbols(real); ls = sorted(n for n in s if n.startswith('lypning_'))\nprint('exported total', len(s), 'lypning_', len(ls)); print(ls)\nprint('LIB_NAME', embed.LIB_NAME, 'LIB_SHARED', build.LIB_SHARED)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:08:09.213Z","count":1,"stdin_sample":null}
{"key":"py-88cea5bd5272","id":"py-88cea5bd5272","program":"p='src/lypning/assets/include/lypning.h'; s=open(p).read()\nold='''/* Append one entry to sys.argv[1:]. 0 on success, -1 on bad arguments. */\nint lypning_request_add_arg(lypning_request *q, const char *arg, size_t len);'''\nnew='''/* Append one entry to sys.argv[1:]. 0 on success, -1 on bad arguments: `q`\n * NULL, or `arg` not UTF-8 — which is routed onward like a NULL request, not\n * dropped, because CPython would have seen that argument and lypning did not. */\nint lypning_request_add_arg(lypning_request *q, const char *arg, size_t len);'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,'w').write(s); print(\"header edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:23:35.574Z","count":1,"stdin_sample":null}
{"key":"py-8950d6a619c0","id":"py-8950d6a619c0","program":"\nimport sys; sys.path.insert(0,'src')\nfrom lypning import embed\nfrom pathlib import Path\nprint(embed.LIB_NAME)\nprint(repr(embed._looks_like_a_library(Path('src/lypning/assets/rust/target/release-lib/liblypning.dylib'))))\nimport _ctypes; print(repr(embed._looks_like_a_library(Path(_ctypes.__file__))))\nprint(repr(embed._looks_like_a_library(Path('/tmp/lypning-home-b/bin/lypning-host'))))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:24:38.914Z","count":1,"stdin_sample":null}
{"key":"py-8b92d5a0353f","id":"py-8b92d5a0353f","program":"\nfrom lypning import embed\ntry:\n    lib = embed.Library(); print('loaded', lib.version)\nexcept embed.LibraryError as e:\n    print('LibraryError:', e)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:20:22.596Z","count":1,"stdin_sample":null}
{"key":"py-8bd0684dbb11","id":"py-8bd0684dbb11","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor v in d['reports']:\n    print('=====', v.get('host') or v.get('name') or list(v.keys())[:3])\n    for f in ('files_created','files_modified','build_and_run','ci_step','left_undone'):\n        print('--',f,':',v.get(f))\nprint('##### VERDICTS')\nfor v in d['verdicts']:\n    print('=====', v.get('host') or v.get('name') or list(v.keys()))\n    print(v.get('open'))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:34.257Z","count":1,"stdin_sample":null}
{"key":"py-8cd66a2f2964","id":"py-8cd66a2f2964","program":"p='main.go'; s=open(p).read()\nold='''import (\n\\t\"errors\"\n\\t\"os\"\n'''\nnew='''import (\n\\t\"os\"\n'''\nassert old in s; s=s.replace(old,new)\nold='''\\t\\terr := cmd.Run()\n\\t\\tvar exit *exec.ExitError\n\\t\\tif errors.As(err, &exit) {\n\\t\\t\\tos.Exit(exit.ExitCode())\n\\t\\t}\n\\t\\tif err != nil {\n'''\nnew='''\\t\\terr := cmd.Run()\n\\t\\tif exit, ok := err.(*exec.ExitError); ok {\n\\t\\t\\tos.Exit(exit.ExitCode())\n\\t\\t}\n\\t\\tif err != nil {\n'''\nassert old in s; s=s.replace(old,new)\nopen(p,'w').write(s)\nprint('quickstart edited', s.count('\\n'), 'lines')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:09:34.199Z","count":1,"stdin_sample":null}
{"key":"py-8ed282311158","id":"py-8ed282311158","program":"import time,re\nt0=time.time()\nwhile time.time()-t0 < 560:\n    s=open('/tmp/lyp-final-pytest.log').read()\n    if '\\nEXIT' in s or s.startswith('EXIT'): break\n    time.sleep(15)\ns=open('/tmp/lyp-final-pytest.log').read()\nm=re.findall(r'\\[ *\\d+%\\]', s)\nprint('progress', m[-1] if m else '?', 'done' if 'EXIT' in s else 'running')\nprint(s[-1200:] if 'EXIT' in s else '')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:03:04.010Z","count":1,"stdin_sample":null}
{"key":"py-8eed7f1aa0e4","id":"py-8eed7f1aa0e4","program":"\n\nASSETS = ROOT / \"src\" / \"lypning\" / \"assets\"\nEMBEDDING = ROOT / \"docs\" / \"EMBEDDING.md\"\n\n\ndef _embedding_section_4() -> str:\n    text = EMBEDDING.read_text(encoding=\"utf-8\")\n    start = text.index(\"\\n## 4.\")\n    end = text.index(\"\\n## 5.\", start)\n    return text[start:end]\n\n\ndef _quickstarts_on_disk() -> set:\n    \"\"\"Every ``quickstart.<ext>`` (or ``quickstart/main.<ext>``) under assets,\n    as ``assets/...`` paths. Build products have no source extension and drop\n    out; ``.o``/``.d`` are excluded by name so a stray object cannot count.\"\"\"\n    found = set()\n    for pattern in (\"examples/*/quickstart.*\", \"*/quickstart.*\",\n                    \"*/quickstart/main.*\", \"*/Sources/quickstart/main.*\"):\n        for p in ASSETS.glob(pattern):\n            if p.is_file() and p.suffix not in (\".o\", \".d\", \".pyc\"):\n                found.add(\"assets/\" + p.relative_to(ASSETS).as_posix())\n    return found\n\n\ndef test_every_host_quickstart_is_documented():\n    \"\"\"``docs/EMBEDDING.md`` section 4 is the one place the hosts are counted\n    (CLAUDE.md: the number of hosts is stated once). So it must name every\n    quickstart that exists, and every quickstart it names must exist -- a ninth\n    binding that lands without its row is a binding nobody finds, and a row\n    for a directory that was deleted sends the reader to a 404. The second\n    half: a host directory without a quickstart is a host without the one file\n    every other host has, and :file:`tests/test_hosts.py` cannot drive it.\n    \"\"\"\n    section = _embedding_section_4()\n    named = {m.group(0) for m in re.finditer(r\"assets/[\\w./-]+\", section)}\n    documented = {p for p in named if re.search(r\"quickstart(\\.\\w+|/main\\.\\w+)$\", p)}\n    on_disk = _quickstarts_on_disk()\n    assert documented == on_disk, (\n        \"docs/EMBEDDING.md section 4 and src/lypning/assets disagree on the quickstarts \"\n        \"-- only in the table: %s; only on disk: %s\"\n        % (sorted(documented - on_disk) or \"none\", sorted(on_disk - documented) or \"none\"))\n    # Every host directory has one: the example dirs by construction, and every\n    # top-level assets directory the table names (the header dir is the ABI,\n    # not a host).\n    host_dirs = {\"assets/examples/%s\" % d.name for d in (ASSETS / \"examples\").iterdir() if d.is_dir()}\n    host_dirs |= {\"assets/\" + p.split(\"/\")[1] for p in named\n                  if p.count(\"/\") >= 1 and p.split(\"/\")[1] not in (\"include\", \"examples\")}\n    without = sorted(d for d in host_dirs\n                     if not any(q.startswith(d + \"/\") for q in on_disk))\n    assert not without, (\n        \"host directories with no quickstart: %s -- every host has one, and it is \"\n        \"the file tests/test_hosts.py drives\" % \", \".join(without))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:12:25.596Z","count":1,"stdin_sample":null}
{"key":"py-906909544f91","id":"py-906909544f91","program":"from pathlib import Path\np = Path(\"src/lypning/build.py\")\ns = p.read_text()\n\nold = \"\"\"from . import UNSUPPORTED_EXIT\nfrom . import engines\nfrom . import paths\n\"\"\"\nnew = \"\"\"from . import UNSUPPORTED_EXIT\nfrom . import embed\nfrom . import engines\nfrom . import paths\n\"\"\"\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''#: What ``--lib`` produces, and the two rules that shape the list. The shared\n#: object is what a harness dlopens or links; the static archive is for a host\n#: that would rather ship one file; the headers are the contract both compile\n#: against, so they are installed BESIDE the library and never left in the\n#: package tree, which a wheel makes read-only.\nLIB_SHARED = \"liblypning.so\"\nLIB_STATIC = \"liblypning.a\"\n'''\nnew = '''#: What ``--lib`` produces, and the two rules that shape the list. The shared\n#: library is what a harness dlopens or links; the static archive is for a host\n#: that would rather ship one file; the headers are the contract both compile\n#: against, so they are installed BESIDE the library and never left in the\n#: package tree, which a wheel makes read-only. The shared library's name is\n#: the platform's (``.dylib`` on macOS) and is decided once, in :mod:`embed`,\n#: because the build, the installer and discovery must agree on it or the\n#: build reports ``ok`` about a file discovery never finds.\nLIB_SHARED = embed.LIB_NAME\nLIB_STATIC = \"liblypning.a\"\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''    from . import embed as lib_embed\n    ok, why = lib_embed.check_refusal_contract(shared)\n'''\nnew = '''    ok, why = embed.check_refusal_contract(shared)\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = s[s.index(\"def _lib_symbols(shared: Path) -> int:\"):s.index(\"def install_library(\")]\nnew = '''def exported_symbols(shared: Path | str) -> set[str]:\n    \"\"\"The names a shared library exports, as C sees them.\n\n    ``nm`` in the platform's dialect: ``-gU`` on macOS, where Mach-O prefixes\n    every C symbol with an underscore that is stripped here so the names match\n    the header; ``-D --defined-only`` elsewhere, where the dynamic symbol table\n    is the one a host links against. Raises :class:`OSError` naming what\n    failed — no ``nm``, or one that would not read the file — rather than\n    answering an empty set, because \"exports nothing\" and \"could not look\" are\n    different facts and a test skipping on the second must say so.\n    \"\"\"\n    nm = shutil.which(\"nm\")\n    if not nm:\n        raise OSError(\"nm is not on PATH\")\n    macho = sys.platform == \"darwin\"\n    cmd = [nm, \"-gU\", str(shared)] if macho else [nm, \"-D\", \"--defined-only\", str(shared)]\n    rc, out = _run(cmd, timeout=60.0)\n    if rc != 0:\n        raise OSError(\"%s exited %d: %s\" % (\" \".join(cmd[:2]), rc, out.strip()[-200:]))\n    names: set[str] = set()\n    for line in out.splitlines():\n        parts = line.split()\n        if len(parts) < 2:\n            continue\n        name = parts[-1]\n        if macho and name.startswith(\"_\"):\n            name = name[1:]\n        names.add(name)\n    return names\n\n\ndef _lib_symbols(shared: Path) -> int:\n    \"\"\"How many ``lypning_*`` symbols the library exports.\n\n    Reported rather than asserted: the number is a build's fingerprint, and a\n    build that suddenly exports two of them is a linker script gone wrong long\n    before anyone's host fails to find a symbol. ``nm`` is not required to be\n    present, and 0 means \"could not tell\", never \"none\".\n    \"\"\"\n    try:\n        return sum(1 for name in exported_symbols(shared) if name.startswith(\"lypning_\"))\n    except OSError:\n        return 0\n\n\ndef _lib_dir() -> Path:\n    return embed.lib_dir()\n\n\ndef _include_dir() -> Path:\n    return embed.include_dir()\n\n\n'''\ns = s.replace(old, new, 1)\n\nold = \"\"\"import socket\nimport subprocess\nimport time\n\"\"\"\nnew = \"\"\"import socket\nimport subprocess\nimport sys\nimport time\n\"\"\"\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = \"\"\"            # `include/` (:func:`install_library`); dropping a `.so` into the\n            # directory the engine finders read would make `find_lypning` offer\n            # a shared object to `os.execv`.\"\"\"\nnew = \"\"\"            # `include/` (:func:`install_library`); dropping a shared library\n            # into the directory the engine finders read would make\n            # `find_lypning` offer it to `os.execv`.\"\"\"\nassert s.count(old) == 1; s = s.replace(old, new, 1)\np.write_text(s)\nprint(\"build.py ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:25:22.049Z","count":1,"stdin_sample":null}
{"key":"py-9095e6db3487","id":"py-9095e6db3487","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor r in d['reports']:\n    print('=====',r['host'])\n    for kk in ('files_created','files_modified','doc_row','build_and_run','ci_step','left_undone'):\n        v=r.get(kk)\n        if isinstance(v,list): v=[x.replace('/Users/user/dev/lypning-bindings/','') for x in v]\n        print('--',kk,':',v)\nprint('===== VERDICTS')\nfor v in d['verdicts']:\n    print(v.get('host'), '::', v.get('open'))\n","argv_tail":["2>"],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:05.983Z","count":1,"stdin_sample":null}
{"key":"py-928521822719","id":"py-928521822719","program":"from pathlib import Path\np = Path(\"src/lypning/cli.py\")\ns = p.read_text()\n\nold = '''def _is_elf(p: Path) -> bool:\n    try:\n        with open(str(p), \"rb\") as fh:\n            return fh.read(4) == b\"\\\\x7fELF\"\n    except OSError:\n        return False\n'''\nnew = '''#: ELF, then Mach-O 64-bit in both byte orders, then a universal (\"fat\") image.\n#: A compiled engine is one of these on every platform this package runs on.\n_NATIVE_MAGICS = (b\"\\\\x7fELF\", b\"\\\\xcf\\\\xfa\\\\xed\\\\xfe\", b\"\\\\xfe\\\\xed\\\\xfa\\\\xcf\", b\"\\\\xca\\\\xfe\\\\xba\\\\xbe\")\n\n\ndef _is_native_binary(p: Path) -> bool:\n    \"\"\"Is this a compiled image (ELF or Mach-O) rather than a script?\"\"\"\n    try:\n        with open(str(p), \"rb\") as fh:\n            return fh.read(4) in _NATIVE_MAGICS\n    except OSError:\n        return False\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = \"    if _is_elf(Path(which)):\\n\"\nnew = \"    if _is_native_binary(Path(which)):\\n\"\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''    try:\n        drift = engines.library_binary_drift()\n        if drift:\n'''\nnew = '''    try:\n        drift = engines.library_binary_drift()\n        if drift is None:\n            # A hole, never an OK: with no usable library there is nothing to\n            # compare the binary against, and \"agree\" about a comparison that\n            # was not made is the silent kind of wrong this row exists to catch.\n            checks.append((NOTE, \"core/library agreement\",\n                           \"not compared — the C ABI or the Rust core is not built, so \"\n                           \"the frontier probes had one artifact to ask\"))\n        elif drift:\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''                   help=\"build the embeddable C ABI (liblypning.so, liblypning.a and the \"\n                        \"headers) into ~/.lypning/lib and ~/.lypning/include — for C, C++, \"'''\nnew = '''                   help=\"build the embeddable C ABI (the shared library, liblypning.a and \"\n                        \"the headers) into ~/.lypning/lib and ~/.lypning/include — for C, C++, \"'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''    s.add_argument(\"--path\", action=\"store_true\", help=\"the shared object itself\")\n    s.add_argument(\"--static\", action=\"store_true\",\n                   help=\"the archive, for a host that will not ship a .so beside its binary\")'''\nnew = '''    s.add_argument(\"--path\", action=\"store_true\", help=\"the shared library itself\")\n    s.add_argument(\"--static\", action=\"store_true\",\n                   help=\"the archive, for a host that will not ship a shared library beside \"\n                        \"its binary\")'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\n\nold = '''With no flag it prints the summary — where the shared object, the archive and\nthe headers are,'''\nnew = '''With no flag it prints the summary — where the shared library, the archive and\nthe headers are,'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\np.write_text(s)\nprint(\"cli.py ok\")\n\np = Path(\"src/lypning/engines.py\")\ns = p.read_text()\nold = '''def library_binary_drift(programs: Sequence[str] = DRIFT_PROBES,\n                         *, timeout: float = 10.0) -> list[tuple[str, str, str]]:\n    \"\"\"Return the probes on which the C ABI and the spawned binary disagree.\n'''\nnew = '''def library_binary_drift(programs: Sequence[str] = DRIFT_PROBES,\n                         *, timeout: float = 10.0) -> list[tuple[str, str, str]] | None:\n    \"\"\"Return the probes on which the C ABI and the spawned binary disagree.\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\nold = '''    Each element is ``(program, binary_verdict, library_verdict)``. An empty\n    list means the probes agree; it does not mean the artifacts are identical.\n    A host-linked library and a musl-static binary can legitimately differ in\n    the last ULP of a libm result, so float-sensitive probes stay out of the\n    default set.\n    \"\"\"\n    if find(LYPNING) is None or run_library(\"\").returncode == 127:\n        return []\n'''\nnew = '''    Each element is ``(program, binary_verdict, library_verdict)``. An empty\n    list means the probes agree; it does not mean the artifacts are identical.\n    A host-linked library and a musl-static binary can legitimately differ in\n    the last ULP of a libm result, so float-sensitive probes stay out of the\n    default set.\n\n    ``None`` means NOT MEASURED — the binary or a usable library is absent, so\n    there was one artifact to ask. It is not an empty list on purpose: \"agree\"\n    and \"could not compare\" are different facts, and a caller that rendered the\n    second as the first would report a comparison nobody made (a hole, never a\n    zero — the rule every unbuilt tier in this package follows).\n    \"\"\"\n    if find(LYPNING) is None or run_library(\"\").returncode == 127:\n        return None\n'''\nassert s.count(old) == 1; s = s.replace(old, new, 1)\np.write_text(s)\nprint(\"engines.py ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:25:38.619Z","count":1,"stdin_sample":null}
{"key":"py-934bd21248cb","id":"py-934bd21248cb","program":"import re,glob,os,subprocess\nt=open('pyproject.toml').read()\nm=re.search(r'\\[tool\\.setuptools\\.package-data\\][^\\[]*', t, re.S)\nglobs=re.findall(r'\"([^\"]+)\"', m.group(0))\ncovered=set()\nfor g in globs:\n    for p in glob.glob('src/lypning/'+g):\n        if os.path.isfile(p): covered.add(os.path.relpath(p,'src/lypning'))\n# shipped = files git tracks or untracked-but-not-ignored under the host dirs\nout=subprocess.run(['git','ls-files','--cached','--others','--exclude-standard','src/lypning/assets/go','src/lypning/assets/swift','src/lypning/assets/lua','src/lypning/assets/node','src/lypning/assets/examples','src/lypning/assets/include','src/lypning/assets/rust/build.rs'],capture_output=True,text=True).stdout.split()\nmissing=[os.path.relpath(p,'src/lypning') for p in out if os.path.relpath(p,'src/lypning') not in covered]\nprint('globs:',len(globs),'files on disk (tracked+untracked-unignored):',len(out),'covered:',len([p for p in out if os.path.relpath(p,'src/lypning') in covered]))\nprint('NOT COVERED:'); print('\\n'.join(missing) or ' (none)')\nextra=[g for g in globs if not glob.glob('src/lypning/'+g)]\nprint('globs matching nothing:',extra)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:57:01.031Z","count":1,"stdin_sample":null}
{"key":"py-956298b2af99","id":"py-956298b2af99","program":"from lypning import paths; print(paths.EXAMPLES_DIR / \"python/quickstart.py\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:23:50.968Z","count":2,"stdin_sample":null}
{"key":"py-98752476646f","id":"py-98752476646f","program":"import json,sys\nrecs=[json.loads(l) for l in open(sys.argv[1])]\nby={}\nfor r in recs: by.setdefault(r['shim'],[]).append(r)\nprint({k:len(v) for k,v in by.items()})\nprint(\"key order swift:\", list(by['swift-embed'][0].keys()))\nprint(\"key order python:\", list(by['python-embed'][0].keys()))\ns,p=by['swift-embed'],by['python-embed']\ndiff=sum(1 for a,b in zip(s,p) if a['exit_code']!=b['exit_code'] or a['program']!=b['program'] or a['argv_tail']!=b['argv_tail'])\nprint(\"swift vs python differ on\", diff, \"of\", len(s))\nprint(\"sample swift:\", json.dumps(s[0])[:200])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:33:00.934Z","count":1,"stdin_sample":null}
{"key":"py-a11b76b78500","id":"py-a11b76b78500","program":"import re\np = \"test.lua\"\ns = open(p).read()\nold = '''check(out == \"['it\\\\\\\\'s', 'b']\\\\nexit=0\\\\n\", \"quickstart: shell quoting survives a quote\", out)'''\nnew = '''check(out == \"[\\\\\"it's\\\\\", 'b']\\\\nexit=0\\\\n\", \"quickstart: shell quoting survives a quote\", out)'''\nassert old in s, \"pattern not found\"\ns = s.replace(old, new)\nopen(p, \"w\").write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:35:04.593Z","count":1,"stdin_sample":null}
{"key":"py-a29e2b90389c","id":"py-a29e2b90389c","program":"import tomllib, pathlib\nroot = pathlib.Path(\"src/lypning\")\npats = tomllib.load(open(\"pyproject.toml\",\"rb\"))[\"tool\"][\"setuptools\"][\"package-data\"][\"lypning\"]\ncovered = set()\nfor p in pats:\n    covered |= {f.resolve() for f in root.glob(p)}\nimport subprocess\ntracked = subprocess.run([\"git\",\"ls-files\",\"src/lypning/assets/examples\",\"src/lypning/assets/node\",\"src/lypning/assets/include\",\"src/lypning/embed.py\"],capture_output=True,text=True).stdout.split()\nmissing = [t for t in tracked if pathlib.Path(t).resolve() not in covered and not t.endswith(\".py\")]\nprint(\"tracked binding files NOT in any package-data glob:\", missing or \"none\")\n# and the reverse: dirs under assets/examples with no glob at all\nfor d in sorted(p for p in (root/\"assets\"/\"examples\").iterdir() if p.is_dir()):\n    print(d.name, \"globs:\", [p for p in pats if p.startswith(\"assets/examples/%s/\" % d.name)])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:07:26.760Z","count":1,"stdin_sample":null}
{"key":"py-a5a007f8f415","id":"py-a5a007f8f415","program":"import time; time.sleep(0.25)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:17:16.626Z","count":1,"stdin_sample":null}
{"key":"py-a988cb58dcd0","id":"py-a988cb58dcd0","program":"\nfrom lypning import embed; o=embed.Library().run('x=0\\nfor i in range(20000000): x+=1\\nprint(x)', step_limit=10_000_000); print(o.status_name, o.exit_code, o.fall_onward, repr(o.stdout), repr(o.stderr[:80]))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:12:03.032Z","count":1,"stdin_sample":null}
{"key":"py-a9d55ae3f3b2","id":"py-a9d55ae3f3b2","program":"p='CHANGELOG.md'; s=open(p).read()\nanchor='## Unreleased\\n\\n'\nentry='''## Unreleased\n\n**2026-09-02** — Three more hosts over the one ABI: Go, Swift, LuaJIT; and a quickstart for every host\n\n- **Go** (`assets/go/`, cgo over the unchanged header, zero modules), **Swift**\n  (`assets/swift/`, a Clang module map over the header, SwiftPM or plain\n  `swiftc`) and **LuaJIT** (`assets/lua/lypning.lua`, `ffi` over the header read\n  at load, no build step) join C, C++, Rust, Node and Python. The table in\n  `docs/EMBEDDING.md` §4 is now the one place the hosts are counted; every other\n  document says \"every host\" and points there.\n- **One quickstart contract, eight files.** Every host has a\n  `quickstart \"<python source>\" [args...]` that runs in-process under a step\n  limit, hands a refusal to `python3 -c` once, and otherwise returns the\n  program's own bytes and exit code. `tests/test_hosts.py` drives all of them\n  through the same five probes and counts the traceback on stderr exactly once,\n  which is the retry-a-failed-program drift no per-host test can see; CI runs\n  the same five probes as one shell function per host, on Linux and on a new\n  macOS job.\n- **macOS is a first-class library platform.** `lypning build --lib` writes\n  `liblypning.dylib` with an `@rpath` install name (a `build.rs` in the core\n  crate, macOS only, and byte-identical `lypning` binary with or without it);\n  the truncation check reads Mach-O the Mach-O way; `doctor` and `status`\n  report a missing library as a hole, not a zero. `pyproject.toml` now claims\n  `Operating System :: MacOS` on that basis.\n- **Run against each other, not assumed to agree.** On 2026-09-02 (macOS arm64;\n  clang, cargo, node 26, go 1.26, swift 6.3, luajit 2.1) `study/hosts/run_all.sh`\n  drove every host over the 393-program study set: each of the eight reported\n  341 ran, 52 refused, 0 other; 3144 capture records; `git status` unchanged.\n- **Packaging.** The wheel ships every binding and quickstart as source (40\n  files under `assets/{examples,go,lua,node,swift}` on 2026-09-02) and no build\n  output; `dist-check` also rejects `.build/`, `.swiftpm/`, `node_modules/` and\n  `.so`/`.dylib`/`.node`/`.class`. Both crates declare `rust-version = \"1.78\"`,\n  the floor the committed lockfile format actually imposes.\n\n'''\nassert s.count(anchor)==1; open(p,'w').write(s.replace(anchor,entry,1)); print('changelog ok')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:00:18.837Z","count":1,"stdin_sample":null}
{"key":"py-a9fcc33f38e9","id":"py-a9fcc33f38e9","program":"import re, pathlib\np = pathlib.Path(\"pyproject.toml\"); s = p.read_text()\n\nold_comment = '''  # The C ABI's contract and its four hosts. Headers and example sources are\n  # SOURCE, not build output — a wheel that shipped the library but not the\n  # header would ship something no one can compile against. Named by extension\n  # rather than by directory for the reason the MANIFEST states at length:\n  # `include-package-data` means a careless glob here puts cargo's `target/` in\n  # everyone's wheel.\n'''\nnew_comment = '''  # The C ABI's contract and every host over it (the table in\n  # docs/EMBEDDING.md section 4 is the one place that counts them). Headers,\n  # bindings and example sources are SOURCE, not build output — a wheel that\n  # shipped the library but not the header would ship something no one can\n  # compile against. Named by extension rather than by directory for the reason\n  # the MANIFEST states at length: `include-package-data` means a careless glob\n  # here puts cargo's `target/`, SwiftPM's `.build/` or npm's `node_modules/`\n  # in everyone's wheel. tests/test_packaging.py holds every glob to matching\n  # at least one file, so a renamed asset fails there and not in a `pip` user's\n  # `lypning build`.\n'''\nassert old_comment in s\ns = s.replace(old_comment, new_comment)\n\nold_block = '''  \"assets/examples/rust/src/*.rs\",\n  \"assets/node/*.js\",\n  \"assets/node/*.ts\",\n  \"assets/node/*.json\",\n  \"assets/node/*.md\",\n  \"assets/node/src/*.rs\",\n  \"assets/node/Cargo.toml\",\n  \"assets/node/Cargo.lock\",\n'''\nnew_block = '''  \"assets/examples/rust/src/*.rs\",\n  \"assets/examples/rust/examples/*.rs\",\n  \"assets/examples/python/*.py\",\n  \"assets/node/*.js\",\n  \"assets/node/*.ts\",\n  \"assets/node/*.json\",\n  \"assets/node/*.md\",\n  \"assets/node/src/*.rs\",\n  \"assets/node/Cargo.toml\",\n  \"assets/node/Cargo.lock\",\n  # build.rs is a crate file like Cargo.toml: without it the addon links on\n  # Linux and fails to link on macOS, where ld64 must be told the Node-API\n  # symbols resolve at dlopen. The core crate's build.rs is the same story\n  # for the library's @rpath install name.\n  \"assets/node/build.rs\",\n  \"assets/rust/build.rs\",\n  # The Go module has zero dependencies, hence no go.sum to ship; the\n  # `quickstart/` main package is source too.\n  \"assets/go/*.go\",\n  \"assets/go/go.mod\",\n  \"assets/go/README.md\",\n  \"assets/go/quickstart/*.go\",\n  # SwiftPM: manifest, the swiftc Makefile for people without it, and the\n  # module map, shim header, sources and tests one level under Sources/ and\n  # Tests/. `*/*` is deliberate over `**`: a recursive glob would follow a\n  # `.build/` that a user's `swift build` left behind.\n  \"assets/swift/Package.swift\",\n  \"assets/swift/Makefile\",\n  \"assets/swift/README.md\",\n  \"assets/swift/Sources/*/*\",\n  \"assets/swift/Tests/*/*\",\n  # LuaJIT: no build step, so nothing here is ever output.\n  \"assets/lua/*.lua\",\n  \"assets/lua/README.md\",\n'''\nassert old_block in s\ns = s.replace(old_block, new_block)\n\nold_cls = '''  \"Operating System :: POSIX :: Linux\",\n'''\nnew_cls = '''  \"Operating System :: POSIX :: Linux\",\n  # Claimed the day `lypning build --lib` and the test suite passed on an\n  # arm64 Mac (2026-09-02, from the host-bindings branch), which is also the\n  # day the macOS job in ci.yml started asserting it. The Rust core's static\n  # musl gate is Linux-only; on macOS the CLI, the library tier and the hosts\n  # over it are what this line promises.\n  \"Operating System :: MacOS\",\n'''\nassert old_cls in s\ns = s.replace(old_cls, new_cls)\np.write_text(s)\n\n# MANIFEST.in\np = pathlib.Path(\"MANIFEST.in\"); s = p.read_text()\nold = '''# The same defence for the three build trees the C ABI's hosts create: a cargo\n# `target/` under an example or under the Node addon is the same gigabyte by\n# another name, and `pip install` must not carry it.\nprune src/lypning/assets/examples/rust/target\nprune src/lypning/assets/node/target\nprune src/lypning/assets/node/node_modules\n'''\nnew = '''# The same defence for every build tree the C ABI's hosts create: a cargo\n# `target/` under an example or under the Node addon is the same gigabyte by\n# another name, and `pip install` must not carry it. SwiftPM writes `.build/`\n# (and `.swiftpm/` for an IDE that opened the package); the Swift Makefile's\n# swiftc path writes `build/`. Go and LuaJIT create no tree here: `go build`\n# writes to GOCACHE and the Lua host has no build step. tests/test_packaging.py\n# pins every prune below, so removing one is a deliberate edit there too.\nprune src/lypning/assets/examples/rust/target\nprune src/lypning/assets/node/target\nprune src/lypning/assets/node/node_modules\nprune src/lypning/assets/swift/.build\nprune src/lypning/assets/swift/.swiftpm\nprune src/lypning/assets/swift/build\n'''\nassert old in s\np.write_text(s.replace(old, new))\n\n# .gitignore\np = pathlib.Path(\".gitignore\"); s = p.read_text()\nold = '''src/lypning/assets/examples/rust/target/\nsrc/lypning/assets/node/target/\nsrc/lypning/assets/examples/*/embed\nsrc/lypning/assets/examples/*/*.o\n'''\nnew = '''src/lypning/assets/examples/rust/target/\nsrc/lypning/assets/node/target/\nsrc/lypning/assets/examples/*/embed\nsrc/lypning/assets/examples/*/quickstart\nsrc/lypning/assets/examples/*/*.o\n# Go drops a binary next to its main package when built without -o; SwiftPM\n# writes .build/ (and .swiftpm/ when an IDE opens it), the swiftc Makefiles\n# write build/ and a bare binary. The study's Go and Swift hosts do the same.\nsrc/lypning/assets/go/quickstart/quickstart\nsrc/lypning/assets/swift/.build/\nsrc/lypning/assets/swift/.swiftpm/\nsrc/lypning/assets/swift/build/\nsrc/lypning/assets/swift/quickstart\n'''\nassert old in s\ns = s.replace(old, new)\nold = '''study/hosts/run_rust/target/\n'''\nnew = '''study/hosts/run_rust/target/\nstudy/hosts/run_go/run_go\nstudy/hosts/run_swift/build/\nstudy/hosts/run_swift/run_swift\n'''\nassert old in s\np.write_text(s.replace(old, new))\n\n# Makefile\np = pathlib.Path(\"Makefile\"); s = p.read_text()\nold = '''\t@$(PYTHON) -c \"import glob, zipfile; bad = sorted(n for w in glob.glob('dist/*.whl') for n in zipfile.ZipFile(w).namelist() if '__pycache__' in n or '/target/' in n or '/build/' in n or n.endswith(('.pyc', '.o', '.a', '.rlib'))); print('wheel: no build output' if not bad else 'wheel CONTAMINATED with build output: ' + ', '.join(bad[:5])); raise SystemExit(bool(bad))\"'''\nnew = '''\t@$(PYTHON) -c \"import glob, zipfile; bad = sorted(n for w in glob.glob('dist/*.whl') for n in zipfile.ZipFile(w).namelist() if '__pycache__' in n or '/target/' in n or '/build/' in n or '/.build/' in n or '/.swiftpm/' in n or '/node_modules/' in n or n.endswith(('.pyc', '.o', '.a', '.rlib', '.so', '.dylib', '.node', '.class'))); print('wheel: no build output' if not bad else 'wheel CONTAMINATED with build output: ' + ', '.join(bad[:5])); raise SystemExit(bool(bad))\"'''\nassert old in s\np.write_text(s.replace(old, new))\n\n# Cargo.toml\np = pathlib.Path(\"src/lypning/assets/rust/Cargo.toml\"); s = p.read_text()\nold = '''license = \"MIT\"\n\n# ZERO runtime'''\nnew = '''license = \"MIT\"\n# The floor is set by `Option::is_some_and` (1.70); measured by grep over the\n# crate, not by a compile under 1.70, since no older toolchain was at hand.\nrust-version = \"1.70\"\n\n# ZERO runtime'''\nassert old in s\np.write_text(s.replace(old, new))\nprint(\"edits ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:07:54.979Z","count":1,"stdin_sample":null}
{"key":"py-abe1dcd59067","id":"py-abe1dcd59067","program":"print(1/0)","argv_tail":["2>"],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:31:05.054Z","count":1,"stdin_sample":null}
{"key":"py-ae86792e7159","id":"py-ae86792e7159","program":"import re\n# 1. core crate\np='src/lypning/assets/rust/Cargo.toml'; s=open(p).read()\nold='''# The floor is set by `Option::is_some_and` (1.70); measured by grep over the\n# crate, not by a compile under 1.70, since no older toolchain was at hand.\nrust-version = \"1.70\"'''\nnew='''# The floor is the committed Cargo.lock, which the wheel ships and `lypning\n# build` hands to cargo: it is lockfile format 4, which cargo before 1.78\n# refuses to read. The source itself needs only `Option::is_some_and` (1.70),\n# measured by grep rather than by a compile under 1.70, but a floor a user\n# cannot actually build at is a claim, not a floor.\nrust-version = \"1.78\"'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new))\n# 2. example crate\np='src/lypning/assets/examples/rust/Cargo.toml'; s=open(p).read()\nold='''# The floor is the crate's, not this file's: `Option::is_some_and` (1.70) in\n# `../../rust/src/eval.rs` and `json.rs`. The let-else on the quickstart's one\n# branch needs only 1.65 and sits under it. Stated so that an older cargo says\n# \"requires rustc 1.70\" instead of failing on a method it has never heard of.\n# (The committed Cargo.lock is format 4, which cargo before 1.78 cannot read;\n# delete it and cargo will write one it can.)\nrust-version = \"1.70\"'''\nnew='''# The floor is the runtime crate's, not this file's, and it is set by the\n# committed Cargo.lock (format 4, unreadable before cargo 1.78) rather than by\n# the source (`Option::is_some_and`, 1.70). The let-else on the quickstart's one\n# branch needs only 1.65 and sits under both. Stated so that an older cargo says\n# \"requires rustc 1.78\" instead of failing on a lockfile it has never heard of.\nrust-version = \"1.78\"'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new))\n# 3. EMBEDDING.md section 4 notes\np='docs/EMBEDDING.md'; s=open(p).read()\nold='''the package's `assets/` in a wheel; the run column is written from the\nrepository root, after `lypning build --lib`.'''\nnew='''the package's `assets/` in a wheel; the run column is written from the\nrepository root, after `lypning build --lib`. Every row finds a checkout's\nlibrary by itself; the Python row imports the package, so in a checkout that\nhas not been `pip install`ed it wants `PYTHONPATH=src` in front.'''\nassert s.count(old)==1; s=s.replace(old,new)\nold='''*Go* finds a checkout's library through the binding's own `#cgo` directives.\nAgainst an installed library it needs `CGO_LDFLAGS=\"$(lypning lib --libs)\"`,\nwhich carries the `-rpath` the binary will need at load time.'''\nnew='''*Go* finds a checkout's library through the binding's own `#cgo` directives.\nAgainst an installed library it needs `CGO_LDFLAGS=\"$(lypning lib --libs)\"`,\nwhich carries the `-rpath` the binary will need at load time. `go run` is fine\nfor the first probe and wrong for the last two: it reports every non-zero exit\nas 1 with an `exit status N` line of its own, so the contract test and CI\n`go build` the binary and run that.'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,'w').write(s)\nprint('edits ok')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:59:37.221Z","count":1,"stdin_sample":null}
{"key":"py-b094c04fd9cf","id":"py-b094c04fd9cf","program":"import time, pathlib, subprocess\np = pathlib.Path(\"/tmp/conformance-lib.txt\")\nfor _ in range(590):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nt = p.read_text(errors=\"replace\") if p.exists() else \"\"\nprint(\"DONE\" if \"conformance exit\" in t else \"STILL RUNNING\", len(t), \"bytes\")\nprint(t[-3500:])\nprint(subprocess.run(\"ps -o pid=,etime=,pcpu=,time=,command= -A | grep '[m] lypning conformance'\", shell=True, capture_output=True, text=True).stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:58:00.807Z","count":1,"stdin_sample":null}
{"key":"py-b45017f98d70","id":"py-b45017f98d70","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'package_data_globs' in v:\n        print('='*80); print('REPORT', v['host'])\n        print(' created:', v['files_created']); print(' modified:', v['files_modified'])\n        print(' globs:', v['package_data_globs']); print(' prunes:', v['manifest_prunes']); print(' gitignore:', v['gitignore_lines'])\n        print(' row:', v['doc_row']); print(' build:', v['build_and_run'])\n        print(' snippet:\\n', v['doc_snippet']); print(' undone:', v['left_undone'][:1500])\n    elif 'probes_pass' in v:\n        print('='*80); print('VERDICT', v['host'], 'builds',v['builds'],'probes',v['probes_pass'],'contract',v['contract_test_passes'])\n        print(' fixed:', v['fixed']); print(' open:', v['open']); print(' notes:', v['review_notes'][:1200])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:02:01.587Z","count":1,"stdin_sample":null}
{"key":"py-b5ccd2b97e07","id":"py-b5ccd2b97e07","program":"p='src/lypning/assets/examples/cpp/quickstart.cpp'\ns=open(p).read()\nold_inc='#include <cstdio>\\n#include <unistd.h>\\n'\nnew_inc='#include <cstdio>\\n#include <fcntl.h>\\n#include <unistd.h>\\n'\nold='        std::freopen(\"/dev/null\", \"r\", stdin);\\n'\nnew='        dup2(open(\"/dev/null\", O_RDONLY), STDIN_FILENO);\\n'\nassert s.count(old_inc)==1 and s.count(old)==1\ns=s.replace(old_inc,new_inc).replace(old,new)\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:21:37.460Z","count":1,"stdin_sample":null}
{"key":"py-b7adab4c6a31","id":"py-b7adab4c6a31","program":"\nimport ast,sys\nfor f in ['src/lypning/build.py','src/lypning/embed.py','src/lypning/cli.py','src/lypning/engines.py','tests/test_embed.py','tests/test_build.py','tests/test_cli.py','tests/test_engines.py']:\n    ast.parse(open(f).read(), f, feature_version=(3,9)); print('ok', f)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:08:09.213Z","count":1,"stdin_sample":null}
{"key":"py-b8060a8aaf0e","id":"py-b8060a8aaf0e","program":"import setuptools; print('system setuptools', setuptools.__version__)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:22:57.606Z","count":1,"stdin_sample":null}
{"key":"py-b9a751462106","id":"py-b9a751462106","program":"import pathlib\np = pathlib.Path(\"lypning.go\"); s = p.read_text()\nold_snip = \"\"\"//  r := lypning.Run(src, nil)\n//  if r.FallOnward { runOnPython3(src) } // lypning ran NOTHING; your existing path\n//  else            { use(r.Stdout) }     // bytes: output is not always UTF-8\n\"\"\"\nnew_snip = \"\"\"//  r := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\n//  if r.FallOnward {\n//      runOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\n//  } else {\n//      use(r.Stdout, r.Stderr, r.ExitCode) // bytes; a traceback IS the answer: exit 1\n//  }\n\"\"\"\nassert old_snip in s; s = s.replace(old_snip, new_snip)\n\nold_nil = \"\"\"    if q == nil {\n        // The same shape the runtime gives a NUL byte in the source: a\n        // refusal of kind \"source\", so a host branches on it like any other.\n        const detail = \"not UTF-8\"\n        return Result{\n            Status:     Unsupported,\n            ExitCode:   UnsupportedExit,\n            Stdout:     []byte{},\n            Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n            Kind:       \"source\",\n            Detail:     detail,\n            FallOnward: true,\n        }\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n    }\n\"\"\"\nnew_nil = \"\"\"    if q == nil {\n        return onward(\"not UTF-8\")\n    }\n    if opts.Filename != \"\" {\n        f := C.CString(opts.Filename)\n        rc := C.lypning_request_set_filename(q, f, C.size_t(len(opts.Filename)))\n        C.free(unsafe.Pointer(f))\n        if rc != 0 {\n            return onward(\"sys.argv[0] not UTF-8\")\n        }\n    }\n    for _, a := range opts.Args {\n        c := C.CString(a)\n        rc := C.lypning_request_add_arg(q, c, C.size_t(len(a)))\n        C.free(unsafe.Pointer(c))\n        if rc != 0 {\n            // The ABI declines an argument that is not UTF-8. Dropping it and\n            // running anyway would run a different program than the one\n            // CPython would see, which is exactly the silent disagreement a\n            // refusal exists to prevent.\n            return onward(\"argv not UTF-8\")\n        }\n    }\n\"\"\"\nassert old_nil in s; s = s.replace(old_nil, new_nil)\n\nold_run = \"\"\"// Run runs src in this goroutine's thread\"\"\"\nnew_run = \"\"\"// onward is the Result for the one thing the ABI will not build a request\n// for: bytes that are not UTF-8, in the source or in argv. It has the same\n// shape the runtime gives a NUL byte in the source, a refusal of kind\n// \"source\", so a host branches on it like any other, and FallOnward is true\n// because CPython would have seen those bytes and has its own message for them.\nfunc onward(detail string) Result {\n    return Result{\n        Status:     Unsupported,\n        ExitCode:   UnsupportedExit,\n        Stdout:     []byte{},\n        Stderr:     []byte(\"lypning: unsupported: source: \" + detail + \"\\\\n\"),\n        Kind:       \"source\",\n        Detail:     detail,\n        FallOnward: true,\n    }\n}\n\n// Run runs src in this goroutine's thread\"\"\"\nassert old_run in s; s = s.replace(old_run, new_run, 1)\nold_doc = \"\"\"// build a request for, a src that is not UTF-8, because that program too still\n// needs an answer and CPython can give it one.\"\"\"\nnew_doc = \"\"\"// build a request for, a src or an argument that is not UTF-8, because that\n// program too still needs an answer and CPython can give it one.\"\"\"\nassert old_doc in s; s = s.replace(old_doc, new_doc)\np.write_text(s)\n\nt = pathlib.Path(\"lypning_test.go\"); s = t.read_text()\nold_t = \"\"\"func TestNULInSourceRefuses(t *testing.T) {\"\"\"\nnew_t = \"\"\"// An argument is bytes too. The ABI declines one that is not UTF-8, and the\n// binding must route the whole program onward rather than run it with one\n// argument fewer than CPython would see.\nfunc TestNonUTF8ArgFallsOnward(t *testing.T) {\n    r := lypning.Run(\"import sys; print(len(sys.argv[1:]))\", &lypning.Options{Args: []string{\"a\\\\xff\", \"b\"}})\n    if !r.FallOnward || r.Status != lypning.Unsupported || r.ExitCode != 90 || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v status %v exit %d stdout %q\", r.FallOnward, r.Status, r.ExitCode, r.Stdout)\n    }\n    if r.Kind != \"source\" || !strings.HasPrefix(string(r.Stderr), \"lypning: unsupported: source: \") {\n        t.Fatalf(\"kind %q stderr %q\", r.Kind, r.Stderr)\n    }\n    r = lypning.Run(\"print(1)\", &lypning.Options{Filename: \"p\\\\xff.py\"})\n    if !r.FallOnward || r.Kind != \"source\" || len(r.Stdout) != 0 {\n        t.Fatalf(\"fall %v kind %q stdout %q\", r.FallOnward, r.Kind, r.Stdout)\n    }\n}\n\nfunc TestNULInSourceRefuses(t *testing.T) {\"\"\"\nassert old_t in s; s = s.replace(old_t, new_t)\nt.write_text(s)\n\nr = pathlib.Path(\"README.md\"); s = r.read_text()\nold_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000})\nif r.FallOnward { runOnPython3(src) }  // lypning ran NOTHING; your existing path\nelse            { use(r.Stdout) }      // []byte: output is not always UTF-8\n\"\"\"\nnew_r = \"\"\"r := lypning.Run(src, &lypning.Options{StepLimit: 10000000}) // no process to kill\nif r.FallOnward {\n    runOnPython3(src) // lypning ran NOTHING; your existing path, unchanged\n} else {\n    use(r.Stdout, r.Stderr, r.ExitCode) // []byte; a traceback IS the answer: exit 1\n}\n\"\"\"\nassert old_r in s; s = s.replace(old_r, new_r)\nold_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` that is not UTF-8 (the ABI will not build a request for one, and that too\nis CPython's to reject with its own message).\"\"\"\nnew_r2 = \"\"\"refusal is a `Result` with `Kind`/`Detail` saying what it declined, and so is a\n`src` or an argument that is not UTF-8 (the ABI will not build a request for\none, and that too is CPython's to reject with its own message).\"\"\"\nassert old_r2 in s; s = s.replace(old_r2, new_r2)\nr.write_text(s)\nprint(\"edits applied\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:30:24.868Z","count":1,"stdin_sample":null}
{"key":"py-b9fee0d7de71","id":"py-b9fee0d7de71","program":"import re, pathlib\np = pathlib.Path('src/napi.rs'); s = p.read_text()\nold = \"\"\"/// `napi_valuetype`. Only the two we branch on are named.\npub const TYPE_UNDEFINED: c_int = 0;\npub const TYPE_NULL: c_int = 1;\n\"\"\"\nnew = \"\"\"/// `napi_valuetype`. Only the three we branch on are named.\npub const TYPE_UNDEFINED: c_int = 0;\npub const TYPE_NULL: c_int = 1;\npub const TYPE_OBJECT: c_int = 6;\n\"\"\"\nassert old in s; s = s.replace(old, new)\nold = \"\"\"pub unsafe fn is_undefined(env: Env, v: Value) -> bool {\n    let mut t: c_int = 0;\n    napi_typeof(env, v, &mut t);\n    t == TYPE_UNDEFINED || t == TYPE_NULL\n}\n\"\"\"\nnew = old + \"\"\"\n/// Is this a plain object — the only shape an options bag can have?\n///\n/// `napi_typeof` answers `object` for an array too (JS's `typeof []` does), so\n/// arrays are asked about separately. A function is `function`, not `object`,\n/// and is out for the same reason a number is: reading `.stepLimit` off any of\n/// them answers `undefined` for every option, which would make `run(src, 42)`\n/// and `run(src, [10])` silently run with NO step limit — the one option a\n/// caller must never lose by accident.\npub unsafe fn is_object(env: Env, v: Value) -> bool {\n    let mut t: c_int = 0;\n    napi_typeof(env, v, &mut t);\n    if t != TYPE_OBJECT {\n        return false;\n    }\n    let mut is_array = false;\n    napi_is_array(env, v, &mut is_array);\n    !is_array\n}\n\"\"\"\nassert old in s; s = s.replace(old, new); p.write_text(s)\n\np = pathlib.Path('src/lib.rs'); s = p.read_text()\nold = \"\"\"    if napi::is_undefined(env, opts) {\n        return Ok(());\n    }\n\"\"\"\nnew = \"\"\"    if napi::is_undefined(env, opts) {\n        return Ok(());\n    }\n    // A number, a string, an array where the options object belongs is a\n    // caller bug, and the cheap reading of it — every property lookup answers\n    // `undefined`, so every option is \"unset\" — is the expensive one: a\n    // `stepLimit` the caller believes is in force is not, and `while True:\n    // pass` hangs the event loop. So it is a TypeError, like every other wrong\n    // type here, and nothing runs.\n    if !napi::is_object(env, opts) {\n        return Err(\"opts must be an object\".to_string());\n    }\n\"\"\"\nassert old in s; s = s.replace(old, new); p.write_text(s)\n\np = pathlib.Path('index.d.ts'); s = p.read_text()\nold = \"\"\"  /** Every module the program imports, in source order. */\n  imports: string[];\"\"\"\nnew = \"\"\"  /**\n   * Every module the program imports. SORTED AND DEDUPLICATED, not in source\n   * order: the question is which modules a program needs, which has no order.\n   * For the one import that decided the tier, read `detail`.\n   */\n  imports: string[];\"\"\"\nassert old in s; s = s.replace(old, new)\nold = \"\"\"  /** Did anything reach the disk? A refusal with `false` here is observably a no-op. */\n  committed: boolean;\n  /**\n   * Run this program on CPython now. True exactly when lypning refused and left\n   * nothing behind. **This is the field to branch on.**\n   */\n  fallOnward: boolean;\"\"\"\nnew = \"\"\"  /**\n   * Did the run pass the commit point — where staged output and staged file\n   * writes are flushed and the run stops being reversible? `true` for any run\n   * that finished, whether or not it touched a file; `false` for a refusal,\n   * which is what makes the program safe to hand to CPython. Branch on\n   * `fallOnward`, which already folds this in.\n   */\n  committed: boolean;\n  /**\n   * Run this program on CPython now. True for every outcome that is not the\n   * program's own answer and left nothing behind: a refusal, a `busy` that ran\n   * nothing, and a `panic` before the commit point. Never true for `ok` or\n   * `error` — a traceback is as much of an answer as a printed line.\n   * **This is the field to branch on.**\n   */\n  fallOnward: boolean;\"\"\"\nassert old in s; s = s.replace(old, new)\nold = \"\"\" * Throws only for a caller type error (source that is not a string, an option\n * of the wrong type). A program outside the subset comes back with\n * `fallOnward === true` and is not an error.\"\"\"\nnew = \"\"\" * Throws only for a caller type error (source that is not a string, `opts`\n * that is not an object, an option of the wrong type). A program outside the\n * subset comes back with `fallOnward === true` and is not an error.\"\"\"\nassert old in s; s = s.replace(old, new); p.write_text(s)\n\np = pathlib.Path('index.js'); s = p.read_text()\nold = \"\"\"// a fallback onto some other addon the user did not name. With it unset there\n// are two places, and they answer two different questions: ~/.lypning/lib is\n// where `lypning build` installs it, which is the wheel-install case where this\n// directory is read-only; the crate's own target/ is the source checkout, where\n// `cargo build --release` here shares an object cache with the rest of the\n// tree. A checkout that has both prefers the deliberate one over the\n// incidental one.\"\"\"\nnew = \"\"\"// a fallback onto some other addon the user did not name. With it unset there\n// are two places, and they answer two different questions. The crate's own\n// target/ is where `cargo build --release` here puts it, which is how the addon\n// is built today — nothing else builds it, `lypning build` included. ~/.lypning/lib\n// ($LYPNING_HOME/lib) is where a copy goes when this directory is read-only,\n// the wheel-install case: build the crate somewhere writable and copy the\n// cdylib there by hand. A checkout that has both prefers the copy that was\n// placed on purpose over the one a build left behind.\"\"\"\nassert old in s; s = s.replace(old, new)\nold = \"\"\"  // `release-lib` is the profile the rest of the tree builds the library with;\n  // it is checked so a developer who used it out of habit is not told the\n  // addon is missing while it sits one directory over.\n  for (const profile of ['release', 'release-lib', 'debug']) {\"\"\"\nnew = \"\"\"  // `release` is what the build line above produces; `debug` is what a bare\n  // `cargo build` does, checked so a developer is not told the addon is\n  // missing while it sits one directory over. No `release-lib`: that profile\n  // belongs to the runtime crate, and this one does not define it.\n  for (const profile of ['release', 'debug']) {\"\"\"\nassert old in s; s = s.replace(old, new); p.write_text(s)\n\np = pathlib.Path('example.js'); s = p.read_text()\nold = \"\"\"// --- 6. the branch a harness must write -------------------------------------\n\nrule('fall onward to CPython');\n\"\"\"\nnew = \"\"\"// --- 6. the branch a harness must write -------------------------------------\n//\n// This is quickstart.js applied to three programs. The `if (r.fallOnward)` is\n// the same one, with the same stepLimit reasoning; what is added is only the\n// bookkeeping that lets the three lines print side by side.\n\nrule('fall onward to CPython');\n\"\"\"\nassert old in s; s = s.replace(old, new); p.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:25:16.186Z","count":1,"stdin_sample":null}
{"key":"py-ba302f2cc4be","id":"py-ba302f2cc4be","program":"\nfrom lypning import embed\nlib = embed.Library()\nprint(lib.path, lib.version, lib.abi)\no = lib.run('print(sum(range(10)))', step_limit=10_000_000)\nprint(o)\no = lib.run('import subprocess; print(1)')\nprint(o)\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:02:49.541Z","count":1,"stdin_sample":null}
{"key":"py-bb16c9d7d3eb","id":"py-bb16c9d7d3eb","program":"import re,io\np='CHANGELOG.md'; s=open(p).read()\nold=\"**2026-09-02** — Three more hosts over the one ABI: Go, Swift, LuaJIT; and a quickstart for every host\\n\"\nassert old in s\ns=s.replace(old, old.rstrip('\\n')+\" · [#25]\\n\",1)\n# reference link list at the bottom, keep it sorted by number: insert before [#1]\nref=\"[#25]: https://github.com/kristerhedfors/lypning/pull/25\\n\"\nassert ref not in s\ns=s.replace(\"[#1]: https://github.com/kristerhedfors/lypning/pull/1\\n\", ref+\"[#1]: https://github.com/kristerhedfors/lypning/pull/1\\n\",1)\nopen(p,'w').write(s); print(\"changelog linked\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:57:44.006Z","count":1,"stdin_sample":null}
{"key":"py-bd3dee1fafc9","id":"py-bd3dee1fafc9","program":"\nimport json; r=json.load(open('/tmp/lypning-host-reports.json'))\nprint(type(r), list(r.keys()) if isinstance(r,dict) else len(r))\nfor k,v in (r.items() if isinstance(r,dict) else enumerate(r)):\n    print('==',k)\n    if isinstance(v,dict):\n        for f in ('files_created','files_modified','package_data_globs','manifest_prunes','gitignore_lines','build_and_run','ci_step','left_undone'):\n            print('  ',f,':',v.get(f))\n        print('   verdict.open:', (v.get('verdict') or {}).get('open'))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:54:18.889Z","count":1,"stdin_sample":null}
{"key":"py-bfc34c2d77ae","id":"py-bfc34c2d77ae","program":"from pathlib import Path\np = Path(\"study/hosts/run_all.sh\")\ns = p.read_text()\ns = s.replace(\"\"\"# The five hosts are five bindings over one C ABI, so they must agree on which\n# programs the subset takes; running them one after the other over an identical\n# directory is what makes a disagreement visible instead of theoretical.\n\"\"\", \"\"\"# Every host is a binding over one C ABI (docs/EMBEDDING.md section 4 is the\n# list), so they must agree on which programs the subset takes; running them one\n# after the other over an identical directory is what makes a disagreement\n# visible instead of theoretical. A host missing here is a host the study never\n# checked, which is why this script runs all of them and skips none: build every\n# driver first with `make -C study/hosts`.\n\"\"\")\ns = s.replace(\"\"\"run_in_scratch \"${STUDY_PYTHON:-python3}\" \"$here/run_py.py\"\n\"\"\", \"\"\"run_in_scratch \"${STUDY_PYTHON:-python3}\" \"$here/run_py.py\"\nrun_in_scratch \"$here/run_go/run_go\"\nrun_in_scratch \"$here/run_swift/run_swift\"\nrun_in_scratch luajit \"$here/run_lua.lua\"\n\"\"\")\np.write_text(s)\n\nm = Path(\"study/hosts/Makefile\")\ns = m.read_text()\ns = s.replace(\"\"\"# The two native hosts, linked against whichever liblypning this machine has.\n# Mirrors assets/examples/{c,cpp}/Makefile: prefer the checkout's freshly built\n\"\"\", \"\"\"# Every study driver that has a build step, linked against whichever liblypning\n# this machine has; `make -C study/hosts` is what run_all.sh expects to have run.\n# Mirrors assets/examples/{c,cpp}/Makefile: prefer the checkout's freshly built\n\"\"\")\ns = s.replace(\"\"\".PHONY: all clean check\nall: run_c run_cpp\n\"\"\", \"\"\".PHONY: all clean check run_rust run_go run_swift node\nall: run_c run_cpp run_rust run_go run_swift node\n\"\"\")\ns = s.replace(\"\"\"clean:\n\trm -f run_c run_cpp\n\"\"\", \"\"\"# The Rust driver compiles the crate and links no library; it is here so one\n# make builds every driver run_all.sh names.\nrun_rust:\n\tcargo build --release --manifest-path $(here)/run_rust/Cargo.toml\n\n# cgo finds the checkout's library through the binding's own #cgo directives;\n# LIBDIR is passed anyway so `make LIBDIR=...` means the same thing for every\n# driver. A directory named run_go exists, so the target is phony on purpose.\nrun_go: | check\n\t@command -v go >/dev/null || { echo \"no go on PATH: the Go driver needs a Go toolchain\"; exit 1; }\n\tcd $(here)/run_go && CGO_LDFLAGS=\"-L$(LIBDIR) -Wl,-rpath,$(LIBDIR)\" go build -o run_go .\n\n# Plain swiftc, no SwiftPM: run_swift/Makefile compiles the binding to a module\n# and links this driver against it.\nrun_swift: | check\n\t@command -v swiftc >/dev/null || { echo \"no swiftc on PATH: the Swift driver needs a Swift toolchain\"; exit 1; }\n\t$(MAKE) -C $(here)/run_swift LIBDIR=$(LIBDIR)\n\n# The Node driver loads the addon in assets/node, which is its own cargo build\n# and links the runtime statically: `lypning build --lib` does not produce it.\nnode:\n\tcargo build --release --manifest-path $(assets)/node/Cargo.toml\n\n# The LuaJIT driver has no build step: lypning.lua reads the header at load.\n\nclean:\n\trm -f run_c run_cpp run_go/run_go\n\t$(MAKE) -C $(here)/run_swift clean\n\"\"\")\nm.write_text(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:10:25.633Z","count":1,"stdin_sample":null}
{"key":"py-c0671bac6644","id":"py-c0671bac6644","program":"import pathlib\np = pathlib.Path(\"embed.c\")\ns = p.read_text()\ndef rep(old, new):\n    global s\n    assert s.count(old) == 1, old\n    s = s.replace(old, new)\n\nrep('''/*\n * embed.c — the C ABI, executable.\n *\n * The one invariant this file holds: **a refusal is a route, not an error**,\n * and it is asserted against the library that was just built rather than\n * described in a comment. The refusal path is the only part of lypning that\n * has ever broken silently — a parser change that turns `unsupported` into a\n * traceback still compiles, still links, still answers `--version` — so the\n * four properties a host branches on (status, exit code, empty stdout,\n * should_fall_onward) are assert()ed here, and this program exits non-zero the\n * moment one of them stops being true.\n *\n * It is also the file a harness author copies. So it does not stop at the\n * refusal: it implements the other half of the mixture, the fork/execvp onto\n * python3, because a demo that prints \"would fall onward\" and stops has\n * demonstrated the easy half. Six programs go in. Three are run here; one is\n * refused and answered by CPython; two are refused and this host declines to\n * route them onward, for two different reasons it prints. Declining is a\n * decision the ABI leaves to the host, and both of those are cases where\n * routing onward would be the mistake.\n''', '''/*\n * embed.c — the contract test for the C ABI. This is NOT the file to copy:\n * that is quickstart.c, next to it, which is the whole host in forty lines.\n * This one is long because it is a test first and a demonstration second.\n *\n * The one invariant this file holds: **a refusal is a route, not an error**,\n * and it is asserted against the library that was just built rather than\n * described in a comment. The refusal path is the only part of lypning that\n * has ever broken silently — a parser change that turns `unsupported` into a\n * traceback still compiles, still links, still answers `--version` — so the\n * four properties a host branches on (status, exit code, empty stdout,\n * should_fall_onward) are assert()ed here, and this program exits non-zero the\n * moment one of them stops being true.\n *\n * It does not stop at the refusal: it implements the other half of the\n * mixture, the fork/execvp onto python3 with stdin forwarded as bytes,\n * because a test that prints \"would fall onward\" and stops has tested the\n * easy half. Six programs go in. Three are run here; one is refused and\n * answered by CPython; two are refused and this host declines to route them\n * onward, for two different reasons it prints. Declining is a decision the\n * ABI leaves to the host, and both of those are cases where routing onward\n * would be the mistake.\n''')\n\nrep(''' * Build: make. Read the Makefile for which library it picked.\n''', ''' * Build and run: make run. Read the Makefile for which library it picked, and\n * for why it runs this from a temp directory rather than from the checkout.\n''')\n\nrep('''            /* A program is entitled to ignore its stdin, and the one above that\n             * sums sys.argv does. That closes the pipe under us: with SIGPIPE\n             * ignored in main() the write returns EPIPE and we simply stop\n             * writing, rather than dying of the program's success. */\n            if (k <= 0 || sent == p->stdin_len) {\n                close(w);\n                w = -1;\n            }''', '''            /* A program is entitled to ignore its stdin, and the one above that\n             * sums sys.argv does. That closes the pipe under us: with SIGPIPE\n             * ignored in main() the write returns EPIPE and we simply stop\n             * writing, rather than dying of the program's success.\n             *\n             * EINTR and EAGAIN are not that. The pipe is still open and poll()\n             * will say when it takes more; closing on them would hand the\n             * program a stdin cut off mid-stream, and the two engines would\n             * then disagree on an input that was never the same. */\n            if ((k < 0 && errno != EINTR && errno != EAGAIN)\n                || sent == p->stdin_len) {\n                close(w);\n                w = -1;\n            }''')\np.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:23:46.206Z","count":1,"stdin_sample":null}
{"key":"py-c0899fa4cff8","id":"py-c0899fa4cff8","program":"\nimport json; r=json.load(open('/tmp/lypning-host-reports.json'))\nfor sect in ('reports','verdicts'):\n    v=r[sect]\n    print('#####',sect, type(v), (list(v.keys()) if isinstance(v,dict) else len(v)))\n    items = v.items() if isinstance(v,dict) else enumerate(v)\n    for k,x in items:\n        print('==',k)\n        if isinstance(x,dict):\n            for f in ('host','files_created','files_modified','package_data_globs','manifest_prunes','gitignore_lines','build_and_run','ci_step','left_undone','open'):\n                if f in x: print('  ',f,':',x[f])\n        else: print('  ',str(x)[:800])\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:54:31.822Z","count":1,"stdin_sample":null}
{"key":"py-c0e5264634fc","id":"py-c0e5264634fc","program":"import json, sys\na=[json.loads(l) for l in open(sys.argv[1])]; b=[json.loads(l) for l in open(sys.argv[2])]\nprint(len(a), len(b), \"records\")\nprint(\"keys equal:\", list(a[0].keys())==list(b[0].keys()), list(a[0].keys()))\ndiff=[(x[\"program\"][:60], x[\"exit_code\"], y[\"exit_code\"]) for x,y in zip(a,b) if (x[\"program\"],x[\"exit_code\"],x[\"argv_tail\"]) != (y[\"program\"],y[\"exit_code\"],y[\"argv_tail\"])]\nprint(\"disagreements:\", len(diff)); print(diff[:5])\nprint(\"shims:\", {x[\"shim\"] for x in a}, {x[\"shim\"] for x in b})","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:34:07.146Z","count":1,"stdin_sample":null}
{"key":"py-c10349b1858a","id":"py-c10349b1858a","program":"import time, pathlib, subprocess\np = pathlib.Path(\"/tmp/conformance-lib.txt\")\nfor _ in range(590):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nt = p.read_text(errors=\"replace\") if p.exists() else \"\"\nprint(\"DONE\" if \"conformance exit\" in t else \"STILL RUNNING\", len(t), \"bytes\")\nprint(t[-3000:])\nprint(subprocess.run(\"ps aux | grep -c '[l]ypning conformance'\", shell=True, capture_output=True, text=True).stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:37:50.748Z","count":1,"stdin_sample":null}
{"key":"py-c18a2e463f1b","id":"py-c18a2e463f1b","program":"import pathlib\np = pathlib.Path(\".github/workflows/ci.yml\"); s = p.read_text()\n\nPROBES = '''        run: |\n          cat >\"$RUNNER_TEMP/probes.sh\" <<'EOF'\n          probes() {\n            # $@ is the host's quickstart command; each line below is one probe.\n            test \"$(\"$@\" 'print(sum(range(10)))')\" = 45\n            test \"$(\"$@\" 'import subprocess; print(1)')\" = 1\n            test \"$(\"$@\" 'import sys; print(sys.argv[1:])' a b)\" = \"['a', 'b']\"\n            rc=0; \"$@\" 'print(1/0)' >/dev/null 2>\"$RUNNER_TEMP/probe.err\" || rc=$?\n            test \"$rc\" -eq 1\n            test \"$(grep -c ZeroDivisionError \"$RUNNER_TEMP/probe.err\")\" -eq 1\n            rc=0; \"$@\" 'import sys; sys.exit(3)' >/dev/null || rc=$?\n            test \"$rc\" -eq 3\n            echo \"$1: 45 in-process; refusal answered once by CPython; argv; traceback exit 1, not retried; exit 3\"\n          }\n          EOF\n'''\n\nhosts_ubuntu = '''      # --- every host over the ABI, each held to the one quickstart contract --\n      #\n      # docs/EMBEDDING.md section 4 is the table of hosts; the steps below are\n      # that table, executed, for every host a Linux runner can build. Each\n      # gates on its own toolchain rather than on the runner image: a tool that\n      # is not there is not a red host, it is a host that says so and exits 0,\n      # because the image changes under us and a missing compiler is not a\n      # wrong answer. What is asserted is the same five probes for every\n      # quickstart: an answer in-process, a refusal answered ONCE by CPython,\n      # argv passed through, a traceback that is exit 1 and NOT retried, and\n      # sys.exit carried unchanged. They are written once, as a shell function,\n      # so no host can pass by having skipped one. ZeroDivisionError is COUNTED\n      # rather than grepped for: a host that fell onward after a traceback\n      # would print it twice, and that is the retry the contract forbids.\n      - name: the quickstart contract, as one function every host is run through\n''' + PROBES + '''      - name: C — the contract example, then the file a host author copies\n        # The Makefile picks the checkout's freshly built library and fails\n        # naming `lypning build --lib` if it is absent. The fallback branch is\n        # execvp(\"python3\"), so the runner's python3 is the second tier.\n        run: |\n          command -v cc >/dev/null || { echo \"no C compiler: C host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          make -C src/lypning/assets/examples/c run\n          make -C src/lypning/assets/examples/c quickstart\n          probes src/lypning/assets/examples/c/quickstart\n      - name: C++ — header-only RAII over the same ABI\n        run: |\n          command -v c++ >/dev/null || { echo \"no c++: C++ host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          make -C src/lypning/assets/examples/cpp run\n          make -C src/lypning/assets/examples/cpp quickstart\n          probes src/lypning/assets/examples/cpp/quickstart\n      - name: Rust — the crate by path, no ABI in between\n        # Nothing to link: this host compiles ../../rust from source, so it is\n        # the one host `build --lib` does nothing for. `cargo run` passes the\n        # example's exit status through, which the sys.exit probe relies on.\n        run: |\n          command -v cargo >/dev/null || { echo \"no cargo: Rust host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          M=src/lypning/assets/examples/rust/Cargo.toml\n          cargo run --release --manifest-path \"$M\"\n          probes cargo run -q --release --manifest-path \"$M\" --example quickstart --\n      - name: Node — a plain cargo build links the addon, node loads it\n        # No npm dependencies, no node-gyp: cargo and node are all it needs.\n        # Loading the addon is the assertion build.rs exists for: the Node-API\n        # symbols resolve from the node process at dlopen, or not at all.\n        run: |\n          { command -v cargo && command -v node; } >/dev/null || { echo \"no cargo or no node: Node host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd src/lypning/assets/node\n          cargo build --release\n          probes node quickstart.js\n          npm test\n      - name: Python — ctypes over the library, stdlib only\n        # No compiler in the loop: the only thing this host needs is the\n        # library `build --lib` produced above, which is why it has no gate.\n        run: |\n          . \"$RUNNER_TEMP/probes.sh\"\n          probes python src/lypning/assets/examples/python/quickstart.py\n      - uses: actions/setup-go@v5\n        id: go\n        # A setup failure (a download outage, a retired version string) is a\n        # missing toolchain, and the step below treats it as one: skipped and\n        # said so, not red. The same rule as every other host's gate.\n        continue-on-error: true\n        with:\n          go-version: stable\n          cache: false   # no go.sum to key on: the module has zero dependencies\n      - name: Go — cgo over the unchanged header\n        # Built, then run: `go run` reports every non-zero exit as 1 with an\n        # \"exit status N\" line of its own, so the sys.exit probe cannot pass\n        # through it. CGO_LDFLAGS is what `lypning lib --libs` printed, the\n        # flags a Go module outside the checkout would use, so this links the\n        # INSTALLED library and not only the one the #cgo directives find.\n        if: steps.go.outcome == 'success'\n        env:\n          CGO_ENABLED: \"1\"\n        run: |\n          command -v go >/dev/null || { echo \"no go: Go host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          export CGO_LDFLAGS=\"$(lypning lib --libs)\"\n          cd src/lypning/assets/go\n          go vet ./...\n          go test ./...\n          go build -o \"$RUNNER_TEMP/go-quickstart\" ./quickstart\n          probes \"$RUNNER_TEMP/go-quickstart\"\n      - name: LuaJIT — the ffi over the header, read at load, no build step\n        # PUC Lua has no ffi, so it is luajit or nothing. An apt outage is a\n        # missing toolchain and not a red host, hence the tolerated install.\n        run: |\n          sudo apt-get install -y -qq luajit >/dev/null 2>&1 || echo \"apt could not install luajit\"\n          command -v luajit >/dev/null || { echo \"no luajit: Lua host not run\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd src/lypning/assets/lua\n          luajit test.lua\n          probes luajit quickstart.lua\n'''\n\nanchor = '''      - name: conformance — the library arm, scored like a spawned one\n        # The whole corpus through the C ABI in this process, against CPython,\n        # by the rules the spawned arms are scored by. It must reach MISMATCH 0\n        # AND agree with the `lypning` arm program for program: the two\n        # disagreeing is the drift this arm exists to catch.\n        run: lypning conformance --engine lypning --engine library\n'''\nassert anchor in s\ns = s.replace(anchor, anchor + hosts_ubuntu)\n\nmacos_job = '''  # --- the library tier on the other operating system the wheel claims -----\n  #\n  # pyproject.toml carries `Operating System :: MacOS`, and this job is what\n  # makes that a fact rather than a classifier. It is not a mirror of `core`:\n  # the static-musl core, its gate and its conformance battery are Linux\n  # artefacts, and `build --rust` on a Mac builds for the host and says so.\n  # What a Mac adds is the host Linux cannot run at all (Swift) and the one\n  # link detail Linux never sees: the library's install name must be\n  # `@rpath/liblypning.dylib`, or every host linked with `lypning lib --libs`\n  # links clean and fails at load. The hosts that need only cc, cargo or node\n  # are proven on ubuntu above and not repeated here.\n  hosts-macos:\n    name: macos — library tier, C, Python and Swift hosts\n    runs-on: macos-latest\n    timeout-minutes: 40\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n      - name: install\n        run: |\n          python -m pip install --upgrade pip\n          pip install -e \".[dev]\"\n      - name: build the core, then the library\n        # Both refuse to report ok until the refusal contract holds on what\n        # they just produced; `lib` and `status` print what was installed and\n        # where, which is the line to read when a host step below fails.\n        run: |\n          lypning build --rust\n          lypning build --lib\n          lypning lib\n          lypning status\n      - name: pytest, on a Mac\n        run: python -m pytest tests -q\n      - name: the quickstart contract, as one function every host is run through\n        # The same function the core job defines, and for the same reason:\n        # the five probes are the contract, and a host passes all of them or\n        # none. See the comment there.\n''' + PROBES + '''      - name: C — compiled against what `lypning lib` printed, run from outside the tree\n        # From $RUNNER_TEMP so nothing can resolve back to the checkout: this\n        # is a user's host, linked with the flags the CLI hands out. `otool`\n        # pins the install name those flags depend on.\n        run: |\n          gcc $(lypning lib --cflags) src/lypning/assets/examples/c/quickstart.c \\\\\n              -o \"$RUNNER_TEMP/quickstart\" $(lypning lib --libs)\n          otool -L \"$RUNNER_TEMP/quickstart\" | grep -q '@rpath/liblypning.dylib'\n          . \"$RUNNER_TEMP/probes.sh\"\n          cd \"$RUNNER_TEMP\" && probes ./quickstart\n      - name: Python — ctypes, which dlopens by absolute path and needs no rpath\n        run: |\n          . \"$RUNNER_TEMP/probes.sh\"\n          probes python src/lypning/assets/examples/python/quickstart.py\n      - name: Swift — the module map over the header, its tests, then the quickstart\n        # Package.swift decides the library directory when it is first\n        # evaluated and SwiftPM caches the decision, which is why the library\n        # is built before this step and the README says to do the same.\n        # macos-latest ships a toolchain; a runner without one says so.\n        run: |\n          command -v swift >/dev/null || { echo \"no swift toolchain: Swift host not built\"; exit 0; }\n          . \"$RUNNER_TEMP/probes.sh\"\n          P=src/lypning/assets/swift\n          swift build -c release --package-path \"$P\" -Xswiftc -warnings-as-errors\n          swift test --package-path \"$P\"\n          probes \"$P/.build/release/quickstart\"\n      - name: nothing above wrote into the tree\n        # Every build here writes to an ignored path or to $RUNNER_TEMP; an\n        # untracked file is a build artefact .gitignore does not know about,\n        # which is the same file that would ride into the next wheel.\n        run: |\n          dirty=$(git status --porcelain)\n          test -z \"$dirty\" || { echo \"$dirty\"; echo \"a host build left files in the tree\"; exit 1; }\n\n'''\nanchor2 = '''  # --- can the tier be built at all -----------------------------------------\n'''\nassert anchor2 in s\ns = s.replace(anchor2, macos_job + anchor2)\np.write_text(s)\nprint(\"ci edit ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:22:00.631Z","count":1,"stdin_sample":null}
{"key":"py-c527e5c6bd85","id":"py-c527e5c6bd85","program":"import time, pathlib, subprocess\np = pathlib.Path(\"/tmp/conformance-lib.txt\")\nfor _ in range(590):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nt = p.read_text(errors=\"replace\") if p.exists() else \"\"\nprint(\"DONE\" if \"conformance exit\" in t else \"STILL RUNNING\", len(t), \"bytes\")\nprint(t[-4000:])\nprint(subprocess.run(\"ps -o pid=,ppid=,etime=,command= -A | awk '$2==3352' | cut -c1-160\", shell=True, capture_output=True, text=True).stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:09:42.634Z","count":1,"stdin_sample":null}
{"key":"py-c5bedbd59124","id":"py-c5bedbd59124","program":"p='src/lypning/embed.py'; s=open(p).read()\nold='''  * It is the **fourth binding**. C, C++, Rust and Node all call the same\n    symbols; a Python one written against the same header is what lets the test\n    suite exercise the ABI without a compiler in the loop.'''\nnew='''  * It is **one host among every host** (the table in ``docs/EMBEDDING.md``\n    section 4), and they all call the same symbols; a Python one written\n    against the same header is what lets the test suite exercise the ABI\n    without a compiler in the loop.'''\nassert s.count(old)==1; open(p,'w').write(s.replace(old,new)); print('embed.py edited')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:21:14.051Z","count":1,"stdin_sample":null}
{"key":"py-c5df66282170","id":"py-c5df66282170","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor name,r in d['reports'].items():\n    print('=====',name)\n    for k in ('files_created','files_modified','package_data_globs','manifest_prunes','gitignore_lines','ci_step','left_undone'):\n        print('--',k); v=r.get(k); print(json.dumps(v,indent=1) if not isinstance(v,str) else v)\nprint('===== VERDICTS')\nfor name,v in d['verdicts'].items():\n    print('--',name); print(json.dumps(v.get('open') if isinstance(v,dict) else v,indent=1))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:44.013Z","count":1,"stdin_sample":null}
{"key":"py-c6ffc95b1615","id":"py-c6ffc95b1615","program":"import re,glob,os,subprocess\nt=open('pyproject.toml').read()\ni=t.index('[tool.setuptools.package-data]'); j=t.index('\\n[',i+1) if '\\n[' in t[i+1:] else len(t)\nglobs=[g for g in re.findall(r'^\\s*\"([^\"]+)\"', t[i:j], re.M)]\ncovered=set()\nfor g in globs:\n    for p in glob.glob('src/lypning/'+g):\n        if os.path.isfile(p): covered.add(os.path.relpath(p,'src/lypning'))\nout=subprocess.run(['git','ls-files','--cached','--others','--exclude-standard','src/lypning/assets/go','src/lypning/assets/swift','src/lypning/assets/lua','src/lypning/assets/node','src/lypning/assets/examples','src/lypning/assets/include','src/lypning/assets/rust/build.rs'],capture_output=True,text=True).stdout.split()\nmissing=[os.path.relpath(p,'src/lypning') for p in out if os.path.relpath(p,'src/lypning') not in covered]\nprint('globs:',len(globs),'files:',len(out),'covered:',len(out)-len(missing)); print('NOT COVERED:', missing or '(none)')\nprint('globs matching nothing:',[g for g in globs if not glob.glob('src/lypning/'+g)])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:57:55.053Z","count":1,"stdin_sample":null}
{"key":"py-c7d899d81985","id":"py-c7d899d81985","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor r in d['reports']:\n    name = r.get('host') or r.get('name') or str(list(r.keys())[:3])\n    if 'plumbing' in json.dumps(r)[:600]:\n        print('=== REPORT'); print(json.dumps(r,indent=1))\nfor v in d['verdicts']:\n    if 'plumbing' in json.dumps(v)[:600]:\n        print('=== VERDICT'); print(json.dumps(v,indent=1))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:21.404Z","count":1,"stdin_sample":null}
{"key":"py-c836fde29397","id":"py-c836fde29397","program":"\nfrom lypning import embed; o=embed.Library().run('import subprocess; print(1)'); print(repr(o.stdout), repr(o.stderr), o.exit_code, o.fall_onward)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:09:25.837Z","count":1,"stdin_sample":null}
{"key":"py-c9eb55ecd244","id":"py-c9eb55ecd244","program":"p='tests/test_docs.py'; s=open(p).read()\ns=s.replace('''    for pattern in (\"examples/*/quickstart.*\", \"*/quickstart.*\",\n                    \"*/quickstart/main.*\", \"*/Sources/quickstart/main.*\"):''','''    for pattern in (\"examples/*/quickstart.*\", \"examples/*/examples/quickstart.*\",\n                    \"*/quickstart.*\", \"*/quickstart/main.*\", \"*/Sources/quickstart/main.*\"):''')\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:33:43.545Z","count":1,"stdin_sample":null}
{"key":"py-ca434c4ccf6a","id":"py-ca434c4ccf6a","program":"p='bindings-implement-wf_90377b2e-a91.js'\ns=open(p).read()\nold=\"  t => agent(RULES + '\\\\n\\\\n' + t.prompt, { label: 'impl:' + t.key, phase: 'Implement', schema: REPORT }),\"\nnew=\"  t => agent(RULES + '\\\\n\\\\n' + t.prompt + (RESUME[t.key] || ''), { label: 'impl:' + t.key, phase: 'Implement', schema: REPORT }),\"\nassert old in s\ns=s.replace(old,new)\nresume = r\"\"\"\nconst RESUME_TEXT = '\\n\\nRESUME NOTE: a previous attempt at exactly this task was interrupted part-way (the session hit a usage limit). The worktree ALREADY CONTAINS its partial work for your files. Do not start over and do not delete it blindly: first run git -C ' + WT + ' status --short and git -C ' + WT + ' diff -- <your owned paths>, read every file under your directory in full, keep what is right, finish what is missing, fix what is wrong, and then run every VERIFY step before reporting. Remove any build artefacts the earlier attempt left inside the tree (compiled binaries, build/ or .build/ dirs, .o files) and report the gitignore/prune lines for them.'\nconst RESUME = { node: RESUME_TEXT, luajit: RESUME_TEXT, go: RESUME_TEXT, swift: RESUME_TEXT, 'python-plumbing': RESUME_TEXT }\n\"\"\"\ns=s.replace(\"phase('Implement')\", resume+\"\\nphase('Implement')\",1)\nopen(p,'w').write(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:01:32.402Z","count":1,"stdin_sample":null}
{"key":"py-cb64c62b5586","id":"py-cb64c62b5586","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_90377b2e-a91/journal.jsonl'\nreports={}; verdicts={}\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'package_data_globs' in v: reports[v['host']]=v\n    elif 'probes_pass' in v: verdicts[v['host']]=v\nout={'reports':list(reports.values()),'verdicts':list(verdicts.values())}\njson.dump(out,open('/tmp/lypning-host-reports.json','w'),indent=1)\nfor v in reports.values():\n    print('==', v['host'][:60]); print(' globs', v['package_data_globs']); print(' prunes', v['manifest_prunes']); print(' gitignore', v['gitignore_lines']); print(' build', v['build_and_run'])\nfor v in verdicts.values():\n    print('## verdict', v['host'][:40], 'open:', [o[:160] for o in v['open']][:6])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:02:52.380Z","count":1,"stdin_sample":null}
{"key":"py-ccfe6bbe5822","id":"py-ccfe6bbe5822","program":"lines=open('/tmp/lyp-verify-conf.log').read().splitlines()\nbody=[l for l in lines if not (l.startswith('?? ') or l.startswith(' M ') or l.startswith('M  ') or l.startswith('A  ') or l.startswith('MM '))]\nprint('\\n'.join(body))\nst=[l for l in lines if l.startswith('?? ') or l.startswith(' M ') or l.startswith('M  ') or l.startswith('MM ')]\nprint('--- git status entries after run:', len(st), 'modified:', sum(1 for l in st if not l.startswith('??')))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:24:04.484Z","count":1,"stdin_sample":null}
{"key":"py-cdd67f4d6adf","id":"py-cdd67f4d6adf","program":"import re, pathlib\np = pathlib.Path(\"src/capi.rs\")\ns = p.read_text()\n\ndef rep(old, new, count=1):\n    global s\n    assert s.count(old) == count, (old, s.count(old))\n    s = s.replace(old, new)\n\n# 1. a shared empty C string, next to cstr() so the two string helpers sit together\nrep('''fn cstr(s: &str) -> CString {\n    CString::new(s.replace('\\\\0', \"\")).unwrap_or_default()\n}\n''', '''fn cstr(s: &str) -> CString {\n    CString::new(s.replace('\\\\0', \"\")).unwrap_or_default()\n}\n\n/// One readable NUL, so the pointer returned for an empty buffer or a NULL\n/// handle is a real address and C code that treats it as a string finds it\n/// terminated.\nstatic EMPTY: [u8; 1] = [0];\n\n/// `\"\"` for every `const char *` accessor handed a NULL handle. A host that\n/// forgot the NULL check and did `strcmp(lypning_route_kind(r), \"module\")`\n/// would otherwise crash inside libc, far from the line that was wrong; an\n/// empty string is the answer a live handle gives when there is nothing to\n/// say, and the same answer is safe here.\nfn empty_cstr() -> *const c_char {\n    EMPTY.as_ptr() as *const c_char\n}\n''')\n\n# 2. remove the old EMPTY definition where it lived\nrep('''/// One readable NUL, so the pointer returned for an empty buffer is a real\n/// address and C code that treats output as a string finds it terminated.\nstatic EMPTY: [u8; 1] = [0];\n\n''', '')\n\n# 3. the accessors: route_engine/kind/detail, result_kind/detail\nfor name in (\"engine\", \"kind\", \"detail\"):\n    rep(f'''        Some(r) => r.{name}.as_ptr(),\n        None => ptr::null(),\n''', f'''        Some(r) => r.{name}.as_ptr(),\n        None => empty_cstr(),\n''', count=2 if name != \"engine\" else 1)\n\n# 4. route_import: NULL handle -> \"\", out of range on a live handle stays NULL\nrep('''/// The `i`th import — **sorted and deduplicated**, not in source order — or\n/// NULL when `i` is out of range. Use `lypning_route_detail` for the one import\n/// that actually decided the tier.\n#[no_mangle]\npub unsafe extern \"C\" fn lypning_route_import(\n    r: *const lypning_route,\n    i: usize,\n) -> *const c_char {\n    match r.as_ref().and_then(|r| r.imports.get(i)) {\n        Some(s) => s.as_ptr(),\n        None => ptr::null(),\n    }\n}''', '''/// The `i`th import — **sorted and deduplicated**, not in source order — or\n/// NULL when `i` is out of range. Use `lypning_route_detail` for the one import\n/// that actually decided the tier.\n///\n/// NULL is the loop terminator and only that: a NULL *handle* answers `\"\"`\n/// like every other string accessor, so a host that indexes a route it never\n/// got does not read a NULL it was not testing for.\n#[no_mangle]\npub unsafe extern \"C\" fn lypning_route_import(\n    r: *const lypning_route,\n    i: usize,\n) -> *const c_char {\n    match r.as_ref() {\n        Some(r) => match r.imports.get(i) {\n            Some(s) => s.as_ptr(),\n            None => ptr::null(),\n        },\n        None => empty_cstr(),\n    }\n}''')\n\n# 5. route_engine doc: say what a NULL handle answers\nrep('''/// `\"lypning\"`, `\"lypning-mp\"` or `\"cpython\"` — the tier that should run it.\n#[no_mangle]''', '''/// `\"lypning\"`, `\"lypning-mp\"` or `\"cpython\"` — the tier that should run it.\n/// `\"\"` for a NULL handle, as for every string accessor here: a route the host\n/// never got names no tier, and a host that failed to check must not crash in\n/// `strcmp` for it.\n#[no_mangle]''')\n\np.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:22:35.564Z","count":1,"stdin_sample":null}
{"key":"py-cf27975dbc8e","id":"py-cf27975dbc8e","program":"import pathlib\np = pathlib.Path(\"Makefile\")\ns = p.read_text()\ndef rep(old, new):\n    global s\n    assert s.count(old) == 1, old\n    s = s.replace(old, new)\n\nrep('''# $(wildcard) rather than a bare path: a missing header or library must reach\n# the message below, not make's own \"No rule to make target\" — or, for the\n# library, a bare `ld: library not found` that names neither the directory that\n# was searched nor the command that fills it.\nheader := $(wildcard $(INCDIR)/lypning.h)\nlib    := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n\n# One message for both halves, so a wheel user with neither reads it once. The\n# two build lines are the same build: `lypning build --lib` runs the cargo line\n# and then asserts the refusal contract on what it produced.\ndefine need_library\n\t@test -n \"$(header)\" -a -n \"$(lib)\" || { \\\\\n\t  test -n \"$(header)\" || echo \"no lypning.h under $(INCDIR).\"; \\\\\n\t  test -n \"$(lib)\"    || echo \"no liblypning.so or liblypning.dylib under $(LIBDIR).\"; \\\\\n\t  echo \"build it:  lypning build --lib\"; \\\\\n\t  echo \"      or:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"; \\\\\n\t  echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"; \\\\\n\t  exit 1; }\nendef\n''', '''# $(wildcard) rather than a bare path: a missing header or library must reach\n# the message below, not make's own \"No rule to make target\" — or, for the\n# library, a bare `ld: library not found` that names neither the directory that\n# was searched nor the command that fills it.\nheader := $(wildcard $(INCDIR)/lypning.h)\nlib    := $(wildcard $(LIBDIR)/liblypning.so $(LIBDIR)/liblypning.dylib)\n\n# A phony prerequisite stands in for whichever of the two is missing. A missing\n# file simply drops out of a rule, so without this an old binary would be \"up\n# to date\" against a library that is not there — and the message would print\n# only for someone who had never built before, which is not who needs it.\n# One message for both halves, so a wheel user with neither reads it once; the\n# two build lines are the same build, `lypning build --lib` runs the cargo line\n# and then asserts the refusal contract on what it produced.\nmissing := $(if $(header),,missing-header) $(if $(lib),,missing-library)\n.PHONY: missing-header missing-library\nmissing-header missing-library:\n\t@test -n \"$(header)\" || echo \"no lypning.h under $(INCDIR).\"\n\t@test -n \"$(lib)\"    || echo \"no liblypning.so or liblypning.dylib under $(LIBDIR).\"\n\t@echo \"build it:  lypning build --lib\"\n\t@echo \"      or:  cd $(assets)/rust && cargo build --lib --features capi --profile release-lib\"\n\t@echo \"or point at an installed copy:  make INCDIR=... LIBDIR=...\"\n\t@exit 1\n''')\n\nrep('''$(BIN): embed.c $(header) $(lib) $(MAKEFILE_LIST)\n\t$(need_library)\n\t$(CC)''', '''$(BIN): embed.c $(header) $(lib) $(missing) $(MAKEFILE_LIST)\n\t$(CC)''')\nrep('''$(QUICK): quickstart.c $(header) $(lib) $(MAKEFILE_LIST)\n\t$(need_library)\n\t$(CC)''', '''$(QUICK): quickstart.c $(header) $(lib) $(missing) $(MAKEFILE_LIST)\n\t$(CC)''')\np.write_text(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:30:12.718Z","count":1,"stdin_sample":null}
{"key":"py-d2383d8d526d","id":"py-d2383d8d526d","program":"p = \"README.md\"\ns = open(p).read()\nold = \"(a source checkout that has already built the crate needs only the second half.)\\n\"\nnew = \"\"\"(a source checkout that has already built the crate needs only the second half.)\n\nThe quickstart's onward path is `os.execute(\"python3 -c ...\")` with the source\nand each argument single-quoted for the shell and stdin from `/dev/null`, so the\ntwo paths agree on stdin (lypning sees an empty stream). It exits with the\nchild's own code, or `128 + n` when the child died of signal `n`, which is the\nshell's convention; both shapes `os.execute` can return (LuaJIT's raw wait\nstatus, or the `ok, \"exit\"|\"signal\", code` triple of a `LUA52COMPAT` build) are\ndecoded. A traceback is the program's own answer: exit 1, never retried.\n\"\"\"\nassert old in s\nopen(p, \"w\").write(s.replace(old, new, 1))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:11:40.781Z","count":1,"stdin_sample":null}
{"key":"py-d4288267bfe9","id":"py-d4288267bfe9","program":"$x=0\\nfor i in range(20000000): x+=1\\nprint(x)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:13:07.443Z","count":1,"stdin_sample":null}
{"key":"py-da40278d1cc8","id":"py-da40278d1cc8","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nprint(type(d), list(d.keys()) if isinstance(d,dict) else len(d))\nfor k,v in (d.items() if isinstance(d,dict) else enumerate(d)):\n    print('=====',k)\n    if isinstance(v,dict):\n        for f in ('files_created','files_modified','build_and_run','ci_step','left_undone'):\n            print('--',f,':',v.get(f))\n        print('-- verdict.open:', (v.get('verdict') or {}).get('open') if isinstance(v.get('verdict'),dict) else v.get('verdict'))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:52.805Z","count":1,"stdin_sample":null}
{"key":"py-da68327bdcac","id":"py-da68327bdcac","program":"import ast,sys; [ast.parse(open(f).read(), feature_version=(3,9)) for f in ['src/lypning/build.py','src/lypning/embed.py','src/lypning/cli.py','src/lypning/engines.py','tests/test_embed.py']]; print('parses as 3.9')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:25:34.447Z","count":1,"stdin_sample":null}
{"key":"py-dc2678a24b26","id":"py-dc2678a24b26","program":"import time, pathlib, subprocess\np = pathlib.Path(\"/tmp/conformance-lib-full.txt\")\nfor _ in range(580):\n    if p.exists() and \"conformance exit\" in p.read_text(errors=\"replace\"):\n        break\n    time.sleep(1)\nt = p.read_text(errors=\"replace\") if p.exists() else \"\"\nprint(\"DONE\" if \"conformance exit\" in t else \"STILL RUNNING\", len(t), \"bytes\")\nprint(t[-3000:])\nprint(subprocess.run(\"pid=$(pgrep -f 'lypning conformance --engine lypning --engine library' | head -1); [ -n \\\"$pid\\\" ] && ps -o pid=,etime=,time= -p $pid && ps -o pid=,ppid=,etime=,command= -A | awk -v p=$pid '$2==p' | cut -c1-160\", shell=True, capture_output=True, text=True).stdout)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:23:51.601Z","count":1,"stdin_sample":null}
{"key":"py-dc8d2fa38e92","id":"py-dc8d2fa38e92","program":"p='src/lypning/assets/examples/c/quickstart.c'; s=open(p).read()\nold='''    lypning_request *q = lypning_request_new(argv[1], strlen(argv[1]));\n    lypning_result *r = NULL;\n    if (q != NULL) {\n        lypning_request_set_step_limit(q, 10000000); /* a call in our own thread has no process to kill */\n        for (int i = 2; i < argc; i++)\n            lypning_request_add_arg(q, argv[i], strlen(argv[i]));\n        r = lypning_run(q);\n    }\n'''\nnew='''    lypning_request *q = lypning_request_new(argv[1], strlen(argv[1]));\n    int ours = q != NULL; /* NULL, or -1 below: bytes that are not UTF-8 are CPython's to answer, not ours to drop */\n    for (int i = 2; ours && i < argc; i++)\n        ours = lypning_request_add_arg(q, argv[i], strlen(argv[i])) == 0;\n    lypning_result *r = NULL;\n    if (ours) {\n        lypning_request_set_step_limit(q, 10000000); /* a call in our own thread has no process to kill */\n        r = lypning_run(q);\n    }\n'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,'w').write(s)\np='src/lypning/assets/examples/c/Makefile'; s=open(p).read()\nold='''# Run from a fresh temp directory and remove it after, carrying the exit\n# status across the cleanup so a failed run still fails make.\ndefine in_tmpdir\n\t@d=$$(mktemp -d) && (cd \"$$d\" && $(1)); s=$$?; rm -rf \"$$d\"; exit $$s\nendef\n\nrun: $(BIN)\n\t$(call in_tmpdir,$(here)/$(BIN))\n'''\nnew='''# Run from a fresh temp directory and remove it after, carrying the exit\n# status across the cleanup so a failed run still fails make. No `@` in here:\n# make strips it only at the start of a recipe, and the valgrind rule below\n# expands this after a `; \\\\` continuation, where the shell would read it as a\n# command named `@d=...`.\ndefine in_tmpdir\nd=$$(mktemp -d) && (cd \"$$d\" && $(1)); s=$$?; rm -rf \"$$d\"; exit $$s\nendef\n\nrun: $(BIN)\n\t@$(call in_tmpdir,$(here)/$(BIN))\n'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,'w').write(s)\nprint(\"edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:19:41.089Z","count":1,"stdin_sample":null}
{"key":"py-e0d45f8aa5ef","id":"py-e0d45f8aa5ef","program":"import ast; ast.parse(open('src/lypning/embed.py').read(), feature_version=(3,9)); print('3.9 ok')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:21:49.103Z","count":1,"stdin_sample":null}
{"key":"py-e2011afe05b9","id":"py-e2011afe05b9","program":"from lypning import embed; L=embed.Library(embed.find_library()); print(L.abi, L.version)","argv_tail":["2>"],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:10:04.693Z","count":1,"stdin_sample":null}
{"key":"py-e4560afb3c5d","id":"py-e4560afb3c5d","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nprint(type(d), list(d.keys()) if isinstance(d,dict) else len(d))\nfor k,v in (d.items() if isinstance(d,dict) else enumerate(d)):\n    if 'python-plumbing' in str(k) or 'plumbing' in json.dumps(v)[:300]:\n        print(k); print(json.dumps(v,indent=1))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:05:41.133Z","count":1,"stdin_sample":null}
{"key":"py-e4801d61dcee","id":"py-e4801d61dcee","program":"import re,io\np=\"Package.swift\"; s=open(p).read()\nold='''// When neither exists the link fails on `-llypning` naming the installed path,\n// which is where the next `lypning build --lib` will put it. The Makefile and\n// README say so in words; a manifest cannot, only choose.\nlet libdir = hasLibrary(checkout) ? checkout : installed\nlet link: [LinkerSetting] = [\n    .unsafeFlags([\"-L\", libdir, \"-Xlinker\", \"-rpath\", \"-Xlinker\", libdir]),\n]\n'''\nnew='''let libdir = hasLibrary(checkout) ? checkout : installed\nlet link: [LinkerSetting] = [\n    .unsafeFlags([\"-L\", libdir, \"-Xlinker\", \"-rpath\", \"-Xlinker\", libdir]),\n]\n\n// When neither has it, the honest answer is one line naming the command that\n// fixes it, not the page of undefined symbols the linker would print. A\n// manifest cannot print, only choose; so it defines a flag and Lypning.swift\n// says the line, at the first compile, before the linker is reached.\nlet missing: [SwiftSetting] = hasLibrary(libdir) ? [] : [.define(\"LYPNING_LIBRARY_MISSING\")]\n'''\nassert old in s; s=s.replace(old,new)\nold2='.target(name: \"Lypning\", dependencies: [\"CLypning\"], linkerSettings: link),'\nnew2='.target(name: \"Lypning\", dependencies: [\"CLypning\"],\\n                swiftSettings: missing, linkerSettings: link),'\nassert old2 in s; s=s.replace(old2,new2); open(p,\"w\").write(s)\n\np=\"Sources/Lypning/Lypning.swift\"; s=open(p).read()\nold='''import CLypning\n'''\nnew='''import CLypning\n\n#if LYPNING_LIBRARY_MISSING\n// Package.swift looked under ../rust/target/release-lib and $LYPNING_HOME/lib\n// (~/.lypning/lib) and found no liblypning. Said here, once, instead of by the\n// linker as a page of undefined symbols after everything else compiled.\n#error(\"no liblypning to link: run `lypning build --lib`, or point LYPNING_HOME at a tree that has lib/liblypning.dylib (or .so)\")\n#endif\n'''\nassert s.count(old)==1; s=s.replace(old,new,1); open(p,\"w\").write(s)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:16:15.847Z","count":1,"stdin_sample":null}
{"key":"py-e7e44023729c","id":"py-e7e44023729c","program":"import sys; sys.path.insert(0,'src'); import lypning; print(lypning.__file__)","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T18:53:47.612Z","count":1,"stdin_sample":null}
{"key":"py-e84a3e30fde5","id":"py-e84a3e30fde5","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor v in d['reports']:\n    print('=====', v.get('host'))\n    print('-- build_and_run :', v.get('build_and_run'))\n    print('-- left_undone :', (v.get('left_undone') or '')[:600])\nprint('##### VERDICTS')\nfor v in d['verdicts']:\n    print('=====', v.get('host'))\n    print(str(v.get('open'))[:600])\n","argv_tail":["2>"],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:42.582Z","count":1,"stdin_sample":null}
{"key":"py-e971aea9bb1e","id":"py-e971aea9bb1e","program":"import re,io\np=\"Tests/LypningTests/LypningTests.swift\"\ns=open(p).read()\nold='''    func testOutputIsBytesNotText() {\n        let r = Lypning.run(\"import sys; sys.stdout.write('\\\\\\\\xff')\")\n        XCTAssertEqual(r.status, .ok)\n        // Whatever the runtime encodes, it must arrive uninterpreted: a byte\n        // copy, not a String round trip that would have replaced it.\n        XCTAssertEqual(r.stdout, Array(\"\\\\u{ff}\".utf8))\n    }\n'''\nnew='''    func testOutputIsBytesNotACString() {\n        // The runtime's output is always UTF-8, so what a binding can get wrong\n        // is not the encoding but the length: a copy through String(cString:)\n        // stops at the first NUL and reports \"a\" for a program that printed\n        // more. The bytes must arrive with their length, NUL included.\n        let r = Lypning.run(\"print('a\\\\\\\\x00b')\")\n        XCTAssertEqual(r.status, .ok)\n        XCTAssertEqual(r.stdout, [0x61, 0x00, 0x62, 0x0a])\n    }\n'''\nassert old in s, \"old block not found\"\ns=s.replace(old,new)\nopen(p,\"w\").write(s)\nprint(\"test edited\")\np=\"Sources/quickstart/main.swift\"\ns=open(p).read()\nold='''var opts = Options(args: args)\nopts.stepLimit = 10_000_000  // in-process there is no process to kill; past this it is a refusal\nlet r = Lypning.run(src, opts)\n'''\nnew='''let r = Lypning.run(src, Options(args: args, stepLimit: 10_000_000))  // in-process there is no process to kill; past this it is a refusal\n'''\nassert old in s\ns=s.replace(old,new)\nopen(p,\"w\").write(s)\nprint(\"quickstart edited; lines:\", s.count(\"\\n\"))\np=\"README.md\"\ns=open(p).read()\nold=\"* `Sources/quickstart/main.swift` is the complete minimal host, 33 lines, and\\n  the exact program every other language's quickstart is.\"\nnew=\"* `Sources/quickstart/main.swift` is the complete minimal host, and the exact\\n  program every other language's quickstart is.\"\nassert old in s\ns=s.replace(old,new)\nopen(p,\"w\").write(s)\nprint(\"readme edited\")\np=\"/Users/user/dev/lypning-bindings/study/hosts/run_swift/Makefile\"\ns=open(p).read()\nold=\"$(BUILD)/Lypning.o: $(swift)/Sources/Lypning/Lypning.swift $(swift)/Sources/CLypning/module.modulemap\\n\"\nnew=\"$(BUILD)/Lypning.o: $(swift)/Sources/Lypning/Lypning.swift $(swift)/Sources/CLypning/module.modulemap $(swift)/Sources/CLypning/shim.h\\n\"\nassert old in s\ns=s.replace(old,new)\nopen(p,\"w\").write(s)\nprint(\"study makefile edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:42:33.952Z","count":1,"stdin_sample":null}
{"key":"py-e9d227d534c0","id":"py-e9d227d534c0","program":"\"\"\"Every host's quickstart, driven through the same five probes, byte for byte.\n\nWhat only this test can see: the bindings over one C ABI drifting apart on the\nrefusal path. Each host has its own contract test, and each of those proves the\nhost against the header. None of them proves the hosts against *each other*,\nand the place they disagree is never the happy path -- ``print(sum(range(10)))``\nis 45 in all of them on the day they are written. It is the refusal: one host\nretries a traceback through CPython, so a program that failed once fails twice;\nanother treats the refusal as an error and exits 1 with a message where the\nothers silently produced CPython's answer; a third forwards ``sys.exit(3)`` as\n1. Every one of those compiles, links and passes its own test. So this file\nruns the one contract every quickstart claims -- ``quickstart \"<source>\"\n[args...]``: in-process, or ``python3 -c`` exactly once, never both -- and\ncompares the bytes, per host, per probe.\n\nThe set of hosts is the table in ``docs/EMBEDDING.md`` section 4;\n:file:`tests/test_docs.py` holds the two in step. Each row here says what the\nhost needs on ``PATH``, how it is built once per session, and the argv that\nruns it. A missing toolchain skips that host and says which tool; a missing\nlibrary skips the module, because the premise is one library under every host\n(the two hosts that do not load it at run time -- Rust, which is the crate,\nand Node, which links it statically -- are gated the same way on purpose: a\nsixth of the table running while the rest is \"not built\" would read as a\nresult about the ABI). A build that fails with the library present is a\nfailure, printed in full, because there is nothing left for it to be but a\nbroken host.\n\nWhere each host is built follows where it already builds: cargo target dirs\nand SwiftPM's ``.build`` stay in the tree, and the C, C++ and Go binaries go to\na scratch directory so the tree gains nothing the build did not already leave.\nEvery probe runs from a fresh temporary cwd with stdin closed (CLAUDE.md\ninvariant 4: a quickstart runs whatever it is handed, and one of the five\nprobes hands it to CPython with no restriction at all).\n\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nimport shutil\nimport subprocess\nimport sys\nfrom pathlib import Path\nfrom typing import Callable, Dict, List, Optional, Tuple\n\nimport pytest\n\nfrom conftest import _INSTALLED_LIBRARY\n\nROOT = Path(__file__).resolve().parents[1]\nASSETS = ROOT / \"src\" / \"lypning\" / \"assets\"\n\n#: Captured at import, before the autouse fixture moves ``$HOME`` and\n#: ``$LYPNING_HOME``. The builds run under this environment, not the test's:\n#: cargo, go and swift keep their caches under the real home, and a build that\n#: started from a fresh ``$HOME`` every session would rebuild the runtime crate\n#: three times over from nothing. The probes themselves run under the moved\n#: environment, which is the point of the fixture.\n_BUILD_ENV: Dict[str, str] = dict(os.environ)\n\n#: How long one build, or one probe, may take. The Rust example compiles the\n#: runtime crate from source the first time; nothing else comes close.\nBUILD_TIMEOUT = 900\nPROBE_TIMEOUT = 120\n\npytestmark = pytest.mark.skipif(\n    _INSTALLED_LIBRARY is None,\n    reason=\"the C ABI is not built (`lypning build --lib`); every host here sits over it\",\n)\n\n\n# --- where the library is, in both shapes of the tree --------------------------\n\ndef _libdir() -> Path:\n    return Path(_INSTALLED_LIBRARY).parent\n\n\ndef _incdir() -> Path:\n    \"\"\"The header beside the library if the library is an installed tree\n    (``$LYPNING_HOME/lib`` has ``$LYPNING_HOME/include``), else the checkout's.\"\"\"\n    installed = _libdir().parent / \"include\"\n    if (installed / \"lypning.h\").is_file():\n        return installed\n    return ASSETS / \"include\"\n\n\ndef _home_of_library() -> str:\n    \"\"\"The ``$LYPNING_HOME`` whose ``lib/`` holds the library the suite found.\n\n    The C, C++ and Swift builds look in ``<checkout>/rust/target/release-lib``\n    first and ``$LYPNING_HOME/lib`` second, and in a checkout that has both they\n    would link the first while ``lypning.embed`` loaded the second. C and C++\n    take an explicit ``LIBDIR`` below and are exact; Swift's manifest only\n    reads the environment, so it is told this and left to its own rule.\n    \"\"\"\n    return str(_libdir().parent)\n\n\n# --- the hosts -------------------------------------------------------------------\n\nCommand = Tuple[List[str], Optional[Path], Dict[str, str]]  # argv, cwd, extra env\n\n\nclass Host:\n    \"\"\"One row of the table: what it needs, how it is built, how it runs.\n\n    ``build`` returns the commands to run once, in order, each with its cwd and\n    any environment the build needs on top of the developer's. ``prefix`` is\n    the argv that runs the quickstart; the probe's source and args go after it.\n    ``env`` is what the quickstart needs at run time to find the library, in\n    the same terms its own README uses (``LYPNING_LIB`` for the two hosts that\n    discover it by name; nothing for the ones that were linked with an rpath).\n    \"\"\"\n\n    def __init__(self, name: str, needs: Tuple[str, ...],\n                 build: Callable[[Path], List[Command]],\n                 prefix: Callable[[Path], List[str]],\n                 env: Optional[Callable[[], Dict[str, str]]] = None) -> None:\n        self.name = name\n        self.needs = needs\n        self.build = build\n        self.prefix = prefix\n        self.env = env or (lambda: {})\n\n    def __repr__(self) -> str:\n        return self.name\n\n\ndef _copy_example(scratch: Path, lang: str) -> Path:\n    \"\"\"The wheel shape of the C and C++ examples: copied somewhere writable and\n    built there unchanged, with the library and header named explicitly.\"\"\"\n    src = ASSETS / \"examples\" / lang\n    dst = scratch / lang\n    dst.mkdir(parents=True, exist_ok=True)\n    for name in (\"Makefile\", \"quickstart.c\", \"quickstart.cpp\"):\n        if (src / name).is_file():\n            shutil.copy2(src / name, dst / name)\n    return dst\n\n\ndef _make_quickstart(lang: str) -> Callable[[Path], List[Command]]:\n    def build(scratch: Path) -> List[Command]:\n        d = _copy_example(scratch, lang)\n        return [([\"make\", \"-C\", str(d), \"quickstart\",\n                  \"LIBDIR=%s\" % _libdir(), \"INCDIR=%s\" % _incdir()], None, {})]\n    return build\n\n\nRUST_MANIFEST = ASSETS / \"examples\" / \"rust\" / \"Cargo.toml\"\nNODE_MANIFEST = ASSETS / \"node\" / \"Cargo.toml\"\nSWIFT_PACKAGE = ASSETS / \"swift\"\n\nHOSTS: Tuple[Host, ...] = (\n    Host(\"c\", (\"make\", \"cc\"),\n         _make_quickstart(\"c\"),\n         lambda s: [str(s / \"c\" / \"quickstart\")]),\n    Host(\"cpp\", (\"make\", \"c++\"),\n         _make_quickstart(\"cpp\"),\n         lambda s: [str(s / \"cpp\" / \"quickstart\")]),\n    Host(\"rust\", (\"cargo\",),\n         lambda s: [([\"cargo\", \"build\", \"--release\", \"--quiet\",\n                      \"--manifest-path\", str(RUST_MANIFEST), \"--example\", \"quickstart\"],\n                     None, {})],\n         lambda s: [str(RUST_MANIFEST.parent / \"target\" / \"release\" / \"examples\" / \"quickstart\")]),\n    Host(\"node\", (\"node\", \"cargo\"),\n         lambda s: [([\"cargo\", \"build\", \"--release\", \"--quiet\",\n                      \"--manifest-path\", str(NODE_MANIFEST)], None, {})],\n         lambda s: [\"node\", str(ASSETS / \"node\" / \"quickstart.js\")]),\n    Host(\"python\", (),\n         lambda s: [],\n         lambda s: [sys.executable, str(ASSETS / \"examples\" / \"python\" / \"quickstart.py\")],\n         lambda: {\"LYPNING_LIB\": str(_INSTALLED_LIBRARY),\n                  \"PYTHONPATH\": os.pathsep.join(\n                      [str(ROOT / \"src\")] + [p for p in os.environ.get(\"PYTHONPATH\", \"\").split(os.pathsep) if p])}),\n    # `go run` would build into GOCACHE and run from there; `go build -o` gives\n    # a binary the probes can run from any cwd. CGO_LDFLAGS is the README's\n    # own mechanism for a library that is not in the checkout's target dir,\n    # appended after the #cgo directives, so it is right in both shapes.\n    Host(\"go\", (\"go\", \"cc\"),\n         lambda s: [([\"go\", \"build\", \"-o\", str(s / \"go\" / \"quickstart\"), \"./quickstart\"],\n                     ASSETS / \"go\",\n                     {\"CGO_LDFLAGS\": \"-L%s -Wl,-rpath,%s\" % (_libdir(), _libdir())})],\n         lambda s: [str(s / \"go\" / \"quickstart\")]),\n    Host(\"swift\", (\"swift\",),\n         lambda s: [([\"swift\", \"build\", \"-c\", \"release\", \"--product\", \"quickstart\",\n                      \"--package-path\", str(SWIFT_PACKAGE)],\n                     None, {\"LYPNING_HOME\": _home_of_library()})],\n         lambda s: [str(SWIFT_PACKAGE / \".build\" / \"release\" / \"quickstart\")]),\n    Host(\"lua\", (\"luajit\",),\n         lambda s: [],\n         lambda s: [\"luajit\", str(ASSETS / \"lua\" / \"quickstart.lua\")],\n         lambda: {\"LYPNING_LIB\": str(_INSTALLED_LIBRARY)}),\n)\n\n\n# --- the probes ----------------------------------------------------------------\n\nclass Probe:\n    \"\"\"One property of the contract, as argv after the quickstart and what it\n    must produce. ``stderr_has`` is a byte string that must appear exactly once\n    -- once, because appearing twice is the drift this file exists to catch.\"\"\"\n\n    def __init__(self, name: str, argv: List[str], stdout: bytes, code: int,\n                 stderr_has: Optional[bytes] = None) -> None:\n        self.name = name\n        self.argv = argv\n        self.stdout = stdout\n        self.code = code\n        self.stderr_has = stderr_has\n\n    def __repr__(self) -> str:\n        return self.name\n\n\nPROBES: Tuple[Probe, ...] = (\n    # Runs in-process; the number is lypning's own.\n    Probe(\"sum\", [\"print(sum(range(10)))\"], b\"45\\n\", 0),\n    # A refusal: lypning wrote nothing, CPython answers once, and nothing on\n    # stderr says \"unsupported\" -- a refusal is not an error at this layer.\n    Probe(\"refusal\", [\"import subprocess; print(1)\"], b\"1\\n\", 0),\n    # The args after the source are sys.argv[1:], in-process.\n    Probe(\"argv\", [\"import sys; print(sys.argv[1:])\", \"a\", \"b\"], b\"['a', 'b']\\n\", 0),\n    # The program's own failure: exit 1, the traceback on stderr, and NOT\n    # retried through CPython -- a host that retries prints it twice.\n    Probe(\"traceback\", [\"print(1/0)\"], b\"\", 1, b\"ZeroDivisionError\"),\n    # The program's own exit code, passed through unchanged.\n    Probe(\"exit\", [\"import sys; sys.exit(3)\"], b\"\", 3),\n)\n\n\n# --- building once per session ---------------------------------------------------\n\nclass _Builds:\n    \"\"\"Lazy, cached per host: the argv prefix, or the reason it cannot run.\"\"\"\n\n    def __init__(self, scratch: Path) -> None:\n        self.scratch = scratch\n        self.ready: Dict[str, List[str]] = {}\n        self.skipped: Dict[str, str] = {}\n        self.failed: Dict[str, str] = {}\n\n    def prefix(self, host: Host) -> List[str]:\n        if host.name not in self.ready and host.name not in self.skipped \\\n                and host.name not in self.failed:\n            self._build(host)\n        if host.name in self.skipped:\n            pytest.skip(self.skipped[host.name])\n        if host.name in self.failed:\n            pytest.fail(self.failed[host.name], pytrace=False)\n        return self.ready[host.name]\n\n    def _build(self, host: Host) -> None:\n        # python3 is what every quickstart falls onward to, so it is a need of\n        # every row, whether or not the row says so.\n        for tool in host.needs + (\"python3\",):\n            if shutil.which(tool, path=_BUILD_ENV.get(\"PATH\")) is None:\n                self.skipped[host.name] = \"%s: `%s` is not installed\" % (host.name, tool)\n                return\n        for argv, cwd, extra in host.build(self.scratch):\n            env = dict(_BUILD_ENV)\n            env.update(extra)\n            try:\n                r = subprocess.run(argv, cwd=str(cwd) if cwd else None, env=env,\n                                   stdin=subprocess.DEVNULL, capture_output=True,\n                                   timeout=BUILD_TIMEOUT)\n            except (OSError, subprocess.SubprocessError) as exc:\n                self.failed[host.name] = \"%s: %s did not run: %s\" % (host.name, argv[0], exc)\n                return\n            if r.returncode != 0:\n                self.failed[host.name] = (\n                    \"%s: build failed (exit %d) with the library at %s:\\n$ %s\\n%s%s\"\n                    % (host.name, r.returncode, _INSTALLED_LIBRARY, \" \".join(argv),\n                       r.stdout.decode(\"utf-8\", \"replace\"), r.stderr.decode(\"utf-8\", \"replace\")))\n                return\n        prefix = host.prefix(self.scratch)\n        # A prefix that names a file names one the build was supposed to leave;\n        # \"not found\" from the probe would blame the probe.\n        if os.sep in prefix[0] and not Path(prefix[0]).is_file():\n            self.failed[host.name] = \"%s: the build reported ok but left no %s\" % (host.name, prefix[0])\n            return\n        self.ready[host.name] = prefix\n\n\n@pytest.fixture(scope=\"module\")\ndef builds(tmp_path_factory) -> _Builds:\n    return _Builds(tmp_path_factory.mktemp(\"hosts\"))\n\n\n# --- the test ----------------------------------------------------------------------\n\nCASES = [(h, p) for h in HOSTS for p in PROBES]\n\n\n@pytest.mark.parametrize(\"host,probe\", CASES, ids=[\"%s-%s\" % (h, p) for h, p in CASES])\ndef test_quickstart(host: Host, probe: Probe, builds: _Builds, lypning_lib, tmp_path) -> None:\n    prefix = builds.prefix(host)\n    cwd = tmp_path / \"cwd\"\n    cwd.mkdir()\n    env = dict(os.environ)\n    env.update(host.env())\n    r = subprocess.run(prefix + probe.argv, cwd=str(cwd), env=env,\n                       stdin=subprocess.DEVNULL, capture_output=True, timeout=PROBE_TIMEOUT)\n    shown = \"%s %s\\nstdout: %r\\nstderr: %r\" % (\n        host.name, probe.name, r.stdout, r.stderr.decode(\"utf-8\", \"replace\"))\n    assert r.stdout == probe.stdout, shown\n    assert r.returncode == probe.code, shown\n    if probe.stderr_has is None:\n        assert r.stderr == b\"\", shown\n    else:\n        assert r.stderr.count(probe.stderr_has) == 1, (\n            \"%s\\n%s must appear exactly once: 0 means the failure was swallowed, \"\n            \"2 means the host retried a program that had already run\" % (shown, probe.stderr_has))\n    assert b\"unsupported\" not in r.stderr, \"a refusal leaked to stderr:\\n\" + shown","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:11:50.959Z","count":1,"stdin_sample":null}
{"key":"py-ea715960ae5b","id":"py-ea715960ae5b","program":"$SRC","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:14:27.163Z","count":1,"stdin_sample":null}
{"key":"py-eb403faddbe0","id":"py-eb403faddbe0","program":"p=\"test.lua\"; s=open(p).read()\nold='''-- A copy of the module in a directory with no ../rust and no $LYPNING_HOME\n-- library: the message names the build command and every place it looked.\nlocal tmp = os.tmpname()\nos.remove(tmp)\n-- os.execute answers 0 (raw status) or true (LUA52COMPAT) for success.\nlocal copied = os.execute(\"mkdir -p \" .. shell_quote(tmp) .. \" && cp \" .. shell_quote(here .. \"/lypning.lua\") .. \" \" ..\n      shell_quote(tmp) .. \"/\")\ncheck(copied == 0 or copied == true, \"copied the module aside\", copied)'''\nnew='''-- A copy of the module in a directory with no ../rust, no ../include and no\n-- $LYPNING_HOME library: the message names the build command and every place\n-- it looked. Nested one level under the temp dir so its ../include is ours,\n-- not /tmp/include, which another host's test may have left behind.\nlocal tmp = os.tmpname()\nos.remove(tmp)\n-- os.execute answers 0 (raw status) or true (LUA52COMPAT) for success.\nlocal copied = os.execute(\"mkdir -p \" .. shell_quote(tmp .. \"/mod\") .. \" && cp \" .. shell_quote(here .. \"/lypning.lua\") .. \" \" ..\n      shell_quote(tmp .. \"/mod/\"))\ncheck(copied == 0 or copied == true, \"copied the module aside\", copied)'''\nassert s.count(old)==1; s=s.replace(old,new)\nold2='''    \"package.path = \" .. string.format(\"%q\", tmp .. \"/?.lua;\") .. \" .. package.path; \" .. code) .. \" 2>&1\")'''\nnew2='''    \"package.path = \" .. string.format(\"%q\", tmp .. \"/mod/?.lua;\") .. \" .. package.path; \" .. code) .. \" 2>&1\")'''\nassert s.count(old2)==1; s=s.replace(old2,new2)\nopen(p,\"w\").write(s); print(\"edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:30:04.787Z","count":1,"stdin_sample":null}
{"key":"py-ed10ad1db4b6","id":"py-ed10ad1db4b6","program":"\nimport json\nd=json.load(open('/tmp/lypning-host-reports.json'))\nfor r in d['reports']:\n    print('=====',r.get('host') or r.get('name') or list(r.keys())[:3])\n    for k in ('files_created','files_modified','package_data_globs','manifest_prunes','gitignore_lines','ci_step','left_undone'):\n        v=r.get(k); print('--',k); print(json.dumps(v,indent=1) if not isinstance(v,str) else v)\nprint('===== VERDICTS')\nfor v in d['verdicts']:\n    print(json.dumps({k:v[k] for k in v if k in ('host','name','open','verdict')},indent=1))\n","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:06:07.641Z","count":1,"stdin_sample":null}
{"key":"py-eda08632e319","id":"py-eda08632e319","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_5840e5e0-41b/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')!='result': continue\n    v=r.get('result') or r.get('value') or {}\n    if 'task' not in v: continue\n    print('='*90); print(v['task'][:80]); print('created:', v['files_created']); print('modified:', v['files_modified'])\n    print('VERIFIED:', v['verified'][:2500]); print('UNDONE:', v['left_undone'][:1500]); print('CHANGELOG NOTES:', v['notes_for_changelog'][:1500])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T06:28:23.229Z","count":1,"stdin_sample":null}
{"key":"py-edd3a047010e","id":"py-edd3a047010e","program":"import pathlib\np = pathlib.Path(\"pyproject.toml\"); s = p.read_text()\nold = '''  # Claimed the day `lypning build --lib` and the test suite passed on an\n  # arm64 Mac (2026-09-02, from the host-bindings branch), which is also the\n  # day the macOS job in ci.yml started asserting it. The Rust core's static\n  # musl gate is Linux-only; on macOS the CLI, the library tier and the hosts\n  # over it are what this line promises.\n'''\nnew = '''  # Claimed 2026-09-02, the day `lypning build --lib`, the library's own tests\n  # (tests/test_embed.py) and every host quickstart passed on an arm64 Mac,\n  # and the day the macos job in ci.yml started running the whole suite there.\n  # The Rust core's static-musl gate is Linux-only; on a Mac the CLI, the\n  # library tier and the hosts over it are what this line promises.\n'''\nassert old in s; p.write_text(s.replace(old, new)); print(\"classifier comment updated\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:52:31.388Z","count":1,"stdin_sample":null}
{"key":"py-efe56470f60f","id":"py-efe56470f60f","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_9f7e1130-3b4/journal.jsonl'\nn=0\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        n+=1; v=r.get('result') or r.get('value') or {}\n        print(n, (v.get('host') or v.get('angle','')[:40] or 'judge'), 'built=',v.get('built'),'ran=',v.get('ran'),'findings=',len(v.get('findings',[])))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:14:14.773Z","count":1,"stdin_sample":null}
{"key":"py-f4fe413cdb34","id":"py-f4fe413cdb34","program":"p='github-push-lacks-workflow-scope.md'; s=open(p).read()\ns=s.rstrip('\\n')+\"\\n\\nPending as of 2026-09-02: the CI commit for PR #25 (quickstart probes per host, macOS job) sits\\nunpushed on branch `bindings-ci` in /Users/user/dev/lypning-bindings, rebased onto main; after\\n`gh auth refresh -h github.com -s workflow`, `git push -u origin bindings-ci` and open its PR.\\n\"\nopen(p,'w').write(s); print('memory updated')","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T11:40:43.208Z","count":1,"stdin_sample":null}
{"key":"py-f54bbc7bff5d","id":"py-f54bbc7bff5d","program":"import ast,sys\nt=ast.parse(open(\"$Q\").read()); names={n.id for n in ast.walk(t) if isinstance(n,ast.Name)}|{n.value.id for n in ast.walk(t) if isinstance(n,ast.Attribute) and isinstance(n.value,ast.Name)}\nfor n in ast.walk(t):\n    if isinstance(n,(ast.Import,ast.ImportFrom)):\n        for a in n.names:\n            nm=(a.asname or a.name).split('.')[0]\n            print(nm, \"used\" if nm in names else \"UNUSED\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:31:05.054Z","count":1,"stdin_sample":null}
{"key":"py-f64b25628ded","id":"py-f64b25628ded","program":"import pathlib\np = pathlib.Path(\"Makefile\")\ns = p.read_text()\nold = '''valgrind: $(BIN)\n\t@command -v valgrind >/dev/null 2>&1 || { \\\\\n\t  echo \"valgrind is not installed on this machine; skipping\"; exit 0; }; \\\\\n\t$(call in_tmpdir,valgrind --leak-check=full --show-leak-kinds=definite$(,)indirect \\\\\n\t         --errors-for-leak-kinds=definite$(,)indirect --error-exitcode=9 \\\\\n\t         --trace-children=no $(here)/$(BIN))\n'''\nnew = '''valgrind_flags := --leak-check=full --show-leak-kinds=definite,indirect \\\\\n                  --errors-for-leak-kinds=definite,indirect --error-exitcode=9 \\\\\n                  --trace-children=no\nvalgrind: $(BIN)\n\t@command -v valgrind >/dev/null 2>&1 || { \\\\\n\t  echo \"valgrind is not installed on this machine; skipping\"; exit 0; }; \\\\\n\t$(call in_tmpdir,valgrind $(valgrind_flags) $(here)/$(BIN))\n'''\nassert s.count(old) == 1\np.write_text(s.replace(old, new))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:25:07.218Z","count":1,"stdin_sample":null}
{"key":"py-f6b7273bd383","id":"py-f6b7273bd383","program":"import zipfile, glob\nw = glob.glob('/tmp/lypwheel/*.whl')[0]\nnames = zipfile.ZipFile(w).namelist()\nprint(\"wheel:\", w, \"entries:\", len(names))\nsel = sorted(n for n in names if any(n.startswith('lypning/assets/'+d) for d in ('go','swift','lua','node','examples')))\nprint(\"--- assets/{go,swift,lua,node,examples} in the wheel (%d):\" % len(sel))\nprint(\"\\n\".join(sel))\nbad = sorted(n for n in names if '__pycache__' in n or '/target/' in n or '/build/' in n or '/.build/' in n or '/.swiftpm/' in n or '/node_modules/' in n or n.endswith(('.pyc', '.o', '.a', '.rlib', '.so', '.dylib', '.node', '.class')))\nprint(\"--- dist-check contamination expression:\", \"wheel: no build output\" if not bad else bad)\nprint(\"--- other assets dirs present:\", sorted({n.split('/')[2] for n in names if n.startswith('lypning/assets/') and n.count('/')>2}))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T05:27:42.075Z","count":1,"stdin_sample":null}
{"key":"py-f8bc102003a8","id":"py-f8bc102003a8","program":"import sys,json; e=json.loads(sys.stdin.readline()); print(repr(e['program'])); print('argv:', e.get('argv_tail'), 'stdin:', e.get('stdin_sample'))","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T01:33:17.930Z","count":1,"stdin_sample":null}
{"key":"py-f91d20c1535b","id":"py-f91d20c1535b","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586/subagents/workflows/wf_9f7e1130-3b4/journal.jsonl'\nfor ln in open(p):\n    try: r=json.loads(ln)\n    except: continue\n    if r.get('type')=='result':\n        v=r.get('result') or r.get('value')\n        print(r.get('label'), '::')\n        print(json.dumps(v,indent=1)[:9000])","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-01T19:03:49.673Z","count":1,"stdin_sample":null}
{"key":"py-fcf8b65aaf68","id":"py-fcf8b65aaf68","program":"import re,io\np=\"lypning.lua\"; s=open(p).read()\nold='''local function readable(path)\n  local f = io.open(path, \"rb\")\n  if f then f:close() return true end\n  return false\nend'''\nnew='''-- A file that can be OPENED and READ: fopen() on a directory succeeds on macOS\n-- and glibc and only the read fails, so open alone would let $LYPNING_LIB=/tmp\n-- through to a dlopen error that names neither the variable nor the fix.\nlocal function readable(path)\n  local f = io.open(path, \"rb\")\n  if not f then return false end\n  local ok = f:read(0) ~= nil\n  f:close()\n  return ok\nend'''\nassert s.count(old)==1; s=s.replace(old,new)\nold2='''if not load_ok then\n  error(\"lypning: cannot load \" .. M.library_path .. \": \" .. tostring(lib), 0)\nend'''\nnew2='''if not load_ok then\n  error(\"lypning: cannot load \" .. M.library_path .. \": \" .. tostring(lib) ..\n        \". Rebuild it for this machine with `\" .. M.BUILD_COMMAND .. \"`.\", 0)\nend'''\nassert s.count(old2)==1; s=s.replace(old2,new2)\nopen(p,\"w\").write(s)\n\np=\"quickstart.lua\"; s=open(p).read()\nold='''    code = status >= 256 and math.floor(status / 256) or status\n'''\nnew='''    code = status >= 256 and math.floor(status / 256) or status % 128\n'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,\"w\").write(s)\n\np=\"test.lua\"; s=open(p).read()\nold='''check(os.execute(\"mkdir -p \" .. shell_quote(tmp) .. \" && cp \" .. shell_quote(here .. \"/lypning.lua\") .. \" \" ..\n      shell_quote(tmp) .. \"/\") == 0, \"copied the module aside\")'''\nnew='''-- os.execute answers 0 (raw status) or true (LUA52COMPAT) for success.\nlocal copied = os.execute(\"mkdir -p \" .. shell_quote(tmp) .. \" && cp \" .. shell_quote(here .. \"/lypning.lua\") .. \" \" ..\n      shell_quote(tmp) .. \"/\")\ncheck(copied == 0 or copied == true, \"copied the module aside\", copied)\n-- $LYPNING_LIB naming a directory, or an empty file, is \"not a readable file\"\n-- too: fopen() on a directory succeeds and only the read fails.\nout = child(\"LYPNING_LIB=\" .. shell_quote(tmp), \"require('lypning')\")\ncheck(out:find(\"LYPNING_LIB\", 1, true) and out:find(\"not a readable file\", 1, true), \"$LYPNING_LIB=<dir> is named, not dlopen'd\", out)'''\nassert s.count(old)==1; s=s.replace(old,new); open(p,\"w\").write(s)\nprint(\"edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:21:38.759Z","count":1,"stdin_sample":null}
{"key":"py-fe7691159540","id":"py-fe7691159540","program":"import pathlib\np = pathlib.Path(\"README.md\"); s = p.read_text()\nold = \"\"\"declared in `src/napi.rs` and resolved from the `node` process at load. Nothing\nelse builds the addon; a read-only install copies the cdylib from `target/release`\nto `$LYPNING_HOME/lib` by hand, and `index.js` says where it looked when it is\nnot there.\n\"\"\"\nnew = \"\"\"declared in `src/napi.rs` and resolved from the `node` process at load. Nothing\nelse builds the addon, `lypning build` included; on a read-only install (a\nwheel) build the crate somewhere writable and copy the cdylib from\n`target/release` into `$LYPNING_HOME/lib` by hand. `index.js` names every place\nit looked when the addon is not there.\n\"\"\"\nassert s.count(old) == 1; p.write_text(s.replace(old, new)); print(\"README edited\")","argv_tail":[],"source":"hook","session":"9b4c7c04-860e-589c-9376-5ff9180ae586","first_seen":"2026-09-02T00:23:08.658Z","count":1,"stdin_sample":null}
