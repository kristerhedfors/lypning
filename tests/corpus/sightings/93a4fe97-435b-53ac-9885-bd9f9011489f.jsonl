{"key":"py-01e1041160a9","id":"py-01e1041160a9","program":"\n\ndef test_a_truncated_transcript_is_re_read_rather_than_resumed(tmp_path):\n    \"\"\"Truncated in place, with more prefix left than the digest window covers.\n\n    The three staleness signals are `(dev, ino)`, `size >= offset`, and a digest\n    of the last `_CACHE_DIGEST_BYTES` consumed bytes. This is the case that\n    needs at least two of them to have been thought about: same inode, and a\n    surviving prefix LONGER than the digest window, so a digest of the head of\n    that prefix — which is what this used to hash — would still match. Believing\n    the cache here serves ids the file no longer contains, which is the silent\n    wrong answer, not a slow one.\n    \"\"\"\n    keep = [_assistant(\"2026-09-02T10:00:0%d.000Z\" % i, \"claude-opus-5\", \"toolu_a\",\n                       \"python3 -c 'print(1)'   # %s\" % (\"p\" * 900))\n            for i in range(6)]\n    rest = [_assistant(\"2026-09-02T10:01:00.000Z\", \"claude-fable-5-1\", \"toolu_b\"),\n            _assistant(\"2026-09-02T10:01:01.000Z\", \"claude-fable-5-1\", \"toolu_c\")]\n    main = _transcripts(tmp_path, keep + rest)\n    surviving = len(\"\".join(r + \"\\n\" for r in keep).encode(\"utf-8\"))\n    assert surviving > harvest._CACHE_DIGEST_BYTES, surviving\n    assert main.stat().st_size > surviving\n\n    log = _write_log(tmp_path, [\n        _hook(\"python3 -c 'print(1)'\", \"toolu_a\", main),\n        _hook(\"python3 -c 'print(2)'\", \"toolu_b\", main),\n        _hook(\"python3 -c 'print(3)'\", \"toolu_c\", main),\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[\"print(2)\"].models == ((\"claude-fable-5-1\", 1),)\n\n    # Truncate in place: same path, same inode, and `toolu_b`/`toolu_c` are gone.\n    with open(str(main), \"r+b\") as fh:\n        fh.truncate(surviving)\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[\"print(1)\"].models == ((\"claude-opus-5\", 1),)\n    assert by_program[\"print(2)\"].models == ()\n    assert by_program[\"print(3)\"].models == ()\n\n\ndef test_a_shrunk_file_is_stale_even_when_the_stored_digest_says_nothing(tmp_path):\n    \"\"\"The one shrink the digest cannot see, which is why the size check stays.\n\n    `_tail_digest` returns `\"\"` when it cannot read its window, and an entry\n    carrying `\"\"` beside a non-zero offset is a shape `_cache_entry_ok` accepts\n    — from an older writer, a transient read failure, or anyone with an editor,\n    since this file is on disk and outlives the version that wrote it. Then the\n    digests compare equal whatever the file now holds, and `st.st_size <\n    entry[\"offset\"]` is the only signal left that the bytes the offset claims\n    are not there.\n    \"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n        _assistant(\"2026-09-02T10:00:01.000Z\", \"claude-opus-5\", \"toolu_b\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_b\", main)])\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n\n    st = os.stat(str(main))\n    cache = harvest._index_cache_path()\n    cache.write_text(json.dumps({\"version\": harvest._CACHE_VERSION, \"files\": {str(main): {\n        \"offset\": st.st_size, \"ino\": st.st_ino, \"dev\": st.st_dev, \"digest\": \"\",\n        \"ids\": {\"toolu_b\": \"claude-opus-5\"}, \"timeline\": []}}}), encoding=\"utf-8\")\n    with open(str(main), \"w\", encoding=\"utf-8\") as fh:\n        fh.write(_assistant(\"2026-09-02T11:00:00.000Z\", \"claude-fable-5-1\", \"toolu_z\") + \"\\n\")\n    # `toolu_b` is not in this file any more. The cache still claims it.\n    assert harvest.parse_log(log)[0].models == ()\n\n\ndef test_the_time_join_includes_the_instant_it_was_asked_about(tmp_path):\n    \"\"\"\"At or before\", not \"strictly before\" — and the tie is the likely case.\n\n    The shim's stamp is second-precision on this host, so a spawn that happens\n    in the same second an assistant record was written lands on exactly its\n    canonical key. `bisect_right(timeline, (key, high)) - 1` includes that\n    record; `bisect_left(timeline, (key, \"\")) - 1` would skip it and hand back\n    whoever spoke before — a different model, silently, on the commonest shape\n    of tie there is.\n    \"\"\"\n    at = \"2026-09-02T01:05:24.000000\"\n    index = harvest._ModelIndex({}, [(\"2026-09-02T01:05:20.100000\", \"claude-sonnet-5\"),\n                                     (at, \"claude-fable-5-1\")])\n    assert index.at(\"2026-09-02T01:05:24Z\") == \"claude-fable-5-1\"\n    assert index.at(\"2026-09-02T01:05:24.000Z\") == \"claude-fable-5-1\"\n    # And one instant earlier is still the earlier model, so this is a boundary\n    # and not a test that any answer would satisfy.\n    assert index.at(\"2026-09-02T01:05:23.999Z\") == \"claude-sonnet-5\"\n    assert index.at(\"2026-09-02T01:05:20.000Z\") is None\n\n    # The same tie through the whole pipeline: the transcript writes the model\n    # at :24.000 and the shim spawns in that second.\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T01:05:20.100Z\", \"claude-sonnet-5\", \"toolu_main\"),\n        _assistant(\"2026-09-02T01:05:24.000Z\", \"claude-fable-5-1\"),\n    ])\n    log = _write_log(tmp_path, [\n        _hook(\"python3 -c 'print(1)'\", \"toolu_main\", main),\n        {\"kind\": \"python_invocation\", \"session\": SESSION,\n         \"ts\": \"2026-09-02T01:05:24Z\", \"program\": UNIQUE, \"argv_tail\": []},\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[UNIQUE].models == ((\"claude-fable-5-1\", 1),)\n\n\n# --- one bad record must not cost every session its export --------------------\n\n\ndef test_one_poisoned_transcript_path_does_not_stop_the_export(project, tmp_path):\n    \"\"\"`parse_log` says it never raises, and the Stop hook is why it must not.\n\n    A `transcript` string comes out of the log — some other program wrote it,\n    this one only read it back — and `os.walk` on one holding a NUL raises\n    ValueError, not OSError. Unguarded, one such line stops the export of EVERY\n    session, on every Stop, silently (the hook still prints its contract line\n    and exits 0), and forever, because the log is append-only.\n    \"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_ok\"),\n    ])\n    _log([\n        {\"kind\": \"bash_command\", \"session\": \"poisoned\", \"ts\": \"2026-09-02T10:00:00.500Z\",\n         \"command\": \"python3 -c %r\" % UNIQUE_HEREDOC, \"transcript\": \"/tmp/tr\\x00ans.jsonl\",\n         \"tool_use_id\": \"toolu_bad\"},\n        {\"kind\": \"bash_command\", \"session\": SESSION, \"ts\": \"2026-09-02T10:00:01.500Z\",\n         \"command\": \"python3 -c %r\" % UNIQUE, \"transcript\": str(main),\n         \"tool_use_id\": \"toolu_ok\"},\n    ])\n    result = harvest._export()\n    published = {}\n    for path, _added, _total, _changed in result.files:\n        for s in harvest.read_sightings(path):\n            published[s.program] = s\n    # The good record is published AND attributed...\n    assert published[UNIQUE].models == ((\"claude-opus-5\", 1),)\n    # ...and the poisoned one costs itself a model and nothing else.\n    assert published[UNIQUE_HEREDOC].models == ()\n\n\ndef test_a_lone_surrogate_in_a_transcript_path_is_the_same_non_event(tmp_path):\n    # The other exception os.walk raises for a string this module did not\n    # choose: UnicodeEncodeError, a ValueError, from the filesystem encoder.\n    log = _write_log(tmp_path, [\n        {\"kind\": \"bash_command\", \"session\": SESSION, \"ts\": \"2026-09-02T10:00:00.500Z\",\n         \"command\": \"python3 -c 'print(1)'\", \"transcript\": \"/tmp/\\udce4/x.jsonl\",\n         \"tool_use_id\": \"toolu_bad\"},\n    ])\n    assert harvest.parse_log(log)[0].models == ()\n\n\n# --- the other harnesses are unattributed, and cost the join nothing ----------\n#\n# opencode's tool hooks (`tool.execute.before/after`, `shell.env`) carry\n# sessionID and callID and no model; OpenHands' PostToolUse payload carries\n# `session_id`, `tool_name`, `tool_input`, `tool_response` and no model, and its\n# hook subprocess env names no model either. Neither has a transcript on disk to\n# join against. So both are unattributed by construction, which is exactly what\n# the hole in `models` is for — and what must not quietly become a WRONG model\n# the day one of them starts writing a field this module reads.\n\n\ndef _opencode_bash(command, run=\"call_1\", ts=\"2026-09-02T10:00:00.500Z\"):\n    return {\"kind\": \"bash_command\", \"ts\": ts, \"session\": \"ses_opencode_1\",\n            \"cwd\": \"/tmp/p\", \"tool\": \"bash\", \"command\": command,\n            \"description\": None, \"transcript\": None, \"host\": \"opencode\",\n            \"run\": run}\n\n\ndef _openhands_bash(command, ts=\"2026-09-02T10:00:00.500Z\"):\n    return {\"kind\": \"bash_command\", \"ts\": ts, \"session\": \"oh-session-1\",\n            \"cwd\": \"/tmp/p\", \"tool\": \"terminal\", \"command\": command,\n            \"description\": None, \"transcript\": None, \"host\": \"openhands\",\n            \"exit_code\": 0}\n\n\ndef test_an_opencode_or_openhands_record_is_unattributed_and_never_wrong(tmp_path):\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_claude\",\n                   \"python3 -c 'print(0)'\"),\n    ])\n    log = _write_log(tmp_path, [\n        _hook(\"python3 -c 'print(0)'\", \"toolu_claude\", main),\n        _opencode_bash(\"python3 -c 'print(1)'\"),\n        _openhands_bash(\"python3 -c 'print(2)'\"),\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[\"print(0)\"].models == ((\"claude-opus-5\", 1),)\n    assert by_program[\"print(1)\"].models == ()\n    assert by_program[\"print(2)\"].models == ()\n\n\ndef test_a_non_claude_record_naming_a_transcript_is_still_not_joined(tmp_path):\n    \"\"\"Neither harness writes `transcript` today. This is the guard for the day\n    one does: its path must not be walked as a Claude transcript, and its ids\n    must not be looked up in one.\"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_x\",\n                   \"python3 -c 'print(1)'\"),\n    ])\n    rec = _opencode_bash(\"python3 -c 'print(1)'\")\n    rec[\"transcript\"] = str(main)\n    rec[\"tool_use_id\"] = \"toolu_x\"          # the same id, in another namespace\n    log = _write_log(tmp_path, [rec])\n    assert harvest.parse_log(log)[0].models == ()\n\n    # And a shim spawn under that session cannot borrow the path either.\n    log = _write_log(tmp_path, [\n        rec,\n        {\"kind\": \"python_invocation\", \"session\": rec[\"session\"],\n         \"ts\": \"2026-09-02T10:00:01Z\", \"program\": UNIQUE, \"argv_tail\": []},\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[UNIQUE].models == ()\n\n\ndef test_the_records_that_carry_no_program_read_no_transcript(tmp_path, monkeypatch):\n    \"\"\"opencode's `{\"kind\":\"note\"}` and `{\"kind\":\"exit\"}` records, and the\n    opt-in `{\"kind\":\"tool_call\"}`, flow through the same prepass the join is\n    built in. None of them names a program, none should reach the index, and\n    none should make the harvest open a transcript.\"\"\"\n    log = _write_log(tmp_path, [\n        {\"kind\": \"note\", \"ts\": \"2026-09-02T10:00:00.000Z\", \"session\": \"ses_1\",\n         \"host\": \"opencode\", \"detail\": \"shim not on PATH\"},\n        {\"kind\": \"exit\", \"ts\": \"2026-09-02T10:00:01.000Z\", \"session\": \"ses_1\",\n         \"host\": \"opencode\", \"run\": \"call_1\", \"exit_code\": 0},\n        {\"kind\": \"tool_call\", \"ts\": \"2026-09-02T10:00:02.000Z\", \"session\": \"ses_1\",\n         \"host\": \"opencode\", \"tool\": \"bash\"},\n    ])\n    indexed = []\n    monkeypatch.setattr(harvest, \"_model_index\",\n                        lambda t, cache=None: indexed.append(t) or harvest._EMPTY_INDEX)\n    assert harvest.parse_log(log) == []\n    assert indexed == []\n    assert not harvest._index_cache_path().exists()\n\n\n# --- --dry-run is real: it opens files and writes none ------------------------\n\n\ndef test_a_dry_run_harvest_writes_nothing_at_all(tmp_path):\n    \"\"\"Invariant 7. The index cache is the only write on the collect path, and\n    it is under $LYPNING_HOME — where a --dry-run must leave no trace.\"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\",\n                   \"python3 -c 'print(1)'\"),\n    ])\n    _log([_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    state = paths.state_dir()\n    before = sorted(p.name for p in state.iterdir()) if state.is_dir() else []\n\n    dry = harvest.collect(persist=False)\n    assert [s.models for s in dry] == [((\"claude-opus-5\", 1),)]\n    after = sorted(p.name for p in state.iterdir()) if state.is_dir() else []\n    assert after == before\n    assert not harvest._index_cache_path().exists()\n\n    # And the same call that is allowed to write does, so this is a test of the\n    # flag rather than of a join that never happened.\n    harvest.collect()\n    assert harvest._index_cache_path().is_file()","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:11:39.914Z","count":1,"stdin_sample":null}
{"key":"py-033b7cc77cc0","id":"py-033b7cc77cc0","program":"\nimport json\nfor l in open('/tmp/hookprobe/events2.jsonl'):\n    o=json.loads(l)\n    print(sorted(o.keys()))\n    print('  agent_id=',o.get('agent_id'),'agent_type=',o.get('agent_type'),'session_id=',o.get('session_id'),'effort=',o.get('effort'))\n    print('  transcript_path=',o.get('transcript_path'))\n    print('  cmd=',(o.get('tool_input') or {}).get('command'),'| tool_use_id=',o.get('tool_use_id'))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:30:39.134Z","count":1,"stdin_sample":null}
{"key":"py-03ae11232862","id":"py-03ae11232862","program":"import re\ns=open('/tmp/advcm/merge.py').read()\ns=s.replace('''    c=random.randint(1,10)\n    return harvest.Sighting''','''    c=sum(n for _,n in ms)+random.randint(0,4)\n    c=max(c,1)\n    return harvest.Sighting''')\ns=s.replace('''    return corpus.Entry(id=\"py-abc\", program=\"print(1)\", count=random.randint(1,5), models=ms,''','''    return corpus.Entry(id=\"py-abc\", program=\"print(1)\", count=max(1,sum(n for _,n in ms)+random.randint(0,3)), models=ms,''')\nopen('/tmp/advcm/merge2.py','w').write(s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T11:28:13.580Z","count":1,"stdin_sample":null}
{"key":"py-03e83b6ae4a5","id":"py-03e83b6ae4a5","program":"import io\np = \"docs/CAPTURE.md\"\ns = io.open(p, encoding=\"utf-8\").read()\n\nold = \"\"\"* `message.model == \"<synthetic>\"` is the CLI's own spelling for a record it\n  wrote without asking a model. It is treated as no model, not as one.\n\"\"\"\nnew = \"\"\"* `message.model == \"<synthetic>\"` is the CLI's own spelling for a record it\n  wrote without asking a model. It is treated as no model, not as one.\n\n**Attribution is Claude Code's only, and by construction.** opencode's tool\nhooks (`tool.execute.before`/`after`, `shell.env`) carry `sessionID` and\n`callID` and no model; the OpenHands SDK's `PostToolUse` payload carries\n`session_id`, `tool_name`, `tool_input` and `tool_response` and no model, and\nits hook subprocess environment names none either. Neither has a transcript on\ndisk to join against. So a record whose `host` is not `claude` is skipped by the\njoin entirely — it never contributes a transcript path to index and never has an\nid looked up — and it stays unattributed, which is precisely what the hole in\n`models` is for. Extending attribution to opencode is possible via a second,\ndifferently shaped mechanism (a `chat.params` hook accumulating model-per-session\nin the plugin closure, an inference rather than a join); it is deliberately not\ndone here, because a reader of `models` could not then tell which of the two\nproduced a name.\n\nThe residual, named rather than closed: the **shim** record carries no `host`.\nIts session tag falls back to `$CLAUDE_CODE_SESSION_ID` from the ambient\nenvironment, so another harness's agent run from inside a Claude Code session's\nshell has its nested python spawns tagged with the Claude session and time-joined\nto the Claude model. Closing that needs a `host` field on the shim's own record,\nwhich is a change to the shim wire format.\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"key namespacing, and the no-double-count property of the fold. Model\nattribution is pinned there too: the id join across the subagent tree, the\ntime join for the shim feed, and the max-versus-sum asymmetry between\n`harvest._combine` and `corpus._combine`.\n\"\"\"\nnew = \"\"\"key namespacing, and the no-double-count property of the fold. Model\nattribution is pinned there too: the id join across the subagent tree, the\ntime join for the shim feed — including the exact-timestamp tie, which\nsecond-precision shim stamps make the common case — the max-versus-sum\nasymmetry between `harvest._combine` and `corpus._combine`, every way the index\ncache can be stale, that an opencode or OpenHands record never acquires a model,\nand that one unwalkable transcript path costs its own session's attribution and\nnot every session's export.\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:24:47.579Z","count":1,"stdin_sample":null}
{"key":"py-0475e46d4bd4","id":"py-0475e46d4bd4","program":"import sys,json;print(list(json.load(sys.stdin).keys()))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:57.724Z","count":2,"stdin_sample":null}
{"key":"py-06d5d588d4de","id":"py-06d5d588d4de","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\na = (\"# is exactly what this module did before the cache existed. A cache miss costs\\n\"\n     \"# one re-read. It cannot cost a model, it cannot invent one, and it cannot\\n\"\n     \"# raise:\\n\"\n     \"# this runs inside a Stop hook, and invariant 5 says a hook fails no session,\\n\"\n     \"# including on its own failures.\\n\")\nb = (\"# is exactly what this module did before the cache existed. A cache miss costs\\n\"\n     \"# one re-read. It cannot cost a model, it cannot invent one, and it cannot\\n\"\n     \"# raise: this runs inside a Stop hook, and invariant 5 says a hook fails no\\n\"\n     \"# session, including on its own failures.\\n\")\nassert a in s\np.write_text(s.replace(a, b, 1), encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:46:21.469Z","count":1,"stdin_sample":null}
{"key":"py-083bf53ef8f4","id":"py-083bf53ef8f4","program":"import io\np = \"tests/test_harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''def test_a_lone_surrogate_in_a_transcript_path_is_the_same_non_event(tmp_path):\n    # The other exception os.walk raises for a string this module did not\n    # choose: UnicodeEncodeError, a ValueError, from the filesystem encoder.\n    log = _write_log(tmp_path, [\n        {\"kind\": \"bash_command\", \"session\": SESSION, \"ts\": \"2026-09-02T10:00:00.500Z\",\n         \"command\": \"python3 -c 'print(1)'\", \"transcript\": \"/tmp/\\\\udce4/x.jsonl\",\n         \"tool_use_id\": \"toolu_bad\"},\n    ])\n    assert harvest.parse_log(log)[0].models == ()\n'''\nnew = '''def test_a_lone_surrogate_in_a_transcript_path_is_the_same_non_event(tmp_path):\n    \"\"\"The other exception `os.walk` raises for a string this module did not\n    choose: UnicodeEncodeError, a subclass of ValueError, out of the filesystem\n    encoder.\n\n    `\\\\ud800`, not one of the `\\\\udc80`-`\\\\udcff` surrogates: those are what\n    `surrogateescape` produces for undecodable BYTES and they encode straight\n    back, so a path built from one raises nothing at all. Only a surrogate with\n    no byte behind it does — asserted here rather than assumed, because a test\n    aimed at the wrong half of the range is a test that passes on any code.\n    \"\"\"\n    bad = \"/tmp/\\\\ud800/x.jsonl\"\n    with pytest.raises(UnicodeEncodeError):\n        list(os.walk(bad))\n    log = _write_log(tmp_path, [\n        {\"kind\": \"bash_command\", \"session\": SESSION, \"ts\": \"2026-09-02T10:00:00.500Z\",\n         \"command\": \"python3 -c 'print(1)'\", \"transcript\": bad,\n         \"tool_use_id\": \"toolu_bad\"},\n    ])\n    assert harvest.parse_log(log)[0].models == ()\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\nif \"\\nimport pytest\\n\" not in s:\n    s = s.replace(\"import json\\nimport os\\n\", \"import json\\nimport os\\n\\nimport pytest\\n\", 1)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:04:13.274Z","count":1,"stdin_sample":null}
{"key":"py-08d9a250f36e","id":"py-08d9a250f36e","program":"import io\np = \"CHANGELOG.md\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = \"\"\"**2026-09-02** — the corpus can be sliced by the model that issued the program\n\n- **A new `models` field** on sightings and corpus records: a per-model\n  histogram of the occurrences behind `count`. It is a subset of them, never a\n  partition — an occurrence that cannot be attributed contributes to `count` and\n  to nothing else, so `count - sum(models)` is the unattributed hole and a merge\n  raises `count` rather than let that go negative. The key is omitted entirely\n  when nothing is known, so records captured before this existed keep the bytes\n  they have.\n- **The hook stays on the hot path it was on.** Nothing in the PreToolUse\n  payload names a model, so the hook writes down the `tool_use_id` it already\n  receives and the join is done at harvest time, against the session transcript\n  and its subagent tree. No new fork, no new file open, no change to the shell\n  hook.\n- **The harvest-time join reads only what was appended.** Transcripts are\n  append-only, so the index is incremental and its offsets are cached under\n  `$LYPNING_HOME` — a cache that is never a source of truth and whose every\n  failure is a full re-read. Measured on this machine on 2026-09-02 over a copy\n  of the real capture log (750 records, 4 sessions, 39.1 MB of transcript\n  trees, best of 9): 1859 ms per harvest without it, 974 ms with it warm.\n- `lypning corpus --stats` now reports entries per model with an explicit\n  `unattributed` row, and `--model NAME` slices the corpus, naming the whole the\n  slice came from.\n- Sightings records now carry unknown keys through, the way corpus records\n  already did — an older harvest was silently stripping fields a newer one had\n  written.\n\"\"\"\nnew = \"\"\"**2026-09-02** — the corpus can be sliced by the model that issued the program ([#27](https://github.com/kristerhedfors/lypning/pull/27))\n\n- **A new `models` field** on sightings and corpus records: a per-model\n  histogram of the occurrences behind `count`. It is a subset of them, never a\n  partition — an occurrence that cannot be attributed contributes to `count` and\n  to nothing else, so `count - sum(models)` is the unattributed hole and a merge\n  raises `count` rather than let that go negative. The key is omitted entirely\n  when nothing is known, so records captured before this existed keep the bytes\n  they have.\n- **The hook stays on the hot path it was on.** Nothing in the PreToolUse\n  payload names a model, so the hook writes down the `tool_use_id` it already\n  receives and the join is done at harvest time, against the session transcript\n  and its subagent tree. No new fork, no new file open, no change to the shell\n  hook.\n- **Attribution is Claude Code's only, by construction.** An opencode tool hook\n  and an OpenHands `PostToolUse` payload carry no model and no key to join one\n  on, so their records stay unattributed — which is what the hole in `models` is\n  for, and is pinned by a test rather than assumed.\n- **The harvest-time join reads only what was appended.** Transcripts are\n  append-only, so the index is incremental and its offsets are cached under\n  `$LYPNING_HOME` — a cache that is never a source of truth and whose every\n  failure is a full re-read. Quoted in bytes because this machine is shared and\n  a wall clock on it measures the machine: on 2026-09-02, over a copy of the\n  real capture log (892 records, 4 sessions, 180 transcript files, 45,642,644 B)\n  a cold harvest scans all of it and every harvest after it scans 0 B, reading\n  734,678 B of staleness digests instead. The same log unmodified, with no ids\n  in it, reads 0 transcript bytes and writes no cache at all.\n- `lypning corpus --stats` now reports entries per model with an explicit\n  `unattributed` row, and `--model NAME` slices the corpus, naming the whole the\n  slice came from.\n- Sightings records now carry unknown keys through, the way corpus records\n  already did — an older harvest was silently stripping fields a newer one had\n  written.\n\"\"\"\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:07:19.390Z","count":1,"stdin_sample":null}
{"key":"py-08f6522b3100","id":"py-08f6522b3100","program":"from pathlib import Path\np = Path(\"tests/test_harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"    monkeypatch.setattr(harvest, \"_model_index\",\n                        lambda t: indexed.append(t) or harvest._EMPTY_INDEX)\"\"\"\nnew = \"\"\"    monkeypatch.setattr(harvest, \"_model_index\",\n                        lambda t, cache=None: indexed.append(t) or harvest._EMPTY_INDEX)\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\n\np = Path(\"src/lypning/corpus.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"    :func:`merge` IS summation, protected from double counting by the\n    identical-record collapse instead. Copying one call site onto the other is\n    how this field goes quietly wrong, and no gate can see it.\n    \"\"\"\nnew = \"\"\"    :func:`merge` IS summation, protected from double counting by the\n    identical-record collapse instead. Copying one call site onto the other is\n    how this field goes quietly wrong, and no gate can see it.\n\n    Neither combiner is responsible for keeping the histogram inside the\n    ``count`` beside it. A per-key max over two disjoint key sets can out-count\n    a scalar max of the counts, and the max sites reconcile that themselves —\n    see :func:`harvest._count_at_least_the_models`, which raises the count\n    rather than trim the evidence.\n    \"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"both re-applied\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T06:16:47.216Z","count":1,"stdin_sample":null}
{"key":"py-0b184029715d","id":"py-0b184029715d","program":"import json\nfor line in open('/tmp/hookprobe/events.jsonl'):\n    line=line.strip()\n    if not line: continue\n    o=json.loads(line)\n    print(o.get('hook_event_name'), '->', sorted(o.keys()))\n    print('   ', json.dumps({k:(v if len(str(v))<160 else str(v)[:160]+'...') for k,v in o.items()}, indent=None)[:900])\n    print()","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:34.489Z","count":1,"stdin_sample":null}
{"key":"py-0badb14c3a6b","id":"py-0badb14c3a6b","program":"import json\nrecs = [\n {\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:00.000Z\",\"session\":\"good\",\"command\":\"python3 -c 'import sys; print(sys.maxsize**2 + 7)'\",\"transcript\":\"/tmp/pt/e2e/nul2/none.jsonl\",\"tool_use_id\":\"g1\"},\n {\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:01.000Z\",\"session\":\"bad\",\"command\":\"python3 -c 'print(2)'\",\"transcript\":\"/tmp/pt/e2e/nul2/t\"+chr(0)+\"x.jsonl\",\"tool_use_id\":\"b1\"},\n]\nwith open(\"/tmp/pt/e2e/nul2/log.jsonl\",\"w\") as fh:\n    for r in recs: fh.write(json.dumps(r)+\"\\n\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:37:39.282Z","count":1,"stdin_sample":null}
{"key":"py-0bc8fe49620c","id":"py-0bc8fe49620c","program":"import json,glob\n# commands issued by subagents of session 947e7e56 / b6365ad8 that are pythonish\nhookcmds=set()\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    try: r=json.loads(line)\n    except ValueError: continue\n    if r.get('kind')=='bash_command': hookcmds.add(r.get('command'))\ntot=0; found=0; ex=None\nfor f in glob.glob('/Users/user/.claude/projects/**/subagents/**/*.jsonl',recursive=True):\n    for line in open(f,'r',encoding='utf-8',errors='replace'):\n        try: r=json.loads(line)\n        except ValueError: continue\n        if r.get('type')!='assistant': continue\n        for b in (r.get('message') or {}).get('content') or []:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                cmd=(b.get('input') or {}).get('command') or ''\n                if 'python' in cmd:\n                    tot+=1\n                    if cmd in hookcmds:\n                        found+=1\n                        if ex is None: ex=(f.split('/')[-1], b['id'], (r.get('message') or {}).get('model'), cmd[:60])\nprint('subagent python Bash tool_uses:',tot,' of which present in hook feed:',found)\nprint('example:',ex)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:16:14.402Z","count":1,"stdin_sample":null}
{"key":"py-0e491760967d","id":"py-0e491760967d","program":"import sys, collections\nsys.path.insert(0,'src')\nfrom lypning import corpus, harvest, paths\nss = corpus.load_sightings()\nprint(\"merged sightings entries:\", len(ss))\n# per file\nimport json\nfrom pathlib import Path\nfor p in sorted(Path('tests/corpus/sightings').glob('*.jsonl')):\n    n=sum(1 for l in open(p) if l.strip())\n    print(\"  %-60s %5d records\" % (p.name, n))\nraw = harvest.collect()\nprint(\"harvest.collect():\", len(raw))\nprint(\"sessions seen in sightings:\", collections.Counter(s.session for s in raw).most_common())","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:01.957Z","count":1,"stdin_sample":null}
{"key":"py-0e78a7ba3c36","id":"py-0e78a7ba3c36","program":"s=open('/tmp/advcm/hook_hostile.py').read()\ns=s.replace('dict(good,session=\"b\",','dict(good,session=\"b\",command=\"python3 -c \\\\\"import hashlib; print(hashlib.md5(b\\'poison-probe\\').hexdigest())\\\\\"\",')\ns=s.replace('print(\"%-34s exit=%d contract=%s stderr=%r published=%d\" % (label, r.returncode, ok, r.stderr[:60], len(pub)))',\n            'names=sorted(p.stem for p in pub); print(\"%-34s exit=%d contract=%s published=%s\" % (label, r.returncode, ok, names))')\nopen('/tmp/advcm/hook_hostile2.py','w').write(s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T11:43:18.599Z","count":1,"stdin_sample":null}
{"key":"py-0f69827cfa53","id":"py-0f69827cfa53","program":"\n\n# --- the time join, and the widths that make a string compare a compare -------\n\n\ndef test_timestamps_of_different_widths_are_ordered_by_the_instant():\n    # The shim writes whole seconds on a host whose `date` has no %3N, the\n    # transcript writes milliseconds, and raw `\"…24Z\" > \"…24.900Z\"` because Z\n    # sorts after `.` — so one canonical width is what makes bisect a search\n    # over time rather than over spelling.\n    assert (harvest._canonical_ts(\"2026-09-02T01:05:24Z\")\n            < harvest._canonical_ts(\"2026-09-02T01:05:24.900Z\"))\n    assert (harvest._canonical_ts(\"2026-09-02T01:05:24.9Z\")\n            == harvest._canonical_ts(\"2026-09-02T01:05:24.900Z\"))\n    assert harvest._canonical_ts(\"2026-09-02T01:05:24\") == harvest._canonical_ts(\n        \"2026-09-02T01:05:24.000Z\")\n    # Not this shape at all: no model, rather than a stamp that sorts anywhere.\n    assert harvest._canonical_ts(\"2026-09-02T01:05:24+02:00\") is None\n    assert harvest._canonical_ts(\"yesterday\") is None\n    assert harvest._canonical_ts(None) is None\n\n\ndef test_a_second_precision_stamp_does_not_join_to_the_model_that_spoke_next(tmp_path):\n    \"\"\"The shim's stamp is second-precision on this host, and the models here\n    change inside that second.\n\n    Compared raw, `2026-09-02T01:05:24Z` sorts after `2026-09-02T01:05:24.900Z`,\n    so \"the latest record at or before the spawn\" would return the model that\n    started speaking 900 ms AFTER it. A wrong model is worse than no model: the\n    unattributed hole is reported, and a lie is not.\n    \"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T01:05:20.100Z\", \"claude-sonnet-5\", \"toolu_main\"),\n        _assistant(\"2026-09-02T01:05:24.900Z\", \"claude-haiku-4-5-20251001\"),\n    ])\n    log = _write_log(tmp_path, [\n        _hook(\"python3 -c 'print(1)'\", \"toolu_main\", main),\n        {\"kind\": \"python_invocation\", \"session\": SESSION,\n         \"ts\": \"2026-09-02T01:05:24Z\", \"program\": UNIQUE, \"argv_tail\": []},\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    assert by_program[UNIQUE].models == ((\"claude-sonnet-5\", 1),)\n\n\n# --- the incremental index ----------------------------------------------------\n#\n# The join runs on every Stop, and the log it reads is append-only: the set of\n# transcripts it asks about grows for as long as the log lives, so a full\n# re-index is a cost that rises with the age of the log and not with the work\n# the turn did. These pin the two halves of the answer — that only appended\n# bytes are read, and that every way the cache can be wrong costs time and\n# never a model.\n\n\ndef _append(path, text):\n    with open(str(path), \"a\", encoding=\"utf-8\") as fh:\n        fh.write(text)\n\n\ndef test_the_second_harvest_reads_only_the_bytes_that_were_appended(tmp_path, monkeypatch):\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\",\n                   \"python3 -c 'print(1)'\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n\n    scanned = []\n    real = harvest._scan_transcript\n    monkeypatch.setattr(harvest, \"_scan_transcript\",\n                        lambda text: scanned.append(text) or real(text))\n    _append(main, _assistant(\"2026-09-02T10:00:10.000Z\", \"claude-fable-5-1\",\n                             \"toolu_b\", \"python3 -c 'print(2)'\") + \"\\n\")\n    log = _write_log(tmp_path, [\n        _hook(\"python3 -c 'print(1)'\", \"toolu_a\", main),\n        _hook(\"python3 -c 'print(2)'\", \"toolu_b\", main, \"2026-09-02T10:00:10.500Z\"),\n    ])\n    by_program = {s.program: s for s in harvest.parse_log(log)}\n    # The first record is still attributed although it was never read again:\n    # that is the cache answering, and it is the whole point of it.\n    assert by_program[\"print(1)\"].models == ((\"claude-opus-5\", 1),)\n    assert by_program[\"print(2)\"].models == ((\"claude-fable-5-1\", 1),)\n    text = \"\".join(scanned)\n    assert \"toolu_b\" in text\n    assert \"toolu_a\" not in text\n\n\ndef test_a_half_written_final_line_is_read_when_it_is_whole(tmp_path):\n    \"\"\"The CLI is appending while this runs, so the last line is routinely half\n    a line. Consuming it would move the offset past bytes that were never really\n    read, and that record would never be seen again.\"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n    ])\n    whole = _assistant(\"2026-09-02T10:00:10.000Z\", \"claude-fable-5-1\", \"toolu_b\")\n    _append(main, whole[:30])  # a line the writer has not finished\n\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_b\", main)])\n    assert harvest.parse_log(log)[0].models == ()\n\n    _append(main, whole[30:] + \"\\n\")\n    assert harvest.parse_log(log)[0].models == ((\"claude-fable-5-1\", 1),)\n\n\ndef test_a_transcript_rewritten_in_place_is_indexed_again_rather_than_resumed(tmp_path):\n    \"\"\"Same path, same inode, no shorter — and a completely different file.\n\n    Append-only is a property of the writer, not of the filesystem: a restored\n    backup or a copied-over path breaks it, and resuming at the stored offset\n    would then splice one file's records onto another's and keep answering with\n    ids that are no longer in it. The head digest is what notices.\n    \"\"\"\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n\n    main.write_text(\"\".join(\n        _assistant(\"2026-09-02T11:00:0%d.000Z\" % i, \"claude-fable-5-1\", \"toolu_b\") + \"\\n\"\n        for i in range(4)), encoding=\"utf-8\")\n    # `toolu_a` is not in this file any more, so nothing can attribute it.\n    assert harvest.parse_log(log)[0].models == ()\n\n\ndef test_every_way_the_cache_can_be_wrong_costs_time_and_not_a_model(tmp_path):\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    expected = ((\"claude-opus-5\", 1),)\n    assert harvest.parse_log(log)[0].models == expected\n    cache = harvest._index_cache_path()\n    assert cache.is_file()\n\n    for corrupt in (\n        \"\",                                             # a zero-length write\n        \"{not json\",                                    # a half-written file\n        json.dumps({\"version\": 999, \"files\": {}}),      # a schema we do not read\n        json.dumps({\"version\": 1, \"files\": \"nope\"}),\n        json.dumps({\"version\": 1, \"files\": {str(main): {\"offset\": \"far\"}}}),\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {\"toolu_a\": 3}, \"timeline\": []}}}),  # a model that is not a name\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {}, \"timeline\": [[1, \"m\"]]}}}),      # a stamp bisect cannot compare\n    ):\n        cache.write_text(corrupt, encoding=\"utf-8\")\n        assert harvest.parse_log(log)[0].models == expected, corrupt\n\n\ndef test_a_cache_that_cannot_be_written_is_not_an_error(tmp_path):\n    # Invariant 5: this runs inside a Stop hook, so an unwritable state dir is\n    # a slower harvest and nothing else. A directory in the cache file's place\n    # fails both the read and the atomic replace.\n    cache = harvest._index_cache_path()\n    paths.ensure_dir(cache)\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n    assert cache.is_dir()  # nothing wrote over it, and nothing raised\n\n\ndef test_the_cache_forgets_transcripts_that_are_gone(tmp_path):\n    # It is a cache of files that exist, not a record of every session that ever\n    # ran here — otherwise nothing ever removes a line from it.\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_a\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_a\", main)])\n    harvest.parse_log(log)\n    stored = json.loads(harvest._index_cache_path().read_text(encoding=\"utf-8\"))\n    assert str(main) in stored[\"files\"]\n\n    main.unlink()\n    other = _transcripts(tmp_path / \"other\", [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-opus-5\", \"toolu_c\"),\n    ])\n    harvest.parse_log(_write_log(tmp_path, [_hook(\"python3 -c 'print(3)'\", \"toolu_c\", other)]))\n    stored = json.loads(harvest._index_cache_path().read_text(encoding=\"utf-8\"))\n    assert str(main) not in stored[\"files\"]\n    assert str(other) in stored[\"files\"]","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:21:49.076Z","count":1,"stdin_sample":null}
{"key":"py-100501a31a00","id":"py-100501a31a00","program":"import json,glob,os\nfor tid,agid,sess in [(\"toolu_017JeurZ5Lzj9ndqgUnPCz3J\",\"a7128fe3098542c7b\",\"1d6f4b17-ed89-41bb-845a-498cb8e75a05\"),\n                      (\"toolu_013fhrXQPFakTXuxq9gNk9U8\",\"a5228b5e71af7dc5f\",\"d0304a22-87ca-4dfc-bef8-8c6e6962f97c\"),\n                      (\"toolu_01Scntp7nHmjBUnR4C5b5hVc\",\"a1844de50fc2dcbb1\",\"c12aae57-823b-434f-8b9a-1eff945fd0c6\")]:\n    base='/Users/user/.claude/projects/-private-tmp-hookprobe'\n    main=f'{base}/{sess}.jsonl'\n    derived=f'{base}/{sess}/subagents/agent-{agid}.jsonl'\n    print('---',tid)\n    print('  derived path exists:', os.path.exists(derived), derived)\n    print('  id in MAIN:', tid in open(main).read())\n    txt=open(derived).read() if os.path.exists(derived) else ''\n    print('  id in SUBAGENT file:', tid in txt)\n    for line in txt.splitlines():\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')=='assistant':\n            for b in ((r.get('message') or {}).get('content') or []):\n                if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('id')==tid:\n                    print('  model in subagent record:', (r.get('message') or {}).get('model'), 'isSidechain:', r.get('isSidechain'), 'agentId:', r.get('agentId'))\n    # main loop model\n    mm=set()\n    for line in open(main):\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')=='assistant': mm.add((r.get('message') or {}).get('model'))\n    print('  MAIN transcript models:', mm)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:09:35.506Z","count":1,"stdin_sample":null}
{"key":"py-1025aa2fc370","id":"py-1025aa2fc370","program":"import re\ndata=open('/tmp/cc258.strings','rb').read()\nfor m in re.finditer(rb'\"PreToolUse\"', data):\n    ctx=data[max(0,m.start()-350):m.start()+350]\n    if b'tool_use_id' in ctx and b'hook_event_name:x(' not in ctx:\n        print('='*70); print(ctx.decode('utf8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:11:27.882Z","count":1,"stdin_sample":null}
{"key":"py-109740135dff","id":"py-109740135dff","program":"import io\np = \"tests/test_harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\n# The digest moved from the head of the consumed prefix to its tail.\ns = s.replace(\"    ids that are no longer in it. The head digest is what notices.\\n\",\n              \"    ids that are no longer in it. The prefix digest is what notices.\\n\")\n\n# The stored key was renamed and the schema version bumped; read the version\n# from the module so a future bump cannot make these cases vacuous.\nold = '''    for corrupt in (\n        \"\",                                             # a zero-length write\n        \"{not json\",                                    # a half-written file\n        json.dumps({\"version\": 999, \"files\": {}}),      # a schema we do not read\n        json.dumps({\"version\": 1, \"files\": \"nope\"}),\n        json.dumps({\"version\": 1, \"files\": {str(main): {\"offset\": \"far\"}}}),\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {\"toolu_a\": 3}, \"timeline\": []}}}),  # a model that is not a name\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {}, \"timeline\": [[1, \"m\"]]}}}),      # a stamp bisect cannot compare\n        # Well-formed and STALE, which is the dangerous one: it names a model,\n        # and believing it would be the silent wrong answer rather than a slow\n        # one. The inode it claims is not this file's.\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n        # Well-formed, right inode, and the head of the file has moved: same\n        # path, different bytes. Only re-reading can tell.\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": os.stat(str(main)).st_ino,\n            \"dev\": os.stat(str(main)).st_dev, \"head\": \"0\" * 32,\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n    ):\n'''\nnew = '''    # Read from the module, not spelled 1: a bumped version would otherwise\n    # reject every case below on the version alone and pass this test without\n    # ever exercising the shape it is about.\n    v = harvest._CACHE_VERSION\n    for corrupt in (\n        \"\",                                             # a zero-length write\n        \"{not json\",                                    # a half-written file\n        json.dumps({\"version\": 999, \"files\": {}}),      # a schema we do not read\n        json.dumps({\"version\": v, \"files\": \"nope\"}),\n        json.dumps({\"version\": v, \"files\": {str(main): {\"offset\": \"far\"}}}),\n        json.dumps({\"version\": v, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"digest\": \"x\",\n            \"ids\": {\"toolu_a\": 3}, \"timeline\": []}}}),  # a model that is not a name\n        json.dumps({\"version\": v, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"digest\": \"x\",\n            \"ids\": {}, \"timeline\": [[1, \"m\"]]}}}),      # a stamp bisect cannot compare\n        # Well-formed and STALE, which is the dangerous one: it names a model,\n        # and believing it would be the silent wrong answer rather than a slow\n        # one. The inode it claims is not this file's.\n        json.dumps({\"version\": v, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"digest\": \"x\",\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n        # Well-formed, right inode, and the consumed prefix has moved: same\n        # path, different bytes. Only re-reading can tell.\n        json.dumps({\"version\": v, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": os.stat(str(main)).st_ino,\n            \"dev\": os.stat(str(main)).st_dev, \"digest\": \"0\" * 32,\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n    ):\n'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:10:24.535Z","count":1,"stdin_sample":null}
{"key":"py-1485e60dc53d","id":"py-1485e60dc53d","program":"import json,glob\nf=glob.glob('/Users/user/.claude/projects/**/subagents/**/*.jsonl',recursive=True)\nprint('subagent transcript files:',len(f))\np=f[0]; print(p)\nfor line in open(p,errors='replace'):\n    try: r=json.loads(line)\n    except ValueError: continue\n    if r.get('type')=='assistant':\n        m=r.get('message') or {}\n        print(' sessionId=',r.get('sessionId'),'isSidechain=',r.get('isSidechain'),'model=',m.get('model'),'agentId=',r.get('agentId'))\n        print(' keys=',sorted(r.keys()))\n        break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:15:38.140Z","count":1,"stdin_sample":null}
{"key":"py-15406f2ccaef","id":"py-15406f2ccaef","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''            # The size check is not made redundant by the digest. A truncation\n            # that leaves more than _CACHE_DIGEST_BYTES of prefix behind still\n            # ends the file INSIDE the region the digest covers, so the digest\n            # read comes up short of `n` and returns \"\" — which happens to\n            # differ from any stored digest, but only by accident of a short\n            # read, and only while the truncation point stays inside that\n            # window. Truncate to a length that is still past the stored offset\n            # minus N and the digest matches again while the ids past the new\n            # end are lies. `st.st_size < entry[\"offset\"]` is the check that\n            # actually knows the file no longer contains the bytes it claims.\n'''\nnew = '''            # The size check is not made redundant by the tail digest, and the\n            # near-miss is worth writing down. A shrink below the stored offset\n            # always shortens the digest's read window, so `_tail_digest`\n            # returns \"\" and the compare usually fires on its own. Usually is\n            # not always: a stored digest can ITSELF be \"\" — that is what\n            # `_tail_digest` yields when the read failed, and an entry carrying\n            # one is a shape `_cache_entry_ok` accepts from a file that outlives\n            # this version and that anyone with an editor can write. Then\n            # \"\" == \"\" and the digest says nothing, and only `st.st_size <\n            # entry[\"offset\"]` knows the file no longer holds the bytes the\n            # offset claims.\n'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"comment corrected\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:03:22.124Z","count":1,"stdin_sample":null}
{"key":"py-17e8de530fa1","id":"py-17e8de530fa1","program":"import re,sys\ndata=open(sys.argv[1],'rb').read()\nhits=[m.start() for m in re.finditer(rb'permission_mode:',data)]\nprint(\"hits\",len(hits))\nseen=set()\nn=0\nfor h in hits:\n    s=data[max(0,h-350):h+250].decode('utf-8','replace')\n    if s in seen: continue\n    seen.add(s); n+=1\n    print(\"---\"); print(s)\n    if n>=8: break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:46.788Z","count":1,"stdin_sample":null}
{"key":"py-195bcff11680","id":"py-195bcff11680","program":"import json,glob,collections,os\nroot=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning')\nper=collections.defaultdict(collections.Counter)\nfor f in sorted(glob.glob(root+'/*.jsonl')):\n    s=os.path.basename(f)[:-6]\n    for l in open(f, errors='replace'):\n        l=l.strip()\n        if not l: continue\n        try: o=json.loads(l)\n        except: continue\n        m=o.get('message')\n        if isinstance(m,dict) and m.get('model'): per[s][m['model']]+=1\nfor s,c in per.items():\n    print(s, dict(c), \"MULTI\" if len([k for k in c if k!='<synthetic>'])>1 else \"\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:47.665Z","count":1,"stdin_sample":null}
{"key":"py-19e2091d7dc4","id":"py-19e2091d7dc4","program":"import json\nh={e:[{\"hooks\":[{\"type\":\"command\",\"command\":\"/tmp/hookprobe/dump4.sh\"}]}] for e in\n   [\"SessionStart\",\"PreModelSwitch\",\"PostModelSwitch\",\"SubagentStart\",\"SubagentStop\",\"PostToolBatch\",\"SessionEnd\"]}\nh[\"PreToolUse\"]=[{\"matcher\":\"Bash\",\"hooks\":[{\"type\":\"command\",\"command\":\"/tmp/hookprobe/dump4.sh\"}]}]\njson.dump({\"hooks\":h},open('/tmp/hookprobe/settings4.json','w'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:34:46.537Z","count":1,"stdin_sample":null}
{"key":"py-1d8a1df10213","id":"py-1d8a1df10213","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\n\nold = '''def _combine(old: Sighting, new: Sighting) -> Sighting:\n    \"\"\"Merge two records of one program. Idempotent in both directions.\n\n    ``count`` is max, not sum: the counts on both sides are derived from the\n    same occurrence keys, so summing them would double the record every time the\n    export ran. Max also means a rotated-away log never shrinks a published\n    count. The stored text and argv win, so a hand-corrected record survives.\n\n    ``models`` follows ``count`` exactly, per model, and for the same reason —\n    :func:`corpus._combine` sums the same-looking field because a corpus merge\n    IS summation, and copying that call here would reintroduce the doubling on\n    every export. The two are deliberately not the same line of code.\n    \"\"\"\n    stamps = [t for t in (old.first_seen, new.first_seen) if t]\n    extra = dict(new.extra)\n    extra.update(old.extra)  # the published record's own unknown keys win\n    return replace(\n        old,\n        models=corpus.merge_models(old.models, new.models, max),\n        extra=extra,\n        source=old.source if SOURCE_RANK.get(old.source, 0) >= SOURCE_RANK.get(new.source, 0) else new.source,\n        session=old.session or new.session,\n        first_seen=min(stamps) if stamps else \"\",\n        count=max(old.count, new.count),\n        argv_tail=old.argv_tail or new.argv_tail,\n        stdin_sample=old.stdin_sample if old.stdin_sample is not None else new.stdin_sample,\n    )\n'''\n\nnew = '''def _count_at_least_the_models(count: int, models: corpus.Models) -> int:\n    \"\"\"``count``, raised to whatever the model histogram can already prove.\n\n    ``sum(models) <= count`` is the promise every record makes to its readers:\n    ``count - sum(models)`` is the unattributed hole, and a negative hole is not\n    a hole. The per-key max in the two merges below cannot keep that promise on\n    its own. It is bounded by a scalar max of the counts only when both sides\n    carry the SAME model keys — and two sessions that ran one program under two\n    different models carry disjoint ones, which is not a corner case but the\n    ordinary shape of a corpus that outlives a model release.\n\n    When the keys are disjoint the occurrence sets behind them are disjoint too,\n    which means the scalar max was UNDERCOUNTING: each side simply never saw the\n    other's runs, and taking the larger of two partial views throws away the\n    evidence in the smaller one. So this is a CORRECTION, not a fudge — where\n    the histogram accounts for more occurrences than the count does, the\n    histogram is the better evidence and the count follows it up.\n\n    It preserves everything the merges are built on. Idempotent: merging a\n    record with itself re-derives the same sum, which the max absorbs.\n    Commutative: neither argument is privileged. And silent on every record\n    captured before models existed — an empty histogram sums to zero and can\n    raise nothing, so no committed line moves.\n    \"\"\"\n    return max(count, sum(n for _, n in models))\n\n\ndef _combine(old: Sighting, new: Sighting) -> Sighting:\n    \"\"\"Merge two records of one program. Idempotent in both directions.\n\n    ``count`` is max, not sum: the counts on both sides are derived from the\n    same occurrence keys, so summing them would double the record every time the\n    export ran. Max also means a rotated-away log never shrinks a published\n    count. The stored text and argv win, so a hand-corrected record survives.\n\n    ``models`` follows ``count`` exactly, per model, and for the same reason —\n    :func:`corpus._combine` sums the same-looking field because a corpus merge\n    IS summation, and copying that call here would reintroduce the doubling on\n    every export. The two are deliberately not the same line of code.\n\n    The two maxes are then reconciled by :func:`_count_at_least_the_models`,\n    which is where the pair stops being able to disagree.\n    \"\"\"\n    stamps = [t for t in (old.first_seen, new.first_seen) if t]\n    extra = dict(new.extra)\n    extra.update(old.extra)  # the published record's own unknown keys win\n    models = corpus.merge_models(old.models, new.models, max)\n    return replace(\n        old,\n        models=models,\n        extra=extra,\n        source=old.source if SOURCE_RANK.get(old.source, 0) >= SOURCE_RANK.get(new.source, 0) else new.source,\n        session=old.session or new.session,\n        first_seen=min(stamps) if stamps else \"\",\n        count=_count_at_least_the_models(max(old.count, new.count), models),\n        argv_tail=old.argv_tail or new.argv_tail,\n        stdin_sample=old.stdin_sample if old.stdin_sample is not None else new.stdin_sample,\n    )\n'''\nassert old in s\ns = s.replace(old, new, 1)\n\nold_fold = '''        stamps = [t for t in (cur.first_seen, entry.first_seen) if t]\n        by_id[entry.id] = replace(\n            cur,\n            source=cur.source if SOURCE_RANK.get(cur.source, 0) >= SOURCE_RANK.get(entry.source, 0) else entry.source,\n            first_seen=min(stamps) if stamps else \"\",\n            count=max(cur.count, entry.count),\n            # Max, like the count beside it and unlike corpus._combine: this\n            # fold re-reads the same sightings on every run, so a sum here would\n            # inflate the corpus a little more each time.\n            models=corpus.merge_models(cur.models, entry.models, max),\n            argv_tail=cur.argv_tail or entry.argv_tail,\n            stdin_sample=cur.stdin_sample if cur.stdin_sample is not None else entry.stdin_sample,\n        )\n'''\nnew_fold = '''        stamps = [t for t in (cur.first_seen, entry.first_seen) if t]\n        # Max, like the count beside it and unlike corpus._combine: this fold\n        # re-reads the same sightings on every run, so a sum here would inflate\n        # the corpus a little more each time. And the same reconciliation as in\n        # :func:`_combine`, for the same reason: a per-key max over two disjoint\n        # key sets can out-count a scalar max of the counts, and the record that\n        # went out with sum(models) > count would tell its readers that its\n        # unattributed hole was negative.\n        models = corpus.merge_models(cur.models, entry.models, max)\n        by_id[entry.id] = replace(\n            cur,\n            source=cur.source if SOURCE_RANK.get(cur.source, 0) >= SOURCE_RANK.get(entry.source, 0) else entry.source,\n            first_seen=min(stamps) if stamps else \"\",\n            count=_count_at_least_the_models(max(cur.count, entry.count), models),\n            models=models,\n            argv_tail=cur.argv_tail or entry.argv_tail,\n            stdin_sample=cur.stdin_sample if cur.stdin_sample is not None else entry.stdin_sample,\n        )\n'''\nassert old_fold in s\ns = s.replace(old_fold, new_fold, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:18:19.675Z","count":1,"stdin_sample":null}
{"key":"py-1f1ba0f9b73e","id":"py-1f1ba0f9b73e","program":"import re,sys\ndata=open(sys.argv[1],'rb').read()\npat=re.compile(rb'permission_mode:')\nhits=[m.start() for m in pat.finditer(data)]\nprint(\"hits\",len(hits))\nseen=set()\nfor h in hits[:40]:\n    s=data[max(0,h-400):h+300].decode('utf-8','replace')\n    if s in seen: continue\n    seen.add(s)\n    print(\"---\")\n    print(s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:38.916Z","count":1,"stdin_sample":null}
{"key":"py-1f7cec7ac775","id":"py-1f7cec7ac775","program":"\nimport sys,json\nfor l in sys.stdin:\n    try: o=json.loads(l)\n    except Exception: continue\n    print(o.get('type'), '|model=', (o.get('message') or {}).get('model') if isinstance(o.get('message'),dict) else None)\n","argv_tail":["2>"],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:12:33.838Z","count":1,"stdin_sample":null}
{"key":"py-20ec466d0805","id":"py-20ec466d0805","program":"\nimport json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_c9a43845-d92/journal.jsonl'\ni=0\nfor l in open(p):\n    o=json.loads(l)\n    if o.get('type')=='result':\n        r=o.get('result') or ''\n        open('/tmp/w3_%d.md'%i,'w').write(r); print(i,len(r),r[:100].replace(chr(10),' ')); i+=1\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T08:23:24.297Z","count":1,"stdin_sample":null}
{"key":"py-22d29031efe7","id":"py-22d29031efe7","program":"import io\np = \"tests/test_corpus.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''def test_a_filtered_report_names_the_slice_and_the_whole():\n    entries = [_entry(id=\"py-1\", models=((\"claude-fable-5-1\", 1),))]\n    s = corpus.stats(entries, model=\"claude-fable-5-1\", population=17)\n    line = [l for l in corpus.render_stats(s).splitlines() if l.startswith(\"entries\")][0]\n    assert \"1 of 17\" in line and \"model: claude-fable-5-1\" in line\n'''\nnew = '''def test_a_filtered_report_names_the_slice_and_the_whole():\n    entries = [_entry(id=\"py-1\", models=((\"claude-fable-5-1\", 1),))]\n    s = corpus.stats(entries, model=\"claude-fable-5-1\", population=17)\n    line = [l for l in corpus.render_stats(s).splitlines() if l.startswith(\"entries\")][0]\n    assert \"1 of 17\" in line and \"model: claude-fable-5-1\" in line\n\n\ndef test_an_empty_model_is_no_filter_rather_than_a_filter_on_nothing():\n    \"\"\"`--model \"\"` is the one spelling where the filter and the header could\n    disagree: the caller skips a falsy filter, so a header rendered on `is not\n    None` would announce a slice over an unfiltered corpus — \"N of N (model: )\".\n    No entry can carry an empty model name, so there is only one honest reading\n    of a falsy one, and it is made here so every caller gets it.\n    \"\"\"\n    entries = [_entry(id=\"py-1\", models=((\"claude-fable-5-1\", 1),)),\n               _entry(id=\"py-2\", program=\"print(2)\")]\n    s = corpus.stats(entries, model=\"\", population=len(entries))\n    assert s.filter_model is None\n    line = [l for l in corpus.render_stats(s).splitlines() if l.startswith(\"entries\")][0]\n    assert \"model:\" not in line\n    assert corpus.stats(entries, model=None).filter_model is None\n'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"corpus test ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:16:23.184Z","count":1,"stdin_sample":null}
{"key":"py-252dd5132288","id":"py-252dd5132288","program":"import re\nd=open('/tmp/cc_strings.txt','rb').read()\ni=d.find(b'function Sa(e,n,r,o){let d=o?.agentType')\nprint(d[i:i+1200].decode('utf-8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:12.350Z","count":1,"stdin_sample":null}
{"key":"py-256f54e2b946","id":"py-256f54e2b946","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\nold = \"\"\"    The numerator of the question this package cannot answer from priors: how\n    many python one-liners a given harness actually types. It is deliberately\n    NOT part of the corpus path — :func:`_raws_from_log` does not read ``host``\n    and :class:`Sighting` does not carry it — because a measurement that\n    changed what gets published would be a measurement nobody could trust.\n\"\"\"\nnew = \"\"\"    The numerator of the question this package cannot answer from priors: how\n    many python one-liners a given harness actually types. It is deliberately\n    NOT part of the corpus path: :class:`Sighting` does not carry ``host``, and\n    what gets published does not depend on it, because a measurement that\n    changed what gets published would be a measurement nobody could trust.\n\n    :func:`_raws_from_log` does read the field now, and in exactly one way that\n    cannot move a record in or out of the corpus: :func:`_joinable` uses it to\n    decide whether a Claude transcript may be consulted for the model. A\n    non-Claude record is published the same either way; the only difference is\n    whether ``models`` is empty, and for those harnesses it is empty regardless\n    because nothing on disk names their model.\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\n# The prepass reads `transcript` before deciding the record is joinable at all.\nold = \"\"\"    for _, rec in records:\n        session = rec.get(\"session\")\n        session = session if isinstance(session, str) and session else None\n        transcript = rec.get(\"transcript\")\n        if not _joinable(rec):\n            continue\n        if isinstance(transcript, str) and transcript:\n\"\"\"\nnew = \"\"\"    for _, rec in records:\n        if not _joinable(rec):\n            continue\n        session = rec.get(\"session\")\n        session = session if isinstance(session, str) and session else None\n        transcript = rec.get(\"transcript\")\n        if isinstance(transcript, str) and transcript:\n\"\"\"\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:38:23.554Z","count":1,"stdin_sample":null}
{"key":"py-262f6d722ebb","id":"py-262f6d722ebb","program":"\nimport sys,json,collections\nc=collections.Counter()\nfor l in sys.stdin: pass\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:06:43.758Z","count":1,"stdin_sample":null}
{"key":"py-26c393c87111","id":"py-26c393c87111","program":"import json,glob,os,collections\ntop=[f for f in glob.glob(os.path.expanduser('~/.claude/projects/*/*.jsonl'))]\nc=collections.Counter()\nfor f in top:\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try:o=json.loads(line)\n        except:continue\n        if o.get('isSidechain') is True: c[(os.path.basename(f),o.get('type'))]+=1\nprint(\"isSidechain=true records in TOP-LEVEL transcripts:\",dict(c) or \"NONE\")\n# subagent file header\nf=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_d689b1e8-4a5/agent-a6fea25cb621e2c05.jsonl')\nfor n,line in enumerate(open(f,errors='replace'),1):\n    o=json.loads(line)\n    print(n,o.get('type'),'| model=',(o.get('message') or {}).get('model') if isinstance(o.get('message'),dict) else None,'| keys=',sorted(k for k in o if k not in ('message',)))\n    if n>=6: break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:25:03.424Z","count":1,"stdin_sample":null}
{"key":"py-2be6ef878ca3","id":"py-2be6ef878ca3","program":"\nimport sys; sys.path.insert(0,'src')\nfrom lypning import corpus\np=[]; e=corpus.load_default(p)\nprint('corpus entries loaded:', len(e), 'problems:', p)\nimport collections; print(collections.Counter(x.source for x in e))\ns=corpus.load_sightings('tests/corpus/sightings'); print('sightings entries:', len(s))\nimport os; print('sighting files:', len([f for f in os.listdir('tests/corpus/sightings') if f.endswith('.jsonl')]))\n","argv_tail":["2>"],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:39.418Z","count":1,"stdin_sample":null}
{"key":"py-2c71d8e2295e","id":"py-2c71d8e2295e","program":"import os,sys,time\nos.environ[\"LYPNING_HOME\"]=\"/tmp/pt/e2e/oldlog/home\"\nsys.path.insert(0, sys.argv[1])\nfrom lypning import harvest\nbest=[]\nfor i in range(3):\n    t=time.time(); s=harvest.parse_log(\"/tmp/pt/e2e/oldlog/log.jsonl\"); best.append(time.time()-t)\nprint(sys.argv[1].split('/')[3], \"sightings\", len(s), \"best %.2fs\"%min(best),\n      \"cache:\", os.path.exists(\"/tmp/pt/e2e/oldlog/home/model-index.json\"))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:44:38.332Z","count":1,"stdin_sample":null}
{"key":"py-2f2b06af523b","id":"py-2f2b06af523b","program":"import json, os, sys, shutil\nfrom pathlib import Path\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nROOT = Path(\"/tmp/pt/e2e/weird\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True)\nos.environ[\"LYPNING_HOME\"] = str(ROOT/\"home\")\nfrom lypning import harvest\nbad = [\"/\", \"//\", \".\", \"..\", \"\", \" \", \"\\x00\", \"/dev/null\", \"/dev/zero\", \"relative/x.jsonl\",\n       \"~/nope.jsonl\", \"/tmp\", \"/tmp/\", \"a\"*400, \"/proc/self/mem\"]\nfor t in bad:\n    log = ROOT/\"l.jsonl\"\n    log.write_text(json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:00.000Z\",\"session\":\"s\",\n        \"command\":\"python3 -c 'print(1)'\",\"transcript\":t,\"tool_use_id\":\"t1\"})+\"\\n\", encoding=\"utf-8\")\n    try:\n        r = harvest.parse_log(log)\n        print(\"ok  %-20r -> %s\" % (t[:20], [dict(s.models) for s in r]))\n    except Exception as e:\n        print(\"RAISED %-20r -> %s: %s\" % (t[:20], type(e).__name__, e))\n# a directory named like a transcript, and a fifo\nos.mkfifo(str(ROOT/\"fifo.jsonl\"))\nfor t in [str(ROOT), str(ROOT/\"fifo.jsonl\")]:\n    log = ROOT/\"l.jsonl\"\n    log.write_text(json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:00.000Z\",\"session\":\"s\",\n        \"command\":\"python3 -c 'print(1)'\",\"transcript\":t,\"tool_use_id\":\"t1\"})+\"\\n\", encoding=\"utf-8\")\n    try:\n        import signal\n        signal.alarm(10)\n        r = harvest.parse_log(log)\n        signal.alarm(0)\n        print(\"ok  %-30s -> %s\" % (t[-30:], [dict(s.models) for s in r]))\n    except Exception as e:\n        print(\"RAISED %-30s -> %s: %s\" % (t[-30:], type(e).__name__, e))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:35:31.764Z","count":1,"stdin_sample":null}
{"key":"py-38646da1302f","id":"py-38646da1302f","program":"import json, os, shutil, subprocess, sys, threading, time, random\nfrom pathlib import Path\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nROOT = Path(\"/tmp/pt/e2e/partial\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True)\nos.environ[\"LYPNING_HOME\"] = str(ROOT/\"home\")\nfrom lypning import harvest\n\ntr = ROOT/\"transcripts\"; tr.mkdir()\nmain = tr/\"s.jsonl\"\nmain.write_text(\"\", encoding=\"utf-8\")\nN = 40\ndef assistant(j):\n    return json.dumps({\"type\":\"assistant\",\"timestamp\":\"2026-09-01T10:%02d:00.000Z\" % j,\n        \"message\":{\"model\":\"M%d\" % j,\"content\":[{\"type\":\"tool_use\",\"id\":\"t%d\"%j,\"name\":\"Bash\",\"input\":{\"command\":\"x\"}}]}})\n\nlog = ROOT/\"log.jsonl\"\nlog.write_text(\"\\n\".join(json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:%02d:00.000Z\"%j,\n    \"session\":\"s\",\"command\":\"python3 -c 'print(%d)'\"%j,\"transcript\":str(main),\"tool_use_id\":\"t%d\"%j})\n    for j in range(N))+\"\\n\", encoding=\"utf-8\")\n\nstop = False\ndef writer():\n    with open(main, \"a\", encoding=\"utf-8\", buffering=1) as fh:\n        for j in range(N):\n            line = assistant(j) + \"\\n\"\n            # write in random chunks with flushes, so readers see half lines\n            i = 0\n            while i < len(line):\n                k = random.randint(1, 60)\n                fh.write(line[i:i+k]); fh.flush()\n                i += k\n                time.sleep(0.0005)\n    globals()['stop'] = True\n\nt = threading.Thread(target=writer); t.start()\nseen = {}\nrounds = 0\nwhile not stop or rounds < 3:\n    rounds += 1\n    for s in harvest.parse_log(log):\n        for m, n in s.models:\n            prev = seen.get(s.program)\n            if prev is not None and prev != m:\n                print(\"CONFLICT\", s.program, prev, \"->\", m)\n            seen[s.program] = m\n    time.sleep(0.002)\nt.join()\n# final round\nfor s in harvest.parse_log(log):\n    for m, n in s.models:\n        prev = seen.get(s.program)\n        if prev is not None and prev != m: print(\"CONFLICT-final\", s.program, prev, m)\n        seen[s.program] = m\nmissing = [j for j in range(N) if \"print(%d)\"%j not in seen]\nwrong = [(p, m) for p, m in seen.items() if m != \"M\" + p[len(\"print(\"):-1]]\nprint(\"rounds\", rounds, \"attributed\", len(seen), \"of\", N, \"missing\", missing, \"wrong\", wrong)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:31:07.128Z","count":1,"stdin_sample":null}
{"key":"py-3935b2fffbfa","id":"py-3935b2fffbfa","program":"import json,glob,os\nb=os.path.expanduser('~/.claude/projects/-private-tmp-hookprobe')\nfor f in glob.glob(b+'/1d6f4b17*'.replace('*','*')+'', recursive=True)+glob.glob(b+'/1d6f4b17-ed89-41bb-845a-498cb8e75a05/**/*.jsonl',recursive=True):\n    if not f.endswith('.jsonl'): continue\n    ms={}\n    for l in open(f,errors='replace'):\n        try:o=json.loads(l)\n        except:continue\n        if o.get('type')=='assistant':\n            m=o['message'].get('model'); ms[m]=ms.get(m,0)+1\n    print(f.replace(b,'…'),ms)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:36:00.729Z","count":1,"stdin_sample":null}
{"key":"py-396c54d1c6c4","id":"py-396c54d1c6c4","program":"import json, os, sys, shutil\nfrom pathlib import Path\n\nROOT = Path(sys.argv[1])\nif ROOT.exists(): shutil.rmtree(str(ROOT))\nSESS = \"11111111-2222-3333-4444-555555555555\"\nproj = ROOT / \"project\"\n(proj / \"tests\" / \"corpus\" / \"sightings\").mkdir(parents=True)\ntr = ROOT / \"transcripts\" / \"-Users-user-dev-x\"\ntr.mkdir(parents=True)\nmain = tr / (SESS + \".jsonl\")\nsub = tr / SESS / \"subagents\"\nsub.mkdir(parents=True)\n\ndef assistant(ts, model, tuid, cmd):\n    return json.dumps({\"type\":\"assistant\",\"timestamp\":ts,\"message\":{\"model\":model,\"content\":[\n        {\"type\":\"tool_use\",\"id\":tuid,\"name\":\"Bash\",\"input\":{\"command\":cmd}}]}})\n\nmain.write_text(\"\\n\".join([\n    json.dumps({\"type\":\"user\",\"timestamp\":\"2026-09-01T10:00:00.000Z\"}),\n    assistant(\"2026-09-01T10:00:01.500Z\",\"claude-opus-5\",\"toolu_main_1\",\"python3 -c 'print(1)'\"),\n]) + \"\\n\", encoding=\"utf-8\")\n\n(sub / \"agent-abc.jsonl\").write_text(\"\\n\".join([\n    assistant(\"2026-09-01T10:00:03.250Z\",\"claude-haiku-4-5\",\"toolu_sub_1\",\"python3 -c 'print(2)'\"),\n]) + \"\\n\", encoding=\"utf-8\")\n\nlog = ROOT / \"invocations.jsonl\"\nlines = [\n json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:01.500Z\",\"session\":SESS,\n   \"command\":\"python3 -c 'print(1)'\",\"transcript\":str(main),\"tool_use_id\":\"toolu_main_1\"}),\n json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:03.250Z\",\"session\":SESS,\n   \"command\":\"python3 -c 'print(2)'\",\"transcript\":str(main),\"tool_use_id\":\"toolu_sub_1\",\n   \"agent_id\":\"agent-abc\",\"agent_type\":\"general-purpose\"}),\n # shim record: no transcript, no id, whole seconds\n json.dumps({\"kind\":\"python_invocation\",\"ts\":\"2026-09-01T10:00:04Z\",\"session\":SESS,\n   \"program\":\"print(3)\",\"argv_tail\":[],\"cwd\":\"/Users/user/dev/x\"}),\n]\nlog.write_text(\"\\n\".join(lines)+\"\\n\", encoding=\"utf-8\")\nprint(str(main))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:20:40.989Z","count":1,"stdin_sample":null}
{"key":"py-3a0e554a34ea","id":"py-3a0e554a34ea","program":"import json,glob,re\nd=open('/tmp/cc258.strings','rb').read()\nm=re.search(rb'hook_event_name:x\\(\"Stop\"\\)',d)\nprint(d[max(0,m.start()-60):m.start()+400].decode('utf8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:16:14.402Z","count":1,"stdin_sample":null}
{"key":"py-3b3954b4094e","id":"py-3b3954b4094e","program":"import json, os, time, glob\nfiles = sorted(glob.glob(os.path.expanduser(\"~/.claude/projects/-Users-user-dev-lypning/*.jsonl\")), key=os.path.getsize)[-1:]\np = files[0]\ndef full():\n    models=[]\n    for line in open(p,encoding='utf-8',errors='replace'):\n        try: ev=json.loads(line)\n        except: continue\n        m=ev.get(\"message\")\n        if isinstance(m,dict) and m.get(\"model\"): models.append(m[\"model\"])\n    return models[-1] if models else None\ndef tail(nbytes=65536):\n    sz=os.path.getsize(p)\n    with open(p,'rb') as fh:\n        fh.seek(max(0,sz-nbytes)); buf=fh.read()\n    lines=buf.decode('utf-8','replace').split(\"\\n\")\n    for line in reversed(lines):\n        if '\"model\"' not in line: continue\n        try: ev=json.loads(line)\n        except: continue\n        m=ev.get(\"message\")\n        if isinstance(m,dict) and m.get(\"model\"): return m[\"model\"]\n    return None\nfor name,fn in ((\"full\",full),(\"tail64k\",tail)):\n    t=time.perf_counter(); v=None\n    for _ in range(20): v=fn()\n    print(\"%-8s %6.2f ms  -> %r\" % (name,(time.perf_counter()-t)/20*1000, v))\nprint(\"size\", os.path.getsize(p))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:26:40.698Z","count":1,"stdin_sample":null}
{"key":"py-3b6fd971950b","id":"py-3b6fd971950b","program":"import io\np=\"/tmp/mut2/src/lypning/corpus.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"    s = Stats(total=len(entries), filter_model=model or None,\\n\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"    s = Stats(total=len(entries), filter_model=model,\\n\"))\nprint(\"mutated\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:02:40.957Z","count":1,"stdin_sample":null}
{"key":"py-3b9464e8a3bf","id":"py-3b9464e8a3bf","program":"from pathlib import Path\np = Path(\"tests/test_harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''    merged = corpus.merge([a], [b])[0]\n    assert merged.count == 3\n    assert merged.models == ((\"claude-fable-5-1\", 2), (\"claude-opus-5\", 1))\n    # And the collapse still holds: the same record twice is one sighting.\n    assert corpus.merge([a], [a])[0].models == ((\"claude-fable-5-1\", 2),)\n'''\nnew = '''    merged = corpus.merge([a], [b])[0]\n    assert merged.count == 3\n    assert merged.models == ((\"claude-fable-5-1\", 2), (\"claude-opus-5\", 1))\n    # Disjoint model keys cannot tell a sum from a max — both answer 2 and 1 —\n    # so the rule is pinned on a SHARED one, which is the only place the two\n    # differ and therefore the only place a call site copied from the merge\n    # above would show up at all.\n    c = corpus.Entry(id=\"py-a\", program=\"print(1)\", count=1, first_seen=\"2026-01-03\",\n                     source=\"hook\", models=((\"claude-fable-5-1\", 1),))\n    shared = corpus.merge([a], [c])[0]\n    assert shared.count == 3\n    assert shared.models == ((\"claude-fable-5-1\", 3),)\n    # The models follow the count exactly, so the histogram is still a subset of\n    # the occurrences it describes.\n    assert sum(n for _, n in shared.models) <= shared.count\n    # And the collapse still holds: the same record twice is one sighting.\n    assert corpus.merge([a], [a])[0].models == ((\"claude-fable-5-1\", 2),)\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\n\np = Path(\"tests/test_cli.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''    assert cli.main([\"corpus\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    assert json.loads(capsys.readouterr().out) == []\n'''\nnew = '''    assert cli.main([\"corpus\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    assert json.loads(capsys.readouterr().out) == []\n\n\ndef test_the_population_a_slice_names_is_the_whole_and_not_the_slice(capsys):\n    \"\"\"`N of M`: M has to be measured BEFORE the filter runs.\n\n    Measured after it, M is the size of the slice and the header reads \"N of N\"\n    for every model that ever existed — a slice reporting itself as the entire\n    corpus, which is the one thing naming the population was added to prevent.\n    No number is written down here; both come from this run.\n    \"\"\"\n    assert cli.main([\"corpus\", \"--stats\", \"--json\"]) == 0\n    whole = json.loads(capsys.readouterr().out)\n    assert whole[\"total\"] > 0\n    assert cli.main([\"corpus\", \"--stats\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    sliced = json.loads(capsys.readouterr().out)\n    assert sliced[\"total\"] == 0\n    assert sliced[\"population\"] == whole[\"total\"]\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:24:24.282Z","count":1,"stdin_sample":null}
{"key":"py-3c10dc20b888","id":"py-3c10dc20b888","program":"import sys,json,collections\nf=sys.argv[1]\nkeys=collections.Counter(); models=collections.Counter(); toolmodel=0\nfor l in open(f):\n    l=l.strip()\n    if not l: continue\n    try: o=json.loads(l)\n    except: continue\n    keys[tuple(sorted(o.keys()))]+=1\n    m=o.get('message')\n    if isinstance(m,dict) and m.get('model'): models[m['model']]+=1\nprint(\"models:\",models)\nfor k,v in keys.most_common(6): print(v,k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:17:50.524Z","count":1,"stdin_sample":null}
{"key":"py-3c9325a8bf80","id":"py-3c9325a8bf80","program":"import io\np = \"docs/CAPTURE.md\"\ns = io.open(p, encoding=\"utf-8\").read()\nstart = s.index(\"<<<<<<< HEAD\")\nend = s.index(\">>>>>>> d91f0b0\", start)\nend = s.index(\"\\n\", end) + 1\nnew = (\n    \"| `LYPNING_SESSION_ID` | the session tag, ours and harness-independent. \"\n    \"Read first, ahead of whatever id the host harness exports; both feeds \"\n    \"consult the same list, so one session's records cannot split across two \"\n    \"tags |\\n\"\n    \"| `LYPNING_HOME` | state directory (default `~/.lypning`) — the shim's bin \"\n    \"dir, the log, the build trees, the transcript index cache |\\n\"\n    \"| `LYPNING_TRANSCRIPTS` | transcript root for the harvest (default \"\n    \"`~/.claude/projects`) — the third feed's root; the model join instead \"\n    \"follows the transcript path each hook record already carries |\\n\"\n)\ns = s[:start] + new + s[end:]\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"capture.md ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T08:42:07.120Z","count":1,"stdin_sample":null}
{"key":"py-3d54e55014b6","id":"py-3d54e55014b6","program":"import json,time,glob,os\nf=max(glob.glob(os.path.expanduser(\"~/.claude/projects/*/*.jsonl\")),key=os.path.getsize)\nprint(f, os.path.getsize(f))\nt=time.time()\nn=0;models={}\nfor line in open(f):\n    line=line.strip()\n    if not line: continue\n    try: ev=json.loads(line)\n    except Exception: continue\n    n+=1\nprint(\"full json parse: %.1f ms for %d lines\"%((time.time()-t)*1000,n))\nt=time.time()\ndata=open(f,'rb').read()\nc=data.count(b'\"model\":\"')\nprint(\"raw read+count: %.1f ms (%d model hits)\"%((time.time()-t)*1000,c))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:06.056Z","count":1,"stdin_sample":null}
{"key":"py-3e6f5724ad8f","id":"py-3e6f5724ad8f","program":"\nimport sys,json\nfor l in sys.stdin:\n    try:o=json.loads(l)\n    except:continue\n    print(sorted(o.keys()))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:33:09.184Z","count":1,"stdin_sample":null}
{"key":"py-3f43f693b974","id":"py-3f43f693b974","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"# as the log lives. Re-indexing all of them from byte zero each time cost 0.92 s\n# per turn boundary on this machine's log on 2026-09-02 (681 records, 4\n# sessions), and that number is a function of the log's AGE, not of the work the\n# turn did. So the index is incremental, and what makes an incremental read\n# correct here is the one property these files have: a transcript is append-only\n# JSONL. Bytes already read never change, which makes a byte offset a complete\n# description of what has been seen, and the delta at the next turn boundary is\n# usually a few kilobytes and often nothing at all.\n\"\"\"\nnew = \"\"\"# as the log lives. Re-indexing all of them from byte zero every time cost 2.44 s\n# per turn boundary on a copy of this machine's log on 2026-09-02 (750 records,\n# 4 sessions, 39.1 MB of transcript trees, best of 7) against 1.08 s for the\n# same log with nothing to join at all — and that gap is a function of the log's\n# AGE, not of the work the turn did. So the index is incremental, and what makes\n# an incremental read correct here is the one property these files have: a\n# transcript is append-only JSONL. Bytes already read never change, which makes\n# a byte offset a complete description of what has been seen, and the delta at\n# the next turn boundary is usually a few kilobytes and often nothing at all.\n# Warm, the same harvest measured 1.49 s.\n\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:45:17.000Z","count":1,"stdin_sample":null}
{"key":"py-3fd81c586642","id":"py-3fd81c586642","program":"import json, os, sys, shutil, random, itertools\nfrom pathlib import Path\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nROOT = Path(\"/tmp/pt/e2e/fuzz\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True)\nos.environ[\"LYPNING_HOME\"] = str(ROOT/\"home\")\nfrom lypning import harvest\n\nVALS = [None, \"\", 0, 1, True, False, [], {}, \"x\", \"\\ud800\", \"a\\x00b\", \"/\", \"..\", 1.5,\n        \"2026-09-01T10:00:00.000Z\", \"not-a-time\", \"2026-09-01\", \"2026-09-01T10:00:00+02:00\",\n        1e300, -1, \"\\U0010ffff\", \"x\"*5000, [1,2], {\"a\":1}]\nfields = [\"kind\",\"ts\",\"session\",\"command\",\"program\",\"transcript\",\"tool_use_id\",\"argv_tail\",\"agent_id\"]\nrandom.seed(7)\nlog = ROOT/\"l.jsonl\"\nfails = {}\nfor trial in range(4000):\n    rec = {\"kind\": random.choice([\"bash_command\",\"python_invocation\",\"exit\", None, 1]),\n           \"command\": \"python3 -c 'print(1)'\", \"program\": \"print(1)\"}\n    for f in fields:\n        if random.random() < 0.6:\n            rec[f] = random.choice(VALS)\n    try:\n        blob = json.dumps(rec)\n    except Exception:\n        continue\n    log.write_text(blob+\"\\n\", encoding=\"utf-8\", errors=\"surrogatepass\") if False else None\n    with open(log, \"w\", encoding=\"utf-8\", errors=\"surrogatepass\") as fh:\n        fh.write(blob+\"\\n\")\n    try:\n        harvest.parse_log(log)\n    except Exception as e:\n        key = (type(e).__name__, str(e)[:60])\n        if key not in fails:\n            fails[key] = blob[:300]\nprint(\"distinct failures:\", len(fails))\nfor k, v in fails.items():\n    print(k, \"->\", v.replace(chr(0), \"<NUL>\"))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:37:03.187Z","count":1,"stdin_sample":null}
{"key":"py-40284d6f53f7","id":"py-40284d6f53f7","program":"import json,glob,os,collections\nbase='/Users/user/.claude/projects'\nallmodels=collections.Counter()\nby_sidechain=collections.Counter()\nby_kind=collections.Counter()\nfiles=glob.glob(base+'/**/*.jsonl',recursive=True)\nprint(\"total jsonl files scanned:\",len(files))\nfor f in files:\n    kind='SUBAGENT' if '/subagents/' in f else ('JOURNAL' if f.endswith('journal.jsonl') else 'MAIN')\n    try: fh=open(f,errors='replace')\n    except: continue\n    for line in fh:\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        m=(r.get('message') or {}).get('model')\n        if m is None: continue\n        allmodels[m]+=1\n        by_sidechain[(bool(r.get('isSidechain')), m)]+=1\n        by_kind[(kind,m)]+=1\nprint(\"\\n== distinct model ids machine-wide (all projects) ==\")\nfor k,v in allmodels.most_common(): print(f\"{v:6d}  {k}\")\nprint(\"\\n== by isSidechain ==\")\nfor k,v in sorted(by_sidechain.items(), key=lambda x:-x[1]): print(f\"{v:6d}  isSidechain={k[0]}  {k[1]}\")\nprint(\"\\n== by file kind ==\")\nfor k,v in sorted(by_kind.items(), key=lambda x:-x[1]): print(f\"{v:6d}  {k[0]:9s} {k[1]}\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:07:57.630Z","count":1,"stdin_sample":null}
{"key":"py-40af3fb7be8c","id":"py-40af3fb7be8c","program":"import sys; sys.path.insert(0,'src'); from lypning import paths; print(paths.CORPUS_FILE); print(paths.SEED_CORPUS_FILE); print(paths.corpus_write_file()); print(paths.sightings_dir()); print(paths.log_path())","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:18:24.456Z","count":1,"stdin_sample":null}
{"key":"py-40e2c8db7f4a","id":"py-40e2c8db7f4a","program":"import io\np = \"src/lypning/corpus.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''    filtered report that did not name its whole would read exactly like a corpus\n    that had shrunk.\n    \"\"\"\n    s = Stats(total=len(entries), filter_model=model,\n              population=len(entries) if population is None else population)\n'''\nnew = '''    filtered report that did not name its whole would read exactly like a corpus\n    that had shrunk.\n\n    An EMPTY ``model`` is no filter, not a filter on the empty name. It has to\n    be, because the caller that does the filtering reads it the same way: no\n    entry can carry an empty model name, so filtering on one would leave\n    nothing, and a caller that skipped the filter while this rendered a slice\n    header would print the whole corpus under \"of N (model: )\". One reading of\n    a falsy name, in one place, and every spelling of ``--model`` agrees.\n    \"\"\"\n    s = Stats(total=len(entries), filter_model=model or None,\n              population=len(entries) if population is None else population)\n'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"F6 ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:03:45.265Z","count":1,"stdin_sample":null}
{"key":"py-4193c12605db","id":"py-4193c12605db","program":"\nimport json,collections\nc=collections.Counter()\nsamples={}\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    line=line.strip()\n    if not line: continue\n    try: r=json.loads(line)\n    except: continue\n    k=r.get('kind'); c[k]+=1\n    if k not in samples: samples[k]=r\nprint(c)\nfor k,v in samples.items():\n    print('---',k)\n    print(json.dumps({kk:(str(vv)[:80]) for kk,vv in v.items()})[:900])\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:08:36.473Z","count":1,"stdin_sample":null}
{"key":"py-41b57e28b98a","id":"py-41b57e28b98a","program":"from pathlib import Path\np = Path(\"CHANGELOG.md\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"- **A new `models` field** on sightings and corpus records: a per-model\n  histogram of the occurrences behind `count`. It is a subset of them, never a\n  partition — an occurrence that cannot be attributed contributes to `count` and\n  to nothing else — and the key is omitted entirely when nothing is known, so\n  records captured before this existed keep the bytes they have.\n- **The hook stays on the hot path it was on.** Nothing in the PreToolUse\n  payload names a model, so the hook writes down the `tool_use_id` it already\n  receives and the join is done at harvest time, against the session transcript\n  and its subagent tree. No new fork, no new file open, no change to the shell\n  hook.\n\"\"\"\nnew = \"\"\"- **A new `models` field** on sightings and corpus records: a per-model\n  histogram of the occurrences behind `count`. It is a subset of them, never a\n  partition — an occurrence that cannot be attributed contributes to `count` and\n  to nothing else, so `count - sum(models)` is the unattributed hole and a merge\n  raises `count` rather than let that go negative. The key is omitted entirely\n  when nothing is known, so records captured before this existed keep the bytes\n  they have.\n- **The hook stays on the hot path it was on.** Nothing in the PreToolUse\n  payload names a model, so the hook writes down the `tool_use_id` it already\n  receives and the join is done at harvest time, against the session transcript\n  and its subagent tree. No new fork, no new file open, no change to the shell\n  hook.\n- **The harvest-time join reads only what was appended.** Transcripts are\n  append-only, so the index is incremental and its offsets are cached under\n  `$LYPNING_HOME` — a cache that is never a source of truth and whose every\n  failure is a full re-read. Measured on this machine on 2026-09-02 over a copy\n  of the real capture log (750 records, 4 sessions, 39.1 MB of transcript\n  trees, best of 7): 2442 ms per harvest without it, 1486 ms with it warm.\n\"\"\"\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:40:49.591Z","count":1,"stdin_sample":null}
{"key":"py-43c484a29dfc","id":"py-43c484a29dfc","program":"import json,glob,collections,os\nroot=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning')\ndef models(f):\n    c=collections.Counter()\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try:o=json.loads(line)\n        except:continue\n        if o.get('type')=='assistant':\n            m=o.get('message')\n            if isinstance(m,dict) and m.get('model'): c[m['model']]+=1\n    return c\nfor main in sorted(glob.glob(root+'/*.jsonl')):\n    sid=os.path.basename(main)[:-6]\n    print(\"SESSION\",sid,\"main:\",dict(models(main)))\n    subs=sorted(glob.glob(root+'/'+sid+'/subagents/**/*.jsonl',recursive=True))\n    agg=collections.Counter()\n    for s in subs: agg+=models(s)\n    if subs: print(\"   subagents(\",len(subs),\"files ):\",dict(agg))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:24:44.947Z","count":1,"stdin_sample":null}
{"key":"py-49de89e22c80","id":"py-49de89e22c80","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold='                or entry[\"digest\"] != _tail_digest(path, entry[\"offset\"])):\\n'\nnew='                ):\\n'\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,new))\nprint(\"digest clause deleted\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:13:45.354Z","count":1,"stdin_sample":null}
{"key":"py-4b00c1e31ccc","id":"py-4b00c1e31ccc","program":"import sys,json; print(list(json.load(sys.stdin).keys()))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:17:28.397Z","count":1,"stdin_sample":null}
{"key":"py-4ba000ada8be","id":"py-4ba000ada8be","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold='                or st.st_size < entry[\"offset\"]\\n'\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"\"))\nprint(\"clause deleted\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:12:24.186Z","count":1,"stdin_sample":null}
{"key":"py-504fb19588f6","id":"py-504fb19588f6","program":"import json,os,collections\np=os.path.expanduser(\"~/.lypning/invocations.jsonl\")\nif not os.path.exists(p):\n    print(\"no log\"); raise SystemExit\nc=collections.Counter(); sess=collections.Counter(); tui=collections.Counter()\nn=0\nfor line in open(p, encoding=\"utf-8\", errors=\"replace\"):\n    line=line.strip()\n    if not line: continue\n    try: r=json.loads(line)\n    except Exception: continue\n    n+=1\n    k=r.get(\"kind\"); c[k]+=1\n    sess[(k, bool(r.get(\"session\")))]+=1\n    tui[(k, bool(r.get(\"tool_use_id\")), bool(r.get(\"transcript\")))]+=1\nprint(\"lines\", n)\nprint(\"kinds\", dict(c))\nprint(\"kind/has-session\", dict(sess))\nprint(\"kind/has-tool_use_id/has-transcript\", dict(tui))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:20:11.804Z","count":1,"stdin_sample":null}
{"key":"py-505213757bed","id":"py-505213757bed","program":"import json,collections,os\nc=collections.Counter(); keys=collections.Counter()\nfor line in open(os.path.expanduser('~/.lypning/invocations.jsonl'),errors='replace'):\n    line=line.strip()\n    if not line: continue\n    try:o=json.loads(line)\n    except:continue\n    c[o.get('kind')]+=1\n    keys[(o.get('kind'),tuple(sorted(o.keys())))]+=1\nprint(dict(c))\nfor k,v in keys.items(): print(v,k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:26:20.916Z","count":1,"stdin_sample":null}
{"key":"py-523bdf0f8399","id":"py-523bdf0f8399","program":"import json,glob,collections\nc=collections.Counter(); ex=None\nfor f in glob.glob('*.jsonl'):\n    for l in open(f, errors='replace'):\n        l=l.strip()\n        if not l: continue\n        try: o=json.loads(l)\n        except: continue\n        m=o.get('message')\n        if not isinstance(m,dict): continue\n        ct=m.get('content')\n        if not isinstance(ct,list): continue\n        for b in ct:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                c[(o.get('type'), m.get('model'))]+=1\n                if ex is None: ex={k:(v if k!='content' else '...') for k,v in m.items()}\nfor k,v in c.most_common(20): print(v,k)\nprint(\"message keys on a tool_use record:\", ex)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:19:26.241Z","count":1,"stdin_sample":null}
{"key":"py-53bd22421037","id":"py-53bd22421037","program":"import json,glob,os,collections\nbase='/Users/user/.claude/projects'\nfor sid_path in glob.glob(base+'/*/*.jsonl'):\n    sid=os.path.basename(sid_path)[:-6]; d=os.path.dirname(sid_path)\n    subs=glob.glob(os.path.join(d,sid,'subagents','**','*.jsonl'),recursive=True)\n    if not subs: continue\n    def models(ps):\n        c=collections.Counter()\n        for p in ps:\n            for line in open(p,'r',encoding='utf-8',errors='replace'):\n                try: r=json.loads(line)\n                except ValueError: continue\n                if r.get('type')!='assistant': continue\n                m=(r.get('message') or {}).get('model')\n                if isinstance(m,str): c[m]+=1\n        return c\n    mm=models([sid_path]); sm=models(subs)\n    if set(k for k in mm if not k.startswith('<')) != set(k for k in sm if not k.startswith('<')):\n        print(sid[:8],'MAIN',dict(mm),'  SUBAGENTS',dict(sm))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:17:19.749Z","count":1,"stdin_sample":null}
{"key":"py-543ee564f204","id":"py-543ee564f204","program":"import json,glob,os,collections\nroot='/Users/user/.claude/projects/-Users-user-dev-lypning'\n# build id->model from ALL files under project\nallmap={}\nfor f in glob.glob(root+'/**/*.jsonl',recursive=True):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        m=(r.get('message') or {}).get('model')\n        for b in ((r.get('message') or {}).get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use':\n                allmap[b['id']]=(m,f)\n# per-log-record: which file holds it (by command match since log lacks tool_use_id)\nlog=[json.loads(l) for l in open('/Users/user/.lypning/invocations.jsonl') if l.strip()]\ncmd2=collections.defaultdict(list)\nfor tid,(m,f) in allmap.items(): pass\n# rebuild command index\nidx=collections.defaultdict(list)\nfor f in glob.glob(root+'/**/*.jsonl',recursive=True):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        m=(r.get('message') or {}).get('model')\n        for b in ((r.get('message') or {}).get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                idx[(b.get('input') or {}).get('command','')].append((f,m,b['id']))\nhit_main=hit_sub=miss=0\nmodels=collections.Counter()\nfor r in log:\n    hits=idx.get(r.get('command',''),[])\n    if not hits: miss+=1; continue\n    f,m,tid=hits[0]\n    if '/subagents/' in f: hit_sub+=1\n    else: hit_main+=1\n    models[m]+=1\nprint(\"log records:\",len(log))\nprint(\"resolved in MAIN transcript:\",hit_main)\nprint(\"resolved only in SUBAGENT file:\",hit_sub, f\"({hit_sub/len(log)*100:.1f}% would be UNATTRIBUTED by a main-only map)\")\nprint(\"unresolved:\",miss)\nprint(\"model distribution of captured python:\",dict(models))\n# derivation check for workflow subagents\nimport re\nsample=[h for c,hs in idx.items() for h in hs if '/subagents/workflows/' in h[0]][:3]\nfor f,m,tid in sample:\n    agid=os.path.basename(f)[len('agent-'):-len('.jsonl')]\n    sess=f.split('/-Users-user-dev-lypning/')[1].split('/')[0]\n    pat=f\"{root}/{sess}/subagents/**/agent-{agid}.jsonl\"\n    print(\"glob derivation:\",pat,\"->\",glob.glob(pat,recursive=True)==[f])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:10:00.972Z","count":1,"stdin_sample":null}
{"key":"py-56982ca4776f","id":"py-56982ca4776f","program":"import re\ndata=open('/tmp/cc258.strings','rb').read()\nm=re.search(rb'session_id:.{0,120}transcript_path', data, re.S)\nif m: print(data[max(0,m.start()-900):m.start()+1200].decode('utf8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:11:00.583Z","count":1,"stdin_sample":null}
{"key":"py-581519dd9d77","id":"py-581519dd9d77","program":"import json,os\nf=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_d689b1e8-4a5/agent-a6fea25cb621e2c05.jsonl')\nfor n,line in enumerate(open(f,errors='replace'),1):\n    o=json.loads(line)\n    if o.get('type')=='assistant':\n        print(json.dumps({k:v for k,v in o.items() if k!='message'},indent=1)[:900])\n        m=o['message']\n        print(\"message keys:\",sorted(m.keys()))\n        print(\"model:\",m.get('model'), \"content types:\",[b.get('type') for b in m.get('content',[])])\n        break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:25:27.487Z","count":1,"stdin_sample":null}
{"key":"py-5b811ed669d4","id":"py-5b811ed669d4","program":"import json,glob,os\nbase=os.path.expanduser('~/.claude/projects/-private-tmp-hookprobe')\nmain=base+'/c12aae57-823b-434f-8b9a-1eff945fd0c6.jsonl'\nsub=glob.glob(base+'/c12aae57-823b-434f-8b9a-1eff945fd0c6/subagents/**/*.jsonl',recursive=True)\ndef mods(f):\n    s={}\n    for line in open(f,errors='replace'):\n        try:o=json.loads(line)\n        except:continue\n        if o.get('type')=='assistant':\n            m=o['message'].get('model'); s[m]=s.get(m,0)+1\n    return s\nprint(\"MAIN  \",os.path.basename(main),mods(main))\nfor f in sub:\n    print(\"SUB   \",f.replace(base,'…'),mods(f))\n    for line in open(f,errors='replace'):\n        o=json.loads(line)\n        if o.get('type')=='assistant':\n            for b in o['message'].get('content') or []:\n                if isinstance(b,dict) and b.get('type')=='tool_use':\n                    print(\"   tool_use\",b.get('id'),b.get('name'),\"model=\",o['message'].get('model'),\"agentId=\",o.get('agentId'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:32:46.877Z","count":1,"stdin_sample":null}
{"key":"py-5cae2993387f","id":"py-5cae2993387f","program":"import json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586.jsonl'\nn=0\nfor line in open(p,errors='replace'):\n    try: r=json.loads(line)\n    except: continue\n    if r.get('type')=='assistant':\n        msg=r.get('message',{})\n        blocks=msg.get('content') or []\n        for b in blocks:\n            if isinstance(b,dict) and b.get('type')=='tool_use':\n                print('ts=',r.get('timestamp'),'sessionId=',r.get('sessionId'))\n                print('model=',msg.get('model'),'tool_use id=',b.get('id'),'name=',b.get('name'))\n                print('record keys=',sorted(r.keys()))\n                n+=1\n                break\n    if n>=3: break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:09:13.929Z","count":1,"stdin_sample":null}
{"key":"py-5edac0a478a4","id":"py-5edac0a478a4","program":"import json,glob,collections\n# tool_use_id collisions across files? and does any subagent file appear for a session whose main transcript is elsewhere\nroot='/Users/user/.claude/projects'\nseen=collections.defaultdict(set)\nfor f in glob.glob(root+'/**/*.jsonl',recursive=True):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        for b in ((r.get('message') or {}).get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use':\n                seen[b['id']].add(f)\ndupes={k:v for k,v in seen.items() if len(v)>1}\nprint(\"total tool_use ids:\",len(seen),\"ids appearing in >1 file:\",len(dupes))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:10:13.427Z","count":1,"stdin_sample":null}
{"key":"py-61a333e349f6","id":"py-61a333e349f6","program":"\nimport json\nfor l in open('/tmp/hookprobe/events4.jsonl'):\n    o=json.loads(l)\n    n=o.get('hook_event_name')\n    print(n, '|', json.dumps({k:v for k,v in o.items() if k!='hook_event_name'})[:420])\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:34:46.537Z","count":1,"stdin_sample":null}
{"key":"py-627dabb6be55","id":"py-627dabb6be55","program":"import json,os,glob,collections\nlog=os.path.expanduser('~/.lypning/invocations.jsonl')\nrecs=[json.loads(l) for l in open(log,errors='replace') if l.strip()]\nprint(\"log records:\",len(recs))\ntp=collections.Counter(r.get('transcript') for r in recs)\nfor k,v in tp.most_common(): print(f\"  {v:4d}  {k}\")\n# build index: command -> list of (file, model, uuid, ts, tool_use_id) over ALL transcripts incl subagents\nidx=collections.defaultdict(list)\nfor f in glob.glob(os.path.expanduser('~/.claude/projects/**/*.jsonl'),recursive=True):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try:o=json.loads(line)\n        except:continue\n        if o.get('type')!='assistant': continue\n        m=o.get('message')\n        if not isinstance(m,dict): continue\n        for b in m.get('content') or []:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                idx[b['input'].get('command')].append((f,m.get('model'),b.get('id'),o.get('timestamp'),o.get('agentId'),o.get('isSidechain')))\nprint(\"distinct Bash commands in transcripts:\",len(idx))\nhit=miss=amb=0; agree=collections.Counter(); crossfile=0\nfor r in recs:\n    c=r.get('command')\n    cands=idx.get(c)\n    if not cands: miss+=1; continue\n    hit+=1\n    models={x[1] for x in cands}\n    files={x[0] for x in cands}\n    if len(cands)>1: amb+=1\n    if len(models)>1: agree['MODEL-AMBIGUOUS']+=1\n    else: agree[list(models)[0]]+=1\n    if r.get('transcript') and any(x[0]!=r['transcript'] for x in cands): crossfile+=1\nprint(\"joined by exact command:\",hit,\"unmatched:\",miss,\"multi-candidate:\",amb)\nprint(\"resolved model distribution:\",dict(agree))\nprint(\"records whose command also/only appears in a file != hook transcript_path:\",crossfile)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:28:15.383Z","count":1,"stdin_sample":null}
{"key":"py-6480a9e73289","id":"py-6480a9e73289","program":"from pathlib import Path\np = Path(\"docs/CAPTURE.md\")\ns = p.read_text(encoding=\"utf-8\")\n\nold = \"\"\"`models` is a histogram over the same occurrences `count` counts, and it is a\nSUBSET of them: `sum(models.values()) <= count`, never `==`. An occurrence that\ncould not be joined to a model contributes to `count` and to nothing else.\nThere is deliberately no `unknown` bucket in the record — a hole named in a\ncommitted file is a hole the next reader treats as data — so the size of it is\nreported rather than stored, as the `unattributed` row of `lypning corpus\n--stats`. The key is omitted entirely when the histogram is empty, which is what\nlets records captured before any of this existed keep the bytes they have.\n\"\"\"\nnew = \"\"\"`models` is a histogram over the same occurrences `count` counts, and it is a\nSUBSET of them: `sum(models.values()) <= count`, and the difference is the\nunattributed hole. An occurrence that could not be joined to a model\ncontributes to `count` and to nothing else. There is deliberately no `unknown`\nbucket in the record — a hole named in a committed file is a hole the next\nreader treats as data — so the size of it is reported rather than stored, as the\n`unattributed` row of `lypning corpus --stats`. The key is omitted entirely when\nthe histogram is empty, which is what lets records captured before any of this\nexisted keep the bytes they have.\n\"\"\"\nassert old in s\ns = s.replace(old, new, 1)\n\nold = \"\"\"* A **shim** occurrence — a nested spawn the hook never saw — has no id. It\n  joins on time: the latest assistant record at or before its timestamp, by\n  plain string compare (both stamps are UTC with a literal `Z`, and the shim's\n  is second-precision on a BSD host, so nothing parses a date). This is the\n  weaker of the two joins and is used only where there is no id.\n\"\"\"\nnew = \"\"\"* A **shim** occurrence — a nested spawn the hook never saw — has no id. It\n  joins on time: the latest assistant record at or before its timestamp. Both\n  stamps are normalised to one fixed width first, and that is not a detail —\n  the transcript writes milliseconds and the shim writes whole seconds on any\n  host whose `date` has no `%3N`, and compared raw `…:24Z` sorts AFTER\n  `…:24.900Z`, so the search would return the model that started speaking after\n  the spawn. This is the weaker of the two joins and is used only where there\n  is no id.\n\"\"\"\nassert old in s\ns = s.replace(old, new, 1)\n\nold = \"\"\"The two merges are **not** the same function, and this is the trap:\n`harvest._combine` and `fold_into_corpus` take the per-model **max**, because\nboth sides count the same occurrence keys and a sum would double the record on\nevery export; `corpus._combine` **sums**, because a corpus merge is summation,\nprotected from double counting by its identical-record collapse. Copying one\ncall site onto the other leaves the counts plausible and slowly wrong.\n\"\"\"\nnew = \"\"\"The transcripts are indexed **incrementally**. The join runs on the cold path,\nbut the cold path is not a free path: the export fires on every Stop, the\ncapture log is append-only, and so the set of transcripts a harvest asks about\ngrows for as long as the log lives. Re-reading all of them each time is a cost\nthat rises with the age of the log rather than with the work the turn did.\nSince a transcript is append-only JSONL, a byte offset is a complete\ndescription of what has already been read, and only the bytes appended since\nthe last harvest are parsed. The offsets and what they yielded live in\n`$LYPNING_HOME/model-index.json`, which is a **cache and never a source of\ntruth**: missing, unreadable, corrupt, truncated, replaced or written by\nanother version, every one of those falls back to reading the file from byte\nzero. It is never committed, deleting it is always safe, and its absence costs\nonly time. Measured on this machine on 2026-09-02, on a copy of the real\ncapture log (750 records, 4 sessions, 39.1 MB of transcript trees, best of 7\ninterleaved runs): 2442 ms per harvest re-indexing every time, 1486 ms with the\ncache warm, against 1079 ms for the same log with nothing to join at all.\n\nThe two merges are **not** the same function, and this is the trap:\n`harvest._combine` and `fold_into_corpus` take the per-model **max**, because\nboth sides count the same occurrence keys and a sum would double the record on\nevery export; `corpus._combine` **sums**, because a corpus merge is summation,\nprotected from double counting by its identical-record collapse. Copying one\ncall site onto the other leaves the counts plausible and slowly wrong.\n\nA per-model max is bounded by a scalar max of the counts only when both sides\ncarry the **same** model keys, and two sessions that ran one program under two\ndifferent models do not. Where the histogram accounts for more occurrences than\n`count` does, `count` is raised to meet it — the disjoint keys mean the two\nsides saw disjoint runs, so the scalar max was undercounting and the histogram\nis the better evidence. That is what keeps `sum(models) <= count` true of a\nrecord that was merged rather than only of one that was derived.\n\"\"\"\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:40:31.132Z","count":1,"stdin_sample":null}
{"key":"py-69667e4c2bdb","id":"py-69667e4c2bdb","program":"import re\ndata=open('/tmp/cc258.strings','rb').read()\nfor pat in [rb'Before tool execution', rb'Input to command is JSON with tool_name and tool_input', rb'Input to command is JSON with tool_name, tool_input']:\n    for m in re.finditer(pat, data):\n        print('### PAT',pat)\n        print(data[max(0,m.start()-300):m.start()+500].decode('utf8','replace'))\n        print('-'*60)\n        break","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:10:36.789Z","count":1,"stdin_sample":null}
{"key":"py-6d40c9cf3e17","id":"py-6d40c9cf3e17","program":"\nimport json,sys,collections\nc=collections.Counter()\nfor line in open('$f'):\n    line=line.strip()\n    if not line: continue\n    try: r=json.loads(line)\n    except: c['UNPARSEABLE']+=1; continue\n    c[(r.get('type'), r.get('isSidechain'))]+=1\nfor k,v in sorted(c.items(), key=str): print(k,v)\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:05:18.914Z","count":1,"stdin_sample":null}
{"key":"py-6d6064ef919e","id":"py-6d6064ef919e","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold='        i = bisect.bisect_right(self.timeline, (key, \"\\\\uffff\")) - 1\\n'\nnew='        i = bisect.bisect_left(self.timeline, (key, \"\")) - 1\\n'\nassert s.count(old)==1, s.count(old)\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,new))\nprint(\"at() turned into strictly-before\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:13:09.832Z","count":1,"stdin_sample":null}
{"key":"py-6d7d317795b5","id":"py-6d7d317795b5","program":"from pathlib import Path\np = Path(\"docs/CAPTURE.md\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"| `LYPNING_HOME` | state directory (default `~/.lypning`) — the shim's bin dir, the log, the build trees |\"\nnew = \"| `LYPNING_HOME` | state directory (default `~/.lypning`) — the shim's bin dir, the log, the build trees, the transcript index cache |\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:41:15.066Z","count":1,"stdin_sample":null}
{"key":"py-708efeb85e9b","id":"py-708efeb85e9b","program":"import json\nblob = json.load(open(\"/tmp/pt/e2e/real/home/model-index.json\"))\nfiles = blob[\"files\"]\nkeys = set()\ndef walk(o):\n    if isinstance(o, dict):\n        for k,v in o.items(): keys.add(k); walk(v)\n    elif isinstance(o, list):\n        for v in o: walk(v)\nwalk(files)\nprint(\"entry keys:\", sorted(keys - set(files)))\nimport itertools\ne = files[sorted(files)[0]]\nprint(\"sample entry keys:\", sorted(e))\nvals = set()\nfor e in files.values():\n    vals.update(e[\"ids\"].values())\nprint(\"distinct model values:\", sorted(vals))\nprint(\"sample ids:\", list(itertools.islice(iter(next(iter(files.values()))[\"ids\"]), 3)))\n# any long strings that could be program text?\nlongest = 0; where=None\nfor p,e in files.items():\n    for k,v in e[\"ids\"].items():\n        if len(k)>longest: longest=len(k); where=(\"id\",k)\n        if len(v)>longest: longest=len(v); where=(\"model\",v)\nprint(\"longest non-path string:\", longest, where)\nprint(\"path keys sample:\", sorted(files)[0])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:38:35.948Z","count":1,"stdin_sample":null}
{"key":"py-71cb56c0c8e2","id":"py-71cb56c0c8e2","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\nold = '''#: Bumped when the stored shape changes. An older or newer number is not\n#: migrated — it is ignored, and the transcripts are read again.\n_CACHE_VERSION = 1\n\n#: How much of the head of a transcript is hashed to prove it is still the same\n#: file. Enough to cover a session's opening records, cheap enough to read on\n#: every harvest of every file.\n_CACHE_HEAD_BYTES = 4096\n\n\ndef _index_cache_path() -> Path:\n    return paths.state_dir() / \"model-index.json\"\n\n\ndef _head_digest(path: Path, offset: int) -> str:\n    \"\"\"A fingerprint of bytes already consumed, or ``\"\"`` if they cannot be read.\n\n    The stat shape says a file is the same file; this says its CONTENT is still\n    the content that produced the stored offset. An append cannot change it — a\n    transcript only ever grows at the end — so a head that has moved means the\n    path was rewritten rather than appended to, and resuming at the old offset\n    would splice one file's records onto another's. mtime is deliberately not\n    stored beside it: a stamp is the weakest signal available here, and it\n    answers the same question worse than reading the bytes does.\n    \"\"\"\n    n = min(offset, _CACHE_HEAD_BYTES)\n    if n <= 0:\n        return \"\"\n    try:\n        with open(str(path), \"rb\") as fh:\n            head = fh.read(n)\n    except (OSError, ValueError):\n        return \"\"\n    if len(head) < n:\n        return \"\"\n    return hashlib.sha256(head).hexdigest()[:32]\n'''\nnew = '''#: Bumped when the stored shape changes. An older or newer number is not\n#: migrated — it is ignored, and the transcripts are read again. 2 is where the\n#: digest moved from the head of the consumed prefix to its tail; see\n#: :func:`_tail_digest`.\n_CACHE_VERSION = 2\n\n#: How many bytes of the consumed prefix are hashed to prove it is still the\n#: prefix that produced the stored offset. Enough to span several transcript\n#: records, cheap enough to read on every harvest of every file.\n_CACHE_DIGEST_BYTES = 4096\n\n\ndef _index_cache_path() -> Path:\n    return paths.state_dir() / \"model-index.json\"\n\n\ndef _tail_digest(path: Path, offset: int) -> str:\n    \"\"\"A fingerprint of the LAST bytes already consumed, or ``\"\"`` if unreadable.\n\n    The stat shape says a file is the same file; this says its CONTENT is still\n    the content that produced the stored offset. An append cannot change it — a\n    transcript only ever grows at the end — so a digest that has moved means\n    the path was rewritten rather than appended to, and resuming at the old\n    offset would splice one file's records onto another's. mtime is\n    deliberately not stored beside it: a stamp is the weakest signal available\n    here, and it answers the same question worse than reading the bytes does.\n\n    The TAIL of the prefix, ``[offset - N, offset)``, and not the head, because\n    the head is the part a rewrite is most likely to leave alone. A transcript\n    opens with the same session id and the same first turns however many times\n    it is copied, restored from backup or rsynced ``--inplace`` over itself;\n    what a rewrite changes is what came after. Hashing the head made exactly\n    that case — same inode, same first bytes, no shorter — pass all three\n    checks and serve ids from a file that no longer says them.\n\n    What this proves and what it does not, exactly: two files whose bytes\n    differ anywhere in the last ``N`` consumed bytes are distinguished. A\n    rewrite that alters the prefix ONLY before ``offset - N`` and leaves the\n    ``N`` bytes before the offset byte-identical is not detected, and the ids\n    already cached for that region stay. That residue is unreachable by an\n    append and needs a rewrite that reproduces a whole window verbatim; it is\n    named rather than closed, because closing it means digesting the whole\n    prefix on every harvest of every transcript, which is the re-read the cache\n    exists to avoid.\n    \"\"\"\n    n = min(offset, _CACHE_DIGEST_BYTES)\n    if n <= 0:\n        return \"\"\n    try:\n        with open(str(path), \"rb\") as fh:\n            fh.seek(offset - n)\n            tail = fh.read(n)\n    except (OSError, ValueError):\n        return \"\"\n    if len(tail) < n:\n        return \"\"\n    return hashlib.sha256(tail).hexdigest()[:32]\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\n\ns = s.replace('    if not isinstance(entry.get(\"head\"), str):\\n',\n              '    if not isinstance(entry.get(\"digest\"), str):\\n')\nold = '''                or st.st_size < entry[\"offset\"]\n                or entry[\"head\"] != _head_digest(path, entry[\"offset\"])):\n            # A different inode, a file that has SHRUNK, or a head that moved:\n            # this path is not the file the offset describes. Resuming inside it\n            # would index bytes that were never read as though they had been.\n'''\nnew = '''                or st.st_size < entry[\"offset\"]\n                or entry[\"digest\"] != _tail_digest(path, entry[\"offset\"])):\n            # A different inode, a file that has SHRUNK, or a prefix whose tail\n            # moved: this path is not the file the offset describes. Resuming\n            # inside it would index bytes that were never read as though they\n            # had been.\n            #\n            # The size check is not made redundant by the digest. A truncation\n            # that leaves more than _CACHE_DIGEST_BYTES of prefix behind still\n            # ends the file INSIDE the region the digest covers, so the digest\n            # read comes up short of `n` and returns \"\" — which happens to\n            # differ from any stored digest, but only by accident of a short\n            # read, and only while the truncation point stays inside that\n            # window. Truncate to a length that is still past the stored offset\n            # minus N and the digest matches again while the ids past the new\n            # end are lies. `st.st_size < entry[\"offset\"]` is the check that\n            # actually knows the file no longer contains the bytes it claims.\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\n\ns = s.replace('''            entry = {\"offset\": 0, \"ino\": st.st_ino, \"dev\": st.st_dev,\n                     \"head\": \"\", \"ids\": {}, \"timeline\": []}''',\n              '''            entry = {\"offset\": 0, \"ino\": st.st_ino, \"dev\": st.st_dev,\n                     \"digest\": \"\", \"ids\": {}, \"timeline\": []}''')\ns = s.replace('                entry[\"head\"] = _head_digest(path, offset)\\n',\n              '                entry[\"digest\"] = _tail_digest(path, offset)\\n')\nassert \"_head_digest\" not in s and '\"head\"' not in s and \"_CACHE_HEAD_BYTES\" not in s\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"F3 ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:02:28.281Z","count":1,"stdin_sample":null}
{"key":"py-7332eb0afa75","id":"py-7332eb0afa75","program":"import sys, json, dataclasses\nsys.path.insert(0,'src')\nfrom lypning import corpus\na = corpus.Entry.from_obj({\"id\":\"py-a\",\"program\":\"print(1)\",\"count\":2,\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"model\":\"A\"})\nb = corpus.Entry.from_obj({\"id\":\"py-a\",\"program\":\"print(1)\",\"count\":2,\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"model\":\"B\"})\nprint(\"ab ->\", corpus.merge([a],[b])[0].to_obj()[\"model\"], \"count\", corpus.merge([a],[b])[0].count)\nprint(\"ba ->\", corpus.merge([b],[a])[0].to_obj()[\"model\"], \"count\", corpus.merge([b],[a])[0].count)\n\n# Now: what if model were a COMPARED dataclass field? emulate with a field not in _rank: count.\nx = corpus.Entry(id=\"py-a\", program=\"print(1)\", first_seen=\"T\", count=1, stdin_sample=None)\ny = corpus.Entry(id=\"py-a\", program=\"print(1)\", first_seen=\"T\", count=5, stdin_sample=None)\nprint(\"rank tie:\", corpus._rank(x) == corpus._rank(y))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:45.160Z","count":1,"stdin_sample":null}
{"key":"py-75e054e75ad6","id":"py-75e054e75ad6","program":"import json,glob,os,collections\nfs=glob.glob(os.path.expanduser(\"~/.claude/projects/*/*.jsonl\"))\ntsex=None; side=0; tot=0; tu=0; withid=0\nfor f in fs:\n    for line in open(f):\n        line=line.strip()\n        if not line: continue\n        try: ev=json.loads(line)\n        except Exception: continue\n        tot+=1\n        if ev.get('isSidechain'): side+=1\n        if tsex is None and ev.get('timestamp'): tsex=ev['timestamp']\n        m=ev.get('message')\n        if isinstance(m,dict) and isinstance(m.get('content'),list):\n            for b in m['content']:\n                if isinstance(b,dict) and b.get('type')=='tool_use':\n                    tu+=1\n                    if b.get('id'): withid+=1\nprint(\"records\",tot,\"sidechain\",side,\"tool_use blocks\",tu,\"with id\",withid)\nprint(\"timestamp example:\",tsex)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:24:42.761Z","count":1,"stdin_sample":null}
{"key":"py-7a78e421e8a3","id":"py-7a78e421e8a3","program":"\nimport sys,json\nfor l in sys.stdin:\n    try:o=json.loads(l)\n    except:continue\n    print(o.get('type'),o.get('subtype'),'| model=',o.get('model') or (o.get('message') or {}).get('model'))\n","argv_tail":["2>"],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:33:36.973Z","count":1,"stdin_sample":null}
{"key":"py-7aa2e5817d25","id":"py-7aa2e5817d25","program":"import io\np=\"/tmp/mut/src/lypning/corpus.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"    s = Stats(total=len(entries), filter_model=model or None,\\n\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"    s = Stats(total=len(entries), filter_model=model,\\n\"))\nprint(\"mutated\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:24:18.521Z","count":1,"stdin_sample":null}
{"key":"py-7d70bc3d65ed","id":"py-7d70bc3d65ed","program":"import json,time,os\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586.jsonl'\nsize=os.path.getsize(p)\nruns=[]\nfor _ in range(5):\n    t0=time.perf_counter()\n    m={}\n    with open(p,'r',encoding='utf-8',errors='replace') as fh:\n        for line in fh:\n            if '\"tool_use\"' not in line:   # cheap prefilter\n                continue\n            try: r=json.loads(line)\n            except ValueError: continue\n            msg=r.get('message')\n            if not isinstance(msg,dict): continue\n            model=msg.get('model')\n            c=msg.get('content')\n            if not isinstance(c,list): continue\n            for b in c:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and isinstance(b.get('id'),str):\n                    m[b['id']]=model\n    runs.append((time.perf_counter()-t0)*1000)\nprint('file',p)\nprint('bytes',size, 'lines', sum(1 for _ in open(p,errors=\"replace\")))\nprint('map entries',len(m))\nprint('ms per run (prefilter):',[round(x,1) for x in runs])\n\n# no-prefilter variant\nruns2=[]\nfor _ in range(3):\n    t0=time.perf_counter()\n    m2={}\n    with open(p,'r',encoding='utf-8',errors='replace') as fh:\n        for line in fh:\n            try: r=json.loads(line)\n            except ValueError: continue\n            msg=r.get('message')\n            if not isinstance(msg,dict): continue\n            model=msg.get('model'); c=msg.get('content')\n            if not isinstance(c,list): continue\n            for b in c:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and isinstance(b.get('id'),str):\n                    m2[b['id']]=model\n    runs2.append((time.perf_counter()-t0)*1000)\nprint('ms per run (full parse):',[round(x,1) for x in runs2], 'entries',len(m2))\nimport collections\nprint('models:',collections.Counter(m2.values()))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:12:37.265Z","count":1,"stdin_sample":null}
{"key":"py-7e0695a345bd","id":"py-7e0695a345bd","program":"import json,os,glob,time,statistics,collections\ndef index_file(p,m):\n    try: fh=open(p,'r',encoding='utf-8',errors='replace')\n    except OSError: return\n    with fh:\n        for line in fh:\n            if '\"tool_use\"' not in line: continue\n            try: r=json.loads(line)\n            except ValueError: continue\n            msg=r.get('message')\n            if not isinstance(msg,dict): continue\n            model=msg.get('model'); c=msg.get('content')\n            if not isinstance(c,list): continue\n            for b in c:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and isinstance(b.get('id'),str):\n                    m[b['id']]=model\nd='/Users/user/.claude/projects/-Users-user-dev-lypning'\nfor sid in ['9b4c7c04-860e-589c-9376-5ff9180ae586','947e7e56-7117-4120-8c1e-24c74bd9401e']:\n    files=[os.path.join(d,sid+'.jsonl')]+sorted(glob.glob(os.path.join(d,sid,'subagents','**','*.jsonl'),recursive=True))\n    mb=sum(os.path.getsize(f) for f in files)/1e6\n    for f in files: index_file(f,{})   # warm\n    runs=[]\n    for _ in range(9):\n        t0=time.perf_counter(); m={}\n        for f in files: index_file(f,m)\n        runs.append((time.perf_counter()-t0)*1000)\n    runs.sort()\n    print('%s files=%d %.1f MB entries=%d  ms min=%.0f median=%.0f max=%.0f'\n          %(sid[:8],len(files),mb,len(m),runs[0],statistics.median(runs),runs[-1]))\n    print('   models in map:',dict(collections.Counter(m.values())))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:16:54.942Z","count":1,"stdin_sample":null}
{"key":"py-7e8577954f2e","id":"py-7e8577954f2e","program":"\nimport json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_da94a6d9-486/journal.jsonl'\ni=0\nfor l in open(p):\n    o=json.loads(l)\n    if o.get('type')=='result':\n        r=o.get('result') or ''\n        open('/tmp/w2_%d.md'%i,'w').write(r)\n        print(i, len(r), r[:110].replace(chr(10),' '))\n        i+=1\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T01:31:37.824Z","count":1,"stdin_sample":null}
{"key":"py-816166f56748","id":"py-816166f56748","program":"from dataclasses import dataclass, field\nfrom typing import Dict, Tuple\n@dataclass(frozen=True)\nclass E:\n    id: str\n    models: Dict[str,int] = field(default_factory=dict)\ntry:\n    {E(\"a\", {\"m\":1})}\n    print(\"dict field hashable: OK\")\nexcept TypeError as e:\n    print(\"dict field -> merge() set insert FAILS:\", e)\n@dataclass(frozen=True)\nclass F:\n    id: str\n    models: Tuple[Tuple[str,int], ...] = ()\nprint(\"tuple field hashable:\", bool({F(\"a\", ((\"m\",1),))}))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:59.594Z","count":1,"stdin_sample":null}
{"key":"py-820e63bcc855","id":"py-820e63bcc855","program":"import json, pathlib\nfrom lypning import harvest, corpus\np = pathlib.Path('/tmp/mtest.jsonl')\nobj = {\"key\":\"py-aaa\",\"id\":\"py-aaa\",\"program\":\"print(1)\",\"argv_tail\":[],\"source\":\"hook\",\n       \"session\":\"s1\",\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"count\":1,\"stdin_sample\":None,\n       \"model\":\"claude-opus-5\"}\np.write_text(json.dumps(obj)+\"\\n\")\nback = harvest.read_sightings(p)\nprint(\"A) sightings round-trip drops model? ->\", harvest.serialise(back).strip())\n\n# corpus side: extra carried but compare=False\ne1 = corpus.Entry.from_obj(dict(obj, id=\"py-b\"))\ne2 = corpus.Entry.from_obj(dict(obj, id=\"py-b\", model=\"claude-fable-5-1\"))\nprint(\"B) Entry.extra:\", e1.extra, \"| e1==e2 (model ignored in equality)?\", e1==e2)\nm = corpus.merge([e1],[e2])\nprint(\"C) merge of two different-model entries -> n=%d model=%r count=%d\"\n      % (len(m), m[0].extra.get(\"model\"), m[0].count))\n# and the seen-set collapse\nm2 = corpus.merge([e1, e2])\nprint(\"D) same group merge -> n=%d model=%r count=%d\" % (len(m2), m2[0].extra.get(\"model\"), m2[0].count))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:28.824Z","count":1,"stdin_sample":null}
{"key":"py-83f0873fc1b2","id":"py-83f0873fc1b2","program":"import io\np = \"tests/test_harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = \"    `\\\\ud800`, not one of the `\\\\udc80`-`\\\\udcff` surrogates: those are what\\n\"\nnew = \"    ``U+D800``, not one of the ``U+DC80``-``U+DCFF`` surrogates: those are what\\n\"\nassert s.count(old) == 1, s.count(old)\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:05:23.333Z","count":1,"stdin_sample":null}
{"key":"py-840b4d8942c2","id":"py-840b4d8942c2","program":"import sys, json, time\np=sys.argv[1]\nt0=time.perf_counter()\ndata=open(p,encoding='utf-8',errors='replace').read()\nt1=time.perf_counter()\nmodels=[]\nfor line in data.split(\"\\n\"):\n    if not line.strip(): continue\n    try: ev=json.loads(line)\n    except: continue\n    m=ev.get(\"message\")\n    if isinstance(m,dict) and m.get(\"model\"): models.append(m[\"model\"])\nt2=time.perf_counter()\nprint(\"bytes\",len(data),\"lines\",data.count(\"\\n\"))\nprint(\"read ms %.1f  parse-all ms %.1f\" % ((t1-t0)*1000,(t2-t1)*1000))\nprint(\"distinct models\", set(models), \"count\", len(models))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:34.759Z","count":1,"stdin_sample":null}
{"key":"py-858458ad9c53","id":"py-858458ad9c53","program":"import json, os, sys, pathlib, collections\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nfrom lypning import harvest, capture\n\nroot = pathlib.Path(os.path.expanduser(\"~/.claude/projects\"))\nfiles = harvest._transcript_files([root])\nwhere = collections.defaultdict(set)   # id -> set(files)\npy_main = py_sub = 0\nfor f in files:\n    sub = \"/subagents/\" in str(f)\n    try:\n        text = f.read_text(encoding=\"utf-8\", errors=\"replace\")\n    except OSError:\n        continue\n    for line in text.split(\"\\n\"):\n        if '\"tool_use\"' not in line:\n            continue\n        try:\n            ev = json.loads(line)\n        except ValueError:\n            continue\n        msg = ev.get(\"message\") if isinstance(ev, dict) else None\n        if not isinstance(msg, dict):\n            continue\n        content = msg.get(\"content\")\n        if not isinstance(content, list):\n            continue\n        for b in content:\n            if not isinstance(b, dict) or b.get(\"type\") != \"tool_use\":\n                continue\n            bid = b.get(\"id\")\n            if isinstance(bid, str) and bid:\n                where[bid].add(str(f))\n            if b.get(\"name\") != \"Bash\":\n                continue\n            inp = b.get(\"input\")\n            cmd = inp.get(\"command\") if isinstance(inp, dict) else None\n            if isinstance(cmd, str) and capture.looks_pythonish(cmd):\n                if sub:\n                    py_sub += 1\n                else:\n                    py_main += 1\ndupes = {k: v for k, v in where.items() if len(v) > 1}\nprint(\"transcript files scanned      %d\" % len(files))\nprint(\"distinct tool_use ids         %d\" % len(where))\nprint(\"ids appearing in >1 file      %d\" % len(dupes))\ntot = py_main + py_sub\nprint(\"pythonish Bash tool_use blocks %d  (main %d, subagent %d)\" % (tot, py_main, py_sub))\nif tot:\n    print(\"subagent share                 %.1f%%\" % (100.0 * py_sub / tot))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:05:47.788Z","count":1,"stdin_sample":null}
{"key":"py-85b99b5c2363","id":"py-85b99b5c2363","program":"import json,glob,collections,os\nfiles=glob.glob(os.path.expanduser('~/.claude/projects/*/*.jsonl'))\nprint(\"transcript files:\",len(files))\nc=collections.Counter(); persess=collections.defaultdict(set); side=collections.Counter()\nbad=0\nfor f in files:\n    try:\n        for line in open(f, errors='replace'):\n            line=line.strip()\n            if not line: continue\n            try: o=json.loads(line)\n            except Exception: bad+=1; continue\n            if o.get('type')!='assistant': continue\n            m=o.get('message')\n            if not isinstance(m,dict): continue\n            mod=m.get('model')\n            if not mod: continue\n            c[mod]+=1\n            persess[f].add(mod)\n            if o.get('isSidechain'): side[mod]+=1\n    except Exception as e:\n        print(\"ERR\",f,e)\nprint(\"bad lines:\",bad)\nprint(\"=== distinct message.model values, counts ===\")\nfor k,v in c.most_common(): print(f\"{v:8d}  {k}\")\nprint(\"=== sidechain-only counts ===\")\nfor k,v in side.most_common(): print(f\"{v:8d}  {k}\")\nmulti=[(f,s) for f,s in persess.items() if len(s)>1]\nprint(\"=== sessions with >1 distinct model:\", len(multi), \"of\", len(persess))\nfor f,s in multi[:20]: print(\"  \",os.path.basename(f), sorted(s))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:07.899Z","count":1,"stdin_sample":null}
{"key":"py-85c327e9d6f1","id":"py-85c327e9d6f1","program":"\"\"\"Load-independent evidence for the incremental transcript index: bytes read.\n\nCounts transcript bytes read per harvest, cold and warm. Not a timing: this\nmachine is shared and its load average has been in the hundreds today, so a\nwall clock here would measure the machine rather than the change.\n\"\"\"\nimport json, os, shutil, sys, tempfile, pathlib\n\ntmp = tempfile.mkdtemp(prefix=\"lypmeasure\")\nos.environ[\"LYPNING_HOME\"] = os.path.join(tmp, \"home\")\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nfrom lypning import harvest\n\nlog = pathlib.Path(tmp) / \"invocations.jsonl\"\nsrc = pathlib.Path(os.path.expanduser(\"~/.lypning/invocations.jsonl\"))\nlines = [l for l in src.read_text(encoding=\"utf-8\").split(\"\\n\") if l.strip()]\nrecords, n_id = [], 0\nfor i, l in enumerate(lines):\n    try:\n        r = json.loads(l)\n    except ValueError:\n        continue\n    # What a log written by this branch looks like: every hook record carries\n    # the id its payload came with.\n    if r.get(\"kind\") == \"bash_command\" and isinstance(r.get(\"transcript\"), str):\n        r[\"tool_use_id\"] = \"toolu_measure_%d\" % i\n        n_id += 1\n    records.append(r)\nlog.write_text(\"\\n\".join(json.dumps(r) for r in records) + \"\\n\", encoding=\"utf-8\")\n\ntranscripts = {r[\"transcript\"] for r in records\n               if isinstance(r.get(\"transcript\"), str) and r[\"transcript\"]}\ntree_bytes, files = 0, 0\nfor t in transcripts:\n    main = pathlib.Path(t)\n    for f in [main] + harvest._transcript_files([main.parent / main.stem / \"subagents\"]):\n        try:\n            tree_bytes += f.stat().st_size\n            files += 1\n        except OSError:\n            pass\n\nread = {\"scan\": 0, \"digest\": 0}\nreal_after, real_digest = harvest._read_after, harvest._tail_digest\ndef counting_after(path, offset):\n    text, new = real_after(path, offset)\n    read[\"scan\"] += new - offset\n    return text, new\ndef counting_digest(path, offset):\n    read[\"digest\"] += min(offset, harvest._CACHE_DIGEST_BYTES)\n    return real_digest(path, offset)\nharvest._read_after = counting_after\nharvest._tail_digest = counting_digest\n\nprint(\"log records                %d\" % len(records))\nprint(\"hook records with an id    %d\" % n_id)\nprint(\"sessions named a transcript %d\" % len(transcripts))\nprint(\"transcript tree files      %d\" % files)\nprint(\"transcript tree bytes      %d\" % tree_bytes)\nfor label in (\"cold\", \"warm 1\", \"warm 2\"):\n    read[\"scan\"] = read[\"digest\"] = 0\n    out = harvest.parse_log(log)\n    print(\"%-7s harvest: scanned %10d B, digested %8d B, %d sightings\"\n          % (label, read[\"scan\"], read[\"digest\"], len(out)))\ncache = pathlib.Path(os.environ[\"LYPNING_HOME\"]) / \"model-index.json\"\nprint(\"cache file bytes           %d\" % cache.stat().st_size)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:04:57.238Z","count":1,"stdin_sample":null}
{"key":"py-87c86400a93c","id":"py-87c86400a93c","program":"import json,glob,re,collections\npat=collections.Counter()\ntz=collections.Counter()\nn=0\nfor f in glob.glob('/Users/user/.claude/projects/**/*.jsonl',recursive=True):\n    for line in open(f,'r',encoding='utf-8',errors='replace'):\n        if '\"timestamp\"' not in line: continue\n        try: r=json.loads(line)\n        except ValueError: continue\n        t=r.get('timestamp')\n        if isinstance(t,str):\n            n+=1\n            pat[re.sub(r'\\d','9',t)]+=1\nprint('transcript timestamps sampled:',n)\nfor k,v in pat.most_common(8): print(' ',k,v)\n# shim/hook log\npat2=collections.Counter()\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    try: r=json.loads(line)\n    except ValueError: continue\n    t=r.get('ts')\n    if isinstance(t,str): pat2[re.sub(r'\\d','9',t)]+=1\nprint('invocations.jsonl ts shapes:', dict(pat2))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:13:29.137Z","count":1,"stdin_sample":null}
{"key":"py-889e4fcb2ecb","id":"py-889e4fcb2ecb","program":"import json, os, sys, shutil, collections\nfrom pathlib import Path\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nROOT = Path(\"/tmp/pt/e2e/real\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True); (ROOT/\"project\").mkdir()\nos.environ[\"LYPNING_HOME\"] = str(ROOT/\"home\")\nos.environ[\"CLAUDE_PROJECT_DIR\"] = str(ROOT/\"project\")\n\nproj = Path(os.path.expanduser(\"~/.claude/projects/-Users-user-dev-lypning\"))\nmains = sorted(p for p in proj.glob(\"*.jsonl\"))\nprint(\"main transcripts:\", len(mains))\nsubdirs = [p for p in proj.iterdir() if p.is_dir()]\nprint(\"session dirs:\", len(subdirs))\ntot_sub = sum(1 for d in subdirs for p in d.rglob(\"*.jsonl\"))\nprint(\"subagent files:\", tot_sub)\n\n# Build a synthetic capture log the way the NEW capture.py would, from real transcripts.\nrecs = []\nwhere = collections.Counter()\nfor m in mains:\n    sess = m.stem\n    files = [m] + sorted((m.parent/sess/\"subagents\").rglob(\"*.jsonl\")) if (m.parent/sess/\"subagents\").is_dir() else [m]\n    for f in files:\n        loc = \"main\" if f == m else \"subagent\"\n        try: text = f.read_text(encoding=\"utf-8\", errors=\"replace\")\n        except OSError: continue\n        for line in text.split(\"\\n\"):\n            if '\"tool_use\"' not in line: continue\n            try: ev = json.loads(line)\n            except ValueError: continue\n            msg = ev.get(\"message\")\n            if not isinstance(msg, dict): continue\n            content = msg.get(\"content\")\n            if not isinstance(content, list): continue\n            for b in content:\n                if not isinstance(b, dict) or b.get(\"type\") != \"tool_use\" or b.get(\"name\") != \"Bash\": continue\n                cmd = (b.get(\"input\") or {}).get(\"command\")\n                if not isinstance(cmd, str): continue\n                recs.append({\"kind\":\"bash_command\",\"ts\":ev.get(\"timestamp\") or \"\",\n                             \"session\":sess,\"command\":cmd,\"transcript\":str(m),\n                             \"tool_use_id\":b.get(\"id\")})\n                where[loc]+=1\nprint(\"bash tool_use blocks:\", len(recs), dict(where))\nlog = ROOT/\"log.jsonl\"\nlog.write_text(\"\\n\".join(json.dumps(r) for r in recs)+\"\\n\", encoding=\"utf-8\")\n\nimport time\nfrom lypning import harvest, corpus\nt0=time.time()\nsightings = harvest.parse_log(log)\nt1=time.time()\nprint(\"sightings:\", len(sightings), \"cold harvest %.2fs\" % (t1-t0))\natt = sum(1 for s in sightings if s.models)\nprint(\"attributed sightings:\", att, \"of\", len(sightings))\nmc = collections.Counter()\nfor s in sightings:\n    for m,n in s.models: mc[m]+=n\nprint(\"models:\", dict(mc))\nt0=time.time(); harvest.parse_log(log); print(\"warm harvest %.2fs\" % (time.time()-t0))\ncp = ROOT/\"home\"/\"model-index.json\"\nprint(\"cache size:\", cp.stat().st_size, \"bytes; files:\", len(json.loads(cp.read_text())[\"files\"]))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:32:05.470Z","count":1,"stdin_sample":null}
{"key":"py-8a8a93ce5676","id":"py-8a8a93ce5676","program":"from pathlib import Path\np = Path(\"tests/test_corpus.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''    merged = corpus.merge([a], [b])[0]\n    assert merged.count == 3\n    assert merged.models == ((\"claude-fable-5-1\", 2), (\"claude-opus-5\", 1))\n    # And the collapse still holds: the same record twice is one sighting.\n    assert corpus.merge([a], [a])[0].models == ((\"claude-fable-5-1\", 2),)\n'''\nnew = '''    merged = corpus.merge([a], [b])[0]\n    assert merged.count == 3\n    assert merged.models == ((\"claude-fable-5-1\", 2), (\"claude-opus-5\", 1))\n    # Disjoint model keys cannot tell a sum from a max — both answer 2 and 1 —\n    # so the rule is pinned on a SHARED key, which is the only place the two\n    # differ and therefore the only place a call site copied from\n    # `harvest._combine` would show up at all.\n    c = corpus.Entry(id=\"py-a\", program=\"print(1)\", count=1, first_seen=\"2026-01-03\",\n                     source=\"hook\", models=((\"claude-fable-5-1\", 1),))\n    shared = corpus.merge([a], [c])[0]\n    assert shared.count == 3\n    assert shared.models == ((\"claude-fable-5-1\", 3),)\n    # And the histogram never over-counts the occurrences it is a subset of.\n    assert sum(n for _, n in shared.models) <= shared.count\n    # And the collapse still holds: the same record twice is one sighting.\n    assert corpus.merge([a], [a])[0].models == ((\"claude-fable-5-1\", 2),)\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\n\np = Path(\"tests/test_cli.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''def test_corpus_model_slice_applies_to_the_records_too(capsys):\n    # A --list or --json that ignored the filter would print the whole corpus\n    # under a filtered heading.\n    assert cli.main([\"corpus\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    assert json.loads(capsys.readouterr().out) == []\n'''\nnew = '''def test_corpus_model_slice_applies_to_the_records_too(capsys):\n    # A --list or --json that ignored the filter would print the whole corpus\n    # under a filtered heading.\n    assert cli.main([\"corpus\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    assert json.loads(capsys.readouterr().out) == []\n\n\ndef test_the_population_a_slice_names_is_the_whole_and_not_the_slice(capsys):\n    \"\"\"`N of M`: M has to be measured BEFORE the filter runs.\n\n    Measured after it, M is len(the slice) and the header reads \"N of N\" for\n    every model that ever existed — a slice that reports itself as the entire\n    corpus, which is the one thing naming the population was added to prevent.\n    No number is written down here: both come from this run.\n    \"\"\"\n    assert cli.main([\"corpus\", \"--stats\", \"--json\"]) == 0\n    whole = json.loads(capsys.readouterr().out)\n    assert whole[\"total\"] > 0\n    assert cli.main([\"corpus\", \"--stats\", \"--json\", \"--model\", \"no-such-model\"]) == 0\n    sliced = json.loads(capsys.readouterr().out)\n    assert sliced[\"total\"] == 0\n    assert sliced[\"population\"] == whole[\"total\"]\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:22:30.072Z","count":1,"stdin_sample":null}
{"key":"py-8aa0097c90c9","id":"py-8aa0097c90c9","program":"import json, os, sys, tempfile, pathlib\nsrc = sys.argv[1]\nsys.path.insert(0, src)\ntmp = tempfile.mkdtemp()\nos.environ[\"LYPNING_HOME\"] = os.path.join(tmp, \"home\")\nfrom lypning import harvest\nlog = pathlib.Path(tmp) / \"invocations.jsonl\"\nrecs = [\n    # one poisoned record: a transcript path holding a NUL\n    {\"kind\": \"bash_command\", \"ts\": \"2026-09-02T10:00:00.000Z\", \"session\": \"poisoned\",\n     \"command\": \"python3 -c 'print(1)'\", \"transcript\": \"/tmp/tr\\x00ans.jsonl\",\n     \"tool_use_id\": \"toolu_bad\", \"host\": \"claude\"},\n    # one perfectly ordinary record\n    {\"kind\": \"bash_command\", \"ts\": \"2026-09-02T10:00:01.000Z\", \"session\": \"good\",\n     \"command\": \"python3 -c 'print(2)'\", \"transcript\": None, \"host\": \"claude\"},\n]\nlog.write_text(\"\\n\".join(json.dumps(r) for r in recs) + \"\\n\", encoding=\"utf-8\")\ntry:\n    out = harvest.parse_log(log)\n    print(\"parse_log OK ->\", sorted(s.program for s in out))\nexcept BaseException as exc:\n    print(\"parse_log RAISED ->\", type(exc).__name__, exc)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:00:26.770Z","count":1,"stdin_sample":null}
{"key":"py-8b40f480d6c3","id":"py-8b40f480d6c3","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''    ``models`` is the per-model histogram of those occurrences, and it counts\n    only the ones a model could be resolved for: ``sum(models) <= count``,\n    never ``==``. Occurrences that could not be joined — a shim record from\n    outside any session, a log line written before the id was captured — are\n    simply absent from it. No ``unknown`` bucket is stored, because a hole named\n    in a committed record is a hole the next reader treats as data.\n'''\nnew = '''    ``models`` is the per-model histogram of those occurrences, and it counts\n    only the ones a model could be resolved for: ``sum(models) <= count``, and\n    the difference is the unattributed hole. Occurrences that could not be\n    joined — a shim record from outside any session, a log line written before\n    the id was captured — are simply absent from it. No ``unknown`` bucket is\n    stored, because a hole named in a committed record is a hole the next reader\n    treats as data.\n\n    That ``<=`` is a promise the merges keep deliberately, not one the\n    arithmetic happens to allow: see :func:`_count_at_least_the_models`, which\n    is what stops two records with different models producing a record whose\n    hole is negative.\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:18:42.972Z","count":1,"stdin_sample":null}
{"key":"py-8c36ff7a928c","id":"py-8c36ff7a928c","program":"import ast, sys\nbad = []\nfor f in (\"src/lypning/harvest.py\",\"src/lypning/capture.py\",\"src/lypning/corpus.py\",\"src/lypning/cli.py\"):\n    tree = ast.parse(open(f, encoding=\"utf-8\").read(), f)\n    for node in ast.walk(tree):\n        if type(node).__name__.startswith(\"Match\"):\n            bad.append((f, node.lineno, \"match statement\"))\nprint(\"3.9 syntax scan:\", bad or \"clean\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:49:35.502Z","count":1,"stdin_sample":null}
{"key":"py-8d9ef0a8c9d1","id":"py-8d9ef0a8c9d1","program":"import re,sys\ndata=open(sys.argv[1],'rb').read()\nfor m in re.finditer(rb'CLAUDE_EFFORT',data):\n    h=m.start()\n    print(\"---\"); print(data[max(0,h-700):h+500].decode('utf-8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:22.110Z","count":1,"stdin_sample":null}
{"key":"py-8f126d9b3fd2","id":"py-8f126d9b3fd2","program":"import sys, json\nsys.path.insert(0,'src')\nfrom lypning import corpus, paths\nn=0\nfor e in corpus.load(paths.CORPUS_FILE):\n    if \"model\" in e.extra: n+=1\nprint(\"corpus records with a model extra:\", n)\nprint(\"total corpus:\", len(corpus.load(paths.CORPUS_FILE)))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:25:21.026Z","count":1,"stdin_sample":null}
{"key":"py-8fd8ca8b9580","id":"py-8fd8ca8b9580","program":"\n\ndef test_a_rewrite_that_keeps_the_opening_bytes_is_still_noticed(tmp_path):\n    \"\"\"Same inode, same first bytes, no shorter — the shape a `cp` over the path,\n    an `rsync --inplace` or a restored backup actually has.\n\n    This is why the digest covers the TAIL of the consumed prefix and not its\n    head. A transcript opens with the same session and the same first turns\n    however many times it is rewritten, so a head digest is the one window such\n    a rewrite is most likely to reproduce exactly; move the window back to the\n    head and this test is the one that goes red.\n    \"\"\"\n    opening = [_assistant(\"2026-09-02T10:00:0%d.000Z\" % (i % 10), \"claude-opus-5\",\n                          \"toolu_h%d\" % i, \"python3 -c 'print(0)'  # %s\" % (\"q\" * 400))\n               for i in range(14)]\n    shared = \"\".join(r + \"\\n\" for r in opening)\n    assert len(shared.encode(\"utf-8\")) > harvest._CACHE_DIGEST_BYTES\n\n    main = _transcripts(tmp_path, opening + [\n        _assistant(\"2026-09-02T10:05:00.000Z\", \"claude-opus-5\", \"toolu_b\",\n                   \"python3 -c 'print(1)'\"),\n    ])\n    log = _write_log(tmp_path, [_hook(\"python3 -c 'print(1)'\", \"toolu_b\", main)])\n    assert harvest.parse_log(log)[0].models == ((\"claude-opus-5\", 1),)\n    st = os.stat(str(main))\n\n    # Rewritten in place: byte-identical opening, and `toolu_b` gone from the\n    # part a head digest never looks at. Longer, so the size check is silent.\n    with open(str(main), \"w\", encoding=\"utf-8\") as fh:\n        fh.write(shared)\n        for i in range(3):\n            fh.write(_assistant(\"2026-09-02T11:00:0%d.000Z\" % i, \"claude-fable-5-1\",\n                                \"toolu_z%d\" % i) + \"\\n\")\n    after = os.stat(str(main))\n    assert after.st_ino == st.st_ino and after.st_size >= st.st_size\n    assert main.read_text(encoding=\"utf-8\").startswith(shared)\n    assert harvest.parse_log(log)[0].models == ()","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:14:56.492Z","count":1,"stdin_sample":null}
{"key":"py-90f55b93296c","id":"py-90f55b93296c","program":"import sys,json;print(sorted(json.loads(sys.stdin.readline()).keys()))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:19:14.304Z","count":1,"stdin_sample":null}
{"key":"py-91f0468af004","id":"py-91f0468af004","program":"import json, os, shutil, subprocess, sys, stat\nfrom pathlib import Path\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\n\nROOT = Path(\"/tmp/pt/e2e/bat\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\nROOT.mkdir(parents=True)\nos.environ[\"LYPNING_HOME\"] = str(ROOT / \"home\")\nos.environ[\"CLAUDE_PROJECT_DIR\"] = str(ROOT / \"project\")\n(ROOT / \"project\").mkdir()\nfrom lypning import harvest\n\nSESS = \"s1\"\ntr = ROOT / \"transcripts\"\ntr.mkdir()\nmain = tr / (SESS + \".jsonl\")\n\ndef assistant(ts, model, tuid):\n    return json.dumps({\"type\":\"assistant\",\"timestamp\":ts,\"message\":{\"model\":model,\"content\":[\n        {\"type\":\"tool_use\",\"id\":tuid,\"name\":\"Bash\",\"input\":{\"command\":\"python3 -c 'pass'\"}}]}})\n\nmain.write_text(assistant(\"2026-09-01T10:00:01.000Z\",\"M1\",\"t1\")+\"\\n\", encoding=\"utf-8\")\n\ndef log(*recs):\n    p = ROOT / \"log.jsonl\"\n    p.write_text(\"\\n\".join(json.dumps(r) for r in recs)+\"\\n\", encoding=\"utf-8\")\n    return p\n\ndef models_of(p):\n    return {s.program: dict(s.models) for s in harvest.parse_log(p)}\n\nL = log({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:01.000Z\",\"session\":SESS,\n         \"command\":\"python3 -c 'print(1)'\",\"transcript\":str(main),\"tool_use_id\":\"t1\"},\n        {\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:09.000Z\",\"session\":SESS,\n         \"command\":\"python3 -c 'print(2)'\",\"transcript\":str(main),\"tool_use_id\":\"t2\"})\n\nprint(\"A baseline:\", models_of(L))\ncache = ROOT / \"home\" / \"model-index.json\"\nprint(\"   cache exists:\", cache.exists())\n\n# E: append a new assistant record with t2\nwith open(main, \"a\", encoding=\"utf-8\") as fh:\n    fh.write(assistant(\"2026-09-01T10:00:09.000Z\",\"M2\",\"t2\")+\"\\n\")\nprint(\"E after append:\", models_of(L))\n\n# F: rewrite the file entirely (same inode, different content, longer)\nmain.write_text(assistant(\"2026-09-01T10:00:01.000Z\",\"M9\",\"t1\")+\"\\n\"\n                + assistant(\"2026-09-01T10:00:09.000Z\",\"M9\",\"t2\")+\"\\n\"\n                + \"x\"*100 + \"\\n\", encoding=\"utf-8\")\nprint(\"F after rewrite:\", models_of(L))\n\n# C: corrupt cache\ncache.write_text(\"{not json\", encoding=\"utf-8\")\nprint(\"C corrupt cache:\", models_of(L))\ncache.write_text(json.dumps({\"version\":1,\"files\":{str(main):{\"offset\":\"x\",\"ino\":1,\"dev\":1,\"head\":\"\",\"ids\":{},\"timeline\":[]}}}), encoding=\"utf-8\")\nprint(\"C bad types:\", models_of(L))\ncache.write_text(json.dumps({\"version\":99,\"files\":{}}), encoding=\"utf-8\")\nprint(\"C bad version:\", models_of(L))\n\n# D: cache offset beyond file\nblob = json.loads(cache.read_text()) if cache.exists() else None\ncache.write_text(json.dumps({\"version\":1,\"files\":{str(main):{\"offset\":10**9,\"ino\":os.stat(main).st_ino,\"dev\":os.stat(main).st_dev,\"head\":\"deadbeef\",\"ids\":{},\"timeline\":[]}}}), encoding=\"utf-8\")\nprint(\"D offset beyond EOF:\", models_of(L))\n\n# B: transcript path does not exist\nL2 = log({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:01.000Z\",\"session\":SESS,\n          \"command\":\"python3 -c 'print(1)'\",\"transcript\":str(tr/\"gone.jsonl\"),\"tool_use_id\":\"t1\"})\nprint(\"B missing transcript:\", models_of(L2))\n\n# A2: transcript unreadable\nos.chmod(main, 0)\ncache.unlink(missing_ok=True)\ntry:\n    print(\"A2 unreadable transcript:\", models_of(L))\nfinally:\n    os.chmod(main, 0o644)\n\n# H: unwritable state dir\nhome = ROOT / \"home\"\ncache.unlink(missing_ok=True)\nos.chmod(home, 0o500)\ntry:\n    print(\"H unwritable home:\", models_of(L), \"cache written:\", cache.exists())\nfinally:\n    os.chmod(home, 0o755)\n\n# I: LYPNING_HOME is a file\nos.environ[\"LYPNING_HOME\"] = str(ROOT / \"afile\")\n(ROOT / \"afile\").write_text(\"x\")\nprint(\"I home-is-a-file:\", models_of(L))\nos.environ[\"LYPNING_HOME\"] = str(home)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:29:34.427Z","count":1,"stdin_sample":null}
{"key":"py-971b8acbe022","id":"py-971b8acbe022","program":"import json,glob,bisect,collections\n\n# 1) index every transcript: tool_use id -> (model, ts), and per-session sorted assistant ts->model\nbyid={}\nsess_assist=collections.defaultdict(list)   # session -> [(ts, model)]\ncmd_index=collections.defaultdict(list)     # (session, command) -> [(ts,id,model)]\nfor f in glob.glob('/Users/user/.claude/projects/**/*.jsonl',recursive=True):\n    for line in open(f,'r',encoding='utf-8',errors='replace'):\n        try: r=json.loads(line)\n        except ValueError: continue\n        if not isinstance(r,dict) or r.get('type')!='assistant': continue\n        msg=r.get('message') or {}\n        model=msg.get('model'); ts=r.get('timestamp'); sid=r.get('sessionId')\n        if isinstance(ts,str) and isinstance(model,str):\n            sess_assist[sid].append((ts,model))\n        c=msg.get('content')\n        if not isinstance(c,list): continue\n        for b in c:\n            if isinstance(b,dict) and b.get('type')=='tool_use':\n                byid[b.get('id')]=(model,ts,sid)\n                if b.get('name')=='Bash':\n                    cmd=(b.get('input') or {}).get('command')\n                    if isinstance(cmd,str):\n                        cmd_index[(sid,cmd)].append((ts,b.get('id'),model))\nfor k in sess_assist: sess_assist[k].sort()\n\ndef model_at(sid, ts):\n    \"\"\"latest assistant record with timestamp <= ts, same session\"\"\"\n    arr=sess_assist.get(sid)\n    if not arr: return None\n    i=bisect.bisect_right(arr,(ts,'￿'))-1\n    return arr[i][1] if i>=0 else None\n\n# 2) replay the real hook feed\nok=miss=nogt=0; disagree=[]\nlag=[]\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    try: rec=json.loads(line)\n    except ValueError: continue\n    if rec.get('kind')!='bash_command': continue\n    sid=rec.get('session'); cmd=rec.get('command'); ts=rec.get('ts')\n    gt=cmd_index.get((sid,cmd))\n    if not gt: nogt+=1; continue\n    gt_ts,gt_id,gt_model=gt[0]\n    got=model_at(sid,ts)\n    lag.append(ts>gt_ts)\n    if got==gt_model: ok+=1\n    else:\n        miss+=1\n        if len(disagree)<3: disagree.append((sid[:8],ts,gt_ts,gt_model,got))\nprint('hook records with a ground-truth tool_use match: %d' % (ok+miss))\nprint('  timestamp-join agreed with tool_use_id join : %d' % ok)\nprint('  disagreed                                   : %d' % miss)\nprint('  no transcript match (older/rotated)         : %d' % nogt)\nprint('  shim ts strictly after assistant ts         : %d/%d' % (sum(lag),len(lag)))\nfor d in disagree: print('  DISAGREE',d)\n\n# 3) concrete matching pair\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    rec=json.loads(line)\n    if rec.get('kind')!='bash_command': continue\n    gt=cmd_index.get((rec.get('session'),rec.get('command')))\n    if gt:\n        print()\n        print('CONCRETE PAIR')\n        print('  hook ts      :',rec['ts'])\n        print('  session_id   :',rec['session'])\n        print('  transcript   :',rec['transcript'])\n        print('  tool_use id  :',gt[0][1])\n        print('  assistant ts :',gt[0][0])\n        print('  message.model:',gt[0][2])\n        print('  command      :',rec['command'][:70])\n        break\nprint()\nprint('distinct sessions in transcripts:',len(sess_assist))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:14:18.219Z","count":1,"stdin_sample":null}
{"key":"py-978fe1c4f232","id":"py-978fe1c4f232","program":"\"\"\"Load-independent evidence for the incremental transcript index: bytes read.\n\nCounts the transcript bytes _read_after hands back, per harvest, cold and warm.\nNot a timing: this machine is shared and its load average has been in the\nhundreds today, so a wall clock here measures the machine, not the change.\n\"\"\"\nimport json, os, shutil, sys, tempfile, pathlib\n\ntmp = tempfile.mkdtemp(prefix=\"lypmeasure\")\nos.environ[\"LYPNING_HOME\"] = os.path.join(tmp, \"home\")\nos.environ[\"TMPDIR\"] = \"/tmp/pt\"\nsys.path.insert(0, \"/Users/user/dev/lypning-capture-model/src\")\nfrom lypning import harvest\n\nlog = pathlib.Path(tmp) / \"invocations.jsonl\"\nshutil.copyfile(os.path.expanduser(\"~/.lypning/invocations.jsonl\"), str(log))\nrecords = [json.loads(l) for l in log.read_text(encoding=\"utf-8\").split(\"\\n\") if l.strip()]\nsessions = {r.get(\"session\") for r in records if r.get(\"session\")}\ntranscripts = {r.get(\"transcript\") for r in records\n               if isinstance(r.get(\"transcript\"), str) and r.get(\"transcript\")}\ntree_bytes = 0\nfiles = 0\nfor t in transcripts:\n    main = pathlib.Path(t)\n    for f in [main] + harvest._transcript_files([main.parent / main.stem / \"subagents\"]):\n        try:\n            tree_bytes += f.stat().st_size\n            files += 1\n        except OSError:\n            pass\n\nread = {\"n\": 0}\nreal = harvest._read_after\ndef counting(path, offset):\n    text, new = real(path, offset)\n    read[\"n\"] += new - offset\n    return text, new\nharvest._read_after = counting\n\nprint(\"log records            %d\" % len(records))\nprint(\"sessions               %d\" % len(sessions))\nprint(\"transcript paths       %d\" % len(transcripts))\nprint(\"transcript tree files  %d\" % files)\nprint(\"transcript tree bytes  %d\" % tree_bytes)\nfor label in (\"cold\", \"warm\", \"warm\"):\n    read[\"n\"] = 0\n    out = harvest.parse_log(log)\n    print(\"%-6s harvest: %10d transcript bytes read, %d sightings\"\n          % (label, read[\"n\"], len(out)))\ncache = pathlib.Path(os.environ[\"LYPNING_HOME\"]) / \"model-index.json\"\nprint(\"cache file bytes       %d\" % cache.stat().st_size)\nattributed = sum(1 for s in out if s.models)\nprint(\"sightings with a model %d of %d\" % (attributed, len(out)))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:04:15.999Z","count":1,"stdin_sample":null}
{"key":"py-9a9f321cd7af","id":"py-9a9f321cd7af","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\na = (\"# is exactly what this module did before the cache existed. A cache miss costs\\n\"\n     \"# a second. It cannot cost a model, it cannot invent one, and it cannot raise:\\n\")\nb = (\"# is exactly what this module did before the cache existed. A cache miss costs\\n\"\n     \"# one re-read. It cannot cost a model, it cannot invent one, and it cannot\\n\"\n     \"# raise:\\n\")\nassert a in s\ns = s.replace(a, b, 1)\na2 = (\"    would splice one file's records onto another's. mtime is deliberately not\\n\"\n      \"    stored in its place: it is the weakest evidence of the three and answers\\n\"\n      \"    the same question worse.\\n\")\nb2 = (\"    would splice one file's records onto another's. mtime is deliberately not\\n\"\n      \"    stored beside it: a stamp is the weakest signal available here, and it\\n\"\n      \"    answers the same question worse than reading the bytes does.\\n\")\nassert a2 in s\ns = s.replace(a2, b2, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:45:55.670Z","count":1,"stdin_sample":null}
{"key":"py-9cfd6de82538","id":"py-9cfd6de82538","program":"import re\ns=open('CHANGELOG.md').read()\nm=re.search(r'<<<<<<< HEAD\\n(.*?)\\n\\|\\|\\|\\|\\|\\|\\| [^\\n]*\\n(.*?)=======\\n(.*?)>>>>>>> [^\\n]*\\n', s, re.S)\nours_side, base, theirs = m.group(1), m.group(2), m.group(3)\nprint(\"BASE EMPTY?\", repr(base)[:40])\nprint(\"--- theirs tail ---\"); print(theirs[-300:])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T14:48:11.552Z","count":1,"stdin_sample":null}
{"key":"py-9eef4d993552","id":"py-9eef4d993552","program":"\nimport json,collections\nf='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_d689b1e8-4a5/journal.jsonl'\nk=collections.Counter(); m=collections.Counter()\nfor l in open(f,errors='replace'):\n    try:o=json.loads(l)\n    except:continue\n    k[o.get('type') or o.get('kind')]+=1\n    for key in ('model','modelId'):\n        if key in o: m[o[key]]+=1\nprint('types:',dict(k)); print('model fields:',dict(m))\nprint(json.dumps(json.loads(open(f).readline()))[:400])\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:33:09.184Z","count":1,"stdin_sample":null}
{"key":"py-9f95a58e885c","id":"py-9f95a58e885c","program":"import re\nd=open('/tmp/cc_strings.txt','rb').read()\ni=d.find(b'matcherMetadata:{fieldToMatch:\"agent_type\"')\nseg=d[i-4000:i+4000].decode('utf-8','replace')\nprint(seg)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:34:31.649Z","count":1,"stdin_sample":null}
{"key":"py-a3bd387a5abe","id":"py-a3bd387a5abe","program":"import io\np = \"docs/HARNESSES.md\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = \"\"\"**So the adapter is the instrument.** Every record now carries a `host` field,\nand `lypning harvest --json` reports counts per host. Run it, read the number\noff your own sessions, and quote it with its date and its model id — never a\nremembered one (invariant 3).\n\"\"\"\nnew = \"\"\"**So the adapter is the instrument.** Every record now carries a `host` field,\nand `lypning harvest --json` reports counts per host. Run it, read the number\noff your own sessions, and quote it with its date and its model id — never a\nremembered one (invariant 3).\n\nThe model id has to come from you here, not from the tool. `lypning corpus\n--model NAME` slices the corpus by the model that issued each program, but that\nattribution is resolved by joining a Claude Code transcript, and neither of\nthese harnesses writes one: an opencode tool hook and an OpenHands `PostToolUse`\npayload carry no model and no key to join one on. Their records are\nunattributed by construction, which `corpus --stats` reports as its\n`unattributed` row rather than hiding. See `docs/CAPTURE.md`, *Which model\nissued it*.\n\"\"\"\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:53:46.541Z","count":1,"stdin_sample":null}
{"key":"py-a3fc499bc605","id":"py-a3fc499bc605","program":"\nimport json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_8b5941f8-d7a/journal.jsonl'\ni=0\nfor l in open(p):\n    o=json.loads(l)\n    if o.get('type')=='result':\n        r=o.get('result') or ''\n        open('/tmp/w4_%d.md'%i,'w').write(r); print(i,len(r),r[:90].replace(chr(10),' ')); i+=1\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T12:17:24.718Z","count":1,"stdin_sample":null}
{"key":"py-a4dddb5d25d1","id":"py-a4dddb5d25d1","program":"import re,sys\ndata=open(sys.argv[1],'rb').read()\ni=data.find(b'UUID correlating a user prompt with all subsequent events')\nprint(data[i-600:i+2500].decode('utf-8','replace'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:14.469Z","count":1,"stdin_sample":null}
{"key":"py-a66a02221400","id":"py-a66a02221400","program":"import io, re\np = \"CHANGELOG.md\"\ns = io.open(p, encoding=\"utf-8\").read()\nhead = \"**2026-09-02** — the corpus can be sliced by the model that issued the program ([#27](https://github.com/kristerhedfors/lypning/pull/27))\"\nstart = s.index(head)\nend = s.index(\"\\n**2026-08-31** — pathlib costs nothing\", start)\nentry = s[start:end].rstrip(\"\\n\") + \"\\n\\n\"\ns = s[:start] + s[end + 1:]\n# Newest first, at the top of ## Unreleased, above the two already on main.\nanchor = \"## Unreleased\\n\\n\"\ni = s.index(anchor) + len(anchor)\ns = s[:i] + entry + s[i:]\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"moved\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:40:31.264Z","count":1,"stdin_sample":null}
{"key":"py-a6dd36007373","id":"py-a6dd36007373","program":"import sys, json, os\nsys.path.insert(0,'/Users/user/dev/lypning/src')\nfrom lypning import harvest, paths\nP = \"/tmp/fwd/project\"\nprog = \"print('fwd compat fixture zz9')\"\nk = harvest.sighting_key(prog)\nold = {\"key\":k,\"id\":k,\"program\":prog,\"argv_tail\":[],\"source\":\"shim\",\"session\":\"sess-x\",\n       \"first_seen\":\"2026-01-01T00:00:00.000Z\",\"count\":1,\"stdin_sample\":None,\n       \"model\":\"claude-opus-5\"}\nf = paths.sightings_dir(P)/\"sess-x.jsonl\"\nf.parent.mkdir(parents=True, exist_ok=True)\nf.write_text(json.dumps(old)+\"\\n\", encoding=\"utf-8\")\nbefore = f.read_text()\n# a newer session appends one more program under the SAME session id\nlog = paths.log_path(); log.parent.mkdir(parents=True, exist_ok=True)\nlog.write_text(json.dumps({\"kind\":\"python_invocation\",\"program\":\"print('fwd other zz9')\",\n                           \"argv_tail\":[],\"session\":\"sess-x\",\"ts\":\"2026-01-02T00:00:00.000Z\"})+\"\\n\",\n               encoding=\"utf-8\")\nharvest.export_sightings(P, quiet=True)\nafter = f.read_text()\nprint(\"BEFORE:\", before.strip()[:200])\nprint(\"AFTER lines:\", after.count(\"\\n\"))\nprint(\"model still present?\", '\"model\"' in after)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:09.018Z","count":1,"stdin_sample":null}
{"key":"py-a76a74e3ebd6","id":"py-a76a74e3ebd6","program":"import re,io\np='MEMORY.md'\ns=open(p).read()\ns=s.replace(\"- [lypning test baseline and TMPDIR](lypning-test-baseline-and-tmpdir.md) — 9 failures are baseline; short TMPDIR required; conformance is not 0 on main either\\n\",\"\")\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T12:31:01.803Z","count":1,"stdin_sample":null}
{"key":"py-a821d1413ca5","id":"py-a821d1413ca5","program":"from pathlib import Path\n\np = Path(\"src/lypning/harvest.py\"); s = p.read_text(encoding=\"utf-8\")\nold = \"\"\"# as the log lives. Re-indexing all of them from byte zero every time cost 2.44 s\n# per turn boundary on a copy of this machine's log on 2026-09-02 (750 records,\n# 4 sessions, 39.1 MB of transcript trees, best of 7) against 1.08 s for the\n# same log with nothing to join at all — and that gap is a function of the log's\n# AGE, not of the work the turn did. So the index is incremental, and what makes\n# an incremental read correct here is the one property these files have: a\n# transcript is append-only JSONL. Bytes already read never change, which makes\n# a byte offset a complete description of what has been seen, and the delta at\n# the next turn boundary is usually a few kilobytes and often nothing at all.\n# Warm, the same harvest measured 1.49 s.\n\"\"\"\nnew = \"\"\"# as the log lives. Re-indexing all of them from byte zero every time cost 1.86 s\n# per turn boundary on a copy of this machine's log on 2026-09-02 (750 records,\n# 4 sessions, 39.1 MB of transcript trees, best of 9 interleaved runs) against\n# 0.70 s for the same log with nothing to join at all — and that gap is a\n# function of the log's AGE, not of the work the turn did. So the index is\n# incremental, and what makes an incremental read correct here is the one\n# property these files have: a transcript is append-only JSONL. Bytes already\n# read never change, which makes a byte offset a complete description of what\n# has been seen, and the delta at the next turn boundary is usually a few\n# kilobytes and often nothing at all. Warm, the same harvest measured 0.97 s.\n\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\n\np = Path(\"docs/CAPTURE.md\"); s = p.read_text(encoding=\"utf-8\")\nold = \"\"\"capture log (750 records, 4 sessions, 39.1 MB of transcript trees, best of 7\ninterleaved runs): 2442 ms per harvest re-indexing every time, 1486 ms with the\ncache warm, against 1079 ms for the same log with nothing to join at all.\"\"\"\nnew = \"\"\"capture log (750 records, 4 sessions, 39.1 MB of transcript trees, best of 9\ninterleaved runs): 1859 ms per harvest re-indexing every time, 974 ms with the\ncache warm, against 704 ms for the same log with nothing to join at all.\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\n\np = Path(\"CHANGELOG.md\"); s = p.read_text(encoding=\"utf-8\")\nold = \"\"\"  of the real capture log (750 records, 4 sessions, 39.1 MB of transcript\n  trees, best of 7): 2442 ms per harvest without it, 1486 ms with it warm.\"\"\"\nnew = \"\"\"  of the real capture log (750 records, 4 sessions, 39.1 MB of transcript\n  trees, best of 9): 1859 ms per harvest without it, 974 ms with it warm.\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"all three updated\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T06:22:18.081Z","count":1,"stdin_sample":null}
{"key":"py-a8e743e4ec57","id":"py-a8e743e4ec57","program":"import json,glob,os\ntid='toolu_013fhrXQPFakTXuxq9gNk9U8'\nfor f in glob.glob(os.path.expanduser('~/.claude/projects/-private-tmp-hookprobe/**/*.jsonl'),recursive=True):\n    for n,line in enumerate(open(f,errors='replace'),1):\n        if tid not in line: continue\n        o=json.loads(line)\n        if o.get('type')!='assistant': continue\n        print(\"file:\",f.replace(os.path.expanduser('~'),'~'),\"line\",n)\n        print(\"  model:\",o['message'].get('model'),\"agentId:\",o.get('agentId'),\"isSidechain:\",o.get('isSidechain'),\"sessionId:\",o.get('sessionId'),\"attributionAgent:\",o.get('attributionAgent'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:31:17.135Z","count":1,"stdin_sample":null}
{"key":"py-a9815d91542d","id":"py-a9815d91542d","program":"\nimport json\nfor l in open('/tmp/hookprobe/events3.jsonl'):\n    o=json.loads(l); print({k:o.get(k) for k in ('session_id','agent_id','agent_type','tool_use_id','transcript_path')}); print('  model?' , 'model' in o, '| cmd', (o.get('tool_input') or {}).get('command'))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:31:44.420Z","count":1,"stdin_sample":null}
{"key":"py-ab39994e53ff","id":"py-ab39994e53ff","program":"import sys, json\nsys.path.insert(0,'src')\nfrom lypning import harvest, corpus\n\n# 1. Sighting.from_obj with an unknown key\nobj = {\"key\":\"py-a\",\"id\":\"py-a\",\"program\":\"print(1)\",\"argv_tail\":[],\"source\":\"shim\",\n       \"session\":\"s\",\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"count\":3,\"stdin_sample\":None,\n       \"model\":\"claude-opus-5\"}\ns = harvest.Sighting.from_obj(obj)\nprint(\"SIGHTING roundtrip:\", json.dumps(s.to_obj()))\nprint(\"model survived?\", \"model\" in s.to_obj())\n\n# 2. missing key today\nobj2 = dict(obj); obj2.pop(\"model\")\nprint(\"missing-key ok:\", harvest.Sighting.from_obj(obj2) is not None)\n\n# 3. corpus.Entry with unknown key\ne = corpus.Entry.from_obj({\"id\":\"py-a\",\"program\":\"print(1)\",\"model\":\"claude-opus-5\"})\nprint(\"ENTRY extra:\", e.extra, \"to_obj:\", json.dumps(e.to_obj()))\n\n# 4. extra excluded from equality -> merge collapses different models\na = corpus.Entry.from_obj({\"id\":\"py-a\",\"program\":\"print(1)\",\"count\":2,\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"model\":\"A\"})\nb = corpus.Entry.from_obj({\"id\":\"py-a\",\"program\":\"print(1)\",\"count\":2,\"first_seen\":\"2026-01-01T00:00:00.000Z\",\"model\":\"B\"})\nprint(\"a==b (extra compare=False):\", a==b)\nm = corpus.merge([a],[b])\nprint(\"merged:\", json.dumps([x.to_obj() for x in m]))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:02.266Z","count":1,"stdin_sample":null}
{"key":"py-ab50f0023ba4","id":"py-ab50f0023ba4","program":"import json,glob,collections\nper=collections.defaultdict(collections.Counter)\nfor f in glob.glob('/Users/user/.claude/projects/**/*.jsonl',recursive=True):\n    for line in open(f,'r',encoding='utf-8',errors='replace'):\n        try: r=json.loads(line)\n        except ValueError: continue\n        if not isinstance(r,dict) or r.get('type')!='assistant': continue\n        m=(r.get('message') or {}).get('model')\n        if isinstance(m,str) and not m.startswith('<'): per[r.get('sessionId')][m]+=1\nmulti=0\nfor s,c in per.items():\n    print(('%s  %s'%(str(s)[:8], dict(c))))\n    if len(c)>1: multi+=1\nprint('sessions:',len(per),'multi-model sessions:',multi)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:15:16.052Z","count":1,"stdin_sample":null}
{"key":"py-ac7eaddac78f","id":"py-ac7eaddac78f","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\nold = \"\"\"    # ARE asked about are read incrementally, from where the last harvest\n    # stopped; see the cache above.\n    by_session: Dict[str, str] = {}\n    needed: Set[str] = set()\n    shim_sessions: Set[str] = set()\n    for _, rec in records:\n        session = rec.get(\"session\")\n        session = session if isinstance(session, str) and session else None\n        transcript = rec.get(\"transcript\")\n        if isinstance(transcript, str) and transcript:\n            if session:\n                by_session.setdefault(session, transcript)\n            if isinstance(rec.get(\"tool_use_id\"), str) and rec.get(\"tool_use_id\"):\n                needed.add(transcript)\n        elif session and rec.get(\"kind\") == \"python_invocation\":\n            shim_sessions.add(session)\n\"\"\"\nnew = \"\"\"    # ARE asked about are read incrementally, from where the last harvest\n    # stopped; see the cache above.\n    #\n    # And only a CLAUDE record is asked about at all. The log is multi-harness\n    # now: `host` is `\"claude\"`, `\"opencode\"` or `\"openhands\"`, and only the\n    # first has a transcript on disk that names a model. Neither of the others\n    # writes a `transcript` today, so this gate changes nothing measurable — it\n    # is here so that the day one of them does, its path is not walked as though\n    # it were a Claude transcript and its ids are not looked up in one. An\n    # absent `host` is treated as Claude because that is what every record\n    # written before #26 is, and what the shim still writes.\n    by_session: Dict[str, str] = {}\n    needed: Set[str] = set()\n    shim_sessions: Set[str] = set()\n    for _, rec in records:\n        session = rec.get(\"session\")\n        session = session if isinstance(session, str) and session else None\n        transcript = rec.get(\"transcript\")\n        if not _joinable(rec):\n            continue\n        if isinstance(transcript, str) and transcript:\n            if session:\n                by_session.setdefault(session, transcript)\n            if isinstance(rec.get(\"tool_use_id\"), str) and rec.get(\"tool_use_id\"):\n                needed.add(transcript)\n        elif session and rec.get(\"kind\") == \"python_invocation\":\n            shim_sessions.add(session)\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"        tag = session or \"invocations\"\n        kind = rec.get(\"kind\")\n        transcript = rec.get(\"transcript\")\n        if not isinstance(transcript, str) or not transcript:\n            # The shim writes no transcript path; its session is the only way\n            # back to one, and it only leads anywhere if a hook record in the\n            # same log named it.\n            transcript = by_session.get(session or \"\", \"\")\n        index = indexes.get(transcript, _EMPTY_INDEX)\n\"\"\"\nnew = \"\"\"        tag = session or \"invocations\"\n        kind = rec.get(\"kind\")\n        transcript = rec.get(\"transcript\")\n        if not isinstance(transcript, str) or not transcript:\n            # The shim writes no transcript path; its session is the only way\n            # back to one, and it only leads anywhere if a hook record in the\n            # same log named it.\n            transcript = by_session.get(session or \"\", \"\")\n        index = indexes.get(transcript, _EMPTY_INDEX) if _joinable(rec) else _EMPTY_INDEX\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\n# The helper, placed just above _raws_from_log.\nold = \"def _raws_from_log(text: str, *, persist: bool = True) -> List[_Raw]:\\n\"\nnew = '''def _joinable(rec: Dict[str, Any]) -> bool:\n    \"\"\"Can this record's model be looked up in a Claude Code transcript?\n\n    Only a Claude record can, and only because Claude Code writes a transcript\n    that names a model. An opencode tool hook and an OpenHands ``PostToolUse``\n    payload carry no model, and there is nothing on disk to join them to: an\n    unattributed record is the right answer for both, and it is the answer the\n    hole in ``models`` exists to hold.\n\n    A MISSING ``host`` is Claude. Every record written before the field existed\n    is one, and the shim still writes none — which is the residual this cannot\n    close, and it is worth naming. A shim record carries a session tag and a\n    timestamp and nothing else, and the session tag it falls back to is\n    ``$CLAUDE_CODE_SESSION_ID`` from the ambient environment. Run a different\n    harness's agent from inside a Claude Code session's shell and its nested\n    python spawns are tagged with the Claude session and time-joined to the\n    Claude model that was speaking. That is the weak join being weak, in the one\n    configuration where it is also wrong rather than merely imprecise; closing\n    it needs a ``host`` on the shim record, which is a change to the shim's own\n    wire format and not this one's to make.\n    \"\"\"\n    host = rec.get(\"host\")\n    return not (isinstance(host, str) and host and host != \"claude\")\n\n\ndef _raws_from_log(text: str, *, persist: bool = True) -> List[_Raw]:\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"host gate ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:08:59.883Z","count":1,"stdin_sample":null}
{"key":"py-ad25b33c55b7","id":"py-ad25b33c55b7","program":"import json,glob,collections\nbyk=collections.defaultdict(set); counts=collections.Counter()\nfor f in sorted(glob.glob('*.jsonl')):\n    for l in open(f, errors='replace'):\n        l=l.strip()\n        if not l: continue\n        o=json.loads(l)\n        byk[o['key']].add(f)\n        counts[o['key']]+=o.get('count',1)\nmulti={k:v for k,v in byk.items() if len(v)>1}\nprint(\"distinct keys:\",len(byk),\"total lines:\",sum(len(v) for v in byk.values()))\nprint(\"keys in >1 session file:\",len(multi))\nfor k,v in list(multi.items())[:8]: print(\"  \",k,sorted(v))\nprint(\"keys with count>1:\",sum(1 for k,c in counts.items() if c>1))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:31.263Z","count":1,"stdin_sample":null}
{"key":"py-af2f1bc33dae","id":"py-af2f1bc33dae","program":"import io\np = \"CHANGELOG.md\"\ns = io.open(p, encoding=\"utf-8\").read()\nstart = s.index(\"<<<<<<< HEAD\\n\")\nmid1 = s.index(\"||||||| parent of\", start)\nmid2 = s.index(\"=======\\n\", mid1)\nend = s.index(\">>>>>>> d91f0b0\", mid2)\nend = s.index(\"\\n\", end) + 1\ntheirs_pr26 = s[start + len(\"<<<<<<< HEAD\\n\"):mid1]\nours = s[mid2 + len(\"=======\\n\"):end - len(s[s.index(\"\\n\", s.index(\">>>>>>> d91f0b0\", mid2)) - 200:])] if False else s[mid2 + len(\"=======\\n\"):s.index(\">>>>>>> d91f0b0\", mid2)]\ns = s[:start] + theirs_pr26 + \"\\n\" + ours + s[end:]\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"changelog ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T08:42:16.943Z","count":1,"stdin_sample":null}
{"key":"py-b0ccfa5d379d","id":"py-b0ccfa5d379d","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"\"\"                        found.append(Path(dirpath) / name)\n        except (OSError, ValueError):\n\"\"\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"\"\"                        found.append(Path(dirpath) / name)\n        except OSError:\n\"\"\"))\nprint(\"F1 mutated\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:04:29.454Z","count":2,"stdin_sample":null}
{"key":"py-b13f472d91b1","id":"py-b13f472d91b1","program":"import json,glob,os,collections\nroot='/Users/user/.claude/projects/-Users-user-dev-lypning'\nsub=sorted(glob.glob(root+'/*/subagents/**/agent-*.jsonl',recursive=True))\nprint(\"subagent transcript files:\",len(sub))\n# collect bash tool_use ids in subagent files\nsubids={}\nfor f in sub:\n    for line in open(f):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        m=r.get('message',{})\n        for b in (m.get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                subids[b['id']]=(f, m.get('model'), (b.get('input') or {}).get('command','')[:120])\nprint(\"subagent Bash tool_use ids:\",len(subids))\n# main transcript ids\nmainids={}\nfor f in sorted(glob.glob(root+'/*.jsonl')):\n    for line in open(f):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        m=r.get('message',{})\n        for b in (m.get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                mainids[b['id']]=(os.path.basename(f), m.get('model'))\nprint(\"main Bash tool_use ids:\",len(mainids))\noverlap=set(subids)&set(mainids)\nprint(\"overlap:\",len(overlap))\n# python-ish subagent commands\npy=[(k,v) for k,v in subids.items() if 'python' in v[2] or 'lypning' in v[2]]\nprint(\"subagent bash calls mentioning python/lypning:\",len(py))\nfor k,v in py[:8]: print(k, os.path.basename(v[0]), v[1], '|', v[2][:90])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:05:58.318Z","count":1,"stdin_sample":null}
{"key":"py-b3b4b95c91e7","id":"py-b3b4b95c91e7","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"            fh.seek(offset - n)\\n\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"            fh.seek(0)\\n\"))\nprint(\"digest window moved back to the head\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:13:45.354Z","count":2,"stdin_sample":null}
{"key":"py-b47dddbdc90e","id":"py-b47dddbdc90e","program":"import json,os\np=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning/947e7e56-7117-4120-8c1e-24c74bd9401e.jsonl')\nprev=None\nfor n,line in enumerate(open(p,errors='replace'),1):\n    o=json.loads(line)\n    m=o.get('message') or {}\n    mod=m.get('model') if isinstance(m,dict) else None\n    if o.get('type')=='assistant' and mod and mod!='claude-fable-5-1':\n        print(\"LINE\",n,\"type\",o.get('type'),\"model\",mod,\"uuid\",o.get('uuid'),\"ts\",o.get('timestamp'),\"isSidechain\",o.get('isSidechain'),\"isApiErrorMessage\",o.get('isApiErrorMessage'))\n        cont=m.get('content')\n        if isinstance(cont,list):\n            for b in cont:\n                if isinstance(b,dict):\n                    print(\"    block\",b.get('type'), str(b.get('text') or b.get('name') or '')[:160])\n        else: print(\"    \",str(cont)[:200])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:24:02.098Z","count":1,"stdin_sample":null}
{"key":"py-b631528266b8","id":"py-b631528266b8","program":"import sys, json\np=sys.argv[1]\nn=0; models=set(); first_model_line=None\nwith open(p, encoding='utf-8', errors='replace') as fh:\n    for i,line in enumerate(fh,1):\n        n+=1\n        try: ev=json.loads(line)\n        except: continue\n        m=ev.get(\"message\")\n        if isinstance(m,dict) and m.get(\"model\"):\n            models.add(m[\"model\"])\n            if first_model_line is None: first_model_line=i\nprint(\"lines\",n,\"models\",models,\"first model at line\",first_model_line)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:23.954Z","count":1,"stdin_sample":null}
{"key":"py-b6d7907aef74","id":"py-b6d7907aef74","program":"import importlib.util as u; print([n for n in ['openhands','openhands_sdk','openhands.sdk'] if u.find_spec(n)])","argv_tail":["2>"],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T08:28:33.379Z","count":1,"stdin_sample":null}
{"key":"py-b9644a7d87e7","id":"py-b9644a7d87e7","program":"import sys,json,collections\nc=collections.Counter()\nfor l in open(sys.argv[1],errors='replace'):\n    l=l.strip()\n    if not l: continue\n    try: o=json.loads(l)\n    except: continue\n    c[(o.get('kind'),tuple(sorted(o.keys())))]+=1\nfor k,v in c.most_common(): print(v,k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:08.882Z","count":1,"stdin_sample":null}
{"key":"py-b991da8d0680","id":"py-b991da8d0680","program":"import io\np = \"/Users/user/dev/lypning-capture-model/tests/test_cli.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''def test_the_population_a_slice_names_is_the_whole_and_not_the_slice(capsys):'''\nnew = '''def test_an_empty_model_prints_the_corpus_and_does_not_claim_a_slice(capsys):\n    # The filter is skipped for a falsy name, so the header must not claim one:\n    # \"N of N (model: )\" over an unfiltered corpus is exactly the reading that\n    # naming the population was added to prevent.\n    assert cli.main([\"corpus\", \"--stats\", \"--model\", \"\"]) == 0\n    line = [l for l in capsys.readouterr().out.splitlines() if l.startswith(\"entries\")][0]\n    assert \"model:\" not in line\n    assert cli.main([\"corpus\", \"--stats\", \"--json\", \"--model\", \"\"]) == 0\n    assert json.loads(capsys.readouterr().out)[\"filter_model\"] is None\n\n\ndef test_the_population_a_slice_names_is_the_whole_and_not_the_slice(capsys):'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"cli test ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:16:23.184Z","count":1,"stdin_sample":null}
{"key":"py-b9993025906c","id":"py-b9993025906c","program":"\nimport json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_d689b1e8-4a5/journal.jsonl'\nfor l in open(p):\n    o=json.loads(l)\n    if o.get('type')=='result':\n        r=o.get('result')\n        print('=====',o.get('label'), len(r) if isinstance(r,str) else type(r))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:01:42.557Z","count":1,"stdin_sample":null}
{"key":"py-ba4b3283e8a0","id":"py-ba4b3283e8a0","program":"import json,time,os,statistics,glob,collections\np='/Users/user/.claude/projects/-Users-user-dev-lypning/9b4c7c04-860e-589c-9376-5ff9180ae586.jsonl'\ndata=open(p,'r',encoding='utf-8',errors='replace').read()  # warm\ndef index(text):\n    m={}\n    for line in text.split('\\n'):\n        if '\"tool_use\"' not in line: continue\n        try: r=json.loads(line)\n        except ValueError: continue\n        msg=r.get('message')\n        if not isinstance(msg,dict): continue\n        model=msg.get('model'); c=msg.get('content')\n        if not isinstance(c,list): continue\n        for b in c:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and isinstance(b.get('id'),str):\n                m[b['id']]=model\n    return m\nruns=[]\nfor _ in range(15):\n    t0=time.perf_counter(); mm=index(open(p,'r',encoding='utf-8',errors='replace').read()); runs.append((time.perf_counter()-t0)*1000)\nruns.sort()\nprint('largest transcript bytes=%d entries=%d' % (os.path.getsize(p), len(mm)))\nprint('read+index ms: min=%.1f median=%.1f max=%.1f' % (runs[0], statistics.median(runs), runs[-1]))\n\n# all transcripts: model diversity + total cost\nfiles=glob.glob('/Users/user/.claude/projects/**/*.jsonl', recursive=True)\ntot=0; c=collections.Counter()\nt0=time.perf_counter()\nfor f in files:\n    try: t=open(f,'r',encoding='utf-8',errors='replace').read()\n    except OSError: continue\n    tot+=len(t)\n    for k,v in index(t).items(): c[v]+=1\nel=(time.perf_counter()-t0)*1000\nprint('all %d transcript files, %.1f MB, indexed in %.0f ms' % (len(files), tot/1e6, el))\nprint('distinct models seen:', dict(c))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:12:58.089Z","count":1,"stdin_sample":null}
{"key":"py-bac731c2c167","id":"py-bac731c2c167","program":"import sys, json\nsys.path.insert(0,'/Users/user/dev/lypning/src')\nfrom lypning import corpus, paths, harvest\n\n# Simulate adding `model` with a None default to the normal form.\norig = corpus.Entry.to_obj\ndef patched(self):\n    o = {\"id\":self.id,\"program\":self.program,\"argv_tail\":list(self.argv_tail),\n         \"source\":self.source,\"first_seen\":self.first_seen,\"count\":self.count,\n         \"stdin_sample\":self.stdin_sample,\"model\":getattr(self,\"model\",None)}\n    for k,v in self.extra.items():\n        o.setdefault(k,v)\n    return o\ncorpus.Entry.to_obj = patched\nout = \"/tmp/corpus_model.jsonl\"\ncorpus.write(corpus.load(paths.CORPUS_FILE), out)\na = paths.CORPUS_FILE.read_bytes(); b = open(out,'rb').read()\nprint(\"byte identical:\", a==b)\nimport difflib\nal=a.decode().splitlines(); bl=b.decode().splitlines()\nprint(\"lines changed:\", sum(1 for x,y in zip(al,bl) if x!=y), \"of\", len(al))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:25:34.868Z","count":1,"stdin_sample":null}
{"key":"py-be4eb9bf19a9","id":"py-be4eb9bf19a9","program":"\nimport json\np='/Users/user/.claude/projects/-Users-user-dev-lypning/93a4fe97-435b-53ac-9885-bd9f9011489f/subagents/workflows/wf_d689b1e8-4a5/journal.jsonl'\nrs=[json.loads(l)['result'] for l in open(p) if json.loads(l).get('type')=='result']\nfor i,r in enumerate(rs):\n    open('/tmp/probe%d.md'%i,'w').write(r)\n    print(i, r[:150].replace(chr(10),' '))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:01:53.539Z","count":1,"stdin_sample":null}
{"key":"py-bfb6921c7bb9","id":"py-bfb6921c7bb9","program":"import json,sys,collections\nc=collections.Counter()\nmodels=collections.Counter()\nkeys=collections.Counter()\nfor line in open(sys.argv[1]):\n    line=line.strip()\n    if not line: continue\n    try: ev=json.loads(line)\n    except Exception: continue\n    c[ev.get('type')]+=1\n    for k in ev: keys[k]+=1\n    m=ev.get('message')\n    if isinstance(m,dict) and m.get('model'): models[m['model']]+=1\nprint(\"types\",c)\nprint(\"topkeys\",keys.most_common(20))\nprint(\"models\",models)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:17:10.979Z","count":1,"stdin_sample":null}
{"key":"py-c2d02e461db1","id":"py-c2d02e461db1","program":"import io\np=\"/tmp/mut/src/lypning/harvest.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"\"\"                        found.append(Path(dirpath) / name)\n        except (OSError, ValueError):\n\"\"\"\nnew=\"\"\"                        found.append(Path(dirpath) / name)\n        except OSError:\n\"\"\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,new))\nprint(\"F1 mutated: ValueError escapes again\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:02:40.957Z","count":1,"stdin_sample":null}
{"key":"py-c4912e16cf75","id":"py-c4912e16cf75","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''def _transcript_files(roots: Iterable[Path]) -> List[Path]:\n    found: List[Path] = []\n    for root in roots:\n        try:\n            if root.is_file():\n                found.append(root)\n                continue\n            for dirpath, dirnames, filenames in os.walk(str(root)):\n                dirnames.sort()\n                for name in sorted(filenames):\n                    if name.endswith(\".jsonl\"):\n                        found.append(Path(dirpath) / name)\n        except OSError:\n            continue\n    return sorted(set(found))\n'''\nnew = '''def _transcript_files(roots: Iterable[Path]) -> List[Path]:\n    \"\"\"Every ``.jsonl`` under these roots. Returns; never raises.\n\n    ``ValueError`` as well as ``OSError``, and that is not defensive padding.\n    A root used to be :func:`transcript_root` and nothing else — an env var or\n    ``~/.claude/projects``, both of them paths this module chose. Since the\n    model join, one of them is the ``transcript`` string out of a capture log,\n    which is a path some other program wrote and this one only read back.\n    ``os.walk`` on such a string raises ``ValueError`` for an embedded NUL and\n    ``UnicodeEncodeError`` (a subclass of it) for a lone surrogate — neither an\n    ``OSError``, and both from inside the C call rather than from a missing\n    file. :func:`parse_log` promises never to raise and the Stop hook is why:\n    one poisoned record must cost its own session's model, not every session's\n    export, silently, at every turn boundary for as long as the append-only log\n    holds that line.\n    \"\"\"\n    found: List[Path] = []\n    for root in roots:\n        try:\n            if root.is_file():\n                found.append(root)\n                continue\n            for dirpath, dirnames, filenames in os.walk(str(root)):\n                dirnames.sort()\n                for name in sorted(filenames):\n                    if name.endswith(\".jsonl\"):\n                        found.append(Path(dirpath) / name)\n        except (OSError, ValueError):\n            continue\n    return sorted(set(found))\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"F1 ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:00:03.008Z","count":1,"stdin_sample":null}
{"key":"py-c4df9d8c870f","id":"py-c4df9d8c870f","program":"import json,glob,collections,os\nroot=os.path.expanduser(\"~/.claude/projects\")\nper=collections.defaultdict(collections.Counter)\nside=collections.Counter()\nn=0\nfor f in glob.glob(root+\"/*/*.jsonl\"):\n    n+=1\n    try: fh=open(f)\n    except OSError: continue\n    with fh:\n        for line in fh:\n            line=line.strip()\n            if not line: continue\n            try: ev=json.loads(line)\n            except Exception: continue\n            m=ev.get('message')\n            if isinstance(m,dict) and isinstance(m.get('model'),str):\n                per[os.path.basename(f)][m['model']]+=1\n                if ev.get('isSidechain'): side[m['model']]+=1\nprint(\"transcript files scanned:\",n)\nmulti=[(k,dict(v)) for k,v in per.items() if len([x for x in v if x!='<synthetic>'])>1]\nprint(\"sessions with >1 real model:\",len(multi),\"of\",len(per))\nfor k,v in multi[:10]: print(\" \",k,v)\nallm=collections.Counter()\nfor v in per.values(): allm.update(v)\nprint(\"all models:\",allm.most_common(12))\nprint(\"sidechain models:\",side.most_common(6))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:46.392Z","count":1,"stdin_sample":null}
{"key":"py-c4f4bb52e322","id":"py-c4f4bb52e322","program":"import re\ndata=open('/tmp/cc258.strings','rb').read()\npats=[rb'PreToolUse', rb'hook_event_name']\nfor m in list(re.finditer(rb'hook_event_name', data))[:6]:\n    print('='*70)\n    print(data[max(0,m.start()-500):m.start()+500].decode('utf8','replace'))\nprint('total hook_event_name:', len(re.findall(rb'hook_event_name',data)))\nprint('total PreToolUse:', len(re.findall(rb'PreToolUse',data)))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:10:47.920Z","count":1,"stdin_sample":null}
{"key":"py-c513c1281d0c","id":"py-c513c1281d0c","program":"import json\np='/Users/user/.claude/projects/-private-tmp-hookprobe/58a182ec-b31e-48a1-acea-8d2ad76bac1b.jsonl'\nfor n,line in enumerate(open(p),1):\n    o=json.loads(line)\n    t=o.get('type')\n    m=o.get('message') or {}\n    model=m.get('model') if isinstance(m,dict) else None\n    blocks=[b.get('type') for b in m.get('content',[]) if isinstance(b,dict)] if isinstance(m.get('content'),list) else m.get('content')\n    ids=[b.get('id') for b in m.get('content',[]) if isinstance(b,dict) and b.get('type')=='tool_use'] if isinstance(m.get('content'),list) else []\n    print(n,t,'model=',model,'blocks=',blocks,'tool_use_ids=',ids,'sidechain=',o.get('isSidechain'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:22:49.712Z","count":1,"stdin_sample":null}
{"key":"py-c5a471bbd747","id":"py-c5a471bbd747","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\n# --- F5: thread a \"write nothing\" flag down to the index cache save. ---\nold = \"def _raws_from_log(text: str) -> List[_Raw]:\\n    records = _decode_log(text)\\n\"\nnew = (\"def _raws_from_log(text: str, *, persist: bool = True) -> List[_Raw]:\\n\"\n       \"    records = _decode_log(text)\\n\")\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"        cache = _IndexCache()\n        indexes = {t: _model_index(t, cache) for t in sorted(needed)}\n        cache.save()\n\"\"\"\nnew = \"\"\"        cache = _IndexCache()\n        indexes = {t: _model_index(t, cache) for t in sorted(needed)}\n        # `persist` is False under --dry-run, and this is the only write on the\n        # path it reaches. Invariant 7 says --dry-run is real: it opens files\n        # and writes none. Loading the cache is still a read, so a dry run is\n        # as fast as a wet one and reports the same sightings; it just leaves\n        # the state dir exactly as it found it.\n        if persist:\n            cache.save()\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = '''def parse_log(path: Optional[Path] = None) -> List[Sighting]:\n    \"\"\"The JSONL both feeds append to, as sightings. Never raises.\n\n    One record per distinct program, sorted by key. A missing log is the normal\n    state of a session that never ran python and yields nothing.\n    \"\"\"\n    target = Path(path) if path is not None else paths.log_path()\n    return _aggregate(_raws_from_log(_read_text(target)))\n'''\nnew = '''def parse_log(path: Optional[Path] = None, *, persist: bool = True) -> List[Sighting]:\n    \"\"\"The JSONL both feeds append to, as sightings. Never raises.\n\n    One record per distinct program, sorted by key. A missing log is the normal\n    state of a session that never ran python and yields nothing.\n\n    ``persist=False`` reads the transcript index cache but does not write it\n    back, which is what a caller that has promised to write nothing needs.\n    \"\"\"\n    target = Path(path) if path is not None else paths.log_path()\n    return _aggregate(_raws_from_log(_read_text(target), persist=persist))\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"def collect(project: Optional[Path] = None, *, log: Optional[Path] = None,\n            transcripts: bool = False) -> List[Sighting]:\"\"\"\nnew = \"\"\"def collect(project: Optional[Path] = None, *, log: Optional[Path] = None,\n            transcripts: bool = False, persist: bool = True) -> List[Sighting]:\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"    counted twice when both are read in the same pass.\n    \\\"\\\"\\\"\n    merged: Dict[str, Sighting] = {}\n    groups = [parse_log(log)]\n\"\"\"\nnew = \"\"\"    counted twice when both are read in the same pass.\n\n    ``persist=False`` makes the whole call read-only — see :func:`parse_log`.\n    It is what ``lypning harvest --dry-run`` passes, and the only thing this\n    path would otherwise write.\n    \\\"\\\"\\\"\n    merged: Dict[str, Sighting] = {}\n    groups = [parse_log(log, persist=persist)]\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"F5 harvest ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:01:11.828Z","count":1,"stdin_sample":null}
{"key":"py-c78716da874b","id":"py-c78716da874b","program":"p='lypning-host-quirks.md'\ns=open(p).read()\nold=\"\"\"- macOS has no `timeout(1)`;\"\"\"\nnew=\"\"\"  The `AF_UNIX path too long` errors go away entirely with a short temp dir:\n  `TMPDIR=/tmp/pt LYPNING_HOME=/tmp/lypcm`. A venv that already has pytest is\n  `/private/tmp/lypning-venv/bin/python` (3.14.5); with the short TMPDIR the\n  baseline is 9 failures (the 6 above plus the 3 `test_pool` traceback cases),\n  and the fuzz/keyword-grid 4 only appear once the Rust core is built.\n- `lypning conformance` does **not** end at MISMATCH 0 on `main` on this host.\n  ~101 corpus entries read live mutating session state (growing transcripts,\n  wall-clock durations, network) and `conformance.is_nondeterministic()` flags\n  only 6 of them, so the rest MISMATCH at random depending on spawn timing.\n  A/B a branch against a clean `git archive main` with the SAME binary (pin\n  `LYPNING_BIN`, since a fresh archive has no built core) before reading a\n  MISMATCH as a regression; on 2026-09-02 a branch showed 4 and main 6, from\n  the same unstable pool. The one deterministic row, `py-cd598c0d0e44`, is a\n  real tier-1 bug owned by main: `lambda *a, **k` yields a traceback at exit 1\n  instead of a clean exit-90 refusal.\n- macOS has no `timeout(1)`;\"\"\"\nassert old in s\nopen(p,'w').write(s.replace(old,new,1))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T12:31:55.901Z","count":1,"stdin_sample":null}
{"key":"py-c7d4a2cfbd35","id":"py-c7d4a2cfbd35","program":"import json,glob,collections,os,sys\nfrom lypning.harvest import extract_with_tails, sighting_key\nfrom lypning.capture import looks_pythonish\nroot=os.path.expanduser('~/.claude/projects/-Users-user-dev-lypning')\nprog_models=collections.defaultdict(collections.Counter)\nprog_sessions=collections.defaultdict(set)\nfor f in sorted(glob.glob(root+'/*.jsonl')):\n    sess=os.path.basename(f)[:-6]\n    for l in open(f, errors='replace'):\n        l=l.strip()\n        if not l: continue\n        try: o=json.loads(l)\n        except: continue\n        m=o.get('message')\n        if not isinstance(m,dict): continue\n        model=m.get('model')\n        ct=m.get('content')\n        if not isinstance(ct,list): continue\n        for b in ct:\n            if not isinstance(b,dict) or b.get('type')!='tool_use' or b.get('name')!='Bash': continue\n            cmd=(b.get('input') or {}).get('command')\n            if not isinstance(cmd,str) or not looks_pythonish(cmd): continue\n            for p,t in extract_with_tails(cmd):\n                k=sighting_key(p)\n                prog_models[k][model]+=1\n                prog_sessions[k].add(sess)\nmulti=[k for k,c in prog_models.items() if len(c)>1]\nmultisess=[k for k,s in prog_sessions.items() if len(s)>1]\nprint(\"distinct programs:\",len(prog_models))\nprint(\"programs seen under >1 model:\",len(multi))\nprint(\"programs seen in >1 session:\",len(multisess))\nfor k in multi[:6]: print(\"  \",k,dict(prog_models[k]))\nprint(\"model totals:\",collections.Counter(m for c in prog_models.values() for m,n in c.items() for _ in range(n)))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:03.385Z","count":1,"stdin_sample":null}
{"key":"py-c7d65442a6cf","id":"py-c7d65442a6cf","program":"from pathlib import Path\np = Path(\"docs/CAPTURE.md\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"* `~/.lypning/invocations.jsonl` stays local. It is not in the repo and must not\n  be committed, pasted into an issue, or attached to a PR. The published\n  `tests/corpus/sightings/*.jsonl` files ARE committed, and go through the same\n  redaction and seed guard as the corpus before they are written.\n\"\"\"\nnew = \"\"\"* `~/.lypning/invocations.jsonl` stays local. It is not in the repo and must not\n  be committed, pasted into an issue, or attached to a PR. The published\n  `tests/corpus/sightings/*.jsonl` files ARE committed, and go through the same\n  redaction and seed guard as the corpus before they are written.\n* `~/.lypning/model-index.json` stays local for the same reason and is never\n  committed, but it is a much smaller thing: byte offsets, `tool_use` ids,\n  model names and timestamps, and no program text at all. It is a cache of what\n  the transcripts already say, and deleting it costs one slow harvest.\n\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:52:32.696Z","count":1,"stdin_sample":null}
{"key":"py-c8fa6371a3ea","id":"py-c8fa6371a3ea","program":"from pathlib import Path\np = Path(\"src/lypning/harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\npairs = [\n(\"\"\"    try:\n        with open(str(path), \"rb\") as fh:\n            head = fh.read(n)\n    except OSError:\n        return \"\"\n\"\"\",\n \"\"\"    try:\n        with open(str(path), \"rb\") as fh:\n            head = fh.read(n)\n    except (OSError, ValueError):\n        return \"\"\n\"\"\"),\n(\"\"\"    try:\n        with open(str(path), \"rb\") as fh:\n            fh.seek(offset)\n            chunk = fh.read()\n    except OSError:\n        return \"\", offset\n\"\"\",\n \"\"\"    try:\n        with open(str(path), \"rb\") as fh:\n            fh.seek(offset)\n            chunk = fh.read()\n    except (OSError, ValueError):\n        return \"\", offset\n\"\"\"),\n(\"\"\"        try:\n            st = os.stat(str(path))\n        except OSError:\n            return _EMPTY_INDEX\n\"\"\",\n \"\"\"        try:\n            st = os.stat(str(path))\n        except (OSError, ValueError):\n            # ValueError as well as OSError: a path out of a JSON log is a\n            # string this module did not choose, and one with a NUL in it is a\n            # ValueError from the C call rather than a missing file.\n            return _EMPTY_INDEX\n\"\"\"),\n]\nfor old, new in pairs:\n    assert old in s, old[:40]\n    s = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:44:22.026Z","count":1,"stdin_sample":null}
{"key":"py-cb8d8242a72a","id":"py-cb8d8242a72a","program":"from pathlib import Path\np = Path(\"tests/test_harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"    monkeypatch.setattr(harvest, \"_model_index\",\n                        lambda t: indexed.append(t) or harvest._EMPTY_INDEX)\"\"\"\nnew = \"\"\"    monkeypatch.setattr(harvest, \"_model_index\",\n                        lambda t, cache=None: indexed.append(t) or harvest._EMPTY_INDEX)\"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\n\np = Path(\"src/lypning/corpus.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = \"\"\"    :func:`merge` IS summation, protected from double counting by the\n    identical-record collapse instead. Copying one call site onto the other is\n    how this field goes quietly wrong, and no gate can see it.\n    \"\"\"\nnew = \"\"\"    :func:`merge` IS summation, protected from double counting by the\n    identical-record collapse instead. Copying one call site onto the other is\n    how this field goes quietly wrong, and no gate can see it.\n\n    Neither combiner is responsible for keeping the histogram inside the\n    ``count`` beside it. A per-key max over two disjoint key sets can out-count\n    a scalar max of the counts, and the max sites reconcile that themselves —\n    see :func:`harvest._count_at_least_the_models`, which raises the count\n    rather than trim the evidence.\n    \"\"\"\nassert old in s\np.write_text(s.replace(old, new, 1), encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:54:28.364Z","count":1,"stdin_sample":null}
{"key":"py-cc727df833a1","id":"py-cc727df833a1","program":"\nimport sys,json\ns=set()\nfor l in sys.stdin:\n    l=l.strip()\n    if l: s.add(tuple(json.loads(l).keys()))\nfor t in s: print(t)\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:17:28.397Z","count":1,"stdin_sample":null}
{"key":"py-cd144e32fc99","id":"py-cd144e32fc99","program":"import io\np = \"/Users/user/dev/lypning-capture-model/src/lypning/cli.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = \"\"\"    if ns.dry_run:\n        sightings = harvest.collect(transcripts=ns.transcripts)\n\"\"\"\nnew = \"\"\"    if ns.dry_run:\n        # persist=False, because --dry-run writes nothing and the transcript\n        # index cache is a write. It is still READ, so the report is the same\n        # report a real run would produce.\n        sightings = harvest.collect(transcripts=ns.transcripts, persist=False)\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"F5 cli ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:01:11.828Z","count":1,"stdin_sample":null}
{"key":"py-cd34bbe8fd95","id":"py-cd34bbe8fd95","program":"import json,glob,bisect,collections,re\nsess_assist=collections.defaultdict(list); cmd_index=collections.defaultdict(list)\nmodels=collections.Counter()\nfor f in glob.glob('/Users/user/.claude/projects/**/*.jsonl',recursive=True):\n    for line in open(f,'r',encoding='utf-8',errors='replace'):\n        try: r=json.loads(line)\n        except ValueError: continue\n        if not isinstance(r,dict) or r.get('type')!='assistant': continue\n        msg=r.get('message') or {}; model=msg.get('model'); ts=r.get('timestamp'); sid=r.get('sessionId')\n        if isinstance(ts,str) and isinstance(model,str):\n            models[model]+=1\n            sess_assist[sid].append((ts,model))\n        c=msg.get('content')\n        if isinstance(c,list):\n            for b in c:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                    cmd=(b.get('input') or {}).get('command')\n                    if isinstance(cmd,str): cmd_index[(sid,cmd)].append((ts,b.get('id'),model))\nprint('assistant model values seen (incl. non-models):',dict(models))\nSYN=lambda m: (not m) or m.startswith('<')\nfilt={k:[(t,m) for t,m in sorted(v) if not SYN(m)] for k,v in sess_assist.items()}\nraw={k:sorted(v) for k,v in sess_assist.items()}\ndef model_at(tab,sid,ts):\n    a=tab.get(sid)\n    if not a: return None\n    i=bisect.bisect_right(a,(ts,'￿'))-1\n    return a[i][1] if i>=0 else None\nrecs=[]\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    try: r=json.loads(line)\n    except ValueError: continue\n    if r.get('kind')=='bash_command': recs.append(r)\ndef score(tab,mangle=lambda t:t,label=''):\n    ok=bad=0\n    for r in recs:\n        gt=cmd_index.get((r.get('session'),r.get('command')))\n        if not gt: continue\n        got=model_at(tab,r.get('session'),mangle(r['ts']))\n        if got==gt[0][2]: ok+=1\n        else: bad+=1\n    print('%-46s ok=%d bad=%d (%.1f%%)'%(label,ok,bad,100*ok/(ok+bad)))\ntrunc=lambda t: re.sub(r'\\.\\d+Z$','Z',t)          # macOS shim: second precision\nscore(raw,label='ms ts, all assistant records')\nscore(filt,label='ms ts, <synthetic> filtered')\nscore(filt,trunc,'SECOND-precision ts (macOS shim), filtered')\nscore(filt,lambda t:trunc(t),'')\n# clock skew sensitivity\nimport datetime\ndef skew(sec):\n    def f(t):\n        d=datetime.datetime.strptime(t,'%Y-%m-%dT%H:%M:%S.%fZ')+datetime.timedelta(seconds=sec)\n        return d.strftime('%Y-%m-%dT%H:%M:%S.%f')[:-3]+'Z'\n    return f\nfor s in (-1,-5,-30,1,5,30):\n    score(filt,skew(s),'ms ts, clock skew %+ds'%s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:14:54.232Z","count":1,"stdin_sample":null}
{"key":"py-cdc4be37f45c","id":"py-cdc4be37f45c","program":"import io, sys\np = \"src/lypning/capture.py\"\ns = io.open(p, encoding=\"utf-8\").read()\n\n# 1. record_command docstring: the run vs tool_use_id note, and correct the\n#    \"which the corpus path never sees\" clause.\nold = \"\"\"    ``harvest`` reads four fields (``kind``, ``command``, ``session``, ``ts``)\n    and ignores the rest, so the extras here cost nothing downstream:\n    :func:`harvest.host_counts` reads ``host``, and ``run``/``exit_code`` are\n    ground truth a harness volunteered which the corpus path never sees.\n    \\\"\\\"\\\"\n\"\"\"\nnew = \"\"\"    ``harvest`` reads four fields (``kind``, ``command``, ``session``, ``ts``)\n    and ignores the rest, so the extras here cost nothing downstream:\n    :func:`harvest.host_counts` reads ``host``, and ``run``/``exit_code`` are\n    ground truth a harness volunteered. ``transcript`` and the Claude mapper's\n    ``tool_use_id`` are the exceptions — the corpus path does read those, to\n    resolve which model issued the command.\n\n    ``run`` and that ``tool_use_id`` are the same concept in two namespaces: a\n    per-tool-call id. They are deliberately NOT unified. Only the Claude one has\n    a transcript carrying ``message.model`` to join against, and feeding an\n    opencode ``callID`` into a Claude transcript index would find nothing while\n    claiming in the code that it might.\n    \\\"\\\"\\\"\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\n# 2. from_claude_event: the docstring paragraph, and the Claude-only decoration.\nold = '''    \"\"\"Claude Code ``PreToolUse``. Only ``tool_name == \"Bash\"`` records.\n\n    Every other tool's input is someone else's data, and capturing it would put\n    file contents the session never ran into a log that gets published.\n    \"\"\"\n'''\nnew = '''    \"\"\"Claude Code ``PreToolUse``. Only ``tool_name == \"Bash\"`` records.\n\n    Every other tool's input is someone else's data, and capturing it would put\n    file contents the session never ran into a log that gets published.\n\n    ``tool_use_id`` is copied out because it is the ONLY join key back to the\n    model that issued this command: the payload carries no model field and no\n    ``CLAUDE_*`` variable exposes one, but the transcript record that holds this\n    same id also holds ``message.model``. Reading that transcript here is out of\n    the question — this runs before every Bash call in the session — so the id\n    is written down and the join is done at harvest time\n    (:mod:`lypning.harvest`). ``agent_id`` and ``agent_type`` are present only\n    when the call came from a subagent, so they are written only when present,\n    and their presence is itself the flag that says so.\n\n    All three are set HERE rather than in :func:`record_command`, because all\n    three are Claude Code's: a harness whose events carry no such id would get a\n    permanent ``\"tool_use_id\": null`` in every record it writes, naming a join\n    that does not exist for it.\n    \"\"\"\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = '''    return record_command(\n        _str(tool_input.get(\"command\")),\n        host=\"claude\",\n        tool=tool,\n        session=_str(event.get(\"session_id\")) or _str(event.get(\"sessionId\")),\n        cwd=_str(event.get(\"cwd\")),\n        description=_str(tool_input.get(\"description\")),\n        transcript=(_str(event.get(\"transcript_path\"))\n                    or _str(event.get(\"transcriptPath\"))),\n    )\n'''\nnew = '''    record = record_command(\n        _str(tool_input.get(\"command\")),\n        host=\"claude\",\n        tool=tool,\n        session=_str(event.get(\"session_id\")) or _str(event.get(\"sessionId\")),\n        cwd=_str(event.get(\"cwd\")),\n        description=_str(tool_input.get(\"description\")),\n        transcript=(_str(event.get(\"transcript_path\"))\n                    or _str(event.get(\"transcriptPath\"))),\n    )\n    if record is None:\n        return None\n    record[\"tool_use_id\"] = (\n        _str(event.get(\"tool_use_id\")) or _str(event.get(\"toolUseId\"))\n    )\n    for key, alias in ((\"agent_id\", \"agentId\"), (\"agent_type\", \"agentType\")):\n        value = _str(event.get(key)) or _str(event.get(alias))\n        if value:\n            record[key] = value\n    return record\n'''\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T08:41:35.501Z","count":1,"stdin_sample":null}
{"key":"py-cdd5584650ad","id":"py-cdd5584650ad","program":"import json, os, shutil\nos.makedirs(\"/tmp/pt/e2e/nul/home\", exist_ok=True)\nos.makedirs(\"/tmp/pt/e2e/nul/project\", exist_ok=True)\nrec = {\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:00.000Z\",\"session\":\"s1\",\n       \"command\":\"python3 -c 'print(1)'\",\n       \"transcript\":\"/tmp/pt/e2e/nul/t\" + chr(0) + \"x.jsonl\",\"tool_use_id\":\"t1\"}\nwith open(\"/tmp/pt/e2e/nul/log.jsonl\",\"w\") as fh:\n    fh.write(json.dumps(rec)+\"\\n\")\nprint(open(\"/tmp/pt/e2e/nul/log.jsonl\").read().replace(chr(0), \"<NUL>\"))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:36:08.727Z","count":1,"stdin_sample":null}
{"key":"py-ce6ea8a49f26","id":"py-ce6ea8a49f26","program":"import json, os\nfrom pathlib import Path\nsess={}\nfor l in open(os.path.expanduser(\"~/.lypning/invocations.jsonl\"),errors=\"replace\"):\n    try: r=json.loads(l)\n    except Exception: continue\n    if isinstance(r,dict) and r.get(\"transcript\"): sess.setdefault(r[\"session\"], r[\"transcript\"])\nprint(\"sessions naming a transcript:\", len(sess))\ntot=0; n=0\nfor s,t in sess.items():\n    p=Path(t)\n    files=[p] if p.is_file() else []\n    sub=p.parent/p.stem/\"subagents\"\n    if sub.is_dir(): files+=list(sub.rglob(\"*.jsonl\"))\n    b=sum(f.stat().st_size for f in files)\n    tot+=b; n+=len(files)\n    print(\"  %s  %d files  %d B\" % (s[:12], len(files), b))\nprint(\"TOTAL: %d files, %d B  (doc quotes 180 files, 45,642,644 B on 2026-09-02)\" % (n, tot))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T11:46:45.955Z","count":1,"stdin_sample":null}
{"key":"py-d3f268798c49","id":"py-d3f268798c49","program":"import re\ndata=open('/tmp/cc258.strings','rb').read()\nidxs=[m.start() for m in re.finditer(rb'tool_use_id', data)]\nprint('occurrences:',len(idxs))\nhits=0\nfor i in idxs:\n    ctx=data[max(0,i-400):i+400]\n    if b'PreToolUse' in ctx or b'hook_event_name' in ctx or b'tool_input' in ctx:\n        hits+=1\n        print('='*70)\n        print(ctx.decode('utf8','replace'))\n        if hits>=6: break\nprint('ctx hits:',hits)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:10:26.516Z","count":1,"stdin_sample":null}
{"key":"py-d5c59bff77e3","id":"py-d5c59bff77e3","program":"from pathlib import Path\np = Path(\"tests/test_harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\n\nold = '''def test_two_sightings_of_one_program_merge_their_models_with_max():\n    # Both sides count the SAME occurrence keys — a session's live log and its\n    # own published file describe the same invocations — so summing here would\n    # double the record on every export, which is the exact bug `count=max`\n    # exists to prevent.\n    a = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=3,\n                         models=((\"claude-fable-5-1\", 3),))\n    b = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=3,\n                         models=((\"claude-fable-5-1\", 3), (\"claude-opus-5\", 1)))\n    merged = harvest._combine(a, b)\n    assert merged.count == 3\n    assert merged.models == ((\"claude-fable-5-1\", 3), (\"claude-opus-5\", 1))\n    assert harvest._combine(merged, merged).models == merged.models\n'''\n\nnew = '''def _the_hole_is_not_negative(record):\n    \"\"\"``count - sum(models)`` is the unattributed hole every reader is invited\n    to compute — the docstring on :class:`harvest.Sighting`, `docs/CAPTURE.md`\n    and the changelog all promise it — so it is asserted on every path that can\n    produce a record, not only on the one that first got it wrong.\"\"\"\n    assert sum(n for _, n in record.models) <= record.count, record\n\n\ndef test_two_sightings_of_one_program_merge_their_models_with_max():\n    # Both sides count the SAME occurrence keys — a session's live log and its\n    # own published file describe the same invocations — so summing here would\n    # double the record on every export, which is the exact bug `count=max`\n    # exists to prevent.\n    a = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=3,\n                         models=((\"claude-fable-5-1\", 3),))\n    b = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=3,\n                         models=((\"claude-fable-5-1\", 3), (\"claude-opus-5\", 1)))\n    merged = harvest._combine(a, b)\n    assert merged.models == ((\"claude-fable-5-1\", 3), (\"claude-opus-5\", 1))\n    # Four occurrences are accounted for by name, so the count is four. The\n    # scalar max of two 3s was the undercount: `b` saw a run of this program\n    # that `a` never did, and taking the larger of two partial views threw it\n    # away. See `_count_at_least_the_models`.\n    assert merged.count == 4\n    _the_hole_is_not_negative(merged)\n    assert harvest._combine(merged, merged) == merged\n\n\ndef test_two_disjoint_models_raise_the_count_instead_of_promising_a_negative_hole():\n    \"\"\"The ordinary cross-session shape, and the one the per-key max broke.\n\n    A scalar max bounds a per-key max only when both sides carry the same model\n    keys. Two sessions that ran one program under two different models do not,\n    and their occurrence sets are then genuinely disjoint — so the count follows\n    the histogram up rather than the histogram being trimmed to the count.\n    \"\"\"\n    a = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=1,\n                         models=((\"claude-opus-5\", 1),))\n    b = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=1,\n                         models=((\"claude-fable-5-1\", 1),))\n    merged = harvest._combine(a, b)\n    assert merged.models == ((\"claude-fable-5-1\", 1), (\"claude-opus-5\", 1))\n    assert merged.count == 2\n    _the_hole_is_not_negative(merged)\n    # Commutative, and idempotent: re-merging re-derives the same sum, which the\n    # max absorbs. A count that grew on every export would be the doubling bug\n    # this whole merge exists to prevent, wearing a new hat.\n    assert harvest._combine(b, a) == merged\n    assert harvest._combine(merged, merged) == merged\n\n\ndef test_a_record_with_no_models_keeps_the_count_it_had():\n    # Almost every committed sighting predates this field. An empty histogram\n    # sums to zero, so the clamp cannot move one of them and no committed line\n    # is rewritten by it.\n    a = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=7)\n    b = harvest.Sighting(key=\"py-a\", program=\"print(1)\", count=2)\n    assert harvest._combine(a, b).count == 7\n\n\ndef test_a_cross_session_merge_never_promises_more_models_than_occurrences(project, tmp_path):\n    \"\"\"End to end, through the public path a Stop hook actually takes.\n\n    Session A published its sighting; session B ran the same program under a\n    different model and is still in the live log. `collect` merges the two, and\n    a reader that computed `count - sum(models)` used to get -1.\n    \"\"\"\n    program = UNIQUE\n    key = harvest.sighting_key(program)\n    root = paths.sightings_dir(project)\n    paths.ensure_dir(root)\n    (root / \"sess-a.jsonl\").write_text(json.dumps({\n        \"key\": key, \"id\": key, \"program\": program, \"argv_tail\": [], \"source\": \"hook\",\n        \"session\": \"sess-a\", \"first_seen\": \"2026-09-01T00:00:00.000Z\", \"count\": 1,\n        \"stdin_sample\": None, \"models\": {\"claude-opus-5\": 1},\n    }) + \"\\\\n\", encoding=\"utf-8\")\n\n    command = \"python3 -c \\\\\"%s\\\\\"\" % program\n    main = _transcripts(tmp_path, [\n        _assistant(\"2026-09-02T10:00:00.000Z\", \"claude-fable-5-1\", \"toolu_b\", command),\n    ])\n    _log([_hook(command, \"toolu_b\", main)])\n\n    merged = {s.key: s for s in harvest.collect(project)}[key]\n    assert merged.models == ((\"claude-fable-5-1\", 1), (\"claude-opus-5\", 1))\n    assert merged.count == 2\n    _the_hole_is_not_negative(merged)\n\n\ndef test_folding_two_models_into_one_corpus_record_keeps_the_hole_non_negative(tmp_path):\n    target = tmp_path / \"corpus.jsonl\"\n    key = harvest.sighting_key(UNIQUE)\n\n    def sighting(model):\n        return harvest.Sighting(key=key, program=UNIQUE, count=1,\n                                models=((model, 1),))\n\n    harvest.fold_into_corpus([sighting(\"claude-opus-5\")], target)\n    harvest.fold_into_corpus([sighting(\"claude-fable-5-1\")], target)\n    e = corpus.load(target)[0]\n    assert e.models == ((\"claude-fable-5-1\", 1), (\"claude-opus-5\", 1))\n    assert e.count == 2\n    _the_hole_is_not_negative(e)\n    # And the fold is still a pure function of its inputs: the same sightings\n    # again move not one byte.\n    before = target.read_bytes()\n    harvest.fold_into_corpus([sighting(\"claude-fable-5-1\")], target)\n    assert target.read_bytes() == before\n\n\ndef test_a_merge_carries_the_unknown_keys_of_both_sides():\n    \"\"\"The forward-compatibility bucket has to survive the MERGE, not just the\n    read and the write.\n\n    That is the path a peer session running older code takes: it reads a\n    published record, merges its own sighting into it, and writes the result\n    back. A merge that kept only the published side's unknown keys would strip\n    everything a newer lypning had just added to the incoming one — which is the\n    silent field loss `extra` exists to prevent, arriving by another door.\n    \"\"\"\n    a = harvest.Sighting(key=\"py-a\", program=\"print(1)\",\n                         extra={\"published_by\": \"an older lypning\"})\n    b = harvest.Sighting(key=\"py-a\", program=\"print(1)\",\n                         extra={\"invented_later\": {\"by\": \"a peer\"}})\n    merged = harvest._combine(a, b)\n    assert merged.extra == {\"published_by\": \"an older lypning\",\n                            \"invented_later\": {\"by\": \"a peer\"}}\n    assert merged.to_obj()[\"invented_later\"] == {\"by\": \"a peer\"}\n    # On a key both sides know, the published record's own value wins: it is the\n    # one already committed, and a merge is not the place to overwrite it.\n    x = harvest.Sighting(key=\"py-a\", program=\"print(1)\", extra={\"k\": \"published\"})\n    y = harvest.Sighting(key=\"py-a\", program=\"print(1)\", extra={\"k\": \"incoming\"})\n    assert harvest._combine(x, y).extra == {\"k\": \"published\"}\n'''\nassert old in s\ns = s.replace(old, new, 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:21:02.073Z","count":1,"stdin_sample":null}
{"key":"py-d81b9f2bb795","id":"py-d81b9f2bb795","program":"import json,glob,collections\nbase='/Users/user/.claude/projects'\nc=collections.Counter()\ntask_tools=collections.Counter()\nfor f in glob.glob(base+'/**/*.jsonl',recursive=True):\n    kind='SUBAGENT' if '/subagents/' in f else 'MAIN'\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        c[(kind, r.get('isSidechain'))]+=1\n        if r.get('type')=='assistant':\n            for b in ((r.get('message') or {}).get('content') or []):\n                if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name') in ('Task','Agent','SendMessage'):\n                    task_tools[(kind,b['name'])]+=1\nprint(\"== all records by (file kind, isSidechain) ==\")\nfor k,v in sorted(c.items(),key=str): print(f\"{v:7d}  {k}\")\nprint(\"== Task/Agent tool_use by file kind ==\")\nfor k,v in task_tools.most_common(): print(v,k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:08:19.726Z","count":1,"stdin_sample":null}
{"key":"py-d96a0f9dc085","id":"py-d96a0f9dc085","program":"import json,sys; print(sorted(json.loads(sys.stdin.read()).keys()))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:46.898Z","count":1,"stdin_sample":null}
{"key":"py-db5b41897fd0","id":"py-db5b41897fd0","program":"import os, sys, time, json\nROOT=\"/tmp/pt/e2e/real\"\nos.environ[\"LYPNING_HOME\"]=ROOT+\"/home\"\nsys.path.insert(0, sys.argv[1])\nfrom lypning import harvest\nlog = ROOT+\"/log.jsonl\"\nbest=[]\nfor i in range(3):\n    t=time.time(); s=harvest.parse_log(log); best.append(time.time()-t)\nprint(sys.argv[1], \"sightings\", len(s), \"times\", [\"%.2f\"%x for x in best], \"best %.2f\"%min(best))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:32:59.176Z","count":1,"stdin_sample":null}
{"key":"py-db9db0c65dd2","id":"py-db9db0c65dd2","program":"import json,os,glob,collections\nlog=os.path.expanduser('~/.lypning/invocations.jsonl')\nrecs=[json.loads(l) for l in open(log,errors='replace') if l.strip()]\nidx=collections.defaultdict(list)\nfor f in glob.glob(os.path.expanduser('~/.claude/projects/**/*.jsonl'),recursive=True):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try:o=json.loads(line)\n        except:continue\n        if o.get('type')!='assistant': continue\n        m=o.get('message')\n        if not isinstance(m,dict): continue\n        for b in m.get('content') or []:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                idx[b['input'].get('command')].append((f,m.get('model'),b.get('id'),o.get('agentId')))\nonly_sub=in_main=0\nex=None\nfor r in recs:\n    cands=idx.get(r.get('command'),[])\n    tp=r.get('transcript')\n    if any(x[0]==tp for x in cands): in_main+=1\n    else:\n        only_sub+=1\n        if ex is None and cands: ex=(r,cands)\nprint(\"log records whose command IS in the hook's transcript_path file:\",in_main)\nprint(\"log records whose command is NOT in that file (only in subagent files):\",only_sub)\nr,c=ex\nprint(\"\\nEXAMPLE record from lypning log:\")\nprint(\"  session   :\",r['session'])\nprint(\"  transcript:\",r['transcript'])\nprint(\"  command   :\",r['command'][:120].replace('\\n','\\\\n'))\nprint(\"  resolved  :\")\nfor f,mod,tid,aid in c:\n    print(\"     file=\",f.replace(os.path.expanduser('~'),'~'))\n    print(\"     model=\",mod,\" tool_use_id=\",tid,\" agentId=\",aid)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:28:31.209Z","count":1,"stdin_sample":null}
{"key":"py-dff78d6b3b4c","id":"py-dff78d6b3b4c","program":"\nimport sys,json\nfor l in sys.stdin:\n    o=json.loads(l)\n    print(sorted(o.keys()))\n    print('  agent_id=',o.get('agent_id'),'agent_type=',o.get('agent_type'),'session_id=',o.get('session_id'),'effort=',o.get('effort'))\n    print('  transcript_path=',o.get('transcript_path'))\n    print('  cmd=',(o.get('tool_input') or {}).get('command'),'tool_use_id=',o.get('tool_use_id'))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:30:05.829Z","count":1,"stdin_sample":null}
{"key":"py-e10cf8afcba6","id":"py-e10cf8afcba6","program":"import json,glob,collections,os\nfiles=sorted(glob.glob(os.path.expanduser('~/.claude/projects/**/*.jsonl'),recursive=True))\ntop=[f for f in files if f.count('/')==len(os.path.expanduser('~/.claude/projects').split('/'))+1]\nprint(\"total jsonl:\",len(files))\nc=collections.Counter(); ctop=collections.Counter(); csub=collections.Counter()\npersess=collections.defaultdict(collections.Counter)\nside=0\nfor f in files:\n    sub='/subagents/' in f\n    for line in open(f, errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: o=json.loads(line)\n        except Exception: continue\n        if o.get('type')!='assistant': continue\n        m=o.get('message')\n        if not isinstance(m,dict): continue\n        mod=m.get('model')\n        if not mod: continue\n        c[mod]+=1\n        (csub if sub else ctop)[mod]+=1\n        persess[f][mod]+=1\n        if o.get('isSidechain'): side+=1\nprint(\"=== ALL message.model values ===\")\nfor k,v in c.most_common(): print(f\"{v:8d}  {k}\")\nprint(\"=== main-transcript only ===\"); \nfor k,v in ctop.most_common(): print(f\"{v:8d}  {k}\")\nprint(\"=== subagent files only ===\")\nfor k,v in csub.most_common(): print(f\"{v:8d}  {k}\")\nprint(\"isSidechain=true assistant records:\",side)\nprint(\"=== per-file model mixes (files with >1) ===\")\nfor f,cc in persess.items():\n    if len(cc)>1: print(\" \", f.replace(os.path.expanduser('~'),'~'), dict(cc))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:23:46.514Z","count":1,"stdin_sample":null}
{"key":"py-e193fc99252e","id":"py-e193fc99252e","program":"import json,collections\nc=collections.Counter()\nfor line in open('/Users/user/.lypning/invocations.jsonl',errors='replace'):\n    try: r=json.loads(line)\n    except ValueError: continue\n    t=r.get('transcript') or ''\n    c['subagent' if '/subagents/' in t else ('main' if t else 'empty')]+=1\nprint(c)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:15:51.757Z","count":1,"stdin_sample":null}
{"key":"py-e307e974016f","id":"py-e307e974016f","program":"import json,os,glob,collections,time\n\ndef index_file(p):\n    m={}\n    try: fh=open(p,'r',encoding='utf-8',errors='replace')\n    except OSError: return m\n    with fh:\n        for line in fh:\n            if '\"tool_use\"' not in line: continue\n            try: r=json.loads(line)\n            except ValueError: continue\n            msg=r.get('message')\n            if not isinstance(msg,dict): continue\n            model=msg.get('model'); c=msg.get('content')\n            if not isinstance(c,list): continue\n            for b in c:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and isinstance(b.get('id'),str):\n                    m[b['id']]=model\n    return m\n\n# ground truth: cmd -> tool_use id, per file\nrecs=[json.loads(l) for l in open('/Users/user/.lypning/invocations.jsonl',errors='replace') if l.strip()]\nrecs=[r for r in recs if r.get('kind')=='bash_command']\nby_tr=collections.defaultdict(list)\nfor r in recs: by_tr[r.get('transcript')].append(r)\n\nfor tr,rs in by_tr.items():\n    d=os.path.dirname(tr); sid=os.path.basename(tr)[:-6]\n    subs=sorted(glob.glob(os.path.join(d,sid,'subagents','**','*.jsonl'),recursive=True))\n    t0=time.perf_counter(); main=index_file(tr); t_main=(time.perf_counter()-t0)*1000\n    t0=time.perf_counter()\n    allm=dict(main)\n    for s in subs: allm.update(index_file(s))\n    t_all=(time.perf_counter()-t0)*1000\n    # command -> id, from all files\n    cmd2id={}\n    for p in [tr]+subs:\n        for line in open(p,'r',encoding='utf-8',errors='replace'):\n            if '\"tool_use\"' not in line: continue\n            try: rr=json.loads(line)\n            except ValueError: continue\n            for b in (rr.get('message') or {}).get('content') or []:\n                if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                    cc=(b.get('input') or {}).get('command')\n                    if isinstance(cc,str): cmd2id.setdefault(cc,b['id'])\n    hit_main=sum(1 for r in rs if cmd2id.get(r['command']) in main)\n    hit_all =sum(1 for r in rs if cmd2id.get(r['command']) in allm)\n    print('%s  recs=%-4d subagent_files=%-4d  id-in-main=%-4d  id-in-main+subs=%-4d  index_ms main=%.1f all=%.1f  mainMB=%.1f allMB=%.1f'\n          % (sid[:8], len(rs), len(subs), hit_main, hit_all, t_main, t_all,\n             os.path.getsize(tr)/1e6, sum(os.path.getsize(x) for x in [tr]+subs)/1e6))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:16:36.098Z","count":1,"stdin_sample":null}
{"key":"py-e35a30224934","id":"py-e35a30224934","program":"import json, os, shutil, subprocess, sys, time, random\nfrom pathlib import Path\nROOT = Path(\"/tmp/pt/e2e/conc\")\nSRC = \"/Users/user/dev/lypning-capture-model/src\"\n\nWORKER = r'''\nimport json, os, sys, time\nsys.path.insert(0, %r)\nos.environ[\"LYPNING_HOME\"] = sys.argv[1]\nfrom lypning import harvest\nlog = sys.argv[2]\nout = {}\nfor i in range(12):\n    try:\n        for s in harvest.parse_log(log):\n            for m, n in s.models:\n                out.setdefault(s.program, {})[m] = n\n    except Exception as e:\n        print(\"EXC \" + type(e).__name__ + \": \" + str(e)); raise SystemExit(1)\nprint(\"OK \" + json.dumps(out, sort_keys=True))\n''' % (SRC,)\n\ndef assistant(ts, model, tuid):\n    return json.dumps({\"type\":\"assistant\",\"timestamp\":ts,\"message\":{\"model\":model,\"content\":[\n        {\"type\":\"tool_use\",\"id\":tuid,\"name\":\"Bash\",\"input\":{\"command\":\"x\"}}]}})\n\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True)\ntr = ROOT/\"transcripts\"; tr.mkdir()\nNSESS = 4\nlogs = []\nfor si in range(NSESS):\n    main = tr/(\"s%d.jsonl\" % si)\n    main.write_text(\"\".join(assistant(\"2026-09-01T10:00:%02d.000Z\" % j, \"M%d\" % si, \"s%dt%d\" % (si, j)) + \"\\n\"\n                            for j in range(20)), encoding=\"utf-8\")\n    log = ROOT/(\"log%d.jsonl\" % si)\n    log.write_text(\"\\n\".join(json.dumps({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:%02d.000Z\" % j,\n        \"session\":\"s%d\" % si, \"command\":\"python3 -c 'print(%d)'\" % (si*100+j),\n        \"transcript\":str(main), \"tool_use_id\":\"s%dt%d\" % (si, j)}) for j in range(20))+\"\\n\", encoding=\"utf-8\")\n    logs.append(log)\n\nw = ROOT/\"worker.py\"; w.write_text(WORKER, encoding=\"utf-8\")\nprocs = []\nfor rep in range(3):\n    for si in range(NSESS):\n        procs.append(subprocess.Popen([sys.executable, str(w), str(ROOT/\"home\"), str(logs[si])],\n                                      stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True))\nbad = 0\nresults = []\nfor p in procs:\n    o, _ = p.communicate()\n    if p.returncode != 0 or not o.startswith(\"OK\"):\n        bad += 1; print(\"FAIL rc=%s out=%s\" % (p.returncode, o[:400]))\n    else:\n        results.append(json.loads(o[3:]))\nprint(\"workers:\", len(procs), \"failures:\", bad)\n# expected: every program attributed to its own session's model\nexpected_bad = 0\nfor si, r in zip([i % NSESS for i in range(len(results))], results):\n    pass\nallprog = {}\nfor r in results:\n    for prog, ms in r.items():\n        allprog.setdefault(prog, set()).update(ms)\nwrong = {p: ms for p, ms in allprog.items() if len(ms) != 1}\nprint(\"programs seen:\", len(allprog), \"ambiguous:\", wrong)\nfor prog, ms in sorted(allprog.items()):\n    n = int(prog[len(\"print(\"):-1])\n    want = \"M%d\" % (n // 100)\n    if list(ms) != [want]:\n        print(\"WRONG\", prog, ms, \"want\", want)\ncache = ROOT/\"home\"/\"model-index.json\"\ntry:\n    blob = json.loads(cache.read_text())\n    print(\"cache ok, files:\", len(blob.get(\"files\", {})))\nexcept Exception as e:\n    print(\"CACHE CORRUPT:\", e)\nprint(\"stray tmp files:\", sorted(p.name for p in (ROOT/\"home\").iterdir() if p.name != \"model-index.json\"))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:30:34.275Z","count":1,"stdin_sample":null}
{"key":"py-e3620ed4c574","id":"py-e3620ed4c574","program":"from pathlib import Path\np = Path(\"tests/test_harvest.py\")\ns = p.read_text(encoding=\"utf-8\")\nold = '''        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {}, \"timeline\": [[1, \"m\"]]}}}),      # a stamp bisect cannot compare\n    ):\n        cache.write_text(corrupt, encoding=\"utf-8\")\n        assert harvest.parse_log(log)[0].models == expected, corrupt\n'''\nnew = '''        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {}, \"timeline\": [[1, \"m\"]]}}}),      # a stamp bisect cannot compare\n        # Well-formed and STALE, which is the dangerous one: it names a model,\n        # and believing it would be the silent wrong answer rather than a slow\n        # one. The inode it claims is not this file's.\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": 1, \"dev\": 1, \"head\": \"x\",\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n        # Well-formed, right inode, and the head of the file has moved: same\n        # path, different bytes. Only re-reading can tell.\n        json.dumps({\"version\": 1, \"files\": {str(main): {\n            \"offset\": 10, \"ino\": os.stat(str(main)).st_ino,\n            \"dev\": os.stat(str(main)).st_dev, \"head\": \"0\" * 32,\n            \"ids\": {\"toolu_a\": \"claude-not-this-one\"}, \"timeline\": []}}}),\n    ):\n        cache.write_text(corrupt, encoding=\"utf-8\")\n        assert harvest.parse_log(log)[0].models == expected, corrupt\n'''\nassert old in s\ns = s.replace(old, new, 1)\nold_imp = \"from __future__ import annotations\\n\\nimport json\\n\"\nassert old_imp in s\ns = s.replace(old_imp, \"from __future__ import annotations\\n\\nimport json\\nimport os\\n\", 1)\np.write_text(s, encoding=\"utf-8\")\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T05:47:59.037Z","count":1,"stdin_sample":null}
{"key":"py-e487268acfdd","id":"py-e487268acfdd","program":"import json,glob,os\nbase='/Users/user/.claude/projects'\nfor f in glob.glob(base+'/*/*.jsonl'):\n    for line in open(f,errors='replace'):\n        line=line.strip()\n        if not line: continue\n        try: r=json.loads(line)\n        except: continue\n        if r.get('type')!='assistant': continue\n        for b in ((r.get('message') or {}).get('content') or []):\n            if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Agent':\n                print(f)\n                print(\"  id:\",b['id'],\"model:\",(r.get('message') or {}).get('model'))\n                print(\"  input keys:\",list((b.get('input') or {}).keys()))\n                inp=b.get('input') or {}\n                print(\"  subagent_type:\",inp.get('subagent_type'), \"desc:\",str(inp.get('description'))[:60])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:08:36.379Z","count":1,"stdin_sample":null}
{"key":"py-e555885b9e8a","id":"py-e555885b9e8a","program":"import io\np = \"docs/CAPTURE.md\"\ns = io.open(p, encoding=\"utf-8\").read()\n\nold = \"\"\"`transcript_path` in the payload always names the MAIN session file, but a Bash\ncall issued by a **subagent** has its `tool_use` block only under\n`<session>/subagents/**/agent-*.jsonl`. Measured on one machine on 2026-09-02,\nmost of this project's captured python is issued by subagents, so the index is\nbuilt from the main transcript **and** that tree; a `tool_use` id is unique\nacross every transcript on disk, so the union cannot collide. Indexing only the\nmain file would leave the majority unattributed, and time-joining over it would\nbe worse — it would file a subagent's program under the parent loop's model,\nwhich is precisely the silent wrong answer the id join exists to avoid.\n\"\"\"\nnew = \"\"\"`transcript_path` in the payload always names the MAIN session file, but a Bash\ncall issued by a **subagent** has its `tool_use` block only under\n`<session>/subagents/**/agent-*.jsonl`. Measured on one machine on 2026-09-02 —\n194 transcript files, 895 python-ish Bash `tool_use` blocks, 716 of them (80.0%)\nin a subagent file — most of this project's captured python is issued by\nsubagents, so the index is built from the main transcript **and** that tree; a\n`tool_use` id is unique across every transcript on disk (4301 distinct ids over\nthose same files, none of them in two), so the union cannot collide. Indexing\nonly the main file would leave four fifths unattributed, and time-joining over\nit would be worse — it would file a subagent's program under the parent loop's\nmodel, which is precisely the silent wrong answer the id join exists to avoid.\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\n\nold = \"\"\"last harvest are parsed. The offsets and what they yielded live in\n`$LYPNING_HOME/model-index.json`, which is a **cache and never a source of\ntruth**: missing, unreadable, corrupt, truncated, replaced or written by\nanother version, every one of those falls back to reading the file from byte\nzero. It is never committed, deleting it is always safe, and its absence costs\nonly time. Measured on this machine on 2026-09-02, on a copy of the real\ncapture log (750 records, 4 sessions, 39.1 MB of transcript trees, best of 9\ninterleaved runs): 1859 ms per harvest re-indexing every time, 974 ms with the\ncache warm, against 704 ms for the same log with nothing to join at all.\n\"\"\"\nnew = \"\"\"last harvest are parsed. The offsets and what they yielded live in\n`$LYPNING_HOME/model-index.json`, which is a **cache and never a source of\ntruth**: missing, unreadable, corrupt, written by another version, describing a\nfile on another device or inode, one that has shrunk, or one whose last\nconsumed bytes have changed — every one of those falls back to reading the file\nfrom byte zero. It is never committed, deleting it is always safe, and its\nabsence costs only time.\n\nThe cost is quoted in bytes rather than seconds on purpose: this machine is\nshared with other agent sessions and its load average reaches the hundreds, so a\nwall clock here measures the machine. Measured on 2026-09-02, on a copy of the\nreal capture log with every hook record carrying an id (892 records, 4 sessions\nnaming a transcript, whose trees are 180 files and 45,642,644 B): a cold harvest\nscans all 45,642,644 B; every harvest after it scans 0 B and reads 734,678 B of\nstaleness digests. Measured the same day, the real log **unmodified** — no\nrecord in it carries an id, because none was written before this existed — reads\n0 transcript bytes and does not create the cache file at all.\n\nWhat the staleness check proves is worth stating exactly, because it is not\n\"the file is unchanged\". It is `(device, inode)`, `size >= offset`, and a digest\nof the last 4096 consumed bytes. A rewrite that keeps the inode, keeps the size,\nand reproduces those 4096 bytes verbatim while altering the prefix before them\nis **not** detected. Reaching it takes a deliberate rewrite; an append cannot,\nand that is the only thing that happens to these files in normal operation. The\ndigest covers the TAIL of the consumed prefix rather than its head for exactly\nthis reason: a copy, a restored backup or an `rsync --inplace` reproduces a\ntranscript's opening records every time, so a head digest would have been the\none window such a rewrite is most likely to leave alone.\n\"\"\"\nassert s.count(old) == 1\ns = s.replace(old, new)\nio.open(p, \"w\", encoding=\"utf-8\").write(s)\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:06:51.599Z","count":1,"stdin_sample":null}
{"key":"py-e55c701792ef","id":"py-e55c701792ef","program":"import io\np = \"tests/test_capture.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = '''def test_a_main_loop_call_carries_no_agent_keys():\n    # `agent_id` is absent from the payload for a main-loop call, and its\n    # presence is what says \"this was a subagent\" — so a null would be a claim\n    # the payload never made.\n    rec = capture.record_bash_command(BASH_EVENT)\n    assert rec[\"tool_use_id\"] is None\n    assert \"agent_id\" not in rec and \"agent_type\" not in rec\n'''\nnew = '''def test_a_main_loop_call_carries_no_agent_keys():\n    # `agent_id` is absent from the payload for a main-loop call, and its\n    # presence is what says \"this was a subagent\" — so a null would be a claim\n    # the payload never made.\n    rec = capture.record_bash_command(BASH_EVENT)\n    assert rec[\"tool_use_id\"] is None\n    assert \"agent_id\" not in rec and \"agent_type\" not in rec\n\n\ndef test_only_the_claude_mapper_writes_a_join_key_no_other_harness_has():\n    \"\"\"`tool_use_id` belongs to `from_claude_event`, not to `record_command`.\n\n    It is a key into a Claude Code transcript and nothing else has one. Set in\n    the shared builder it would put a permanent `\"tool_use_id\": null` into every\n    opencode and OpenHands record, naming a join those harnesses do not have —\n    and opencode's `run` (its `callID`) is the same concept in another\n    namespace, deliberately not unified with it for the same reason.\n    \"\"\"\n    neutral = capture.record_command(\"python3 -c 1\", host=\"opencode\", tool=\"bash\",\n                                     run=\"call_1\")\n    assert \"tool_use_id\" not in neutral\n    assert neutral[\"run\"] == \"call_1\"\n    for mapper, event in (\n        (capture.from_opencode_event,\n         {\"tool\": \"bash\", \"args\": {\"command\": \"python3 -c 1\"}, \"callID\": \"call_1\"}),\n        (capture.from_openhands_event,\n         {\"tool_name\": \"terminal\", \"tool_input\": {\"command\": \"python3 -c 1\"},\n          \"session_id\": \"s\", \"working_dir\": \"/w\"}),\n    ):\n        rec = mapper(event)\n        assert rec is not None\n        assert \"tool_use_id\" not in rec\n        assert \"agent_id\" not in rec and \"agent_type\" not in rec\n    # And the Claude mapper still writes it, always, possibly null.\n    assert \"tool_use_id\" in capture.from_claude_event(BASH_EVENT)\n'''\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:24:03.016Z","count":1,"stdin_sample":null}
{"key":"py-e6be4602b328","id":"py-e6be4602b328","program":"\nimport json,collections\nc=collections.Counter()\nkeys=collections.Counter()\nfor line in open('/Users/user/.lypning/invocations.jsonl'):\n    line=line.strip()\n    if not line: continue\n    try: r=json.loads(line)\n    except: c['UNPARSEABLE']+=1; continue\n    for k in r: keys[k]+=1\n    c[r.get('transcript')]+=1\nprint('KEYS:', dict(keys))\nfor k,v in c.most_common(): print(v, k)\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:05:36.290Z","count":1,"stdin_sample":null}
{"key":"py-e6e6a78f072a","id":"py-e6e6a78f072a","program":"import json,glob,os,collections\nroot='/Users/user/.claude/projects/-Users-user-dev-lypning'\ndef bash_calls(files):\n    out={}\n    for f in files:\n        for line in open(f):\n            line=line.strip()\n            if not line: continue\n            try: r=json.loads(line)\n            except: continue\n            if r.get('type')!='assistant': continue\n            m=r.get('message',{})\n            for b in (m.get('content') or []):\n                if isinstance(b,dict) and b.get('type')=='tool_use' and b.get('name')=='Bash':\n                    out.setdefault((b.get('input') or {}).get('command',''),[]).append((f,b['id'],m.get('model')))\n    return out\nsub=bash_calls(sorted(glob.glob(root+'/*/subagents/**/agent-*.jsonl',recursive=True)))\nmain=bash_calls(sorted(glob.glob(root+'/*.jsonl')))\nlog=[json.loads(l) for l in open('/Users/user/.lypning/invocations.jsonl') if l.strip()]\nprint(\"log records:\",len(log))\ninmain=insub=inboth=neither=0\nsub_examples=[]\nfor r in log:\n    c=r.get('command','')\n    a=c in main; b=c in sub\n    if a and b: inboth+=1\n    elif a: inmain+=1\n    elif b:\n        insub+=1\n        if len(sub_examples)<6: sub_examples.append((r,sub[c][0]))\n    else: neither+=1\nprint(\"command matches MAIN only:\",inmain,\" SUB only:\",insub,\" BOTH:\",inboth,\" NEITHER:\",neither)\nfor r,(f,tid,model) in sub_examples:\n    print('---')\n    print(' log.transcript =',r['transcript'])\n    print(' log.session    =',r['session'])\n    print(' found in       =',f)\n    print(' tool_use_id    =',tid,' model=',model)\n    print(' cmd            =',r['command'][:100])","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:06:23.430Z","count":1,"stdin_sample":null}
{"key":"py-ee026f10b7b5","id":"py-ee026f10b7b5","program":"import json, collections\nk=collections.Counter()\nfor ln in open('src/lypning/assets/corpus/corpus.jsonl'):\n    ln=ln.strip()\n    if ln: k.update(json.loads(ln).keys())\nprint(k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:46.898Z","count":1,"stdin_sample":null}
{"key":"py-ef13940c12d3","id":"py-ef13940c12d3","program":"import json,glob,os\nneedle='cc_strings.txt'\nfor f in glob.glob(os.path.expanduser('~/.claude/projects/**/*.jsonl'),recursive=True):\n    for line in open(f,errors='replace'):\n        if needle not in line: continue\n        try:o=json.loads(line)\n        except:continue\n        if o.get('type')!='assistant': continue\n        m=o['message']\n        for b in m.get('content') or []:\n            if isinstance(b,dict) and b.get('type')=='tool_use' and needle in json.dumps(b.get('input','')):\n                print(\"file:\",f.replace(os.path.expanduser('~'),'~'))\n                print(\"  model:\",m.get('model'),\"tool_use_id:\",b.get('id'),\"agentId:\",o.get('agentId'),\"isSidechain:\",o.get('isSidechain'),\"sessionId:\",o.get('sessionId'),\"attributionAgent:\",o.get('attributionAgent'),\"effort:\",o.get('effort'))\n                print(\"  cmd:\",str(b['input'].get('command'))[:90].replace('\\n','\\\\n'))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:29:03.901Z","count":1,"stdin_sample":null}
{"key":"py-f289e2c1abc8","id":"py-f289e2c1abc8","program":"import io\np=\"/tmp/mut/src/lypning/cli.py\"; s=io.open(p,encoding=\"utf-8\").read()\nold=\"        sightings = harvest.collect(transcripts=ns.transcripts, persist=False)\\n\"\nassert s.count(old)==1\nio.open(p,\"w\",encoding=\"utf-8\").write(s.replace(old,\"        sightings = harvest.collect(transcripts=ns.transcripts)\\n\"))\nprint(\"F5 mutated: dry-run persists again\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T10:02:22.992Z","count":1,"stdin_sample":null}
{"key":"py-f454091d0b28","id":"py-f454091d0b28","program":"\nimport sys,json\nfor l in sys.stdin:\n    o=json.loads(l)\n    print(o['kind'],'| session',o.get('session'),'| transcript',o.get('transcript'))\n    print('   cmd:',str(o.get('command'))[:80].replace(chr(10),'\\\\n'))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:29:24.116Z","count":1,"stdin_sample":null}
{"key":"py-f4592239e4a7","id":"py-f4592239e4a7","program":"import json, os, shutil, subprocess, sys, time\nfrom pathlib import Path\nSRC=\"/Users/user/dev/lypning-capture-model/src\"\nROOT=Path(\"/tmp/pt/e2e/exprace\")\nif ROOT.exists(): shutil.rmtree(str(ROOT))\n(ROOT/\"home\").mkdir(parents=True); (ROOT/\"project\").mkdir()\n\n# one shared log with many sessions and many programs -> big sightings files\nrecs=[]\nfor si in range(3):\n    for j in range(400):\n        recs.append({\"kind\":\"bash_command\",\"ts\":\"2026-09-01T10:00:00.000Z\",\"session\":\"sess%d\"%si,\n                     \"command\":\"python3 -c 'import sys; x=%d; print(sys.maxsize*x)'\"%(si*1000+j)})\nlog=ROOT/\"log.jsonl\"\nlog.write_text(\"\\n\".join(json.dumps(r) for r in recs)+\"\\n\", encoding=\"utf-8\")\n\nW = r'''\nimport os, sys\nsys.path.insert(0, %r)\nos.environ[\"LYPNING_HOME\"]=sys.argv[1]; os.environ[\"CLAUDE_PROJECT_DIR\"]=sys.argv[2]; os.environ[\"LYPNING_LOG\"]=sys.argv[3]\nfrom lypning import harvest\nfor i in range(6):\n    harvest._export()\nprint(\"OK\")\n''' % (SRC,)\nw=ROOT/\"w.py\"; w.write_text(W, encoding=\"utf-8\")\nps=[subprocess.Popen([sys.executable,str(w),str(ROOT/\"home\"),str(ROOT/\"project\"),str(log)],\n    stdout=subprocess.PIPE,stderr=subprocess.STDOUT,text=True) for _ in range(6)]\nfor p in ps:\n    o,_=p.communicate()\n    if p.returncode: print(\"worker fail\", p.returncode, o[-500:])\nd=ROOT/\"project\"/\"tests\"/\"corpus\"/\"sightings\"\nbad=0\nfor f in sorted(d.iterdir()):\n    n=0\n    for k,line in enumerate(f.read_text(encoding=\"utf-8\", errors=\"replace\").splitlines(),1):\n        try: json.loads(line)\n        except Exception as e:\n            bad+=1; print(\"CORRUPT\", f.name, \"line\", k, str(e)[:80], repr(line[:80]))\n        n+=1\n    print(f.name, n, \"lines\")\nprint(\"corrupt lines:\", bad)\nprint(\"stray tmp:\", sorted(p.name for p in d.iterdir() if p.name.endswith(\".tmp\")))","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T07:43:53.803Z","count":1,"stdin_sample":null}
{"key":"py-f63b02d8ea11","id":"py-f63b02d8ea11","program":"\nimport json\nfor l in open('/tmp/hookprobe/events4.jsonl'):\n    o=json.loads(l)\n    if o.get('hook_event_name')=='SubagentStop':\n        print('agent_transcript_path =',o.get('agent_transcript_path'))\n    if o.get('hook_event_name')=='PostToolBatch':\n        for tc in o.get('tool_calls',[]):\n            if tc.get('tool_name')=='Agent':\n                print('Agent tool_input =',json.dumps({k:v for k,v in tc['tool_input'].items() if k!='prompt'}))\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:36:00.729Z","count":1,"stdin_sample":null}
{"key":"py-f8c90d77f79a","id":"py-f8c90d77f79a","program":"\nimport re\nd=open('/tmp/cc258.strings','rb').read()\nfor m in list(re.finditer(rb'transcript_path:',d))[:4]:\n    print(d[max(0,m.start()-250):m.start()+250].decode('utf8','replace')); print('-'*50)\n","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T00:15:51.757Z","count":1,"stdin_sample":null}
{"key":"py-f8fb8dbad84f","id":"py-f8fb8dbad84f","program":"import re\np='CHANGELOG.md'\ns=open(p).read()\npat=re.compile(r'<<<<<<< HEAD\\n(.*?)\\n\\|\\|\\|\\|\\|\\|\\| [^\\n]*\\n(.*?)=======\\n(.*?)>>>>>>> [^\\n]*\\n', re.S)\nm=pat.search(s)\nhd, base, ours = m.group(1), m.group(2), m.group(3)\n# ours (the model-attribution entry) newest-first, then #25's entry\nmerged = ours.rstrip('\\n') + '\\n\\n' + hd.rstrip('\\n') + '\\n'\ns = s[:m.start()] + merged + s[m.end():]\nopen(p,'w').write(s)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T14:48:23.599Z","count":1,"stdin_sample":null}
{"key":"py-f9b492b1617a","id":"py-f9b492b1617a","program":"import re\nd=open('/tmp/cc_strings.txt','rb').read()\nfor m in re.finditer(rb'hook_event_name:\"PreToolUse\"', d):\n    i=m.start()\n    print(\"=== @\",i)\n    print(d[i-700:i+500].decode('utf-8','replace'))\n    print()","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:20:43.005Z","count":1,"stdin_sample":null}
{"key":"py-fc142da7ee45","id":"py-fc142da7ee45","program":"import io\np = \"src/lypning/harvest.py\"\ns = io.open(p, encoding=\"utf-8\").read()\nold = \"\"\"# The catch, measured on this machine on 2026-09-02: `transcript_path` always\n# names the MAIN session file, but a Bash call issued by a SUBAGENT has its\n# tool_use block only under `<session>/subagents/**/agent-*.jsonl`, and most of\n# the python captured in this project comes from subagents. An index built from\n# `transcript_path` alone would therefore leave the majority of it unresolved —\n# and a time join over the main file alone would do something worse, attributing\n# a subagent's program to the parent loop's model, which is exactly the silent\n# wrong answer this join exists to avoid. Both are indexed, which is safe\n# because a tool_use id is unique across transcripts: over this machine's 152\n# transcript files on 2026-09-02, 2751 distinct ids and not one of them in two\n# files, so the union of two indexes cannot collide and a re-scan cannot\n# double-count.\n#\n# THE COLD PATH IS NOT A FREE PATH. `_export` runs on every Stop — every turn\n# boundary — and the log it reads is append-only, so the set of transcripts a\n# harvest asks about never shrinks: it accumulates one more session for as long\n# as the log lives. Re-indexing all of them from byte zero every time cost 1.86 s\n# per turn boundary on a copy of this machine's log on 2026-09-02 (750 records,\n# 4 sessions, 39.1 MB of transcript trees, best of 9 interleaved runs) against\n# 0.70 s for the same log with nothing to join at all — and that gap is a\n# function of the log's AGE, not of the work the turn did. So the index is\n# incremental, and what makes an incremental read correct here is the one\n# property these files have: a transcript is append-only JSONL. Bytes already\n# read never change, which makes a byte offset a complete description of what\n# has been seen, and the delta at the next turn boundary is usually a few\n# kilobytes and often nothing at all. Warm, the same harvest measured 0.97 s.\n\"\"\"\nnew = \"\"\"# The catch: `transcript_path` always names the MAIN session file, but a Bash\n# call issued by a SUBAGENT has its tool_use block only under\n# `<session>/subagents/**/agent-*.jsonl`, and most of the python captured in\n# this project comes from subagents — over the 194 transcript files under this\n# machine's `~/.claude/projects` on 2026-09-02, 895 python-ish Bash `tool_use`\n# blocks, 716 of them (80.0%) in a subagent file. An index built from\n# `transcript_path` alone would therefore leave four fifths of it unresolved —\n# and a time join over the main file alone would do something worse, attributing\n# a subagent's program to the parent loop's model, which is exactly the silent\n# wrong answer this join exists to avoid. Both are indexed, which is safe\n# because a tool_use id is unique across transcripts: over those same 194 files,\n# 4301 distinct ids and not one of them in two files, so the union of two\n# indexes cannot collide and a re-scan cannot double-count.\n#\n# THE COLD PATH IS NOT A FREE PATH. `_export` runs on every Stop — every turn\n# boundary — and the log it reads is append-only, so the set of transcripts a\n# harvest asks about never shrinks: it accumulates one more session for as long\n# as the log lives. The cost is quoted in BYTES READ rather than in seconds\n# deliberately: this machine is shared with other agent sessions and its load\n# average reaches the hundreds, so a wall clock here measures the machine. On\n# 2026-09-02, over a copy of this machine's capture log (892 records, 4 sessions\n# naming a transcript, whose trees are 180 files and 45,642,644 B) with every\n# hook record carrying an id: a cold harvest scans all 45,642,644 B, and each\n# harvest after it scans 0 B and reads 734,678 B of staleness digests — 62x less\n# I/O, and flat in the age of the log rather than linear in it.\n#\n# What makes an incremental read correct here is the one property these files\n# have: a transcript is append-only JSONL. Bytes already read never change,\n# which makes a byte offset a complete description of what has been seen, and\n# the delta at the next turn boundary is usually a few kilobytes and often\n# nothing at all.\n#\n# And a log that asks nothing pays nothing: measured the same day, the same real\n# log UNMODIFIED — no record in it carries an id, because none was written\n# before this existed — reads 0 transcript bytes and does not create the cache\n# file at all.\n\"\"\"\nassert s.count(old) == 1\nio.open(p, \"w\", encoding=\"utf-8\").write(s.replace(old, new))\nprint(\"ok\")","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:06:18.542Z","count":1,"stdin_sample":null}
{"key":"py-feed3aa86183","id":"py-feed3aa86183","program":"\n\ndef test_harvest_dry_run_writes_nothing_under_the_state_dir(capsys):\n    \"\"\"Invariant 7: `--dry-run` is real — it opens files and writes none.\n\n    The transcript index cache is a write, it lives under `$LYPNING_HOME`, and\n    the model join reaches it through `collect()`. Before this was threaded, a\n    dry run left `model-index.json` behind.\n    \"\"\"\n    from lypning import harvest, paths\n\n    root = Path(str(paths.state_dir())) / \"projects\" / \"-tmp-p\"\n    root.mkdir(parents=True, exist_ok=True)\n    main = root / \"sess.jsonl\"\n    main.write_text(json.dumps({\n        \"type\": \"assistant\", \"timestamp\": \"2026-09-02T10:00:00.000Z\",\n        \"message\": {\"model\": \"claude-fable-5-1\", \"content\": [\n            {\"type\": \"tool_use\", \"id\": \"toolu_a\", \"name\": \"Bash\",\n             \"input\": {\"command\": \"python3 -c 'print(1)'\"}}]}}) + \"\\n\", encoding=\"utf-8\")\n    log = paths.log_path()\n    paths.ensure_dir(log.parent)\n    log.write_text(json.dumps({\n        \"kind\": \"bash_command\", \"session\": \"sess\", \"ts\": \"2026-09-02T10:00:00.500Z\",\n        \"command\": \"python3 -c 'print(1)'\", \"transcript\": str(main),\n        \"tool_use_id\": \"toolu_a\"}) + \"\\n\", encoding=\"utf-8\")\n\n    state = paths.state_dir()\n    before = sorted(p.name for p in state.rglob(\"*\"))\n    assert cli.main([\"harvest\", \"--dry-run\", \"--json\"]) == 0\n    report = json.loads(capsys.readouterr().out)\n    assert report[\"mode\"] == \"dry-run\" and report[\"sightings\"] >= 1\n    assert sorted(p.name for p in state.rglob(\"*\")) == before\n    assert not harvest._index_cache_path().exists()","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-02T09:55:05.122Z","count":1,"stdin_sample":null}
{"key":"py-ff283f522442","id":"py-ff283f522442","program":"import json,collections\nc=collections.Counter()\nfor l in open('src/lypning/assets/corpus/corpus.jsonl'):\n    l=l.strip()\n    if l: c[tuple(json.loads(l).keys())]+=1\nfor k,v in c.most_common(): print(v,k)","argv_tail":[],"source":"hook","session":"93a4fe97-435b-53ac-9885-bd9f9011489f","first_seen":"2026-09-01T19:21:57.724Z","count":1,"stdin_sample":null}
