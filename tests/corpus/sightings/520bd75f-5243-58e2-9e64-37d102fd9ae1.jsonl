{"key":"py-00b782b132c9","id":"py-00b782b132c9","program":"import pathlib\np = pathlib.Path(\"CHANGELOG.md\"); s = p.read_text()\nanchor = \"## Unreleased\\n\\n\"\nentry = '''## Unreleased\n\n**2026-09-15** — Close four engine mismatches from the Stage 0a replay ledger ([claude/next-round-7dilm6](https://github.com/kristerhedfors/lypning/compare/main...claude/next-round-7dilm6))\n\n- `enumerate(iterable=…, start=…)` binds both of Argument Clinic's names, so\n  the first spelling answers instead of dying with a TypeError CPython never\n  raises; the same parameter given by name and position is the given-twice\n  TypeError CPython raises.\n- `divmod` over a float and a zero divisor says `float divmod()`, the wording\n  `float_divmod` owns, rather than `//`'s `float floor division by zero`;\n  `zero_div` folds it into the 3.14 respelling with the rest.\n- A `\\\\u` or `\\\\U` escape naming a lone surrogate refuses as `escape` rather\n  than dying as a SyntaxError CPython does not raise: CPython compiles the\n  literal and no UTF-8 string here can hold it.\n- `is` between two dict views refuses as `dict-view`: the value carries the\n  dict's `Rc`, not the view's, so neither True nor False was a fact.\n- Manual Qwen round-02 handoff, first session: engine built against 3.12,\n  planner and starter preparation exercised, ledger audited; no training,\n  no Docker boundary and no GPU on this worker, so every launch gate that\n  needs an operator, a candidate image or hardware remains open.\n\n'''\nassert s.count(anchor) == 1\np.write_text(s.replace(anchor, entry, 1)); print(\"changelog written\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:41:17.343Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-04128d7ae43e","id":"py-04128d7ae43e","program":"import pytest; print('pytest', pytest.__version__)","argv_tail":[">","work/round-02/uv.log","2>"],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:45:09.041Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-0e636abae346","id":"py-0e636abae346","program":"import pathlib\ndef sub(path, old, new):\n    p = pathlib.Path(path); s = p.read_text(); assert s.count(old) == 1, (path, old[:60]); p.write_text(s.replace(old, new)); print(\"edited\", path)\n\nsub(\"src/lypning/assets/rust/src/ops.rs\",\n'''        step: Option<Value>,\n    ) -> R<Value> {\n        let step = match &step {\n            None | Some(Value::None) => 1i64,''',\n'''        step: Option<Value>,\n    ) -> R<Value> {\n        // `d[:2]` is a SLICE used as a dict KEY, and CPython never validates it\n        // as a slice: the lookup hashes it first. Slices became hashable in\n        // 3.12, so the answer there is `KeyError: slice(None, 2, None)` — the\n        // repr of the three ORIGINAL objects, whatever their types — and before\n        // that the hashing itself is `TypeError: unhashable type: 'slice'`.\n        // This fell through to `not_subscriptable`, a message CPython never\n        // prints for a dict and the wrong CLASS from 3.12 on. Found by the\n        // corpus battery against a 3.12 reference (four sightings slicing a\n        // seeded JSON object), where CI's 3.11 reference agreed on the class.\n        if let Value::Dict(_) = base {\n            if REF_PY_MINOR >= 12 {\n                let r = |v: &Option<Value>| -> R<String> {\n                    match v {\n                        None => Ok(\"None\".to_string()),\n                        Some(x) => fmt::repr(x),\n                    }\n                };\n                return Err(key_err(format!(\"slice({}, {}, {})\", r(&lo)?, r(&hi)?, r(&step)?)));\n            }\n            return Err(type_err(\"unhashable type: 'slice'\"));\n        }\n        let step = match &step {\n            None | Some(Value::None) => 1i64,''')\n\nsub(\"src/lypning/assets/rust/src/eval.rs\",\n'''            Target::Slice { base, lo, hi } => {\n                let bv = self.eval(base)?;\n                let Value::List(l) = &bv else {''',\n'''            Target::Slice { base, lo, hi } => {\n                let bv = self.eval(base)?;\n                // `d[:2] = v` STORES a slice as a dict key from 3.12 on, a key\n                // this engine has no value for; before 3.12 the key cannot be\n                // hashed. Neither is the `does not support slice assignment`\n                // below, which CPython says of a tuple and never of a dict.\n                // The lookup half is `ops::slice`.\n                if let Value::Dict(_) = &bv {\n                    if REF_PY_MINOR >= 12 {\n                        return Err(unsupported(\"slice-key\", \"a slice stored as a dict key\"));\n                    }\n                    return Err(type_err(\"unhashable type: 'slice'\"));\n                }\n                let Value::List(l) = &bv else {''')\n\nt = \"tests/test_semantics.py\"\nsub(t, '''    (\n        # Argument Clinic gives `enumerate` TWO names, and the keyword guard''',\n'''    (\n        # A slice as a dict KEY. CPython hashes the key before it validates\n        # anything, and slices became hashable in 3.12 — so `d[:2]` is\n        # `KeyError: slice(None, 2, None)` there and `TypeError: unhashable\n        # type: 'slice'` before, and the message shows the ORIGINAL objects,\n        # whatever their types. The engine said `'dict' object is not\n        # subscriptable`: the wrong words on every version and the wrong class\n        # from 3.12 on. Four sightings slicing a seeded JSON object found it,\n        # against the 3.12 reference round-02 pins; CI's 3.11 agreed on the\n        # class and never saw it.\n        \"a-slice-as-a-dict-key-says-what-the-host-says\",\n        \"d = {'a': 1}\\\\n\"\n        \"for label, fn in (('d[:2]', lambda: d[:2]), ('d[1:2:3]', lambda: d[1:2:3]),\\\\n\"\n        \"                  (\\\\\"d['a':]\\\\\", lambda: d['a':]), ('d[::0]', lambda: d[::0]),\\\\n\"\n        \"                  ('d[None:None]', lambda: d[None:None])):\\\\n\"\n        \"    try:\\\\n\"\n        \"        fn()\\\\n\"\n        \"        print(label, 'no error')\\\\n\"\n        \"    except Exception as e:\\\\n\"\n        \"        print(label, '|', type(e).__name__, '|', e)\\\\n\",\n    ),\n    (\n        # Argument Clinic gives `enumerate` TWO names, and the keyword guard''')\n\nsub(t, '''def test_is_between_two_dict_views_refuses_rather_than_guessing(lypning_bin):''',\n'''def test_storing_a_slice_as_a_dict_key_refuses_or_raises_as_the_host_does(lypning_bin):\n    \"\"\"`d[:2] = 5` stores a slice key from 3.12 on — a key this engine has no\n    value for — and is `TypeError: unhashable type: 'slice'` before that. The\n    engine answered `'dict' object does not support slice assignment` on every\n    version, a sentence CPython says of a tuple and never of a dict. Which\n    half applies is decided by the reference the binary was built against, so\n    the test asks CPython first rather than pinning a version.\n    \"\"\"\n    program = \"d = {'a': 1}\\\\nd[:2] = 5\\\\nprint(sorted(map(str, d)))\"\n    theirs = engines.run(engines.CPYTHON, program)\n    if theirs.returncode == 127:\n        pytest.skip(\"no reference CPython\")\n    ours = engines.run(engines.LYPNING, program, binary=lypning_bin)\n    if theirs.returncode == 0:\n        assert ours.returncode == UNSUPPORTED_EXIT, \"answered %r instead of refusing\" % ours.stdout\n        assert ours.stdout == \"\" and \"slice-key\" in ours.stderr, ours.stderr\n    else:\n        assert ours.returncode == 1 and \"unhashable type: 'slice'\" in ours.stderr, ours.stderr\n        assert ours.stdout == theirs.stdout\n\n\ndef test_is_between_two_dict_views_refuses_rather_than_guessing(lypning_bin):''')\n\nsub(\"CHANGELOG.md\",\n'''- `is` between two dict views refuses as `dict-view`: the value carries the\n  dict's `Rc`, not the view's, so neither True nor False was a fact.''',\n'''- `is` between two dict views refuses as `dict-view`: the value carries the\n  dict's `Rc`, not the view's, so neither True nor False was a fact.\n- A slice as a dict key says what the host says: `KeyError` with the slice's\n  repr from 3.12, where slices hash, and `unhashable type: 'slice'` before;\n  storing one refuses as `slice-key`. Four corpus sightings found it against\n  the 3.12 reference this round pins; CI's 3.11 reference agreed on the class.''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:08:12.596Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-101a576d1808","id":"py-101a576d1808","program":"import json\nseen=set()\nfor l in open(\"nemotron/data/engine-mismatches.jsonl\"):\n    r=json.loads(l)\n    key=(r[\"case_id\"], r[\"program\"])\n    if key in seen: continue\n    seen.add(key)\n    print(\"=====\", r[\"case_id\"], \"|\", r[\"found_by\"][:60])\n    print(r[\"program\"][:1500])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:36:18.481Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-107dbdeb09dc","id":"py-107dbdeb09dc","program":"import json, glob, subprocess, tempfile, os, time, shutil\nenv = dict(os.environ, LYPNING_CAPTURE=\"0\", LYPNING_HARVEST=\"0\")\nprogs = {}\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r = json.loads(l)\n        if r.get(\"id\") in (\"py-2fa2dc1d1f8f\", \"py-c667db469d9e\"): progs[r[\"id\"]] = r[\"program\"]\nbins = {\"base\": \"/home/user/lypning/work/base-home/bin/lypning\", \"new\": os.path.expanduser(\"~/.lypning/bin/lypning\")}\nfor pid, prog in progs.items():\n    best = {k: 1e9 for k in bins}; allr = {k: [] for k in bins}\n    for rnd in range(3):\n        for k, b in bins.items():\n            d = tempfile.mkdtemp(); t = time.perf_counter()\n            subprocess.run([b, \"-c\", prog], cwd=d, capture_output=True, env=env)\n            ms = (time.perf_counter() - t) * 1000; allr[k].append(round(ms)); best[k] = min(best[k], ms); shutil.rmtree(d, ignore_errors=True)\n    print(pid, \"best\", {k: round(v) for k, v in best.items()}, \"all\", allr)","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:37:15.780Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-13e4f9c056e2","id":"py-13e4f9c056e2","program":"import pathlib, re\nN = \"nemotron/\"\ndef sub(path, old, new, count=1):\n    p = pathlib.Path(path); s = p.read_text()\n    c = s.count(old); assert c == count, (path, c, old[:60])\n    p.write_text(s.replace(old, new)); print(\"edited\", path)\n\n# --- nemotron/README.md: one pointer, not two paragraphs of history\nsub(N+\"README.md\", '''**The target model is `Qwen/Qwen3.8-27B`. The directory is called `nemotron/` and\nits CLI `nt` for historical reasons only** — the pipeline was built against\nNVIDIA Nemotron 3.5 Lightning and switched on 2026-09-11 (`SWITCH.md`), and the\nnames stayed because `CLAUDE.md`, the tests and the site cite them. Renaming them\nwould edit a dozen files to change nothing that runs. Every measurement in this\ntree is Qwen's unless its own provenance line says otherwise; a Nemotron number\nis never a decision input for a Qwen arm, and `LADDER.md` §0 lists the three that\nleaked into one anyway.\n\nThe original target — for reading the history, never for planning:\n`nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-BF16` (30B total, 3B active,\nhybrid Mamba-2 + MoE + attention, reasoning on by default). The NVFP4 release is\ninference-only and is not the training checkpoint.\n''', '''**The target model is `Qwen/Qwen3.8-27B`.** The directory and its `nt` CLI\npredate the target switch of 2026-09-11; `SWITCH.md` is the one place the\nearlier model is named, and it lists the numbers from that era that must never\nbe read as Qwen's. Every measurement in this tree is Qwen's unless its own\nprovenance line says otherwise.\n''')\nsub(N+\"README.md\", '''3. **Training** — Qwen rank-16 LoRA; the standalone historical runner is\n   `gpu/lypning_lora.py`, not the earlier Nemotron NeMo recipe.''',\n'''3. **Training** — Qwen rank-16 LoRA; the standalone historical runner is\n   `gpu/lypning_lora.py`.''')\nsub(N+\"README.md\", '''export NTX_BASE_URL=http://127.0.0.1:8000/v1     # vLLM serving the BF16 checkpoint\nexport NTX_MODEL=nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-BF16''',\n'''export NTX_BASE_URL=http://127.0.0.1:8000/v1     # vLLM serving the BF16 checkpoint\nexport NTX_MODEL=Qwen/Qwen3.8-27B''')\nsub(N+\"README.md\", '''Serving recipe from the model card (vLLM `v0.27.1`, single H100 80GB, BF16):\n`--reasoning-parser nemotron_v3` splits thinking out of `content` for us. Sampling\nfollows the card: temperature 1.0, top_p 0.95. `--no-thinking` sets\n`chat_template_kwargs.enable_thinking=false`.''',\n'''Serve with a reasoning parser so thinking is split out of `content`\n(`--reasoning-parser qwen3` on vLLM); the extractor also handles a server that\nhands thinking back inline. Sampling defaults are `pipeline.backends`'s\n(temperature 1.0, top_p 0.95); `--no-thinking` sets\n`chat_template_kwargs.enable_thinking=false`.''')\n\n# --- LADDER.md §0: the retired table moves to SWITCH.md\np = pathlib.Path(N+\"LADDER.md\"); s = p.read_text()\nstart = s.index(\"## 0. Ground rules — Qwen, not Nemotron\")\nend = s.index(\"## 1. What \\\"adapted to our interpreter\\\" means\")\nnew0 = '''## 0. Ground rules\n\nThe target is `Qwen/Qwen3.8-27B` (`SWITCH.md`). Most of the apparatus carried\nover from the earlier target unchanged: the corpus, the case gates, the frozen\nsplit, the engine fingerprint, `nt legality`, gates A/B/C, the serving-stack\nprecondition, and the deployment argument — the harness model must be the tuned\nmodel, which means opencode or OpenHands pointed at the tuned Qwen.\n\nA number measured on the earlier model is not a weaker Qwen number; it is a\nnumber about a different experiment, and its only safe use is history. Three\nsuch numbers had been used as decision inputs here; `SWITCH.md` lists them\nunder *Retired numbers* with what replaces each. The Qwen noise floors are the\ntwo serving-stack nulls, **−0.22pp** and **+1.57pp** ΔSLR (`REVIEW.md` §1); the\nQwen thinking budget and the thinking-on effect on this eval are their own\nmeasurements, still unmeasured.\n\n'''\np.write_text(s[:start] + new0 + s[end:]); print(\"edited\", N+\"LADDER.md\")\n\n# --- SWITCH.md: the one home\nsub(N+\"SWITCH.md\", '''# Target model: Qwen/Qwen3.8-27B\n\nSwitched 2026-09-11 from `nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-BF16`.''',\n'''# Target model: Qwen/Qwen3.8-27B\n\nSwitched 2026-09-11 from `nvidia/NVIDIA-Nemotron-3.5-Lightning-30B-A3B-BF16`.\n**This file is the one home of the earlier target's name.** Every other\ndocument says *the earlier model* and points here; run directories and data\nfiles under `runs/` and `data/` keep the names they were recorded under,\nbecause a provenance line is a measurement too.\n\n## Retired numbers\n\nMeasured on the earlier model, used as decision inputs for Qwen anyway, and\nretired from `LADDER.md` §0 on 2026-09-14:\n\n| number | where it came from | why it does not apply |\n|---|---|---|\n| pass@1 35.1%, rewrite slice 17.3% thinking-on vs 9.6% off | the earlier model on vLLM/A100, 2026-09-11 | different model, different serving stack. It was used to argue for thinking-on eval; for Qwen the thinking effect on this eval is **unmeasured** |\n| \"12 of 74 cases flip between thinking on/off on an identical checkpoint\" | the same run | it was used as the noise floor. The Qwen noise floors are the two serving-stack nulls: **−0.22pp** and **+1.57pp** ΔSLR (`REVIEW.md` §1) |\n| 505,576 thinking tokens vs 22,254 | the same run | the Qwen thinking budget is its own measurement |''')\n\n# --- PREREGISTRATION.md\nsub(N+\"PREREGISTRATION.md\", '''`keep=4`. That file is Nemotron's (`SWITCH.md` §98 says so in this same tree:\n\"off-policy for Qwen … must be re-sampled\"), so it was Nemotron's yield''',\n'''`keep=4`. That file is the earlier model's (`SWITCH.md` §98 says so in this\nsame tree: \"off-policy for Qwen … must be re-sampled\"), so it was that model's yield''')\nsub(N+\"PREREGISTRATION.md\", \"| `headroom-k16` | Nemotron | 12 of 43 (27.9%) | 0.119 | ~43 |\",\n    \"| `headroom-k16` | the earlier model (`SWITCH.md`) | 12 of 43 (27.9%) | 0.119 | ~43 |\")\nsub(N+\"PREREGISTRATION.md\", \"Qwen out-yields Nemotron by 1.33x\", \"Qwen out-yields the earlier model by 1.33x\")\nsub(N+\"PREREGISTRATION.md\", '''never once passed in Qwen's 16 draws; the \"55 of 66\" first written here was\nNemotron's rate on the train pool''', '''never once passed in Qwen's 16 draws; the \"55 of 66\" first written here was\nthe earlier model's rate on the train pool''')\n\n# --- gpu/README.md (retired plan)\nsub(N+\"gpu/README.md\", \"> `a3-highgpu-8g`, Nemotron 3.5 Lightning, and `nt-gpu`, none of which is the\",\n    \"> `a3-highgpu-8g`, the earlier target model, and `nt-gpu`, none of which is the\")\nsub(N+\"gpu/README.md\", '''> [\"*.out_proj\"]` — correct for Nemotron, wrong for Qwen3.5, see `../SWITCH.md`''',\n    '''> [\"*.out_proj\"]` — correct for the earlier model, wrong for Qwen3.5, see `../SWITCH.md`''')\n\n# --- gpu/lora_qwen38_27b.yaml comments\nsub(N+\"gpu/lora_qwen38_27b.yaml\", '''#   parameter is active on every token, unlike Nemotron 3.5 Lightning's\n#   30B-A3B where only ~3B are. That is roughly 9x the FLOPs per token for the\n#   same step count, and it is the single most important difference between\n#   this recipe and the Nemotron one beside it.''',\n'''#   parameter is active on every token, unlike the earlier MoE target\n#   (SWITCH.md) where only ~3B of 30B were. That is roughly 9x the FLOPs per\n#   token for the same step count, and it is the single most important\n#   difference between this recipe and the earlier one.''')\nsub(N+\"gpu/lora_qwen38_27b.yaml\", \"  # The Nemotron recipe overrides num_nextn_predict_layers / mtp_use_repeated_layer\",\n    \"  # The earlier recipe overrides num_nextn_predict_layers / mtp_use_repeated_layer\")\nsub(N+\"gpu/lora_qwen38_27b.yaml\", '''    # is dense. Setting ep_size or a dispatcher here would be cargo-culted from\n    # the Nemotron recipe.''', '''    # is dense. Setting ep_size or a dispatcher here would be cargo-culted from\n    # the earlier recipe.''')\nsub(N+\"gpu/lora_qwen38_27b.yaml\", '''  # The Nemotron cookbook excludes it for a real reason: nemotron_v3/layers.py\n  # passes `outproj_weight=self.out_proj.weight` INTO a fused kernel (layers.py\n  # :454-455), so a LoRA wrapping the module is bypassed.''',\n'''  # The earlier model's cookbook excludes it for a real reason (SWITCH.md): its\n  # layers pass `outproj_weight=self.out_proj.weight` INTO a fused kernel, so a\n  # LoRA wrapping the module is bypassed.''')\n\n# --- gpu/lypning_lora.py comments\nsub(N+\"gpu/lypning_lora.py\", '''       here for that reason until 2026-09-13, on a rationale copied from the\n       Nemotron cookbook -- where it is correct, because nemotron_v3/layers.py\n       :454-455 passes `outproj_weight=self.out_proj.weight` into the kernel.''',\n'''       here for that reason until 2026-09-13, on a rationale copied from the\n       earlier model's cookbook -- where it is correct, because that model's\n       layers pass `outproj_weight=self.out_proj.weight` into the kernel\n       (SWITCH.md).''')\nsub(N+\"gpu/lypning_lora.py\", '''    # a full-price run on the wrong data by omitting a flag: sft/v1 was\n    # rejection-sampled from Nemotron, is off-policy for Qwen, and its first''',\n'''    # a full-price run on the wrong data by omitting a flag: sft/v1 was\n    # rejection-sampled from the earlier model, is off-policy for Qwen, and its first''')\n\n# --- pipeline strings and docstrings\nsub(N+\"pipeline/__init__.py\", '\"\"\"Steps 1-2 of the Nemotron LoRA pipeline: corpus and evaluation.',\n    '\"\"\"Steps 1-2 of the LoRA pipeline: corpus and evaluation.')\nsub(N+\"pipeline/cli.py\", 'description=\"Nemotron LoRA pipeline: corpus and eval\"', 'description=\"LoRA pipeline: corpus and eval\"')\nsub(N+\"pipeline/extract.py\", '''Nemotron 3.5 Lightning is a reasoning model with thinking ON by default, so a\nraw completion is reasoning followed by an answer. Two ways that arrives: a\nserver started with ``--reasoning-parser nemotron_v3`` splits it into''',\n'''Qwen3.8 is a reasoning model with thinking ON by default, so a raw completion\nis reasoning followed by an answer. Two ways that arrives: a server started\nwith a reasoning parser (``--reasoning-parser qwen3`` on vLLM) splits it into''')\nsub(N+\"pipeline/stats.py\", '''    A model name is free text chosen by whoever launched the run, so it is not\n    an identity: in this tree's own recorded runs two different endpoints both\n    answer to \"nemotron\".''',\n'''    A model name is free text chosen by whoever launched the run, so it is not\n    an identity: in this tree's own recorded runs two different endpoints both\n    answer to one model name.''')\nsub(N+\"pipeline/backends.py\", \"            # Nemotron's chat template reads this; vLLM/SGLang forward it.\",\n    \"            # Qwen's chat template reads this; vLLM/SGLang forward it.\")\n\n# --- tests\nsub(N+\"tests/test_money_path.py\", '''    transformers modeling_qwen3_5.py:662, declared nn.Linear at :540); Nemotron\n    passes the weight into a kernel, which is where the exclusion came from and\n    where it belongs.''',\n'''    transformers modeling_qwen3_5.py:662, declared nn.Linear at :540); the\n    earlier model (SWITCH.md) passes the weight into a kernel, which is where\n    the exclusion came from and where it belongs.''')\nsub(N+\"tests/test_recorded.py\", \"    # against; the Nemotron baselines above are history from before the switch.\",\n    \"    # against; the baselines above from before the switch are history.\")\np = pathlib.Path(N+\"tests/test_comparability.py\"); s = p.read_text()\nn1 = s.count('\"nemotron\"'); n2 = s.count(\"nemotron-lora-v1\"); n3 = s.count(\"nemotron @\")\ns = s.replace('\"nemotron\"', '\"qwen38\"').replace(\"nemotron-lora-v1\", \"qwen38-lora-v1\").replace(\"nemotron @\", \"qwen38 @\")\ns = s.replace(\"one model name nemotron\", \"one model name qwen38\").replace(\"recorded model 'nemotron'\", \"recorded model 'qwen38'\")\np.write_text(s); print(\"edited test_comparability.py\", n1, n2, n3)","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:39:29.720Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-1f554133c2f9","id":"py-1f554133c2f9","program":"import pathlib\ne = pathlib.Path(\"/tmp/claude-0/-home-user-lypning/520bd75f-5243-58e2-9e64-37d102fd9ae1/scratchpad/ledger-entry.md\").read_text()\nfills = {\n \"CORPUS_LOADED loaded, CORPUS_GRADED graded\": \"6,317 programs graded, 2,747 skipped by the tool's own split (9,064 loaded)\",\n \"CONFORMANCE_LINE\": \"`lypning` 2923 MATCH / 3394 UNSUPPORTED / **0 MISMATCH**; `lypning-l` 4560 / 1757 / **0**; both dispatchers 6317 / 0 / 0 and **agree 6317/6317**; routing IDEAL 5979, WASTED 227, LATE 111, **UNSAFE 0**. MATCH did not move — none of the five is a program the corpus has, and the grader compares exception classes, which is the point of the last section.\",\n \"CORPUS_TIME_LINE\": \"`corpus-time --repeat 3` against main's binary over the 6,323 programs both timed: 14.10 → 15.91 s, **1.128× slower on its face**. 1.66 s of the 1.81 s is two programs that create 60,000 and 22,000 files in the sandbox; re-timed interleaved on the idle box, the 60,000-file one spans 2.8–12.2 s on the BASE binary alone across three rounds, and the 22,000-file one is 1769 ms base against 1705 ms new. That is the filesystem, not the engine, and the only change on that path is one keyword lookup per `open`. The other 6,321 programs moved +0.15 s on ~12.4 s, +1.2%, inside the ±3% deadband: **not slower**.\",\n \"PYTEST_LINE\": \"8375 passed, 553 skipped, 0 failed\",\n \"NEMOTRON_LINE\": \"409 passed, 1 failed — `test_setup_failure_is_distinct_from_program_exit_127`, which this container fails on every branch because its `unshare` reports a missing interpreter as exit 127 itself\",\n \"DOCTOR_LINE\": \"19 checks, 0 FAIL, 3 WARN\",\n \"GATE_LINE\": \"PASS, both variants inside their block budgets\",\n}\nfor k, v in fills.items():\n    assert e.count(k) == 1, k\n    e = e.replace(k, v)\np = pathlib.Path(\"docs/HILLCLIMB.md\"); s = p.read_text()\nmarker = \"<!-- lypning-hillclimb: newest entry is inserted directly below this line -->\\n\"\nassert s.count(marker) == 1\ns = s.replace(marker, marker + \"\\n\" + e.rstrip(\"\\n\") + \"\\n\\n\", 1)\np.write_text(s); print(\"ledger entry inserted\"); print(e[:300])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:39:40.506Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-296d9bcd8634","id":"py-296d9bcd8634","program":"import json\nrows=[json.loads(l) for l in open(\"nemotron/data/engine-mismatches.jsonl\") if l.strip()]\nprint(\"keys:\", sorted(rows[0].keys()))\nfor r in rows:\n    short={k:(str(v)[:90]) for k,v in r.items() if k not in (\"program\",\"source\",\"code\")}\n    print(json.dumps(short))","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:35:57.366Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-2e0047ce7bce","id":"py-2e0047ce7bce","program":"import json, glob\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r=json.loads(l)\n        if r.get(\"id\")==\"py-4cd9100cb501\": print(r[\"program\"], end=\"\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:22:17.505Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-2ea7ccdb374f","id":"py-2ea7ccdb374f","program":"import json, glob, subprocess\nwant = {\"py-124f4d3b1c9d\",\"py-1e4ac9962dfb\",\"py-2319b4eaa414\",\"py-b9028b32e2d3\"}\nseen=set()\nfor f in glob.glob(\"tests/corpus/*.jsonl\")+glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r=json.loads(l)\n        i=r.get(\"id\")\n        if i in want and i not in seen:\n            seen.add(i)\n            print(\"=====\", i, f); print(r[\"program\"][:600])\n            for exe in [\"/root/.lypning/bin/lypning\", \"/usr/bin/python3.12\", \"/usr/bin/python3.11\"]:\n                p=subprocess.run([exe,\"-c\",r[\"program\"]],capture_output=True,text=True,timeout=30)\n                print(\"  \", exe.split(\"/\")[-1], \"exit\", p.returncode, \"|\", (p.stderr.strip().splitlines() or [\"\"])[-1][:120])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:04:55.129Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-32193e1ab2c2","id":"py-32193e1ab2c2","program":"import json, glob, subprocess, tempfile, os, time, shutil\nenv = dict(os.environ, LYPNING_CAPTURE=\"0\", LYPNING_HARVEST=\"0\")\nprogs = {}\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r = json.loads(l)\n        if r.get(\"id\") in (\"py-2fa2dc1d1f8f\", \"py-c667db469d9e\"): progs[r[\"id\"]] = r[\"program\"]\nbins = {\"base\": \"work/base-home/bin/lypning\", \"new\": os.path.expanduser(\"~/.lypning/bin/lypning\")}\nfor pid, prog in progs.items():\n    best = {k: 1e9 for k in bins}\n    for rnd in range(3):\n        for k, b in bins.items():\n            d = tempfile.mkdtemp(); t = time.perf_counter()\n            subprocess.run([b, \"-c\", prog], cwd=d, capture_output=True, env=env)\n            best[k] = min(best[k], (time.perf_counter() - t) * 1000); shutil.rmtree(d, ignore_errors=True)\n    print(pid, {k: round(v, 1) for k, v in best.items()})","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:36:58.593Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-33a4ea878cef","id":"py-33a4ea878cef","program":"$src","argv_tail":["2>"],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:09:09.556Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-34fa673c977b","id":"py-34fa673c977b","program":"import pathlib\np = pathlib.Path(\"tests/test_semantics.py\"); s = p.read_text()\ndef sub(old, new):\n    global s\n    assert s.count(old) == 1, old\n    s = s.replace(old, new)\n\nsub('''        \"show('divmod', lambda: divmod(1, 0))\\\\n\"\n''', '''        \"show('divmod', lambda: divmod(1, 0))\\\\n\"\n        # `float_divmod` has its own message — `float divmod()` — and the\n        # engine went through `//` first, so this said `float floor division\n        # by zero`. Stage 0a witness ntx-cab30d587088; both operand orders.\n        \"show('float divmod', lambda: divmod(1.0, 0))\\\\n\"\n        \"show('divmod by float', lambda: divmod(1, 0.0))\\\\n\"\n        \"show('bool divmod', lambda: divmod(True, 0.0))\\\\n\"\n''')\n\nsub('''        \"for v in ([], None, {}, (), 'nope'):\\\\n\"\n        \"    show('float()', lambda: float(v))\\\\n\"\n        \"    show('int()', lambda: int(v))\\\\n\",\n    ),\n]\n''', '''        \"for v in ([], None, {}, (), 'nope'):\\\\n\"\n        \"    show('float()', lambda: float(v))\\\\n\"\n        \"    show('int()', lambda: int(v))\\\\n\",\n    ),\n    (\n        # Argument Clinic gives `enumerate` TWO names, and the keyword guard\n        # knew one: `enumerate(iterable=xs, start=1)` died at exit 1 with\n        # `'iterable' is an invalid keyword argument`, a TypeError CPython\n        # never raises. Stage 0a witness ntx-b47b0b10247f. The two TypeErrors\n        # CPython DOES raise — given twice, and missing — are printed too, so\n        # the binder's wording is pinned alongside the answer.\n        \"enumerate-takes-iterable-by-name\",\n        \"print(list(enumerate(iterable='ab', start=1)))\\\\n\"\n        \"print(list(enumerate(iterable='ab')))\\\\n\"\n        \"print(list(enumerate('ab', start=1)))\\\\n\"\n        \"print(list(enumerate(start=2, iterable='ab')))\\\\n\"\n        \"for fn in (lambda: enumerate('ab', iterable='cd'), lambda: enumerate(start=1),\\\\n\"\n        \"           lambda: enumerate('ab', strict=True)):\\\\n\"\n        \"    try:\\\\n\"\n        \"        fn()\\\\n\"\n        \"    except TypeError as e:\\\\n\"\n        \"        print(repr(str(e)))\\\\n\",\n    ),\n]\n''')\n\nsub('''#: Every method name `str` and `bytes` both implement.''',\n'''def test_a_lone_surrogate_escape_refuses_rather_than_dying_as_a_syntax_error(lypning_bin):\n    \"\"\"`'\\\\\\\\udcff'` is a legal str in CPython — it is how surrogateescape spells\n    an undecodable byte — and no UTF-8 string here can hold it.\n\n    The lexer answered SyntaxError at exit 1, which the dispatcher passes\n    through unchanged as the program's own failure, so CPython was never asked.\n    Stage 0a witness ntx-590d4adaea1e. Above U+10FFFF CPython itself raises a\n    SyntaxError, and that one stays a SyntaxError.\n    \"\"\"\n    for program in (\n        'print(repr(\"a\\\\\\\\udcff\"))',\n        'print(len(\"\\\\\\\\ud800\"))',\n        'x = \"\\\\\\\\U0000dfff\"\\\\nprint(x)',\n    ):\n        r = engines.run(engines.LYPNING, program, binary=lypning_bin)\n        assert r.returncode == UNSUPPORTED_EXIT, \"answered %r instead of refusing\" % r.stdout\n        assert r.stdout == \"\", \"output escaped before the refusal\"\n        assert \"unsupported: escape:\" in r.stderr, r.stderr\n\n    r = engines.run(engines.LYPNING, 'print(\"\\\\\\\\U00110000\")', binary=lypning_bin)\n    assert r.returncode == 1 and \"SyntaxError\" in r.stderr, r.stderr\n    r = engines.run(engines.LYPNING, 'print(\"\\\\\\\\u00e9\\\\\\\\U0001F600\")', binary=lypning_bin)\n    assert not r.refused and r.stdout == \"é😀\\\\n\", r.stderr\n\n\ndef test_is_between_two_dict_views_refuses_rather_than_guessing(lypning_bin):\n    \"\"\"A view carries the DICT's `Rc`, so neither answer to `is` is a fact.\n\n    CPython builds a new view per call — `d.values() is d.values()` is False —\n    and an alias is one object — `v = d.values(); v is v` is True. `is_same`\n    had no arm, so both answered False; Stage 0a witness ntx-bd566d4c9697 is\n    the alias, a wrong answer at exit 0. The views' own kind takes it to\n    CPython, and `is` against anything that is not a view still answers.\n    \"\"\"\n    for program in (\n        'd = {\"a\": 1}\\\\nv = d.values()\\\\nprint(v is v)',\n        'd = {\"a\": 1}\\\\nprint(d.values() is d.values())',\n        'd = {\"a\": 1}\\\\nk = d.keys()\\\\nprint(k is not d.keys())',\n        'd = {\"a\": 1}\\\\nprint(d.items() is d.items())',\n    ):\n        r = engines.run(engines.LYPNING, program, binary=lypning_bin)\n        assert r.returncode == UNSUPPORTED_EXIT, \"answered %r instead of refusing\" % r.stdout\n        assert r.stdout == \"\", \"output escaped before the refusal\"\n        assert \"dict-view\" in r.stderr, r.stderr\n\n    r = engines.run(engines.LYPNING, 'd = {\"a\": 1}\\\\nprint(d.values() is None, d.keys() is d)',\n                    binary=lypning_bin)\n    assert not r.refused, r.stderr\n    assert r.stdout.strip() == \"False False\"\n\n\n#: Every method name `str` and `bytes` both implement.''')\np.write_text(s); print(\"tests written\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:41:47.400Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-3a54f3c44df4","id":"py-3a54f3c44df4","program":"import json, glob\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r=json.loads(l)\n        if r.get(\"id\")==\"py-4cd9100cb501\":\n            print(f); print(r[\"program\"][:800]); print(\"argv_tail:\", r.get(\"argv_tail\"), \"stdin:\", (r.get(\"stdin_sample\") or \"\")[:80])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:22:04.772Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-3c763cecf18a","id":"py-3c763cecf18a","program":"import pathlib\np = pathlib.Path(\"CHANGELOG.md\"); s = p.read_text()\nanchor = \"## Unreleased\\n\\n\"\nentry = '''## Unreleased\n\n**2026-09-15** — Hillclimb 83: five wrong answers from the un-replayed ledger surfaces, and one home for the earlier model's name ([#77](https://github.com/kristerhedfors/lypning/pull/77))\n\n- Sweep the seven surfaces the ledger's un-replayed witnesses name with\n  authored programs against the 3.12 reference round-02 pins; fix what they\n  found, one mechanism per commit: list IndexErrors name the operation,\n  `format(nan, '%')` and `#` on the empty type say what the host says,\n  `bytes(source=)` and `open(file=)` bind their Argument Clinic names while\n  `sorted`, `min`, `sum` and `reversed` count before naming, and a `|` union\n  of classes refuses as `class-union` instead of killing the program at its\n  first annotated `def`. Ledger entry in `docs/HILLCLIMB.md`.\n- Name the earlier target model in one place: `SWITCH.md` carries its name and\n  the retired numbers; every other document says \"the earlier model\". Remove\n  the retired GCP launch scripts and the earlier model's LoRA recipe. Run\n  records, data files and the mismatch ledger keep their recorded names.\n\n'''\nassert s.count(anchor) == 1; p.write_text(s.replace(anchor, entry, 1)); print(\"changelog written\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:41:25.601Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-40e3d58bc09f","id":"py-40e3d58bc09f","program":"import pathlib\ndef sub(path, old, new):\n    p = pathlib.Path(path); s = p.read_text(); assert s.count(old) == 1, (path, old[:70]); p.write_text(s.replace(old, new)); print(\"edited\", path)\nsub(\"src/lypning/assets/rust/src/ops.rs\", '''            (Mod, Value::Bytes(_), _) => {''',\n'''            // `int | str` builds a `types.UnionType` in CPython, a value this\n            // engine does not have — and it is usually an ANNOTATION, evaluated\n            // at `def` time, so `def f(b: bytes | str)` killed the whole program\n            // at exit 1 with a TypeError CPython never raises. Refused like a\n            // class subscript: one spawn, and CPython answers. `None` is a\n            // member too (`int | None`); `None | None` is CPython's own\n            // TypeError and still reaches the generic arm. Found by the\n            // authored sweep over the ntx-b38207f0de4f surface.\n            (BitOr, l, r)\n                if (is_class(l) || matches!(l, Value::None))\n                    && (is_class(r) || matches!(r, Value::None))\n                    && !(matches!(l, Value::None) && matches!(r, Value::None)) =>\n            {\n                return Err(unsupported(\n                    \"class-union\",\n                    \"a `|` union of classes (a types.UnionType)\",\n                ))\n            }\n            (Mod, Value::Bytes(_), _) => {''')\nsub(\"src/lypning/assets/rust/src/ops.rs\", '''fn not_subscriptable(v: &Value) -> LypningError {''',\n'''/// Whether `v` names a class, by the one map that knows (`callable_kind`).\nfn is_class(v: &Value) -> bool {\n    matches!(\n        crate::value::callable_kind(v),\n        Some(crate::value::Callable::Class(_))\n    )\n}\n\nfn not_subscriptable(v: &Value) -> LypningError {''')\nsub(\"tests/test_semantics.py\", '''def test_open_binds_file_by_name(lypning_bin, tmp_path):''',\n'''def test_a_union_of_classes_refuses_rather_than_dying(lypning_bin):\n    \"\"\"`bytes | str` is a `types.UnionType` in CPython, a value this engine has\n    no representation for — and it is usually an annotation, evaluated at\n    `def` time, so a whole program died at exit 1 on its first `def`. It\n    refuses as `class-union` now, and `None` is a member (`int | None`) while\n    `None | None` stays CPython's own TypeError. Authored sweep over the\n    ntx-b38207f0de4f surface.\n    \"\"\"\n    for program in (\n        \"def f(b: bytes | str) -> bytes | str:\\\\n    return b\\\\nprint(f(b'x'))\",\n        \"print(int | None)\",\n        \"x = None | str\",\n        \"print(isinstance(1, int | str))\",\n    ):\n        r = engines.run(engines.LYPNING, program, binary=lypning_bin)\n        assert r.returncode == UNSUPPORTED_EXIT, \"answered %r instead of refusing\" % r.stdout\n        assert r.stdout == \"\" and \"class-union\" in r.stderr, r.stderr\n    for program in (\"print(None | None)\", \"print(1 | 'a')\", \"print(str | 1)\"):\n        r = engines.run(engines.LYPNING, program, binary=lypning_bin)\n        assert r.returncode == 1 and \"TypeError\" in r.stderr, r.stderr\n    r = engines.run(engines.LYPNING, \"print(5 | 2, {1} | {2}, True | False)\", binary=lypning_bin)\n    assert not r.refused and r.stdout == \"7 {1, 2} True\\\\n\", r.stderr\n\n\ndef test_open_binds_file_by_name(lypning_bin, tmp_path):''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:18:05.909Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-4195dba0a69c","id":"py-4195dba0a69c","program":"import ast; ast.parse(open('/tmp/claude-0/-home-user-lypning/520bd75f-5243-58e2-9e64-37d102fd9ae1/scratchpad/sweep.py').read()); print('syntax ok')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:08:41.693Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-506e782efb66","id":"py-506e782efb66","program":"d={\"a\":1}\ntry: d[1:2:3]\nexcept Exception as e: print(type(e).__name__, e)\ntry: d[slice(1,2)]\nexcept Exception as e: print(type(e).__name__, e)\ntry: {}[[1]]\nexcept Exception as e: print(type(e).__name__, e)","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:06:20.623Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-6e2e100f1e49","id":"py-6e2e100f1e49","program":"\nimport json\nfor l in open('tests/corpus/sightings/520bd75f-5243-58e2-9e64-37d102fd9ae1.jsonl'):\n    r=json.loads(l); print(r['id'], repr(r['program'][:70]), {k:v for k,v in r.items() if k not in ('program','id','key')})\n","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:53:02.667Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-726431f28eac","id":"py-726431f28eac","program":"import json, glob, subprocess, tempfile, os\nfrom lypning import conformance\nwant = {\"py-124f4d3b1c9d\",\"py-1e4ac9962dfb\",\"py-2319b4eaa414\",\"py-b9028b32e2d3\"}\nprogs={}\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r=json.loads(l)\n        if r.get(\"id\") in want: progs[r[\"id\"]]=r[\"program\"]\nfor i,p in progs.items():\n    seeds=conformance.seed_files(p)\n    print(\"=====\", i, list(seeds))\n    for exe in [\"/root/.lypning/bin/lypning\", \"/usr/bin/python3.12\"]:\n        d=tempfile.mkdtemp()\n        for rel,b in seeds.items():\n            os.makedirs(os.path.dirname(os.path.join(d,rel)) or d, exist_ok=True); open(os.path.join(d,rel),\"wb\").write(b)\n        r=subprocess.run([exe,\"-c\",p],cwd=d,capture_output=True,text=True,timeout=30)\n        print(\"  \", exe.split(\"/\")[-1], \"exit\", r.returncode, \"| out:\", repr(r.stdout[:80]), \"| err:\", (r.stderr.strip().splitlines() or [\"\"])[-1][:110])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:05:54.717Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-73eb4c20eedc","id":"py-73eb4c20eedc","program":"import json, glob, subprocess, tempfile, os\nfrom lypning import conformance\nwant = {\"py-124f4d3b1c9d\",\"py-1e4ac9962dfb\",\"py-2319b4eaa414\",\"py-b9028b32e2d3\"}\nprogs={}\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r=json.loads(l)\n        if r.get(\"id\") in want: progs[r[\"id\"]]=r[\"program\"]\nfor i,p in progs.items():\n    seeds=conformance.seed_files(p); outs=[]\n    for exe in [\"/root/.lypning/bin/lypning\", \"/usr/bin/python3.12\"]:\n        d=tempfile.mkdtemp()\n        for rel,b in seeds.items():\n            os.makedirs(os.path.dirname(os.path.join(d,rel)) or d, exist_ok=True); open(os.path.join(d,rel),\"wb\").write(b)\n        r=subprocess.run([exe,\"-c\",p],cwd=d,capture_output=True,text=True,timeout=30)\n        outs.append((r.returncode, r.stdout, (r.stderr.strip().splitlines() or [\"\"])[-1]))\n    print(i, \"AGREE\" if outs[0]==outs[1] else \"DIFFER\", outs[0][2][:70])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:09:09.556Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-8871f4b0acdf","id":"py-8871f4b0acdf","program":"import pathlib\np = pathlib.Path(\"/tmp/claude-0/-home-user-lypning/520bd75f-5243-58e2-9e64-37d102fd9ae1/scratchpad/sweep.py\")\ns = p.read_text()\nold = \"\"\"'\"a\\\\\\\\'b\\\\\\\\\"c\"'\"\"\"\nnew = \"\"\"'\"a\\\\'b\\\\\\\\\"c\"'\"\"\"\nassert s.count(old) == 1, s.count(old)\np.write_text(s.replace(old, new)); print(\"fixed\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:08:41.693Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-8c071acec508","id":"py-8c071acec508","program":"import pathlib\ndef sub(path, old, new):\n    p = pathlib.Path(path); s = p.read_text(); assert s.count(old) == 1, (path, old[:70]); p.write_text(s.replace(old, new)); print(\"edited\", path)\nt = \"tests/test_semantics.py\"\nsub(t, '''        \"def op():\\\\n\"\n        \"    open(file='f.txt', mode='w').write('hi')\\\\n\"\n        \"    return open(file='f.txt').read()\\\\n\"\n        \"show('open file kw', op)\\\\n\"\n''', '')\nsub(t, '''def test_storing_a_slice_as_a_dict_key_refuses_or_raises_as_the_host_does(lypning_bin):''',\n'''def test_open_binds_file_by_name(lypning_bin, tmp_path):\n    \"\"\"`open(file='f.txt', mode='w')` is legal CPython; the engine read only the\n    positional slot and died with `missing required argument 'file'`. In a\n    temp cwd, because the program writes — a CASES row runs where pytest\n    runs, and the corpus's net is not this suite's (invariant 4).\n    \"\"\"\n    program = (\"open(file='f.txt', mode='w').write('hi')\\\\n\"\n               \"print(open(file='f.txt').read(), open('f.txt', mode='r').read())\\\\n\"\n               \"try:\\\\n    open('f.txt', file='g.txt')\\\\n\"\n               \"except TypeError as e:\\\\n    print(e)\\\\n\")\n    ours = engines.run(engines.LYPNING, program, binary=lypning_bin, cwd=tmp_path)\n    theirs = engines.run(engines.CPYTHON, program, cwd=tmp_path)\n    if theirs.returncode == 127:\n        pytest.skip(\"no reference CPython\")\n    assert not ours.refused, ours.stderr\n    assert (ours.returncode, ours.stdout) == (theirs.returncode, theirs.stdout), ours.stderr\n\n\ndef test_storing_a_slice_as_a_dict_key_refuses_or_raises_as_the_host_does(lypning_bin):''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:18:05.909Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-9109a8403cf9","id":"py-9109a8403cf9","program":"import json\nd = json.load(open(\"/root/.claude/projects/-home-user-lypning/520bd75f-5243-58e2-9e64-37d102fd9ae1/tool-results/mcp-github-pull_request_read-1789504902015.txt\"))\nfiles = d if isinstance(d, list) else d.get(\"files\", d)\nmine = {\"nemotron/README.md\",\"nemotron/LADDER.md\",\"nemotron/PREREGISTRATION.md\",\"nemotron/SWITCH.md\",\"nemotron/gpu/README.md\",\"nemotron/gpu/lora_qwen38_27b.yaml\",\"nemotron/gpu/lypning_lora.py\",\"nemotron/pipeline/__init__.py\",\"nemotron/pipeline/backends.py\",\"nemotron/pipeline/cli.py\",\"nemotron/pipeline/extract.py\",\"nemotron/pipeline/stats.py\",\"nemotron/tests/test_comparability.py\",\"nemotron/tests/test_money_path.py\",\"nemotron/tests/test_recorded.py\",\"CHANGELOG.md\",\"docs/HILLCLIMB.md\",\"tests/test_semantics.py\",\"nemotron/gpu/startup.sh\",\"nemotron/gpu/launch.sh\",\"nemotron/gpu/reap.sh\",\"nemotron/gpu/budget.py\",\"nemotron/nt-gpu\",\"nemotron/gpu/lora_rank16.yaml\"}\nnames = [f[\"filename\"] for f in files]\nprint(len(names), \"files in #76\")\nprint(\"overlap with my PR:\", sorted(set(names) & mine))\nprint(\"sample:\", names[:25])","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:41:52.778Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-9178d921b75b","id":"py-9178d921b75b","program":"print(bytes(source=b'x'), bytes(source=[65]), bytes(source=2))","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:14:22.423Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-95cca336c7ca","id":"py-95cca336c7ca","program":"import pathlib\np = pathlib.Path(\"nemotron/gpu/README.md\"); s = p.read_text()\nold = \"> — and the $29.52/hr rate. Do not launch from this file.\\n\"\nassert s.count(old) == 1\ns = s.replace(old, old + \">\\n> The scripts this page describes (`launch.sh`, `startup.sh`, `reap.sh`,\\n> `budget.py`, `nt-gpu`) were removed on 2026-09-15; they served the earlier\\n> model and nothing live called them. The page stays as the record.\\n\")\np.write_text(s); print(\"gpu/README noted\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:40:03.890Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-a847901a83a5","id":"py-a847901a83a5","program":"bytes('a', source='b')","argv_tail":["2>"],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:14:22.423Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-a873049c8903","id":"py-a873049c8903","program":"import ast; ast.parse(open('tests/test_semantics.py').read()); print('syntax ok')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:08:12.596Z","count":2,"stdin_sample":null,"models":{"claude-fable-5-1":2}}
{"key":"py-adefa9c33cd9","id":"py-adefa9c33cd9","program":"d={\"a\":1}\ntry: del d[:2]\nexcept Exception as e: print(type(e).__name__, e)\nd[:2]=5; print(d)","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T19:07:25.290Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-b1beef6ac4fd","id":"py-b1beef6ac4fd","program":"import ast,sys; ast.parse(open('tests/test_semantics.py').read()); print('syntax ok')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:41:47.400Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-be60103d137e","id":"py-be60103d137e","program":"import pathlib\np = pathlib.Path(\"src/lypning/assets/rust/src/builtins.rs\"); s = p.read_text()\ndef sub(old, new):\n    global s\n    assert s.count(old) == 1, old[:60]\n    s = s.replace(old, new)\n\nsub('''            // Argument Clinic names the parameter; `arg1` writes the generic\n            // `missing 1 required positional argument`, which is a Python-level\n            // function's wording and not this one's.\n            let v = match crate::args::bind(&args, &kw, 0, \"iterable\", \"enumerate\")? {\n                Some(v) => v,''',\n'''            // Argument Clinic names the parameter; `arg1` writes the generic\n            // `missing 1 required positional argument`, which is a Python-level\n            // function's wording and not this one's.\n            //\n            // Two TypeErrors around that name changed wording at 3.11, and\n            // neither is the binder's. Measured on 3.10.20 / 3.11.15 / 3.12.3 /\n            // 3.13.13 on 2026-09-15: `enumerate('ab', iterable='cd')` is the\n            // ordinary given-twice sentence on 3.10 and `'iterable' is an\n            // invalid keyword argument for enumerate()` from 3.11; and\n            // `enumerate(start=1)` — the only keyword-only call the guard above\n            // lets through without an iterable — is `missing required argument\n            // 'iterable' (pos 1)` on 3.10 and `'start' is an invalid keyword\n            // argument for enumerate()` from 3.11. A type's `tp_new` reads its\n            // keywords through a different Clinic path than a function does,\n            // which is why `round(2.5, number=9)` keeps the binder's wording.\n            let named = kw.iter().any(|(k, _)| k.as_ref() == \"iterable\");\n            if REF_PY_MINOR >= 11 && named && args.first().is_some() {\n                return Err(type_err(\n                    \"'iterable' is an invalid keyword argument for enumerate()\",\n                ));\n            }\n            if REF_PY_MINOR >= 11 && args.is_empty() && !named && !kw.is_empty() {\n                return Err(type_err(\n                    \"'start' is an invalid keyword argument for enumerate()\",\n                ));\n            }\n            let v = match crate::args::bind(&args, &kw, 0, \"iterable\", \"enumerate\")? {\n                Some(v) => v,''')\n\nsub('''            let float_arm = matches!(a, Value::Float(_)) || matches!(b, Value::Float(_));\n            if float_arm && as_num(&a).is_some() && as_num(&b).is_some() && !truthy(&b)? {\n                return Err(zero_div(\"float divmod()\"));\n            }''',\n'''            // ...and the TypeError names `divmod()`, not the `//` it was\n            // computed through: `divmod('a', 1)` is `unsupported operand\n            // type(s) for divmod(): 'str' and 'int'` on every CPython above.\n            if as_num(&a).is_none() || as_num(&b).is_none() {\n                return Err(type_err(format!(\n                    \"unsupported operand type(s) for divmod(): '{}' and '{}'\",\n                    type_name(&a),\n                    type_name(&b)\n                )));\n            }\n            let float_arm = matches!(a, Value::Float(_)) || matches!(b, Value::Float(_));\n            if float_arm && !truthy(&b)? {\n                return Err(zero_div(\"float divmod()\"));\n            }''')\np.write_text(s)\n\nt = pathlib.Path(\"tests/test_semantics.py\"); s = t.read_text()\nold = '''        \"for fn in (lambda: enumerate('ab', iterable='cd'), lambda: enumerate(start=1),\\\\n\"\n        \"           lambda: enumerate('ab', strict=True)):\\\\n\"'''\nnew = '''        \"for fn in (lambda: enumerate('ab', iterable='cd'), lambda: enumerate(start=1),\\\\n\"\n        \"           lambda: enumerate('ab', strict=True), lambda: divmod('a', 1.0),\\\\n\"\n        \"           lambda: divmod(1, 'a')):\\\\n\"'''\nassert s.count(old) == 1; s = s.replace(old, new)\nold = '''        # never raises. Stage 0a witness ntx-b47b0b10247f. The two TypeErrors\n        # CPython DOES raise — given twice, and missing — are printed too, so\n        # the binder's wording is pinned alongside the answer.'''\nnew = '''        # never raises. Stage 0a witness ntx-b47b0b10247f. The TypeErrors\n        # CPython DOES raise — given twice, missing, unknown — are printed too,\n        # because their wording changed at 3.11 and is not the binder's; and\n        # `divmod` over a non-number names `divmod()`, not the `//` it is\n        # computed through.'''\nassert s.count(old) == 1; s = s.replace(old, new)\nt.write_text(s); print(\"edited\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:45:06.936Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-ca76d9277d7a","id":"py-ca76d9277d7a","program":"import subprocess, os\nenv=dict(os.environ, LYPNING_CAPTURE=\"0\")\nspecs=[a+s+h+z+w for a in [\"\",\"<\",\">\",\"^\",\"=\"] for s in [\"\",\"+\",\"-\",\" \"] for h in [\"\",\"#\"] for z in [\"\",\"0\"] for w in [\"\",\"0\",\"1\",\"6\",\"10\"]]\nprog=\"v = 1e300\\nfor spec in %r:\\n    try:\\n        r = format(v, spec)\\n    except Exception as e:\\n        r = 'ERR:' + type(e).__name__ + ':' + str(e)\\n    print(repr(v) + '|' + spec + '|' + r)\\n\" % specs\na=subprocess.run([os.path.expanduser(\"~/.lypning/bin/lypning-l\"),\"-c\",prog],capture_output=True,text=True,env=env).stdout.splitlines()\nb=subprocess.run([\"/usr/bin/python3.12\",\"-c\",prog],capture_output=True,text=True,env=env).stdout.splitlines()\nn=0\nfor x,y in zip(a,b):\n    if x!=y:\n        print(\"L :\",x); print(\"py:\",y); n+=1\n        if n>4: break","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:10:35.428Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-cc17609b68ae","id":"py-cc17609b68ae","program":"import pathlib\np = pathlib.Path(\"src/lypning/assets/rust/src/builtins.rs\"); s = p.read_text()\nold = '''            // Three TypeErrors, in CPython's order, measured on 3.10 through\n            // 3.13 on 2026-09-15: the TOTAL count first (`sum([1], start=1,\n            // x=2)` is `takes at most 2 arguments (3 given)`, keywords\n            // included), then an unknown name in `bad_kw`'s version-dependent\n            // sentence, then a missing iterable as `takes at least 1\n            // positional argument (0 given)`. The old text here — `takes no\n            // keyword arguments (got 'x')` — is a sentence CPython never says\n            // of `sum`.\n            if args.len() + kw.len() > 2 {\n                return Err(type_err(format!(\n                    \"sum() takes at most 2 arguments ({} given)\",\n                    args.len() + kw.len()\n                )));\n            }\n            if let Some((k, _)) = kw.iter().find(|(k, _)| k.as_ref() != \"start\") {\n                return Err(bad_kw(\"sum\", k));\n            }\n            if args.is_empty() {\n                return Err(type_err(\"sum() takes at least 1 positional argument (0 given)\"));\n            }'''\nnew = '''            // Three TypeErrors, in CPython's order, measured on 3.10 through\n            // 3.13 on 2026-09-15: the TOTAL count first (`sum([1], start=1,\n            // x=2)` is `takes at most 2 arguments (3 given)`, keywords\n            // included), then a missing iterable as `takes at least 1\n            // positional argument (0 given)` — `sum(iterable=xs)` says this,\n            // not a word about the name — and only then an unknown name in\n            // `bad_kw`'s version-dependent sentence. The old text here —\n            // `takes no keyword arguments (got 'x')` — is a sentence CPython\n            // never says of `sum`.\n            if args.len() + kw.len() > 2 {\n                return Err(type_err(format!(\n                    \"sum() takes at most 2 arguments ({} given)\",\n                    args.len() + kw.len()\n                )));\n            }\n            if args.is_empty() {\n                return Err(type_err(\"sum() takes at least 1 positional argument (0 given)\"));\n            }\n            if let Some((k, _)) = kw.iter().find(|(k, _)| k.as_ref() != \"start\") {\n                return Err(bad_kw(\"sum\", k));\n            }'''\nassert s.count(old) == 1; p.write_text(s.replace(old, new)); print(\"edited\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:15:43.927Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-d11014529716","id":"py-d11014529716","program":"Refuse a `|` union of classes instead of dying on the annotation\n\n`bytes | str` is a `types.UnionType` in CPython, a value this engine has\nno representation for — and it is usually an annotation, evaluated at\n`def` time, so a whole program died at exit 1 on its first `def` with a\nTypeError CPython never raises. It refuses as `class-union` now, like a\nclass subscript: one spawn, and CPython answers. `None` is a member\n(`int | None`); `None | None` stays CPython's own TypeError. Found by the\nauthored sweep over a Stage 0a ledger witness's surface.\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_01Axms76U8XVFzsnzr8ei7Zs","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:20:18.326Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-d5b8b2acebc0","id":"py-d5b8b2acebc0","program":"import pathlib\nR = \"src/lypning/assets/rust/src/\"\ndef sub(path, old, new):\n    p = pathlib.Path(path); s = p.read_text(); assert s.count(old) == 1, (path, old[:70]); p.write_text(s.replace(old, new)); print(\"edited\", path)\n\n# ---- M1: list index wording\nsub(R+\"methods.rs\", 'Some(v) => ops::norm_index(int_val(v)?, n, \"pop index\")?,',\n    '''// `pop index out of range`: `norm_index` appends the words, so the\n                // caller names only the operation. \"pop index\" here doubled it.\n                Some(v) => ops::norm_index(int_val(v)?, n, \"pop\")?,''')\nsub(R+\"ops.rs\", 'let i = norm_index(crate::eval::int_val(&idx)?, n, \"list\")?;',\n    '''// A STORE names the operation: `list assignment index out of range`,\n                // where a read says `list index out of range`. Both stores said\n                // the read's words. Measured on 3.10 through 3.13, 2026-09-15.\n                let i = norm_index(crate::eval::int_val(&idx)?, n, \"list assignment\")?;''')\nsub(R+\"ops.rs\", 'let i = norm_index(crate::eval::int_val(idx)?, n, \"list\")?;',\n    '''// `del L[i]` is a store too, and says so — see `set_item`.\n                let i = norm_index(crate::eval::int_val(idx)?, n, \"list assignment\")?;''')\n\n# ---- M2: formatter\nsub(R+\"fmt.rs\", '''                group_float(&s, sp.grouping)\n            } else {\n                return Ok(pad_signed(nonfinite_sign(*f, &sp), &nonfinite(*f, false), &sp, true));\n            };''',\n    '''                // `#` keeps the point on the empty type as it does on `g`:\n                // `format(1e300, '#')` is `1.e+300`, and `str(float)` has no\n                // point to keep when the value prints in exponent form.\n                keep_point(group_float(&s, sp.grouping), sp.alt)\n            } else {\n                return Ok(pad_signed(nonfinite_sign(*f, &sp), &nonfinite(*f, false), &sp, true));\n            };''')\nsub(R+\"fmt.rs\", '''            let f = float_of(v)? * 100.0;\n            let digits = group_float(&format!(\"{:.*}\", sp.precision.unwrap_or(6), f.abs()), sp.grouping);\n            keep_point(format!(\"{digits}%\"), sp.alt)''',\n    '''            let f = float_of(v)?;\n            // `f`, `e` and `g` take the nonfinite exit above; this arm did not,\n            // so Rust's `{}` spelled the value: `format(nan, '%')` was `NaN%`\n            // where CPython says `nan%`. Same suffix, same sign slot.\n            if !f.is_finite() {\n                let body = format!(\"{}%\", nonfinite(f, false));\n                return Ok(pad_signed(nonfinite_sign(f, &sp), &body, &sp, true));\n            }\n            let f = f * 100.0;\n            let digits = group_float(&format!(\"{:.*}\", sp.precision.unwrap_or(6), f.abs()), sp.grouping);\n            keep_point(format!(\"{digits}%\"), sp.alt)''')\n\n# ---- M3: builtins keyword names\nsub(R+\"builtins.rs\", '''        \"bytes\" => {\n            // An `encoding` is only meaningful for a str source, and CPython says so''',\n    '''        \"bytes\" => {\n            // `source` is Argument Clinic's name for the first parameter, so\n            // `bytes(source='ab', encoding='utf-8')` is legal and `b'ab'`; every\n            // arm below read only the positional slot and raised a TypeError\n            // CPython never does. Re-seated as the first positional, so the\n            // arms keep their one reading of the argument list. Measured on\n            // 3.10 through 3.13, 2026-09-15, including the given-twice text.\n            let mut kw = kw;\n            let seated;\n            let args: &Args = match kw.iter().position(|(k, _)| k.as_ref() == \"source\") {\n                Some(i) => {\n                    if !args.is_empty() {\n                        return Err(type_err(\n                            \"argument for bytes() given by name ('source') and position (1)\",\n                        ));\n                    }\n                    let (_, v) = kw.remove(i);\n                    seated = std::iter::once(v).chain(args.iter().cloned()).collect::<Args>();\n                    &seated\n                }\n                None => args,\n            };\n            // An `encoding` is only meaningful for a str source, and CPython says so''')\nsub(R+\"builtins.rs\", '''        \"open\" => {\n            let path = match args.first() {\n                Some(Value::Str(s)) => s.to_string(),\n                // `open(Path('x'))` is `__fspath__`, and `to_str` knows it.\n                #[cfg(feature = \"cap-pathlib\")]\n                Some(p @ Value::Path(..)) => fmt::to_str(p)?,''',\n    '''        \"open\" => {\n            // `file` is a keyword too — `open(file='f.txt', mode='w')` — and\n            // this read the positional slot alone, so the call died as\n            // `missing required argument 'file'` with the file right there.\n            let path = match crate::args::bind(&args, &kw, 0, \"file\", \"open\")? {\n                Some(Value::Str(s)) => s.to_string(),\n                // `open(Path('x'))` is `__fspath__`, and `to_str` knows it.\n                #[cfg(feature = \"cap-pathlib\")]\n                Some(p @ Value::Path(..)) => fmt::to_str(&p)?,''')\nsub(R+\"builtins.rs\", '''        \"sorted\" => {\n            reject_unknown_kw(\"sort\", &kw, &[\"key\", \"reverse\"])?;\n            let v = args\n                .first()\n                .cloned()\n                .ok_or_else(|| type_err(\"sorted expected 1 argument, got 0\"))?;''',\n    '''        \"sorted\" => {\n            // CPython counts the positionals before it reads a keyword's name:\n            // `sorted(iterable=xs)` is `sorted expected 1 argument, got 0`, not\n            // a complaint about `iterable`. The guard ran first here.\n            let v = args\n                .first()\n                .cloned()\n                .ok_or_else(|| type_err(\"sorted expected 1 argument, got 0\"))?;\n            reject_unknown_kw(\"sort\", &kw, &[\"key\", \"reverse\"])?;''')\nsub(R+\"builtins.rs\", '''        \"min\" | \"max\" => {\n            reject_unknown_kw(name, &kw, &[\"key\", \"default\"])?;\n            // `min()` is a TypeError about the ARGUMENT LIST, not a ValueError''',\n    '''        \"min\" | \"max\" => {\n            // `min()` is a TypeError about the ARGUMENT LIST, not a ValueError''')\nsub(R+\"builtins.rs\", '''            if args.is_empty() {\n                return Err(type_err(format!(\"{name} expected at least 1 argument, got 0\")));\n            }\n            let want_max = name == \"max\";''',\n    '''            if args.is_empty() {\n                return Err(type_err(format!(\"{name} expected at least 1 argument, got 0\")));\n            }\n            // After the count, as `sorted` above: `min(iterable=xs)` names the\n            // count, not the keyword.\n            reject_unknown_kw(name, &kw, &[\"key\", \"default\"])?;\n            let want_max = name == \"max\";''')\nsub(R+\"builtins.rs\", '''            if let Some((k, _)) = kw.iter().find(|(k, _)| k.as_ref() != \"start\") {\n                return Err(type_err(format!(\n                    \"sum() takes no keyword arguments (got '{k}')\"\n                )));\n            }\n            let start = crate::args::bind(&args, &kw, 1, \"start\", \"sum\")?''',\n    '''            //\n            // Three TypeErrors, in CPython's order, measured on 3.10 through\n            // 3.13 on 2026-09-15: the TOTAL count first (`sum([1], start=1,\n            // x=2)` is `takes at most 2 arguments (3 given)`, keywords\n            // included), then an unknown name in `bad_kw`'s version-dependent\n            // sentence, then a missing iterable as `takes at least 1\n            // positional argument (0 given)`. The old text here — `takes no\n            // keyword arguments (got 'x')` — is a sentence CPython never says\n            // of `sum`.\n            if args.len() + kw.len() > 2 {\n                return Err(type_err(format!(\n                    \"sum() takes at most 2 arguments ({} given)\",\n                    args.len() + kw.len()\n                )));\n            }\n            if let Some((k, _)) = kw.iter().find(|(k, _)| k.as_ref() != \"start\") {\n                return Err(bad_kw(\"sum\", k));\n            }\n            if args.is_empty() {\n                return Err(type_err(\"sum() takes at least 1 positional argument (0 given)\"));\n            }\n            let start = crate::args::bind(&args, &kw, 1, \"start\", \"sum\")?''')\nsub(R+\"builtins.rs\", '''    \"isinstance\", \"iter\", \"len\", \"list\", \"map\", \"next\", \"oct\", \"ord\", \"range\", \"repr\", \"set\",\n    \"tuple\",\n];''',\n    '''    \"isinstance\", \"iter\", \"len\", \"list\", \"map\", \"next\", \"oct\", \"ord\", \"range\", \"repr\", \"set\",\n    // `reversed(sequence=xs)` is `reversed() takes no keyword arguments`;\n    // without this entry it fell through to `arg1`'s missing-argument text.\n    \"reversed\", \"tuple\",\n];''')\n\n# ---- tests\nt = \"tests/test_semantics.py\"\nsub(t, '''    (\n        # A slice as a dict KEY. CPython hashes the key before it validates''',\n    '''    (\n        # Three IndexErrors that name the OPERATION. `L.pop(9)` said `pop index\n        # index out of range` — the caller passed the words `norm_index` adds —\n        # and both stores (`L[9] = x`, `del L[9]`) said the read's `list index\n        # out of range` where CPython says `list assignment`. Found by an\n        # authored sweep over the surface a Stage 0a witness (ntx-a53924799ac1)\n        # swept; wording only reaches stdout through `print(e)`.\n        \"list-index-errors-name-the-operation\",\n        \"def show(label, fn):\\\\n\"\n        \"    try:\\\\n\"\n        \"        print(label, fn())\\\\n\"\n        \"    except Exception as e:\\\\n\"\n        \"        print(label, '|', type(e).__name__, '|', e)\\\\n\"\n        \"for i in (-3, -1, 0, 1, 2, 9):\\\\n\"\n        \"    show('pop %d' % i, lambda: [1, 2].pop(i))\\\\n\"\n        \"    def st(i=i):\\\\n\"\n        \"        L = [1, 2]; L[i] = 7; return L\\\\n\"\n        \"    show('set %d' % i, st)\\\\n\"\n        \"    def de(i=i):\\\\n\"\n        \"        L = [1, 2]; del L[i]; return L\\\\n\"\n        \"    show('del %d' % i, de)\\\\n\"\n        \"    show('get %d' % i, lambda: [1, 2][i])\\\\n\"\n        \"show('pop empty', lambda: [].pop())\\\\n\",\n    ),\n    (\n        # `format(nan, '%')` was `NaN%`: the `%` arm never took the nonfinite\n        # exit `f`, `e` and `g` take, so Rust spelled the value. And `#` on the\n        # EMPTY presentation type keeps the point as it does on `g`, which\n        # `str(1e300)` has none of: `format(1e300, '#')` is `1.e+300`. Both from\n        # the authored sweep over the ntx-6ae7c8d5c3d8 surface.\n        \"format-nonfinite-percent-and-alt-on-the-empty-type\",\n        \"vals = [float('nan'), float('inf'), float('-inf'), 1e300, -1e300, 1e16, 1.5, 100.0, 0.0, 1e-7]\\\\n\"\n        \"specs = ['%', '+%', ' %', '10.2%', '010%', '<8%', '#', '#10', '+#', '#,', '#.0', '#.3', '#g', '#.0g', '#e']\\\\n\"\n        \"for v in vals:\\\\n\"\n        \"    for spec in specs:\\\\n\"\n        \"        try:\\\\n\"\n        \"            print(repr(v), spec, repr(format(v, spec)))\\\\n\"\n        \"        except Exception as e:\\\\n\"\n        \"            print(repr(v), spec, '|', type(e).__name__, '|', e)\\\\n\",\n    ),\n    (\n        # Argument Clinic names that the engine read only positionally, and the\n        # TypeErrors around them in CPython's order. `bytes(source=…)` and\n        # `open(file=…)` died with TypeErrors CPython never raises; `sorted`,\n        # `min` and `sum` named the keyword where CPython counts the positionals\n        # first; `reversed` takes no keywords at all and said something else.\n        # Authored sweep over the ntx-b47b0b10247f surface.\n        \"builtins-take-argument-clinic-names-and-count-first\",\n        \"def show(label, fn):\\\\n\"\n        \"    try:\\\\n\"\n        \"        print(label, repr(fn()))\\\\n\"\n        \"    except Exception as e:\\\\n\"\n        \"        print(label, '|', type(e).__name__, '|', e)\\\\n\"\n        \"show('bytes source enc', lambda: bytes(source='ab', encoding='utf-8'))\\\\n\"\n        \"show('bytes source bytes', lambda: bytes(source=b'xy'))\\\\n\"\n        \"show('bytes source list', lambda: bytes(source=[65, 66]))\\\\n\"\n        \"show('bytes source int', lambda: bytes(source=2))\\\\n\"\n        \"show('bytes twice', lambda: bytes('a', source='b'))\\\\n\"\n        \"show('bytes enc kw', lambda: bytes('é', encoding='utf-8', errors='strict'))\\\\n\"\n        \"def op():\\\\n\"\n        \"    open(file='f.txt', mode='w').write('hi')\\\\n\"\n        \"    return open(file='f.txt').read()\\\\n\"\n        \"show('open file kw', op)\\\\n\"\n        \"show('sorted iterable kw', lambda: sorted(iterable=[2, 1]))\\\\n\"\n        \"show('sorted key kw', lambda: sorted([2, 1], key=None, reverse=True))\\\\n\"\n        \"show('min iterable kw', lambda: min(iterable=[1]))\\\\n\"\n        \"show('min default kw', lambda: min([], default=5))\\\\n\"\n        \"show('sum iterable kw', lambda: sum(iterable=[1]))\\\\n\"\n        \"show('sum start kw', lambda: sum([1, 2], start=10))\\\\n\"\n        \"show('sum start only', lambda: sum(start=1))\\\\n\"\n        \"show('sum three', lambda: sum([1], 2, 3))\\\\n\"\n        \"show('sum start and extra', lambda: sum([1], start=1, x=2))\\\\n\"\n        \"show('reversed sequence kw', lambda: list(reversed(sequence=[1, 2])))\\\\n\"\n        \"show('reversed', lambda: list(reversed([1, 2])))\\\\n\",\n    ),\n    (\n        # A slice as a dict KEY. CPython hashes the key before it validates''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:14:22.423Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-da148396ad85","id":"py-da148396ad85","program":"import json, glob\nwant = {\"py-2fa2dc1d1f8f\", \"py-c667db469d9e\", \"py-2959ed3b0fdc\"}\nfor f in glob.glob(\"tests/corpus/sightings/*.jsonl\"):\n    for l in open(f):\n        r = json.loads(l)\n        if r.get(\"id\") in want:\n            print(\"=====\", r[\"id\"], f.split(\"/\")[-1]); print(r[\"program\"][:700]); print(\"argv:\", r.get(\"argv_tail\"), \"stdin:\", bool(r.get(\"stdin_sample\")))","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:23:59.888Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-e08244f91f3e","id":"py-e08244f91f3e","program":"import pathlib\np = pathlib.Path(\"src/lypning/assets/rust/src/builtins.rs\"); s = p.read_text()\nold = '''            if as_num(&a).is_none() || as_num(&b).is_none() {\n                return Err(type_err(format!('''\nnew = '''            // Not `as_num`: a WIDE integer is deliberately not a `Num`, and\n            // `divmod(2**70, 3)` is exact on `cap-bigint` — `binop` takes it\n            // before any `i64` is asked for.\n            let numeric = |v: &Value| {\n                #[cfg(feature = \"cap-re\")]\n                if matches!(v, Value::ReFlag(_)) {\n                    return true;\n                }\n                matches!(v, Value::Bool(_) | Value::Int(_) | Value::Float(_))\n            };\n            if !numeric(&a) || !numeric(&b) {\n                return Err(type_err(format!('''\nassert s.count(old) == 1; p.write_text(s.replace(old, new)); print(\"edited\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:51:10.436Z","count":2,"stdin_sample":null,"models":{"claude-fable-5-1":2}}
{"key":"py-e42592bf2373","id":"py-e42592bf2373","program":"import pathlib\np = pathlib.Path(\"tests/test_semantics.py\"); s = p.read_text()\nold = '''    r = engines.run(engines.LYPNING, \"print(5 | 2, {1} | {2}, True | False)\", binary=lypning_bin)\n    assert not r.refused and r.stdout == \"7 {1, 2} True\\\\n\", r.stderr'''\nnew = '''    r = engines.run(engines.LYPNING, \"print(5 | 2, len({1} | {2}), True | False)\", binary=lypning_bin)\n    assert not r.refused and r.stdout == \"7 2 True\\\\n\", r.stderr'''\nassert s.count(old) == 1; p.write_text(s.replace(old, new)); print(\"edited\")","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T20:20:18.326Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-ec893baf0326","id":"py-ec893baf0326","program":"import re, pathlib\ndef sub(path, old, new, count=1):\n    p = pathlib.Path(path); s = p.read_text()\n    assert s.count(old) == count, (path, s.count(old))\n    p.write_text(s.replace(old, new)); print(\"edited\", path)\n\n# 1. ops.rs: `is` between two dict views refuses.\nsub(\"ops.rs\",\n'''    if let (Value::Float(x), Value::Float(y)) = (a, b) {\n        if x.is_nan() || y.is_nan() {\n            return Err(unsupported(\n                \"nan-identity\",\n                \"`is` over a NaN, which is not equal to itself, so CPython decides it by object identity\",\n            ));\n        }\n    }\n    Ok(false)\n}''',\n'''    if let (Value::Float(x), Value::Float(y)) = (a, b) {\n        if x.is_nan() || y.is_nan() {\n            return Err(unsupported(\n                \"nan-identity\",\n                \"`is` over a NaN, which is not equal to itself, so CPython decides it by object identity\",\n            ));\n        }\n    }\n    // A dict view carries the DICT's `Rc`, not its own. CPython builds a new\n    // view on every call, so `d.values() is d.values()` is False, and an alias\n    // is one object, so `v = d.values(); v is v` is True. Nothing here can tell\n    // the two apart: `is_same` has no arm, so both fell through to False, and\n    // an arm over the dict's `Rc` would answer True for both. Found by the\n    // Stage 0a replay (ntx-bd566d4c9697) as a wrong answer at exit 0; refused\n    // under the kind the views already own.\n    if let (Value::DictView(..), Value::DictView(..)) = (a, b) {\n        return Err(unsupported(\n            \"dict-view\",\n            \"`is` between two dict views, whose identity here is the dict's rather than the view's\",\n        ));\n    }\n    Ok(false)\n}''')\n\n# 2. builtins.rs: enumerate takes `iterable` by name; divmod's float wording.\nsub(\"builtins.rs\",\n'''            if let Some((k, _)) = kw.iter().find(|(k, _)| k.as_ref() != \"start\") {\n                return Err(type_err(format!(\n                    \"'{k}' is an invalid keyword argument for enumerate()\"\n                )));\n            }\n            // Argument Clinic names the parameter; `arg1` writes the generic\n            // `missing 1 required positional argument`, which is a Python-level\n            // function's wording and not this one's.\n            let v = match args.first() {\n                Some(v) => v.clone(),''',\n'''            // Both of Argument Clinic's names are real: `enumerate(iterable=xs,\n            // start=1)` is accepted on every CPython this engine targets, and\n            // the guard below knew only `start`, so the first spelling died at\n            // exit 1 with a TypeError CPython never raises. Found by the Stage\n            // 0a replay (ntx-b47b0b10247f).\n            if let Some((k, _)) = kw\n                .iter()\n                .find(|(k, _)| !matches!(k.as_ref(), \"start\" | \"iterable\"))\n            {\n                return Err(type_err(format!(\n                    \"'{k}' is an invalid keyword argument for enumerate()\"\n                )));\n            }\n            // Argument Clinic names the parameter; `arg1` writes the generic\n            // `missing 1 required positional argument`, which is a Python-level\n            // function's wording and not this one's.\n            let v = match crate::args::bind(&args, &kw, 0, \"iterable\", \"enumerate\")? {\n                Some(v) => v,''')\n\nsub(\"builtins.rs\",\n'''        \"divmod\" => {\n            let (a, b) = (arg1(name, &args)?, args.get(1).cloned().unwrap_or(Value::None));\n            let q = it.binop(crate::ast::BinOp::FloorDiv, &a, &b)?;''',\n'''        \"divmod\" => {\n            let (a, b) = (arg1(name, &args)?, args.get(1).cloned().unwrap_or(Value::None));\n            // `float_divmod` has its own zero-divisor message, `float divmod()`,\n            // and this went through `//` first, which says `float floor\n            // division by zero`: a different message at the same exit 1, and\n            // the program's own exit is never rescued. The integer arm needs\n            // nothing — `integer division or modulo by zero` IS divmod's\n            // wording there. Found by the Stage 0a replay (ntx-cab30d587088);\n            // `zero_div` owns the 3.14 respelling.\n            let float_arm = matches!(a, Value::Float(_)) || matches!(b, Value::Float(_));\n            if float_arm && as_num(&a).is_some() && as_num(&b).is_some() && !truthy(&b)? {\n                return Err(zero_div(\"float divmod()\"));\n            }\n            let q = it.binop(crate::ast::BinOp::FloorDiv, &a, &b)?;''')\n\n# 3. err.rs: the 3.14 unification covers divmod's float message too.\nsub(\"err.rs\",\n'''        \"integer division or modulo by zero\" | \"integer modulo by zero\"\n        | \"float division by zero\" | \"float floor division by zero\"\n        | \"float modulo\" if REF_PY_MINOR >= 14 => \"division by zero\",''',\n'''        \"integer division or modulo by zero\" | \"integer modulo by zero\"\n        | \"float division by zero\" | \"float floor division by zero\"\n        | \"float modulo\" | \"float divmod()\" if REF_PY_MINOR >= 14 => \"division by zero\",''')\n\n# 4. lex.rs: a lone surrogate escape is a refusal, not a SyntaxError.\nsub(\"lex.rs\",\n'''        None => Err(LypningError::syntax(line, \"invalid unicode escape\")),\n    }\n}''',\n'''        // A lone surrogate is a legal `str` in CPython — `'\\\\udcff'` is how\n        // surrogateescape spells an undecodable byte — and no UTF-8 string can\n        // hold one. That is not a syntax error, because CPython compiles it: it\n        // is a program this engine cannot run. It answered SyntaxError at exit\n        // 1, which the dispatcher returns unchanged as the program's own; a\n        // refusal sends it on to CPython instead. Found by the Stage 0a replay\n        // (ntx-590d4adaea1e). Above U+10FFFF stays a SyntaxError, as in CPython.\n        None if (0xD800..=0xDFFF).contains(&v) => Err(unsupported(\n            \"escape\",\n            \"\\\\\\\\u escape naming a lone surrogate, which no UTF-8 string can hold\",\n        )),\n        None => Err(LypningError::syntax(line, \"invalid unicode escape\")),\n    }\n}''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:40:43.884Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
{"key":"py-ec8dcb41a706","id":"py-ec8dcb41a706","program":"import pathlib\ndef sub(path, old, new):\n    p = pathlib.Path(path); s = p.read_text(); assert s.count(old) == 1, (path, old[:50]); p.write_text(s.replace(old, new)); print(\"edited\", path)\nsub(\"src/lypning/assets/rust/src/builtins.rs\",\n'''            // ...and the TypeError names `divmod()`, not the `//` it was\n            // computed through: `divmod('a', 1)` is `unsupported operand\n            // type(s) for divmod(): 'str' and 'int'` on every CPython above.''',\n'''            // ...and the TypeError names `divmod()`, not the `//` it was\n            // computed through: `divmod('a', 1)` is `unsupported operand\n            // type(s) for divmod(): 'str' and 'int'` on 3.10 through 3.13,\n            // measured 2026-09-15.''')\nsub(\"src/lypning/assets/rust/src/value.rs\",\n'''        // `DictView` is deliberately NOT here: `d.keys() is d.keys()` is False\n        // in CPython — two view objects over one dict — and the `Rc` these\n        // carry is the DICT's, so `ptr_eq` would answer True.''',\n'''        // `DictView` is deliberately NOT here: `d.keys() is d.keys()` is False\n        // in CPython — two view objects over one dict — and the `Rc` these\n        // carry is the DICT's, so `ptr_eq` would answer True. The catch-all\n        // False below is wrong for the alias (`v = d.keys(); v is v`), so\n        // `ops::identity` refuses the pair as `dict-view` before it gets here.''')","argv_tail":[],"source":"hook","session":"520bd75f-5243-58e2-9e64-37d102fd9ae1","first_seen":"2026-09-15T18:47:25.385Z","count":1,"stdin_sample":null,"models":{"claude-fable-5-1":1}}
