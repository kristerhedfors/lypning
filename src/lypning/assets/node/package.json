{
  "name": "lypning",
  "version": "0.1.0",
  "description": "Run agent-typed Python one-liners in the node process, or be told to run them on CPython",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "dependencies": {},
  "files": [
    "index.js",
    "index.d.ts",
    "quickstart.js",
    "example.js",
    "README.md",
    "Cargo.toml",
    "Cargo.lock",
    "build.rs",
    "src"
  ],
  "scripts": {
    "build": "cargo build --release --manifest-path Cargo.toml",
    "quickstart": "node quickstart.js",
    "example": "node example.js",
    "test": "node quickstart.js 'print(sum(range(10)))' && node quickstart.js 'import subprocess; print(1)' && node example.js"
  }
}
